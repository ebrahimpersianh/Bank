package ir.sadteam.loancalc.ads

/**
 * پیاده‌سازیِ تبلیغاتِ این فلیور (کافه‌بازار ادز).
 *
 * ⏳ **شناسه عمداً خالیه.** کاربر گفته «زیرساخت رو الان بساز، شناسه‌ها رو به‌موقعش می‌دم».
 * تا وقتی [bannerZoneId] خالیه، [AdManager.isReady] مقدارِ false برمی‌گردونه و [AdBanner]
 * هیچی رندر نمی‌کنه - یعنی اپ دقیقاً مثلِ الان بی‌تبلیغ کار می‌کنه، بدونِ جای خالی یا باکسِ خراب.
 *
 * **برای فعال‌کردن**: فقط همین یه رشته رو با Zone IDِ واقعیِ پنل پر کن و وابستگیِ SDK رو به
 * app/build.gradle.kts (فقط همین فلیور) اضافه کن. هیچ‌جای دیگه‌ای از اپ لازم نیست عوض بشه.
 */
object FlavorAdManager : AdManager {
    override val bannerZoneId: String = ""
}
