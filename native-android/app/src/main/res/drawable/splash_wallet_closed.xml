<?xml version="1.0" encoding="utf-8"?>
<!--
  اولین فریمِ اسپلشِ خودِ اندروید (قبل از آماده‌شدنِ Compose).

  🚨 **حاشیه داخلِ خودِ فایلِ تصویر است، نه با `inset`.** بارِ قبل این‌جا یک
  `<inset android:inset="25%">` بود و کار نکرد (گزارشِ کاربر با اسکرین‌شات: باز هم یک
  مربعِ سبزِ بریده). دلیلش این است که اندازه‌ی ذاتیِ `InsetDrawable` با حاشیه‌ی درصدی
  حسابِ حاشیه را نمی‌کند، پس کتابخانه‌ی اسپلش تصویر را روی کلِ بومِ ۲۸۸ کش می‌دهد و
  فقط ۱۹۲ِ مرکزی دیده می‌شود - یعنی همان بزرگ‌نمایی و بُرش.

  `jibak_splash_icon` همان نشان است با هنری که ۰٫۴۶ِ بوم را می‌گیرد، پس کاملاً داخلِ
  همان ۱۹۲ِ دیده‌شده می‌نشیند - بی هیچ حاشیه‌ی محاسبه‌شونده در زمانِ اجرا.
-->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:gravity="center"
    android:src="@drawable/jibak_splash_icon" />
