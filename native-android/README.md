# وام من — نسخه‌ی Kotlin بومی (در حال ساخت)

این پوشه شروع بازنویسی کامل اپ به Kotlin/Jetpack Compose هست (به‌جای Capacitor/WebView فعلی تو `www/`).
پروژه‌ی وب فعلی دست‌نخورده و در کنارش باقی می‌مونه.

## وضعیت فعلی

- ✅ **موتور محاسبه‌ی وام** (`core/`) — پورت کامل و تست‌شده‌ی `computeLoan` (وام عادی + قرض‌الحسنه)
  و توابع تاریخ شمسی/فرمت اعداد فارسی از `www/index.html`. تست‌ها (`core/src/test/`) مقادیرشون
  مستقیم از اجرای نسخه‌ی JS واقعی گرفته شدن تا تطابق دقیق تضمین بشه.
- ✅ **تب «وام بانکی»** (`app/src/main/kotlin/.../ui/BankLoanScreen.kt` + `ResultScreen.kt`) —
  فرم ورودی کامل (پرتکرارها، بانک‌ها/خدمات اعتباری، تاریخ، مبلغ، نرخ، تعداد اقساط، فاصله، تنفس)
  و صفحه‌ی نتیجه (نمودار حلقه‌ای، اعداد، جدول کامل اقساط).
- ✅ **معماری چندماژوله** — ماژول جدید `data/` (Room برای «وام‌های من»، DataStore برای
  auth/subscribed/guest_mode، Retrofit مطابق دقیق `server/`'s API contract)، Hilt برای DI
  (`di/AppModule.kt`)، Navigation-Compose به‌جای سوییچ دستی تب‌ها تو `MainActivity`.
- ✅ **تب «وام‌های من» (`ui/myloans/`)** — زنجیره‌ی کامل Compose → Hilt ViewModel → Repository →
  Room سیم‌کشی شده. فرم افزودن وام دستی (`AddManualLoanScreen`، پورت دقیق اعتبارسنجی
  `saveManualLoan`، با فرم انتخاب تاریخ شروع)، حذف وام، و بازکردن جزئیات یه وام (`LoanDetailScreen`)
  با وضعیت پرداخت **مستقل هر قسط** (`rows[].paid`، تپ‌کردن رو هر ردیف toggle می‌کنه - نه یه آستانه‌ی
  ترتیبی)، تاریخ سررسید واقعی (از startDate/intervalDays محاسبه می‌شه، پورت دقیق حلقه‌ی
  `renderTable`)، ویرایش دستی مبلغ هر قسط (پورت `confirmEditInstallment`، با سوال «همین مبلغ رو
  بقیه هم بگیرن؟» بعد از ذخیره - `setAllRowsInstallment`)، و تاخیر پرداخت (`paidLate`/`paidDate`،
  پورت `openPayModal`/`payOnTime`/`confirmLatePayment`: تپ رو قسطِ پرداخت‌نشده یه انتخاب
  «به‌موقع»/«با تاخیر» نشون می‌ده، حالت دوم یه انتخابگر تاریخ واقعی پرداخت هم داره).
- ✅ **تب‌های محاسبه‌گر و سود سپرده** (`AffordScreen`/`DepositScreen`) — پورت مستقیم
  `calculateAffordability`/`calculateDeposit`، با فرمول‌هاشون تو `core/` (`AffordabilityCalculator`،
  `DepositCalculator`) تست‌شده.
- ✅ **رفع باگ ارقام فارسی/عربی** — `cleanNum`/`cleanNumDecimal` (پورت از وب) به `core/` اضافه
  شد و تو همه‌ی ورودی‌های عددی وصل شده (قبلش با کیبورد فارسی مقدار صفر parse می‌شد).
- ✅ **گیت ورود اجباری + حالت مهمان** (`ui/auth/`) — `AuthViewModel`/`LoginScreen` پورت
  checkLoginGateOnStart/sendPhoneOtp/confirmPhoneOtp/continueAsGuest هستن؛ اولین بار که اپ باز
  می‌شه (و هنوز نه لاگین نه مهمونه) `LoginScreen` اجباری نشون داده می‌شه، بعد از ورود موفق یا
  انتخاب مهمان دیگه هیچ‌وقت دوباره نشون داده نمی‌شه (دقیقاً مثل وب). session تو DataStore
  (`AuthPrefs`) ذخیره می‌شه.
- ✅ **محدودیت «۱ وام رایگان» + خرید واقعی اشتراک کافه‌بازار** (`canSaveAnotherLoan` تو
  `MyLoansScreen`، `subscription/`) — بعد از اولین وام، دکمه‌ی افزودن برای مهمون‌ها `LoginScreen`
  غیراجباری باز می‌کنه، برای کاربرهای واردشده‌ی بدون اشتراک `SubscriptionScreen` (۴ پلن پلکانی).
  خرید با **SDK بومی رسمی Poolakey** انجام می‌شه (`com.github.cafebazaar.Poolakey:poolakey` از
  JitPack - همون کتابخونه‌ای که پلاگین Capacitor نسخه‌ی وب هم زیرش داره، ولی اینجا مستقیم، نه از
  پشت یه پل جاوااسکریپت). `SubscriptionManager` (تو `subscription/`) عمداً Hilt-managed نیست چون
  به خودِ Activity نیاز داره (`activityResultRegistry`) - تو `MainActivity.onCreate` ساخته و
  connect می‌شه، `onDestroy` disconnect؛ بقیه‌ی صفحه‌ها از یه `CompositionLocal`
  (`LocalSubscriptionManager`) بهش دسترسی دارن. کلید RSA و شناسه‌ی محصولات دقیقاً همونایی هستن که
  برای اپ وب (همون applicationId مشترک `ir.sadteam.loancalc`) قبلاً تو پنل کافه‌بازار ثبت شدن.
  بعد از خرید موفق، سرور (همون `POST /api/subscription/verify` که برای وب هم استفاده می‌شه)
  مستقل تایید می‌کنه - به کلاینت اعتماد نمی‌شه.
- ✅ **صفحه‌ی تنظیمات + خروج از حساب** (`ui/settings/`) — دسترسی از یه آیکون چرخ‌دنده تو نوار
  بالای اپ (`LoanCalcApp`'s `TopAppBar`، معادل `.app-header` تو وب)؛ کارت حساب (وضعیت ورود/مهمان،
  خروج) و اندازه فونت (کوچک/متوسط/بزرگ) پیاده شدن - یادآوری سررسید و بقیه‌ی بخش‌های تنظیمات وب
  هنوز جای دیگه‌ای تو اپ بومی وجود ندارن که معنی داشته باشه پیاده بشن.
- ✅ **سینک ابری واقعی** — `LoanRepository.pushToServer` (پورت `syncLoansToServer`) بعد از هر
  تغییر محلی، اگه لاگین باشیم، بی‌صدا PUT می‌کنه؛ `syncAfterLogin` هم فقط یه‌بار بلافاصله بعد از
  ورود موفق: اگه فقط سرور داده داشت جایگزین می‌شه، اگه هر دو داده‌ی متفاوت داشتن `LoginScreen` از
  کاربر می‌پرسه (پورت `openConfirmModal`)، وگرنه محلی به سرور پوش می‌شه. تشخیص «فرق دارن» بر
  اساس (id, paidCount) هر وامه، نه مقایسه‌ی بایت‌به‌بایت JSON مثل وب.
- ✅ **تم روشن** (`ui/theme/`) — پورت `toggleTheme`/`body.light`: `AppColorPalette` (تیره/روشن،
  دقیقاً هم‌رنگ `:root`/`body.light` تو وب) از یه `CompositionLocal` (`LocalAppColors`) پخش می‌شه؛
  `AppBg`/`AppSurface`/... که همه‌ی صفحه‌ها همین‌جوری import می‌کردن، حالا `@Composable get()` هستن
  نه val ثابت - یعنی هیچ صفحه‌ای برای پشتیبانی از تم روشن نیازی به تغییر نداشت. انتخاب کاربر تو
  DataStore (`UiPrefs`) ذخیره می‌شه؛ دکمه‌ی تغییر تم گوشه‌ی نوار بالای اپه (معادل `.theme-btn`).
- ✅ **اندازه فونت** (پورت `.app.fs-small/fs-medium/fs-large` تو وب) — یه `LocalDensity` override
  رو ریشه‌ی Compose (`MainActivity`) هم dp هم sp رو با هم مقیاس می‌کنه، دقیقاً معادل زوم CSS رو کل
  کانتینر `.app`؛ مقدار (۰.۹/۱/۱.۱۵) تو `UiPrefs.fontScale` ذخیره می‌شه.
- ✅ **یادآوری سررسید** (`notifications/`) — کاملاً native-only (وب اصلاً این قابلیت رو نداره تا
  ازش پورت بشه). `ReminderScheduler` یه `PeriodicWorkRequest` روزانه (هر ۲۴ ساعت، `WorkManager`
  - نه `AlarmManager` خام - چون WorkManager خودش زمان‌بندی رو حتی بعد از ری‌استارت گوشی حفظ می‌کنه،
  بدون نیاز به `BroadcastReceiver` دستی برای `BOOT_COMPLETED`) با `ExistingPeriodicWorkPolicy.KEEP`
  ثبت می‌کنه. `DueDateReminderWorker` (`@HiltWorker`، تزریق `LoanRepository`) هر بار همه‌ی وام‌ها رو
  چک می‌کنه و برای هر قسط پرداخت‌نشده‌ای که سررسیدش امروز یا فرداست یه نوتیف مجزا می‌ده. سوییچ فعال/
  غیرفعال تو تنظیمات (`SettingsScreen`) رو Android 13+ مجوز `POST_NOTIFICATIONS` رو runtime درخواست
  می‌کنه (`ActivityResultContracts.RequestPermission`)، پایین‌تر از اون نیازی به مجوز نیست.
  «امروز» واقعی از یه تبدیل جلالی↔میلادی دقیق نجومی (`core/JalaliCalendar.kt`، الگوریتم
  jalaali-js/Borkowski با جدول واقعی سال‌های کبیسه) به‌دست میاد - نه تقویم ساده‌شده‌ی
  `PersianCalendar` بالا که فقط برای فاصله‌ی روزهای اقساط از رو یه startDate دلخواهه، نه گرفتن
  «امروز». الگوریتم با ده‌ها هزار تاریخ در برابر کتابخونه‌ی پایتون jdatetime تایید شده
  (`JalaliCalendarTest.kt`).
- ✅ **گزارش خطا (کرش ریپورت)** (`crash/CrashReporter.kt`) — `ApiService.reportCrash` از قبل تعریف
  شده بود ولی هیچ‌جا صدا زده نمی‌شد؛ حالا واقعاً وصله. پورت `reportCrash`/`window.onerror`/
  `unhandledrejection` تو www/index.html: یه `Thread.setDefaultUncaughtExceptionHandler` سراسری
  (معادل native هر دو قلاب وب، چون exception بدون catchِ کوروتین‌های ساختاریافته هم نهایتاً به
  همین handler می‌رسه) حداکثر ۵ گزارش در هر بار باز شدن اپ به `POST /api/crash` می‌فرسته (با
  `runBlocking`+تایم‌اوت کوتاه، نه fire-and-forget، چون فرصت پردازش قبل از کشته‌شدن اپ محدوده)، بدون
  سرویس ثالث (نه Sentry نه Crashlytics) - دقیقاً مثل وب.
- ✅ **درباره‌برنامه + حریم خصوصی** تو `SettingsScreen` (پورت `toggleAbout`/`togglePrivacy`، متن
  عینِ وب) - یه `AccordionCard` مشترک، تپ رو عنوان باز/بسته می‌کنه. «تنظیمات پیشرفته یادآوری» (صدای
  اعلان، سفارشی‌سازی به‌ازای هر وام) و فرم «نظرات و مشکلات» عمداً پورت نشدن - رو خودِ وب هم صرفاً
  UI نمایشیه (اولی طبق کامنت خودِ وب هیچ‌وقت واقعاً وصل نشده، دومی فقط تو localStorage نوشته می‌شه و
  هیچ‌جا خونده نمی‌شه) - یادآوری سررسید بالا genuinely کار می‌کنه و جایگزین بهتریه.
- ✅ **پالایش‌های ظاهری از رو مقایسه‌ی عکس واقعی گوشی با وب** — کاربر عکس واقعی اپ نصب‌شده رو
  فرستاد، با اسکرین‌شات وب (گرفته‌شده تو مرورگر با Playwright) تطبیق داده شد و چندتا فرق واقعی پیدا
  و فیکس شد: آیکون تغییر تم برعکس بود (حالا مثل وب: تاریک=ماه، روشن=خورشید)، دراپ‌داون ماه تو
  تاریخ دو خط می‌شد (`singleLine` کم بود)، بج واحد (ریال/درصد/ماه) تو کادر ورودی‌ها اصلاً نبود
  (`suffix` اضافه شد)، اسلایدر پیش‌فرض Material3 خیلی حجیم‌تر از اسلایدر باریک وب بود
  (`ui/components/SlimSlider.kt` - تراک ۵px + دستگیره‌ی حلقه‌ای ۱۹px، دقیقاً اندازه‌ی وب)، و ابزار
  «یا برعکس: نرخ سود رو پیدا کن» تو تب محاسبه‌گر کلاً پورت نشده بود (`RateFinderCalculator` تو
  `core/`، تست‌شده با مقادیر واقعی از اجرای JS وب با node).
- ✅ **پشتیبان‌گیری/بازیابی وام‌ها** (`ui/myloans/MyLoansScreen.kt`) — پورت `exportBackup`/
  `importBackup` تو www/index.html، ولی چون مکانیزم Capacitor Filesystem+Share وب معنی نداره،
  native از Storage Access Framework خودِ اندروید استفاده می‌کنه:
  `ActivityResultContracts.CreateDocument`/`OpenDocument` (بدون نیاز به مجوز storage خاصی).
  `LoanRepository.exportBackupJson`/`importBackupJson` همون شکل JSON خام وب رو می‌خونن/می‌نویسن
  (`toWebMap`/`replaceAllWithServerData` که از قبل برای سینک سرور وجود داشتن، دوباره استفاده شدن).
  بازیابی مثل وب کل لیست رو **جایگزین** می‌کنه، نه merge. دکمه‌های «بازیابی»/«پشتیبان‌گیری» +
  «افزودن دستی وام» به‌جای FAB، به‌صورت دکمه‌های تمام‌عرض بالای لیست (مثل وب) جابه‌جا شدن.
- 🚫 **قفل اثر انگشت عمداً پورت نمی‌شه** - طبق CLAUDE.md، این قابلیت قبلاً از خودِ وب هم به تصمیم
  صریح کاربر کامل حذف شد چون ورود/مهمان‌بودن الان به‌جاش مدیریت می‌شه؛ همون منطق اینجا هم صدق
  می‌کنه. اگه دوباره خواسته شد باید صریحاً درخواست بشه، نه فرض پیش‌فرض این بازنویسی.
- ✅ **سه‌تا مشکل فنی که کاربر گزارش داد** (اپ نصب‌شده رو گوشی واقعی تست کرد):
  - **لوگو نداشت** - `AndroidManifest.xml` هیچ `android:icon` نداشت و هیچ فایل mipmap‌ای هم تو
    پروژه نبود. آیکون واقعی از همون `resources/icon.png` (منبع مشترک با اپ وب) با Pillow به ۵
    دانسیتی (mdpi تا xxxhdpi) resize و به‌عنوان `ic_launcher`/`ic_launcher_round` اضافه شد. آیکون
    adaptive (لایه‌ی foreground/background جدا برای اندروید ۸+) هنوز اضافه نشده - فعلاً فقط آیکون
    legacy (که رو همه‌ی نسخه‌های اندروید درست کار می‌کنه، فقط شکل ماسک‌شده‌ی خیلی مدرن نداره).
  - **اینترو/اسپلش‌اسکرین نداشت** - فقط یه صفحه‌ی خالی موقع باز شدن. با `androidx.core:core-
    splashscreen` (API استاندارد اسپلش‌اسکرین اندروید ۱۲+، رو نسخه‌های قدیمی‌تر هم fallback درست
    کار می‌کنه) + `Theme.LoanCalc.Splash` (پس‌زمینه‌ی هم‌رنگ اپ + آیکون خودِ اپ) اضافه شد.
    `installSplashScreen()` تو `MainActivity.onCreate` (قبل از `super.onCreate`) صدا زده می‌شه.
  - **رو نسخه‌ی قبلی نصب نمی‌شد** ("conflicts with an existing package") - چون
    `.github/workflows/build-native-android.yml` برخلاف `build-apk.yml` (اپ وب) هیچ‌وقت کلید
    امضای دیباگ ثابت (`ci-debug.keystore`، همون فایل مشترک ریشه‌ی ریپو) رو نصب نمی‌کرد؛ هر بار CI
    ران می‌شد Gradle یه کلید دیباگ تصادفی *جدید* می‌ساخت، پس هر APK با کلید متفاوتی امضا می‌شد و
    اندروید نصب رو رو نسخه‌ی قبلی رد می‌کرد. فیکس دقیقاً همون استپی هست که `build-apk.yml` سال‌هاست
    داره (`cp ci-debug.keystore ~/.android/debug.keystore` قبل از build).

## بازطراحی بزرگ از رو یه اپ رقیب (VAMMAN)

کاربر یه اپ رقیب واقعی (اسکرین‌شات‌های کامل onboarding/داشبورد/تنظیمات/آمار/تقویم‌مالی) فرستاد و
حدود ۱۵ تا تغییر بزرگ خواست تا شبیه اون بشیم (ظاهر + چندتا فیچر جدید کامل). گفت خودم به ترتیبی که
صلاح می‌دونم شروع کنم. وضعیت هر آیتم:

- ✅ فاصله‌ی لبه‌ی صفحه‌ها و اندازه فونت‌های کوچیک (جزئیات بالا).
- ✅ **گیت مجوز اول اپ** (`ui/onboarding/PermissionGateScreen.kt`) - پورت مفهومی گیت VAMMAN، ولی
  فقط با دوتا شرط که واقعاً رو کارکرد یادآوری سررسید ما تاثیر دارن (نوتیفیکیشن + معافیت از
  بهینه‌سازی باتری) - شرط سومِ رقیب («Exact Alarm») عمداً اضافه نشد چون معماری یادآوری ما
  WorkManager هست نه AlarmManager خام، پس اون مجوز اصلاً به کارمون نمیاد. این گیت قبل از گیت
  ورود/مهمان نشون داده می‌شه و برخلاف اون، هر بار اپ باز می‌شه دوباره ارزیابی می‌شه (نه فقط یه‌بار)
  چون کاربر می‌تونه بعداً از تنظیمات گوشی این مجوزها رو خاموش کنه.
- ✅ **صفحه‌ی پشتیبانی** (`ui/settings/SettingsScreen.kt`, `SupportContacts`) - یه `AccordionCard`
  جدید تو تنظیمات با سه ردیف (ایمیل/تلگرام/بله)، هرکدوم با تپ یه Intent مناسب (mailto:/لینک) باز
  می‌کنه. **مقادیر `SUPPORT_EMAIL`/`SUPPORT_TELEGRAM_URL`/`SUPPORT_BALE_URL` فعلاً placeholder ان**
  (`support@example.com` و لینک‌های نمونه) - کاربر باید اینا رو با ایمیل/آیدی واقعی خودش عوض کنه،
  چون این اطلاعات رو نباید حدس زد.
- ✅ **خروج با دوبار دکمه‌ی برگشت** (`MainActivity.kt`) - فقط رو تب پیش‌فرض («وام بانکی») فعاله؛
  بار اول یه Toast («برای خروج، دوباره دکمه‌ی برگشت رو بزن») نشون می‌ده، اگه ظرف ۲ ثانیه دوباره
  برگشت بزنی اپ می‌بنده. رو بقیه‌ی تب‌ها/تنظیمات، رفتار عادی برگشت (تعویض تب/بستن تنظیمات) دست‌نخورده.
- ✅ **انتخابگر تاریخ با تقویم گرید + بازه‌ی سال بیشتر** - بازه‌ی سال هر سه‌تا دراپ‌داون تاریخ
  (`BankLoanScreen`, `AddManualLoanScreen`, `LoanDetailScreen`) از ۱۳۹۸-۱۴۰۶ به ۱۳۸۰-۱۴۱۰ گسترش
  پیدا کرد. کنار تاریخ دریافت وام تو `BankLoanScreen` یه آیکون تقویم اضافه شد که
  `ui/components/CalendarPickerScreen.kt` رو باز می‌کنه - یه گرید ماهانه‌ی واقعی (ناوبری ماه/سال،
  دکمه‌ی «امروز»، هایلایت روز جاری/انتخاب‌شده) که طول واقعی ماه و روز هفته رو از توابع جدید
  `JalaliCalendar.daysInMonth`/`dayOfWeekSaturdayFirst` می‌گیره (هر دو در برابر jdatetime تایید
  شدن، مثل بقیه‌ی JalaliCalendar). این پورت *مفهومیه* از یه تصویر مرجع تقویم فارسی که کاربر
  فرستاد، نه کپی کد کسی. فعلاً فقط رو `BankLoanScreen` وصله؛ وصل‌کردنش به فرم افزودن وام دستی هم
  یه پیگیری کوچیک باقی‌مونده‌ست.
- ✅ **صفحه‌ی مزایا بعد از گیت مجوز** (`ui/onboarding/BenefitsScreen.kt`) - پورت مفهومی صفحه‌ی
  خوش‌آمد امکانات رقیب: «امکانات رایگان» (چک سبز) در برابر «امکانات اشتراکی» (قفل طلایی، رنگ
  `AppAccent`)، فقط یه‌بار تو کل عمر نصب نشون داده می‌شه (فلگ جدید `benefits_seen` تو `AuthPrefs`،
  بین گیت مجوز و گیت ورود/مهمان تو `AppRoot`). محتوا دقیقاً منطبق با مدل واقعی این اپه (نه کپی متن
  رقیب) - محدودیت واقعی ما فقط «یک وام رایگان» + سینک ابریه، نه امکانات ساختگی دیگه‌ای که رقیب داره.
- ✅ **انیمیشن خوش‌آمدگویی شبیه چت‌بات** (`ui/onboarding/WelcomeScreen.kt`) - بعد از حل شدن گیت
  ورود/مهمان، هر بار (نه فقط یه‌بار) یه پیام «خوش اومدی، [شماره یا مهمان] 👋» + معرفی کوتاه با
  fade+slide تدریجی نشون داده می‌شه (`WelcomeMessageScreen`، خودکار بعد از ~۱.۸ ثانیه رد می‌شه)،
  بعد صفحه‌ی اصلی با یه افکت swoosh سریع (۳۵۰ میلی‌ثانیه، fade + اسلاید از بالا-چپ) میاد تو
  (`AnimatedAppEntrance`، رو `AppRoot` وصل شده). نام از `AuthViewModel.phone` (با `toFa`) یا
  «مهمان» گرفته می‌شه.
- ✅ **فیلتر/مرتب‌سازی «وام‌های من»** (`ui/myloans/MyLoansScreen.kt`) - لیبل «فیلتر» بالا-چپ لیست
  (`LoanSortMenu`، `DropdownMenu`) یه منوی مرتب‌سازی محلی باز می‌کنه: جدیدترین (پیش‌فرض)،
  قدیمی‌ترین، نام (الفبا)، بیشترین مبلغ، بیشترین پیشرفت پرداخت. فقط ترتیب نمایش لیست عوض می‌شه،
  داده‌ای تغییر نمی‌کنه.
- ✅ **جدول اقساط با ارتفاع ثابت (۵ ردیف + اسکرول مستقل)** (`ui/myloans/LoanDetailScreen.kt`) -
  به‌جای این‌که همه‌ی ~۱۵ قسط تو یه `LazyColumn` سرتاسری با بقیه‌ی صفحه قاطی بشه، صفحه‌ی جزئیات وام
  حالا یه `Column` معمولیه (با `verticalScroll` برای کل صفحه در صورت نیاز) که یه بلوک با کادر و
  ارتفاع ثابت (`installmentRowHeight * 5` = ۵×۵۶dp) داره؛ داخل همون بلوک یه `LazyColumn` مستقل
  فقط ردیف‌های قسط رو نشون می‌ده و خودش جدا اسکرول می‌شه. منطق پرداخت/ویرایش/تاخیر هیچ تغییری نکرده.
- ✅ **قفل امنیتی PIN + اثر انگشت** (`ui/security/`) - **برگردوندن تصمیم قبلی حذف بایومتریک تو وب
  (رجوع کن به بخش «قفل اثر انگشت کاملاً حذف شده» بالا)، این‌بار با درخواست صریح جدید کاربر برای
  native.** پیش‌فرض کاملاً خاموشه (نه PIN نه بایومتریک فعاله) تا هیچ‌کس رفتار قبلی رو نبینه. تو
  تنظیمات یه بخش «امنیت» جدید اضافه شده: سوییچ «قفل با اثر انگشت» (فقط اگه `biometricAvailable()`
  گوشی سنسور/داده‌ی ثبت‌شده داشته باشه فعال می‌شه، با `BiometricPrompt` واقعی - `androidx.biometric`)
  و «قفل با PIN» (هش SHA-256 تو DataStore، هیچ‌وقت خودِ PIN، `core/Security.kt` + `SecurityPrefs`).
  `AppLockViewModel.unlocked` با شروع هر پروسه‌ی تازه false شروع می‌شه و با `ON_STOP` روی
  `ProcessLifecycleOwner` (یعنی کل اپ رفت پس‌زمینه) دوباره false می‌شه - برگشتن به اپ همیشه دوباره
  قفل می‌خواد، نه فقط قفل اولیه. `LockScreen` قبل از همه‌ی گیت‌های دیگه (حتی گیت مجوز) تو `AppRoot`
  چک می‌شه. **`BiometricPrompt` نیاز به `FragmentActivity` داره، پس `MainActivity` از
  `ComponentActivity` به `FragmentActivity` تغییر کرد** (یه superclass ایمن‌تر، بدون تاثیر رو بقیه‌ی
  کد چون FragmentActivity خودش زیرکلاس ComponentActivity هست).
- ✅ **بازطراحی تنظیمات به‌صورت پنل روی صفحه** (`MainActivity.kt`, `ui/settings/SettingsScreen.kt`)
  - به‌جای return زودهنگام که کل صفحه رو با تنظیمات جایگزین می‌کرد، حالا یه overlay واقعیه: یه scrim
  نیمه‌شفاف (تپ روش می‌بنده) + پنل با عرض ۸۵٪ صفحه که از سمت گیره‌ی تنظیمات با اسلاید سریع
  (۲۵۰ میلی‌ثانیه) میاد تو، طوری که لبه‌ی صفحه‌ی زیرش همیشه دیده بمونه (پورت مفهومی الگوی
  side-panel رقیب VAMMAN). بالای پنل هم یه آواتار (آیکون Person تو یه دایره) + برچسب وضعیت
  («نسخه عادی»/«نسخه اشتراکی») اضافه شده؛ اگه مشترک باشیم، کادر دور آواتار طلایی (`AppAccent`) و
  ضخیم‌تر (۳dp به‌جای ۱dp) می‌شه.
- ✅ **داشبورد وضعیت مالی بالای «وام‌های من»** (`ui/myloans/MyLoansScreen.kt`, `DashboardSummary`)
  - سه کارت بزرگ و خوانا مثل داشبورد رقیب: «وضعیت کلی بدهی‌ها» (مجموع `installment×(n−paidCount)`
  همه‌ی وام‌ها)، «مجموع اقساط ماهانه» (جمع `installment` همه‌ی وام‌ها - تقریبی، فرض دوره‌ی ماهانه)،
  و «تحلیل درآمد» (یه فیلد ورودی درآمد ماهانه‌ی دستی - **مفهوم کاملاً جدید، قبلاً هیچ‌جای این پروژه
  درآمد ذخیره نمی‌شد**؛ `IncomePrefs` تو `:data`) که نسبت اقساط به درآمد رو با آستانه‌ی رایج
  ۳۰٪/۵۰٪ (وضعیت مطلوب/محتاط باش/فشار مالی بالا) نشون می‌ده. برخلاف رقیب که این یه صفحه‌ی داشبورد
  جدا (خانه) بود، چون معماری ۴-تبیِ ثابتِ این اپ (رجوع کن به CLAUDE.md) عوض نشد، این بالای «وام‌های
  من» اضافه شده - جایی که داده‌ی وام‌ها از قبل در دسترسه، نه به‌عنوان تب پنجم.
- ✅ **تقویم مالی** (`ui/calendar/FinancialCalendarScreen.kt` + `FinancialCalendarViewModel.kt`) -
  یه ورودی جدید تو تنظیمات («تقویم مالی» + دکمه‌ی «مشاهده») یه گرید تقویم شمسی مستقل باز می‌کنه که
  روزهای دارای سررسید قسط رو با یه نقطه‌ی رنگی مشخص می‌کنه (سبز = همه‌ی اقساط اون روز پرداخت‌شده،
  قرمز = حداقل یکی پرداخت‌نشده)؛ تپ رو یه روز، لیست همه‌ی اقساط سررسیدشده‌ی همون روز (از همه‌ی
  وام‌ها، نه فقط یکی) پایین گرید نشون داده می‌شه. سررسیدها مستقیم از منطق موجود `getRows`
  (LoanRepository) گرفته می‌شن - محاسبه‌ی سررسید هیچ تغییری نکرده، فقط برای همه‌ی وام‌ها جمع شده.
- ✅ **آمار و گزارشات + خروجی PDF** (`ui/stats/`) - یه ورودی جدید تو تنظیمات («آمار و گزارشات» +
  دکمه‌ی «مشاهده») یه صفحه با ۶ کارت آماری بزرگ (تعداد وام‌ها، مجموع مبلغ، مجموع پرداخت‌شده، مانده‌ی
  کل، اقساط پرداخت‌شده از کل، درصد پیشرفت) + یه نمودار دونات ساده (رسم دستی با `Canvas`/`drawArc`،
  بدون کتابخونه‌ی نمودار جدید) نشون می‌ده - همه از رو همون فیلدهای `LoanEntity` که `DashboardSummary`
  هم استفاده می‌کنه حساب می‌شن (`StatsViewModel.summarize`)، نه منبع داده‌ی جدید. دکمه‌ی «دانلود
  گزارش PDF» با `StatsPdfExporter` یه PDF واقعی می‌سازه - عمداً از `android.graphics.pdf.PdfDocument`
  خودِ Android SDK استفاده شده (نه یه کتابخونه‌ی PDF شخص‌ثالث جدید)، و چون `Canvas.drawText` خام
  bidi فارسی/عربی رو درست shape/reorder نمی‌کنه، متن با `StaticLayout` (`TextDirectionHeuristics.RTL`)
  رسم می‌شه تا فارسی درست (نه برعکس/بهم‌ریخته) نمایش داده بشه. PDF از طریق SAF
  (`CreateDocument("application/pdf")`) ذخیره می‌شه، دقیقاً هم‌الگو با پشتیبان‌گیری JSON موجود.
- این آخرین آیتم از لیست ۱۵تایی بازطراحی VAMMAN بود - همه با CI جدا تایید و merge شدن.

## پولیش عمومی UI (بعد از تکمیل بازطراحی VAMMAN)

کاربر گفت رابط کاربری کلاً «خشک»ه، بخواد افکت/جلوه‌ی بصری اضافه بشه. یه لیست کارِ جدا (نه بخشی از
بازطراحی VAMMAN) برای این باز شد:

- ✅ **عمق/سایه‌ی کارت‌ها** (`ui/components/AppCard.kt`) - قبلاً `AppCard` فقط یه `Column` دستی با
  `background`+`border` بود (کاملاً flat). حالا از `Surface` متریال۳ استفاده می‌کنه
  (`tonalElevation=3.dp` + `shadowElevation=2.dp`) که هم سایه‌ی ظریف هم روشن‌شدن تونالِ سطح (سیستم
  عمق رسمی متریال۳ برای تم تیره) رو مجانی می‌ده - همه‌ی کارت‌های اپ (چون همه از همین کامپوننت
  استفاده می‌کنن) یه‌جا عمق گرفتن.
- ✅ **ترنزیشن بین تب‌های پایین** (`MainActivity.kt`, `NavHost`) - قبلاً تعویض ۴ تب اصلی یه کاتِ
  ناگهانی بود (هیچ enter/exitTransition ای ست نشده بود). حالا fade+scaleIn ظریف (۲۲۰ میلی‌ثانیه)
  داره - پورت حس اسلاید switchTab وب، ولی ساده‌شده (fade+scale، نه اسلاید جهت‌دار کامل).
  `popEnterTransition`/`popExitTransition` هم برای سازگاری با `saveState`/`restoreState` تب‌ها ست شدن.
- ✅ **بازگشت فنری آیکون تب فعال** (`MainActivity.kt`, `BottomNavItem`) - پورت easing فنری
  `cubic-bezier(.34,1.56,.64,1)` وب (`.nav-item .ic svg`) - آیکون تب انتخاب‌شده حالا با یه
  `spring(dampingRatio=MediumBouncy)` یه‌کم بزرگ‌تر (۱.۱۵×) می‌شه، نه فقط رنگش عوض بشه.
- ✅ **گرادینت رو دکمه‌های اصلی CTA** (`ui/components/GradientButton.kt`) - یه کامپوننت جدید
  جایگزین همه‌ی ۱۱ جای اپ که قبلاً `Button(colors = ButtonDefaults.buttonColors(containerColor =
  AppPrimary))` (رنگ صاف تخت) بودن؛ ظاهر/رفتار (ripple، enabled/disabled، شکل گرد) عین `Button`
  معمولیه، فقط پس‌زمینه‌ش یه گرادینت خطی از `AppPrimary` به `AppPrimaryDim`ه. محتوای هر call site
  (معمولاً یه `Text`) بدون تغییر کار می‌کنه چون امضاش هم مثل `Button` یه `RowScope.() -> Unit`ه.
- ✅ **افکت press-scale رو کارت‌های قابل‌تپ** (`ui/components/PressScale.kt`, `pressScaleClickable`)
  - پورت `:active { transform: scale(...) }` وب رو `.chip`/`.preset-card`/`.bank-item`/`.loan-card`
  (رجوع کن به CLAUDE.md) - یه `Modifier` قابل‌استفاده‌ی مجدد که خودش هم `clickable` رو با یه
  `interactionSource` مشترک انجام می‌ده (نه یه Modifier جدا از کلیک، چون press-state باید دقیقاً
  همون تعامل کلیک رو ببینه). جایگزین `.clickable(...)` تو `BankTile`، `PresetCard`، `AppChip`، کارت
  وام «وام‌های من»، و ردیف‌های آکاردئون/پشتیبانی تنظیمات شد.
- ✅ **`AnimatedContent` برای تعویض صفحه‌های داخلی** - سه جا که یهو (بدون هیچ ترنزیشنی) با یه
  return زودهنگام عوض می‌شدن، حالا با یه fade+scale ظریف (شبیه ترنزیشن تب‌های پایین) عوض می‌شن:
  `BankLoanTab` (فرم→نتیجه، تو `MainActivity.kt`)، `MyLoansScreen` (لیست↔ورود/اشتراک/افزودن/جزئیات
  - state های قبلی دست‌نخورده موندن، فقط با یه `screenKey` مشتق‌شده به یه `AnimatedContent` واحد
  وصل شدن)، و `SettingsScreen` (اصلی↔ورود/تقویم‌مالی/آمار - بدنه‌ی اصلی به یه کامپوزبل جدا
  `SettingsMainContent` استخراج شد تا تو یه شاخه‌ی `when` جا بشه). این آخرین آیتم از این پولیش UI بود.

## پرکردن شکاف با اپ رقیب (تحلیل APK واقعی VAMMAN)

کاربر فایل APK واقعی اپ رقیب رو فرستاد؛ با `androguard` (بدون decompile/کپی کد - فقط manifest،
permissions، لیست activity/service/receiver، و رشته‌های فارسی استخراج‌شده از dex) بررسی شد که چی
داره که این پروژه نداره، بعد کاربر گفت «هرچی داره من نداریم اضافه کن». لیست کامل کارها تو تسک‌های
این session (#29 تا #37) پیگیری می‌شه؛ اولین و بزرگ‌ترینش:

- ✅ **ماژول «امور چک»** (`ui/cheque/`, `data/ChequeRepository.kt`, `data/db/Cheque*Entity.kt`) -
  چک‌های دریافتی/پرداختی با فیلد کامل (نوع، مبلغ، شماره، بانک/شعبه، طرف حساب، تاریخ سررسید شمسی،
  وضعیت، یادداشت)، مدیریت وضعیت (`ChequeStatus`: وضع‌نشده/پاس‌شده/برگشت‌خورده/مسترد) با `AppChip`،
  بایگانی، و دسته‌چک (`ChequeBookEntity` - بازه‌ی سریال + پیشنهاد خودکار شماره‌ی بعدی وقتی یه چک از
  رو همون دسته ثبت می‌شه). معماری کاملاً هم‌الگو با ماژول وام (Room با `id = System.currentTimeMillis()`
  دستی، پشتیبان‌گیری JSON جدا با Gson، `ChequeViewModel` با Hilt). یادآوری سررسید چک (امروز/فردا) به
  همون `DueDateReminderWorker` وام اضافه شد، با namespace جدای نوتیف (`"cheque_${id}".hashCode()`).
  دکمه‌ی «استعلام چک صیادی» تو جزئیات چک فعلاً به یه URL **صراحتاً جای‌گیرنده** وصله (`SAYAD_INQUIRY_URL_PLACEHOLDER`
  با کامنت `// TODO`، مطابق قانون پروژه علیه حدس‌زدن URL واقعی) - باید بعداً با سرویس واقعی استعلام
  بانک مرکزی جایگزین بشه. ورودی از پنل تنظیمات («امور چک» کنار «آمار و گزارشات») باز می‌شه، نه یه تب
  پایین جدا (برای این‌که ۴ تب اصلی طبق تصمیم قبلی کاربر ثابت بمونه).

- ✅ **ماژول «حساب بانکی»** (`ui/account/`, `data/AccountRepository.kt`, `data/db/Account*Entity.kt`) -
  چند حساب بانکی مستقل، هرکدوم با موجودی اولیه + دفترچه‌ی تراکنش (واریز/برداشت با توضیح و تاریخ
  شمسی). موجودی فعلی («موجودی فعلی») همیشه **محاسبه‌شده** از رو `initialBalance` + جمع تراکنش‌هاست
  (`AccountRepository.currentBalance`)، نه یه فیلد جدا که ممکنه دستی از واقعیت جا بمونه. همون معماری
  هم‌الگو با وام/چک (Room، `id` دستی، پشتیبان‌گیری JSON جدا، `AccountViewModel` با Hilt). ورودی از
  پنل تنظیمات («حساب‌های بانکی» کنار «امور چک»).
- ✅ **چند منبع درآمد + آستانه‌ی امن درست ۶۵٪** (`core/Income.kt`, `data/IncomeRepository.kt`,
  `MyLoansScreen.DashboardSummary`) - قبلاً «تحلیل درآمد» بالای «وام‌های من» فقط یه عدد تکی
  (`IncomePrefs` تو DataStore) می‌گرفت و دو آستانه‌ی حدسی ۳۰٪/۵۰٪ داشت. حالا چند منبع درآمد مستقل
  (هرکدوم با اسم، مبلغ، و نوع `IncomeType`: ثابت/متغیر) تو Room نگه‌داری می‌شن (جایگزین کامل
  `IncomePrefs` که حذف شد)، جمع کل‌شون مبنای نسبت اقساط-به-درآمده، و آستانه‌ی «وضعیت مطلوب»/«فشار
  مالی بالا» روی ۶۵٪ درسته - عددی که از رشته‌های واقعی dex رقیب استخراج شد، نه حدسی.

- ✅ **خروجی اکسل (.xlsx) کنار PDF** (`ui/stats/StatsXlsxExporter.kt`) - عمداً بدون هیچ کتابخونه‌ی
  جدید (نه Apache POI، نه مشابه) - فرمت xlsx در واقع یه فایل zip با چندتا XML ثابت (OOXML) هست، پس
  دستی با `java.util.zip.ZipOutputStream` خودِ جاوا ساخته می‌شه (بدون sharedStrings.xml، با
  inline-string سلول‌ها که برای این حجم داده کافیه). محتوا هم‌ارز PDF فعلیه (جدول هر وام + جمع کل)،
  فقط تو یه شیت اکسل به‌جای متن. دکمه‌ی «دانلود اکسل» کنار «دانلود PDF» تو `StatsScreen` اضافه شد.
- ✅ **نمودار خطی «تاریخچه پرداخت»** (`StatsViewModel.paymentHistory`, `PaymentHistoryLineChart` تو
  `StatsScreen.kt`) - کنار نمودار دونات موجود، یه نمودار خطی هم اضافه شد که مجموع تجمعی اقساط
  پرداخت‌شده رو به تفکیک ماه شمسی نشون می‌ده (بر اساس `paidDate` واقعی اگه با تاخیر پرداخت شده،
  وگرنه `dueDate` - از همون rows که `LoanDetailScreen`/`getRows` قبلاً برای وضعیت پرداخت هر قسط
  استفاده می‌کنه، نه یه منبع داده‌ی جدید). رسم دستی با `Canvas` (بدون کتابخونه‌ی نمودارِ جدید)، عین
  دونات موجود.
- ✅ **قفل خودکار با timeout + قفل موقت بعد از تلاش‌های ناموفق** (`AppLockViewModel.kt`) - قبلاً هر بار
  که کل اپ می‌رفت پس‌زمینه (`ProcessLifecycleOwner` ON_STOP)، بی‌قید‌وشرط دوباره قفل می‌شد. حالا موقع
  ON_STOP فقط زمان رفتن به پس‌زمینه ثبت می‌شه؛ موقع ON_START (برگشت به اپ) فقط اگه فاصله‌ی زمانی از
  یه آستانه‌ی قابل‌تنظیم (`autoLockTimeoutMinutes` تو `SecurityPrefs` - بی‌درنگ/۱/۵/۱۵ دقیقه، چیپ‌های
  جدید تو «امنیت» تنظیمات، کنار سوییچ اثر انگشت/PIN) بیشتر بود، دوباره قفل می‌شه؛ پیش‌فرض بی‌درنگه،
  یعنی رفتار قبلی برای کسی که تنظیم نکرده عوض نمی‌شه. علاوه‌براین، `attemptPin` (جایگزین `verifyPin`ِ
  مستقیم قبلی تو `LockScreen`) بعد از ۵ تلاش ناموفق پشت‌سرهم یه قفل موقت ۳۰ ثانیه‌ای فعال می‌کنه
  (پورت مفهومی قفل موقت ضدـبروت‌فورس اپ رقیب)، با شمارش معکوس و دکمه‌ی غیرفعال تا پایان مهلت.
- ✅ **پشتیبان‌گیری خودکار روزانه** (`notifications/AutoBackupWorker.kt`, `AutoBackupScheduler.kt`,
  `ui/settings/AutoBackupViewModel.kt`) - هم‌الگو با یادآوری سررسید (`WorkManager` هر ۲۴ ساعت،
  نه `AlarmManager` خام)، فقط به‌جای نوتیف، یه اسنپ‌شات تازه از وام/چک/حساب رو تو فضای داخلی اپ
  (`context.filesDir/auto_backups` - نه SAF، چون بدون تعامل کاربر اجرا می‌شه) می‌نویسه، جایگزین
  اسنپ‌شات قبلی (یه نسخه‌ی واحد، نه آرشیو). سوییچ «پشتیبان‌گیری خودکار روزانه» تو تنظیمات (کنار
  «یادآوری سررسید») پیش‌فرض خاموشه؛ اگه روشن باشه، تاریخ آخرین پشتیبان (تبدیل‌شده به شمسی) و یه دکمه‌ی
  «بازیابی از پشتیبان خودکار» (می‌خونه و جایگزین وام/چک/حساب فعلی می‌کنه) هم نشون داده می‌شه.
- ✅ **سرچ تو منوی تنظیمات** (`SettingsScreen.kt`) - یه `OutlinedTextField` بالای پنل تنظیمات؛ هر
  کارت/آکاردئون (اندازه فونت، یادآوری سررسید، پشتیبان‌گیری خودکار، تقویم مالی، آمار و گزارشات، امور
  چک، حساب‌های بانکی، امنیت، پشتیبانی، درباره برنامه، حریم خصوصی) فقط وقتی نشون داده می‌شه که عنوانش
  (یا برای «امنیت»، چندتا مترادف مثل «قفل»/«PIN»/«اثر انگشت» هم) شامل متن سرچ باشه؛ عبارت خالی همه رو
  نشون می‌ده (رفتار قبلی بدون تغییر). کارت پروفایل/ورود بالای صفحه همیشه دیده می‌شه، چون وضعیت حساب
  نیست، یه «تنظیم» فیلترپذیر.
- ✅ **پیوست عکس رسید رو وام/چک** (`data/AttachmentStorage.kt`, `ui/components/PhotoAttachmentCard.kt`) -
  آخرین آیتم از این لیست پرکردن شکاف (#29 تا #37). عکس با `ActivityResultContracts.PickVisualMedia`
  (Photo Picker مدرن اندروید، نه Intent گالری خام - نیازی به مجوز READ_EXTERNAL_STORAGE نداره)
  انتخاب می‌شه؛ چون Uri های گالری همیشه پایدار نیستن، بلافاصله به فضای داخلی اپ
  (`context.filesDir/attachments`) کپی می‌شه و مسیر مطلق تو یه ستون جدید `photoPath` (رو هر دوی
  `LoanEntity`/`ChequeEntity`، nullable با پیش‌فرض null - نیازی به migration دستی یا تغییر جای دیگه‌ی
  کد نبود) ذخیره می‌شه. کامپوننت مشترک `PhotoAttachmentCard` (نمایش thumbnail با Coil - از قبل
  دیپندنسی پروژه بود، نه کتابخونه‌ی جدید - + دکمه‌ی تعویض/حذف) رو جزئیات وام و جزئیات چک هر دو
  استفاده می‌شه.

هیچ موردی از لیست شکاف‌های شناسایی‌شده باقی نمونده.

## پالایش‌های بعدی (درخواست‌های تک‌موردیِ کاربر)

- ✅ **انتخاب سریع سال/ماه تو تقویم گریدی** (`ui/components/CalendarPickerScreen.kt`) - قبلاً هدر تقویم
  فقط اسم‌ماه/سال رو نشون می‌داد و تنها راه رفتن به یه سال دور، تپِ ماه‌به‌ماهِ فلش‌ها بود (خیلی کند).
  حالا اسم ماه و سال تو هدر هرکدوم یه چیپ قابل‌تپن: تپ رو سال → یه گرید اسکرول‌شونده‌ی سال‌ها (۱۴۱۰
  بالا تا ۱۳۵۰ پایین)، تپ رو ماه → گرید ۱۲ ماه؛ بعد از انتخاب به گرید روزها برمی‌گرده. بازه‌ی سال
  انتخابگر چرخونه‌ای (`WheelDatePickerScreen`) و دراپ‌داون‌های تاریخ (فرم‌های افزودن وام/چک/تراکنش) هم
  از ۱۳۸۰-۱۴۱۰ به ۱۳۵۰-۱۴۱۰ گسترده شد که با تقویم یکی باشه.
- ✅ **رفع «نصب رو نسخه‌ی قبلی»** - رجوع کن به بخش امضای دیباگ تو CLAUDE.md: امضای دیباگ حالا مستقیم
  تو `app/build.gradle.kts` به `ci-debug.keystore` کامیت‌شده پین شده (نه `~/.android/debug.keystore`)،
  و `versionCode` تو workflow از `github.run_number` تزریق می‌شه.
- ✅ **رفع باگ بازیابی دسته‌چک** - `ChequeRepository.importBackupJson` قبلاً دسته‌چک‌ها رو با upsert تو
  حلقه بازمی‌گردوند، پس دسته‌چک‌های قدیمیِ خارج از فایل بکاپ باقی می‌موندن؛ حالا `ChequeBookDao.replaceAll`
  (clear + insert) داره، هم‌الگو با بازیابی چک/حساب.
- ✅ **بهینه‌سازی محاسبه‌ی موجودی حساب** - `AccountsScreen`/`AccountDetailScreen` موجودی هر حساب رو
  حالا فقط وقتی حساب‌ها/تراکنش‌ها عوض می‌شن (با `remember`) حساب می‌کنن، نه هر recomposition به‌ازای هر
  کارت.
- ✅ **تغییر تاریخ اینلاین تو صفحه‌ی «وام بانکی»** (`ui/components/InlineJalaliDateRow.kt`) - قبلاً
  تاریخ دریافت وام فقط با رفتن به یه صفحه‌ی جدا (چرخونه/تقویم) عوض می‌شد؛ حالا سه دراپ‌داونِ روز/ماه/سال
  مستقیم تو خودِ کارت هست و بدون خروج از صفحه عوض می‌شه (آیکون تقویم برای تقویم گریدی کامل کنارش مونده).
  تریگرِ چرخونه‌ی تمام‌صفحه از این کارت حذف شد.
- ✅ **لوگوی بانک رو کارت وام‌های ذخیره‌شده** (`ui/components/BankBadge.kt`) - کارت هر وام تو «وام‌های
  من» حالا لوگوی بانک رو لبه‌ی سمت راست (leading در RTL) نشون می‌ده، از رو اسم بانک تو
  `banks`/`creditServices`؛ اگه مچ نشد (وام دستی/«مشخص‌نشده») fallback به حرف اول رو بج `AppPrimaryDim`
  - عین `renderLoansList` وب.
- ✅ **بجِ وضعیت پرداخت قسط** (`LoanDetailScreen`) - وضعیت هر قسط (پرداخت شد/با تأخیر/پرداخت نشده) به‌جای
  متن رنگی ساده، حالا تو یه باکس گوشه‌گردِ رنگی (پس‌زمینه‌ی کم‌رنگِ همون رنگ) نشون داده می‌شه تا واضح‌تر
  دیده بشه.
- ✅ **حذف نوشته‌ی توضیح زیر وام‌های پرتکرار** (`BankLoanScreen`) - قبلاً با زدن بعضی وام‌های پرتکرار یه
  متن «📋 طبق قانون بانک مرکزی...» (`presetNote`) زیر ردیف کارت‌ها ظاهر می‌شد؛ به‌خواست کاربر کامل حذف
  شد (خودِ فیلد `note` تو دیتای پریست دست‌نخورده موند، فقط نمایشش برداشته شد). انتخاب پریست همچنان
  مبلغ/نرخ/تعداد اقساط رو مثل قبل پر می‌کنه.
- ✅ **اینتروِ دوناتی موقع باز شدن اپ** (`ui/onboarding/SplashIntroScreen.kt`, `res/drawable/splash_donut.xml`)
  - کاربر گفت اینتروِ قبلی (که تو اپ وب هست) یه «دایره» بود؛ همون اسپلشِ `#splash` تو `www/index.html`
  (زمینه‌ی تیره + حلقه‌ی دونات با کمانِ طلاییِ ۳۰٪ و سبزِ ۷۰٪ + «وام من» + سه نقطه‌ی چشمک‌زن + «Powered
  By Sad Team») پورت شد. دو تیکه: (۱) `SplashIntroScreen` کامپوزبل که با `Canvas` دونات رو می‌کشه و
  ~۱.۳ ثانیه اول (هر بار باز شدن اپ، قبل از همه‌ی گیت‌ها تو `AppRoot`) نشون داده می‌شه؛ (۲) یه
  VectorDrawable دونات که به‌عنوان `windowSplashScreenAnimatedIcon`ِ اسپلشِ سیستمی (اندروید ۱۲+) ست شد
  تا حتی فلاشِ کوتاهِ اسپلشِ سیستمی هم به‌جای آیکونِ ماشین‌حساب، همون دونات باشه. **آیکونِ لانچر
  (`ic_launcher` = ماشین‌حساب+سکه) عمداً دست‌نخورده موند** - فقط اینترو/اسپلش عوض شد.
- ✅ **رفع لگ: بیلد release تو CI** - کاربر گفت اپ لگ داره (اسکرول/تعویض تب گیر می‌کنه). ریشه‌ش این
  بود که فقط APK **debug** ساخته/نصب می‌شد: فلگ `debuggable` بهینه‌سازی ART/AOT رو خاموش می‌کنه و
  runtime کامپوز هم چک اضافه داره، پس debug ذاتاً سنگینه (ربطی به کد ما نداشت). حالا workflow یه
  `assembleRelease` هم می‌سازه (**آرتیفکت `loan-calculator-native-release-apk` - برای نصب همین رو
  بردار**، debug فقط برای توسعه‌ست) که با همون کلید ثابت امضا می‌شه (رو نصب قبلی می‌شینه). `minify`
  عمداً خاموشه چون R8 با reflection های Gson ریسک کرش داره و اینجا امکان تست runtime نیست.
- ✅ **دور دوم رفع لگ + درخواست‌های ریز کاربر (از رو اسکرین‌شات‌ها)**:
  - **حذف صفحه‌ی اضافه‌ی اول اپ**: ماسکِ دایره‌ایِ اسپلشِ سیستمیِ اندروید ۱۲ آیکونِ دوناتی رو زشت
    می‌بُرید (اسکرین‌شات کاربر)؛ آیکون اسپلش سیستمی حالا کاملاً شفافه (`splash_empty.xml`) - فقط یه
    پس‌زمینه‌ی تیره‌ی خالی و بلافاصله اینتروِ دوناتیِ Compose.
  - **متن حروفی مبلغ همیشه تک‌خطی** (`ui/components/AutoShrinkText.kt`): اگه جا نشه فونت قدم‌قدم
    کوچیک می‌شه، هیچ‌وقت به خط دوم نمی‌شکنه (رو «مبلغ وام» و مبلغ منبع درآمد اعمال شد).
  - **افزودن سررسید اقساط به تقویم خودِ گوشی** (`calendar/DeviceCalendarExporter.kt` + دکمه تو
    `LoanDetailScreen` + مجوزهای `READ/WRITE_CALENDAR` تو مانیفست، درخواست runtime): اقساط
    پرداخت‌نشده به‌صورت رویداد تمام‌روز با عنوان «[وام] قسط ۳ اسم‌وام (۱۲,۷۹۴,۰۰۰ ریال)» تو تقویم
    درج می‌شن - مستقیم با `CalendarContract` (نه Intent تک‌رویدادی، چون وام می‌تونه ۱۲۰ قسط باشه).
  - **فیلد مبلغ منبع درآمد**: جداکننده‌ی هزارگان + معادل حروفی تومانی زیرش (عین «مبلغ وام»).
  - **رفع لگ واقعی تو خود کد**: ردیف‌های افقی بانک‌ها (۳۴ لوگو)/خدمات اعتباری/وام‌های پرتکرار از
    `Row(horizontalScroll)` (که همه رو یک‌جا compose می‌کرد) به **`LazyRow`** تبدیل شدن - فقط
    آیتم‌های قابل‌دیدن compose می‌شن؛ نشانگر اسکرول با `lazyRowScrollbar` جدید (نسخه‌ی LazyListState
    ای `horizontalScrollbar`) حفظ شد. **و یادآوریِ مهم: APK ریلیز رو نصب کن نه دیباگ.**
  - **زرق‌وبرق بیشتر**: شیمر (نوار براقِ گذرا) رو همه‌ی `GradientButton` ها (فقط تو فاز draw، بدون
    recomposition)، ورود پلکانی (stagger) بخش‌های صفحه‌ی نتیجه، pop فنری FAB «وام‌های من»، چرخش
    فنری آیکون تم موقع تعویض، و شمارش صعودی اعداد بزرگ داشبورد (`ui/components/CountUp.kt`).
- ✅ **انیمیشن‌های «پریمیوم»** (به‌درخواست کاربر: «هرجور خودت می‌دونی افکت اضافه کن، حس محصول پریمیوم
  بده») - همه ارزون و GPU-friendly:
  - **اسلاید جهت‌دارِ فنری بین ۴ تب پایین** (پورت کامل slide-l/slide-r وب): جهت از رو فاصله‌ی ایندکس
    تب قبلی/جدید حساب می‌شه، ورود با spring و خروج با tween کوتاه - جایگزین fade+scale قبلی. تعویض
    فرم→نتیجه‌ی «وام بانکی» هم همین اسلاید جهت‌دار رو گرفت.
  - **قرصِ (pill) تب فعال** تو نوار پایین حالا با fade نرم ظاهر/محو می‌شه (قبلاً یهو بود).
  - **دونات نتیجه با sweep از صفر «کشیده» می‌شه** (۹۰۰ms، همون easing اسپلش وب) + شمارش صعودی اعداد
    (قسط ماهانه و کل بازپرداخت) رو `Double` برده شد تا رو مبالغ میلیاردی خطای گردکردن Float دیده نشه.
  - **`Modifier.animateItem()`** رو لیست وام‌ها/چک‌ها/حساب‌ها/تراکنش‌ها: اضافه/حذف/جابه‌جایی آیتم‌ها
    دیگه پرش یهویی نیست، با انیمیشن placement نرم انجام می‌شه.

- ✅ **دور سوم (سلیقه‌ی نهایی کاربر: «افکت تک، نه ده تا»)**:
  - **چرخش دوناتِ اینترو**: حلقه‌ی اسپلش موقع ورود یه دور کامل ۳۶۰ درجه با همون easing نرم می‌چرخه و
    می‌ایسته - این تنها افکتِ «امضا»ی اپه (خواسته‌ی کاربر: «دایره بچرخه خیلی پریمیوم می‌شه»).
  - **حذف افکت‌های پر زرق‌وبرق قبلی**: شیمرِ تکرارشونده‌ی `GradientButton` و چرخش آیکون تم حذف شدن
    (کاربر: «نمی‌خوام ده تا انیمیشن روش بذاری، می‌خوام تک باشه»). انیمیشن‌های ظریف و یه‌باره (اسلاید
    تب، شمارش اعداد، sweep دونات نتیجه، stagger نتیجه، pop فنری FAB، animateItem لیست‌ها) موندن.
  - **رفع «سوال اول دیگه نمیاد»**: `android:allowBackup="false"` شد - بکاپ خودکار اندروید موقع نصب
    مجدد، DataStore (فلگ‌های benefitsSeen/گیت ورود-مهمان/...) رو از نصب قبلی برمی‌گردوند و سوال‌های
    اولین اجرا دیگه ظاهر نمی‌شدن. حالا نصب تازه واقعاً تازه‌ست؛ داده‌ی کاربر با پشتیبان‌گیری داخل اپ +
    سینک ابری + بکاپ خودکار روزانه محافظت می‌شه، نه بکاپ سیستمی.

- ✅ **دور چهارم (تصحیح برداشت اشتباه دور سوم + امنیت اشتراک + زیبایی)**: کاربر توضیح داد که «تک بودن»
  تو دور سوم رو اشتباه فهمیدم - منظورش این بود که اپ تو زیبایی منحصربه‌فرد باشه، نه اینکه افکت‌ها کم
  بشن؛ پس شیمرِ `GradientButton` برگردوندیم.
  - **اسکرول‌بار/اسلایدر شیک‌تر**: دستگیره‌ی `Scrollbar.kt` (هر ۴ نسخه) به‌جای رنگِ صافِ یک‌دست، گرادینتِ
    «وسط پررنگ، لبه‌ها روشن‌تر» گرفت. دستگیره‌ی `SlimSlider` گرادینتِ شعاعی + هایلایتِ نورِ کوچیک (حسِ
    گویِ جلاخورده)، اندازه‌ی پایه کوچیک‌تر، حرکت با spring به‌جای tween، و لرزشِ ویبره‌ی اشتراکی دقیقاً
    لحظه‌ی شروعِ کشیدن.
  - **ویبره‌ی واقعی** (`ui/haptics/Haptics.kt`, مجوز `VIBRATE`): تا الان فقط هپتیکِ ظریفِ Compose
    (`TextHandleMove`) بود که رو خیلی گوشی‌ها اصلاً حس نمی‌شه؛ حالا `rememberBuzz()` یه لرزشِ واقعی
    (`VibrationEffect.createOneShot`) می‌ده، تو `pressScaleClickable` و `GradientButton` و شروعِ کشیدنِ
    `SlimSlider` صدا زده می‌شه - **ویژگی اشتراکیه**: فقط اگه `subscribed=true` باشه واقعاً لرزش رخ می‌ده؛
    تنظیمات یه سوییچ «ویبره» داره که برای غیرمشترک قفله (تپ روش پیشنهاد اشتراک می‌ده).
  - **تمِ طلایی** (`ui/theme/Color.kt`+`Theme.kt`+`ThemeViewModel.kt`): `darkTheme: Boolean` جاش رو به
    `ThemeMode { LIGHT, DARK, GOLD }` سه‌حالته داد (`UiPrefs.themeMode`، با مهاجرتِ خودکار از کلید
    بولینِ قدیمی). آیکونِ تمِ بالای صفحه حالا بین هر سه می‌چرخه، ولی **تاریک/طلایی هر دو ویژگیِ
    اشتراکی‌ان** - تپش برای غیرمشترک یه دیالوگ «این ویژگی اشتراکیه» نشون می‌ده. تنظیمات هم یه کارتِ
    «تم» با ۳ چیپ داره (طلایی/تاریک قفل‌دار برای غیرمشترک).
  - **آیکونِ تبِ «سود سپرده»**: `Icons.Savings` (که رو گوشی شکلِ خوکِ قلک درمیاد - گزارش کاربر) با
    `Icons.TrendingUp` مینیمال‌تر عوض شد.
  - **منوی تنظیمات**: بالاش دیگه زیرِ نوارِ وضعیتِ گوشی گم نمی‌شه (`statusBarsPadding`)، تا ته
    قابل‌اسکرول شد (`verticalScroll`+`navigationBarsPadding` - قبلاً اصلاً اسکرول نداشت و کارت‌های
    پایینی مثل «امور چک» عملاً غیرقابل‌دسترس بودن، که به نظر می‌رسه ریشه‌ی واقعیِ «افزودن دسته‌چک رو
    نمی‌بینم» بوده)، هدر پروفایل برای مشترک‌ها یه گرادینتِ طلاییِ ظریف پشتش گرفت، و یه کارتِ «ارتقا به
    اشتراک» + دکمه‌ی «مشاهده پلن‌ها» برای کاربرهای واردشده‌ی غیرمشترک اضافه شد (قبلاً از تنظیمات راهی
    به صفحه‌ی خرید نبود).
  - **عکس رسیدِ مخصوصِ هر قسط** (نه یه عکسِ کلی رو کل وام): `LoanRepository.setRowPhoto/removeRowPhoto`
    یه `photoPath` رو خودِ هر ردیفِ قسط (تو JSON rows، بدون نیاز به migration) ذخیره می‌کنه؛
    `LoanDetailScreen` رو هر قسطِ **پرداخت‌شده** یه آیکونِ دوربین داره که یه دیالوگ با عنوانِ «وام: X —
    قسط شماره‌ی Y» و `PhotoAttachmentCard` باز می‌کنه - چون همیشه از رو یه ردیفِ مشخصِ همون وام باز
    می‌شه، «کدوم وام/کدوم قسط» خودش تضمین می‌شه (نیازی به دو تا دراپ‌داونِ انتخابِ جدا نبود).
  - **افزودن به تقویم**: باگِ واقعی پیدا شد - قبلاً فقط رو *یه* تقویم (اولین/primary) درج می‌شد، پس اگه
    گوشی هم تقویمِ محلی هم حسابِ گوگل داشت، تو یکی‌شون دیده نمی‌شد؛ `DeviceCalendarExporter` حالا رو
    *همه‌ی* تقویم‌های قابل‌نوشتنِ گوشی درج می‌کنه. پیامِ موفقیت هم از `Toast` سیستمی (که اندروید ۱۲+/
    بعضی رام‌ها آیکونِ اپ رو کنارش می‌چسبونن) به یه بنرِ داخلِ خودِ اپ عوض شد (هم‌الگو با هینتِ خروجِ
    MainActivity).
  - **صفحه‌ی ورود**: پس‌زمینه‌ی صریح گرفت (قبلاً نداشت، پس رنگِ تیره‌ی پیش‌فرضِ ویندو از زیرش رد می‌شد
    و همیشه مشکی دیده می‌شد، حتی تو تمِ روشن). فیلدِ شماره حالا پیشوندِ ثابتِ «+۹۸» داره و کاربر فقط
    ۱۰ رقمِ بعدش (بدونِ صفرِ اول) رو تایپ می‌کنه؛ فرمتِ ارسالی به سرور (`۰۹xxxxxxxxx`) دست‌نخورده موند.
  - **اسمِ اینترو**: «Powered By Sad Team» به «SADTeam» چسبیده عوض شد.
  - **دوره‌ی آزمایشیِ رایگانِ ۷روزه + امنیتِ اشتراک** (`server/src/subscriptionStatus.js`): کاربر
    خواست کاربرهایی که با شماره موبایل وارد می‌شن یه هفته کامل رایگان دسترسی داشته باشن، بعدش دقیقاً
    مثلِ غیرمشترک محدود بشن، بدونِ هیچ راهِ دور زدن. `isSubscribed()` حالا علاوه بر `subscribed`/
    `subscribed_until`، اگه هنوز تو ۷ روزِ اولِ بعد از **`created_at`** ردیفِ `users` باشیم هم true
    برمی‌گردونه؛ `created_at` فقط یه‌بار (موقعِ اولین `verify-otp` موفقِ همون شماره) ست می‌شه و هیچ‌جای
    دیگه‌ای دست‌کاری نمی‌شه - یعنی خروج از حساب، پاک‌کردنِ داده‌ی اپ، حذف/نصبِ دوباره، یا نصب رو گوشیِ
    دیگه، هیچ‌کدوم دوره‌ی آزمایشی رو ریست نمی‌کنن (فقط با شماره‌تلفنِ دیگه می‌شه یه دوره‌ی جدید گرفت -
    محدودیتِ ذاتیِ هر مدلِ «یه‌بار به‌ازای شماره»ست). اجرای واقعیِ محدودیت («۱ وام رایگان») همیشه سمتِ
    سرور (`routes/loans.js` PUT، ۴۰۳ اگه بیش از ۱ وام و `isSubscribed=false`) چک می‌شه، نه کلاینت -
    یعنی حتی اگه یه کلاینتِ دستکاری‌شده بگه «من مشترکم»، سرور مستقل خودش تصمیم می‌گیره. یه حفره‌ی
    واقعی هم پیدا و رفع شد: کلاینت هیچ‌وقت `GET /api/auth/me` رو صدا نمی‌زد (تو کامنتِ قبلی «فاز بعد»
    علامت خورده بود ولی هیچ‌وقت پیاده نشده بود)، پس وضعیتِ اشتراک/آزمایشیِ محلی می‌تونست کهنه بمونه؛
    حالا `AuthRepository.refreshSubscriptionStatus()` هر بار اپ باز می‌شه صدا زده می‌شه
    (`LoanCalcApp`). تنظیمات هم اگه دلیلِ مشترک‌بودن فقط دوره‌ی آزمایشی باشه، «چند روز مونده» رو
    نشون می‌ده.
  - **امکاناتِ اشتراکی** (`ui/onboarding/BenefitsScreen.kt`): لیست به ذخیره‌ی نامحدودِ چک/دسته‌چک،
    پشتیبان‌گیریِ ابریِ کاملِ وام‌ها، تمِ تاریک/طلایی، ویبره، و ارسالِ بازخورد گسترش پیدا کرد، با
    لحنِ رسمی‌تر.
  - **بررسیِ «چرا سوالِ مجوزِ باتری/نوتیف بعد از پاک‌کردنِ داده دوباره نمیاد» (بدونِ نیاز به تغییرِ
    کد)**: `PermissionGateScreen` وضعیتِ *واقعیِ* اجازه‌ی سیستم رو چک می‌کنه
    (`ContextCompat.checkSelfPermission`/`PowerManager.isIgnoringBatteryOptimizations`)، نه یه فلگِ
    محلی؛ این مجوزها به UID اپ گره خوردن و پاک‌کردنِ داده‌ی اپ (برخلافِ حذف/نصبِ دوباره) پاکشون
    نمی‌کنه. یعنی این یه باگ نیست - گیت درست تشخیص می‌ده مجوزها از قبل هست و خودش رو skip می‌کنه.
  - **موارد بازمانده برای دورِ بعد** (نیاز به منبعِ بیشتر یا تست رو سرورِ واقعی دارن): استایلِ
    الهام‌گرفته از اپ Digipay (لینکِ اپ تو پیامِ کاربر واقعاً نیومد - نیاز به لینک/اسکرین‌شاتِ واقعی)،
    بازطراحیِ کاملِ بخشِ چک/استعلامِ صیاد (فعلاً placeholder)، راه‌اندازیِ واقعیِ ملی‌پیامک برای OTP
    (منتظرِ توکنِ کنسولِ کاربر)، و تستِ زنده‌ی سرور/اشتراک رو VPS (این سندباکس بهش SSH نداره).

## محدودیت مهم محیط توسعه

این پروژه تو سندباکس فعلی (که `dl.google.com` توش مسدوده) قابل build نیست چون Android Gradle
Plugin و همه‌ی کتابخونه‌های androidx/Compose فقط رو Google's Maven میزبانی می‌شن. برای همین:
- کد به‌صورت دستی و دقیق review شده ولی توسط کامپایلر واقعی تأیید نشده
- یه workflow جدا (`.github/workflows/build-native-android.yml`) اضافه شده که رو گیت‌هاب اکشن
  (که اینترنت کامل داره) واقعاً build و تست می‌کنه — نتیجه‌ی واقعی رو از اونجا ببین

## اجرای تست‌های موتور محاسبه (بدون نیاز به اندروید)

```bash
cd native-android
./gradlew :core:test
```

## ساخت APK دیباگ (نیاز به دسترسی به dl.google.com)

```bash
cd native-android
./gradlew :app:assembleDebug
```

(از این به بعد پروژه `gradlew` داره — دیگه نیازی به نصب جدا Gradle نیست. اگه محیطت هم مثل سندباکس فعلی
`dl.google.com`/`services.gradle.org` رو بلاک کرده، دانلود اولیه‌ی distribution هم fail می‌شه؛ رو یه
ماشین معمولی با اینترنت کامل (یا رو گیت‌هاب اکشن) مشکلی نداره.)
