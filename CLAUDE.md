## ⚠️ قانونِ اولِ همیشگی - کاربر فنی نیست، ساده توضیح بده
کاربرِ این پروژه پس‌زمینه‌ی فنی نداره. **بعدِ هر تغییرِ فنی (خصوصاً روی سرور/دیتابیس/زیرساخت)، خودت -
بدونِ اینکه بپرسه - یه خلاصه‌ی خیلی ساده بده**: چی عوض شد، چرا مهمه، خطری داره یا نه، کاربر باید
کاری بکنه یا نه. از اصطلاحاتِ فنی (migration، CI، deploy، Room…) خودداری کن یا در حدِ یه جمله
معنیش رو بگو. جزئیاتِ فنیِ این فایل برای خودِ توئه، نه چیزی که باید تحویل بدی. این قانون نتیجه‌ی
بازخوردِ صریحِ کاربره («چرا قبلاً بهم نگفتی») - جدی بگیرش.

## مصرفِ توکن (بدونِ تناقض با قانونِ بالا)
- قبل از نوشتن فایل رو بخون؛ اگه عوض نشده دوباره نخون. فایلِ بزرگ‌تر از ۱۰۰KB فقط در صورتِ لزوم.
- کوچیک‌ترین تغییرِ درست، نه بازنویسیِ اضافی. کامنتِ بدیهی ننویس.
- فایلِ گزارش/خلاصه/چنج‌لاگ (SUMMARY.md, NOTES.md…) خودسرانه نساز - فقط اگه صریح خواسته شد.
- **هیچ‌کدوم از این‌ها به کوتاه‌کردنِ توضیحِ سادهٔ فارسی به کاربر ربط نداره** - قانونِ بالا اولویت داره.

## الگوی کاریِ کاربر
معمولاً چند بازخوردِ پشتِ‌هم می‌فرسته و می‌گه **«ذخیره کن»** - یعنی فقط ثبت کن، کد نزن. بعد می‌گه
**«همه رو انجام بده»**. وقتی گفت «ذخیره کن»، فقط تسک ثبت کن.
**وقتی طرحِ مرجعِ کاربر یه رندرِ سه‌بعدی/نوریِ پیچیده‌ست** (نه چیدمانِ تخت)، از همون اول بپرس
می‌شه خودِ تصویر رو مستقیم استفاده کرد - سه دورِ بازسازیِ دستیِ اسپلش شکست خورد تا بالاخره خودِ عکس گذاشته شد.

---

## پروژه
اپِ اندرویدِ فارسی برای کافه‌بازار/مایکت، تیمِ «Sad Team». **نامِ نمایشی: «جیبک»**
(۲۱ مرداد از «حسابدار من» عوض شد - `applicationId` عمداً دست‌نخورده موند).
`applicationId` همیشه `ir.sadteam.loancalc` - **هیچ‌وقت عوضش نکن** (وگرنه از دیدِ استورها اپِ جدیده
و همه‌ی نصب‌ها/نظرها می‌پره). عنوانِ صفحه‌ی استور مستقله، کاربر باید دستی تو پنل عوضش کنه.

اپ اولش محاسبه‌گرِ اقساطِ وام بود، الان یه اپِ حسابداریِ شخصیِ کامله (دخل/خرج، بودجه، دارایی،
گزارش، طلب‌وبدهی)؛ وام/چک هم بخشی از همونن. **۱۰۰٪ Kotlin** (اپِ وبِ `www/` کاملاً حذف شد):
- **کلاینت**: `native-android/` - Kotlin + Compose، چندماژوله: `:app` (UI)، `:data` (Room +
  SQLCipher، DataStore، Retrofit)، `:core` (محاسباتِ خالص، بدونِ وابستگیِ اندرویدی)، Hilt،
  Navigation-Compose. جزئیاتِ فیچرها تو `native-android/README.md`.
- **بک‌اند**: `server/` - Kotlin + Ktor + SQLite، ورودِ اجباری با OTP، سینکِ ابری، اشتراکِ
  کافه‌بازار/مایکت. جزئیات تو `server/README.md`.

**سندباکس نمی‌تونه `native-android` رو بیلد کنه** (`dl.google.com` مسدوده) - تنها راهِ تایید،
اجرای `.github/workflows/build-native-android.yml`ه، نه حدس‌زدن از رو کد. `server/` همین‌جا بیلد/تست می‌شه.

**۵ تبِ نوارِ پایین**: خانه / دارایی / گزارش / بودجه / سررسید. «وام» و «چک» تب نیستن - از
«سررسید»/«خانه» به‌عنوانِ صفحه‌ی پوش‌شده باز می‌شن. تبِ وام زیرناوبریِ داخلی داره
(بانکی/محاسبه‌گر/سپرده/وام‌های من). ماژولِ «امور چک» کامله (پیوستِ عکس، دسته‌چک + شناسه‌ی صیادی،
استعلام، یادآوری، بایگانی، بکاپ) - **قبل از شروعِ هر فیچرِ «باقی‌مونده» اول کدِ واقعی رو چک کن**؛
یه‌بار همین لیست اشتباهاً «پیاده‌نشده» علامت خورده بود در حالی که کامل بود.

قفلِ امنیتی PIN+اثرِ انگشت پیاده‌ست (`ui/security/LockScreen.kt`, `AppLockViewModel.kt`, با
`BiometricPrompt` - به همین خاطر `MainActivity` از `ComponentActivity` به `FragmentActivity` عوض شده).

---

## سرور (VPS آروان‌کلاود - زنده)
- IP `37.152.187.44`، دامنه `server-8a887fd3-2459-4e6d-ace8-884dc36ca3db.ir-thr-fr1.arvancompute.ir`
  (HTTPS با certbot، تمدیدِ خودکار). nginx رو ۸۰/۴۴۳ به `localhost:3000` پراکسی می‌کنه
  (`/etc/nginx/sites-available/loan-api`).
- جار تو `~/VameMan/` با pm2 (`loan-calc-api`). `.env` کنارِ jar. (پوشه قبلاً `~/loan-server` بود.)
- کد: `server/src/main/kotlin/ir/sadteam/loancalc/server/` - `Application.kt`, `Db.kt` (اسکیما +
  مایگریشن), `Auth.kt`, `Sms.kt`, `SubscriptionStatus.kt`, `Cafebazaar.kt`, `Myket.kt`,
  `routes/{Auth,Loans,Subscription,Crash,Backup,CreditRates,AppVersion}Routes.kt`.
- **دیپلویِ خودکار**: هر پوشِ `main` که `server/**` رو عوض کنه → `build-server.yml` بیلد/ارسال/چکِ
  سلامت/جایگزینی (بکاپِ `.bak`)/`pm2 restart` + برگردوندنِ خودکار اگه بالا نیاد.
  **دستی jar آپلود نکن** - سه‌بار همین کار سرورِ زنده رو خوابونده.
- ورک‌فلوهای کمکی: `set-melipayamak-env.yml`، `set-cafebazaar-env.yml`، `check-server-health.yml`
  (این آخری ۵ کرشِ آخرِ `crash_reports` رو هم چاپ می‌کنه - برای دیباگِ کرشِ گوشیِ کاربر).
- `toUserRow()` ستون‌ها رو با اسم می‌خونه: **هر `SELECT ... FROM users` باید همه‌ی ستون‌ها (از جمله
  `subscription_tier`) رو داشته باشه** وگرنه `SQLException`.

### OTP — ✅ کامل و تست‌شده، کارِ باقی‌مونده نداره
ملی‌پیامک (SOAP، `SendByBaseNumber`)، پترنِ تاییدشده `bodyId=496763` که هشِ SMS Retriever
(`FuPsBGlTa0q`) تو خطِ آخرشه. کاربر با شماره‌ی واقعی تست کرد و رسید.
- مقادیرِ حساس عمداً اینجا نیستن (GitHub Secrets + `~/VameMan/.env`). برای تغییر: سکرت‌ها رو آپدیت
  کن و `set-melipayamak-env.yml` رو دستی اجرا کن.
- **درس**: این سرویس‌ها «رمزِ ورودِ پنل» و «APIKeyِ وبسرویس» جدا دارن. تو `MELIPAYAMAK_PASSWORD`
  حتماً اونی که برای **وبسرویس** نوشته شده - یه‌بار همین اشتباه «ارسالِ کد ناموفق» داد.
- تعویضِ سرویسِ پیامکی = فقط `Sms.kt` (`sendOtpSms`).
- **هشِ SMS Retriever** به بسته‌نام + کلیدِ امضا بستگی داره (دیباگ/ریلیز دو هشِ متفاوت).
  `ui/auth/SmsRetrieverHash.kt` هشِ واقعی رو لاگ می‌کنه (`adb logcat -s SmsRetrieverHash`).
  **کلیدِ امضای ریلیز عوض شد → هشِ جدید باید تو پترنِ ملی‌پیامک هم آپدیت بشه** (تایپِ دستی فال‌بکه).

### آپدیتِ خودکار
جدولِ تک‌ردیفه‌ی `app_version` (`routes/AppVersionRoutes.kt`, `GET /api/app-version`) آخرین
versionCode + لینکِ هر استور. **دستیه** - بعدِ هر انتشارِ واقعی رو VPS بزن:
```bash
sqlite3 ~/VameMan/data.sqlite \
  "UPDATE app_version SET latest_version_code = <شماره‌نسخه>,
   cafebazaar_url = 'https://cafebazaar.ir/app/ir.sadteam.loancalc',
   myket_url = 'https://myket.ir/app/ir.sadteam.loancalc' WHERE id = 1;"
```

---

## دستورهای واقعیِ بیلد/تست (تاییدشده از رو ورک‌فلوها، نه حدس)
هیچ‌کدوم از این‌ها تو سندباکس برای `native-android` کار نمی‌کنن (`dl.google.com` مسدوده) - این‌ها
همون چیزین که CI اجرا می‌کنه. `server/` رو ولی همین‌جا واقعاً می‌شه اجرا کرد.

**اپِ اندروید** (از `native-android/`):
```bash
./gradlew :core:test                          # تستِ موتورِ محاسبه (JVM، سریع)
./gradlew :app:testCafebazaarDebugUnitTest    # تستِ اسکرین‌شاتِ Paparazzi
./gradlew :app:assembleRelease                # هر دو فلیور (cafebazaar + myket)
./gradlew :data:connectedDebugAndroidTest     # تستِ migration - امولاتورِ واقعی لازم داره
```
**سرور** (از `server/`):
```bash
./gradlew test
./gradlew fatJar        # جارِ runnable که به VPS فرستاده می‌شه
```
**ساختارِ ریشه**: `native-android/` (اپ: `app`/`data`/`core`) · `server/` (Ktor) ·
`.github/workflows/` · `ci-debug.keystore` (کلیدِ ثابتِ دیباگ، پاکش نکن).

**ورک‌فلوها**: `build-native-android.yml` (بیلدِ APK + ریلیز) · `build-server.yml` (دیپلویِ خودکار)
· `instrumented-tests.yml` (امولاتور) · `record-paparazzi-snapshots.yml` (بازسازیِ عکس‌های مرجع،
دستی) · `publish-release.yml` · `set-*-env.yml` · `check-server-health.yml` · `check-recent-signups.yml`.

---

## 🔑 درس‌ها و قواعدِ فنیِ ماندگار (مهم‌ترین بخش - قبل از هر کارِ مرتبط بخون)

### دیتابیس و Migration (پرریسک‌ترین بخش)
- **🚨 دو منبعِ حقیقت برای یه عدد = فاجعه.** `LoanEntity` هم ستونِ `paidCount` داره هم یه کپی از
  همون عدد تو `dataJson`. `updateRowPayment` فقط ستون رو به‌روز می‌کرد، ولی `toWebMap` (چیزی که به
  **سرور و فایلِ پشتیبان** می‌ره) از `dataJson` می‌خوند → عددِ کهنه بیرون می‌رفت. محلی سالم دیده
  می‌شد، ولی بعدِ خروج (که محلی رو پاک می‌کنه) و ورودِ دوباره، پیشرفتِ **همه‌ی وام‌ها صفر** می‌شد و
  همه «عقب‌افتاده» می‌شدن. رفع: `withPaidCount()` هر دو رو با هم می‌نویسه، `toWebMap`/`fromWebMap`
  از رو خودِ `loan_rows` می‌شمرن، و `repairPaidCounts()` داده‌ی خرابِ قبلی رو خودش ترمیم می‌کنه.
  **درس: هر عددی که هم ستون داره هم تو یه blobِ JSON کپی شده، باید یه نقطه‌ی نوشتنِ واحد داشته باشه.**
- **هر `CREATE INDEX` تو migrationِ دستی باید عیناً تو `indices = [...]`ِ همون `@Entity` هم اعلام بشه.**
  یه‌بار نشد و نسخه‌ی ۱.۰.۳۱۵ برای هر کسی که آپدیت می‌کرد کرش می‌کرد
  (`IllegalStateException: Migration didn't properly handle`). فقط مسیرِ آپگرید - نصبِ تازه این باگ رو نمی‌گیره.
- **هر migrationِ جدید باید تستِ `migrateN` تو `data/src/androidTest/.../MigrationTest.kt` بگیره**
  (الگو تو کامنتِ همون فایل). `instrumented-tests.yml` امولاتورِ واقعی بالا میاره؛ اگه با
  «Not enough space» شکست خورد، `-partition-size` رو بیشتر کن (نه دنبالِ باگِ KVM بگرد).
  ⚠️ **محدودیتِ فعلی**: `data/schemas/` فقط `12.json` رو داره، پس migrationهای بعدی فعلاً
  قابلِ‌تستِ خودکار نیستن (حالتِ «قبل» در دسترس نیست).
- دیتابیس `net.sqlcipher`ه نه SQLiteِ استاندارد → تستِ migration رو JVM ممکن نیست، امولاتور لازمه.
- `exportSchema=true`؛ اسکیمای هر نسخه تو `native-android/data/schemas/`.
- ردیف‌های قسط تو جدولِ جدای `loan_rows`. `LoanRepository.getRows()` **suspend**ه - هیچ‌وقت مستقیم
  تو `remember{}` صداش نکن (کرشِ ترد اصلی)؛ الگوی درست: `var x by remember { mutableStateOf(...) }`
  + `LaunchedEffect(key) { x = ... }`. `getOrMigrateRows` فال‌بکِ خودترمیم‌شونده‌ست.
- `AccountRepository.addTransaction`: `id` پارامترِ اختیاریه - **هر جا تو حلقه تراکنش می‌سازی
  شمارنده‌ی صریح پاس بده** (قبلاً `System.currentTimeMillis()` بود و تو حلقه‌ی سریع `@Upsert`
  بی‌صدا تراکنش‌ها رو رو هم می‌نوشت).

### تاریخِ فارسی (`core/PersianDate.kt`)
- `addDays`/`addMonths` **باید مسیرِ جدا برای عددِ منفی داشته باشن** - `repeat(n)` تو Kotlin برای
  nِ منفی بی‌صدا هیچ‌کار نمی‌کنه (باگِ «روزِ قبل» و نمودارِ ۷روزه). تستِ رگرسیون داره.
- برای سررسید، اگه `intervalDays % 30 == 0` از `addMonths` استفاده کن نه `addDays` (تقویمِ فارسی
  ماهِ یکسانِ ۳۰روزه نداره) وگرنه روزِ سررسید می‌لغزه.
- `addMonths` برای clampِ روز از `JalaliCalendar.daysInMonth` استفاده می‌کنه (اسفندِ کبیسه).
- **«تاریخ دریافت وام» خودش سررسیدِ قسطِ اوله** (تصمیمِ صریحِ کاربر). استثنا: وام‌های دارای دوره‌ی
  تنفس. تاریخ‌ها ذخیره نمی‌شن، پویا حساب می‌شن.

### فرمول‌های حساسِ محاسبه (`core/LoanCalculator.kt`)
- وام‌های عادی (نرخ > ۴٪): اقساط مساوی نزولی، i = نرخ سالانه/100 × (فاصله هر قسط به روز/365).
- **قرض‌الحسنه (≤ ۴٪)**: سودِ مرکب ندارن. قسط‌های ۱، ۱۳، ۲۵، ۳۷… (ابتدای هر سال) فقط کارمزدِ
  سالانه‌ان (روی مانده‌ی همون لحظه) و اصلِ وام توشون نیست؛ اصل فقط بین `n − ⌈n/12⌉` قسطِ دیگه
  مساوی تقسیم می‌شه. تو `LoanMethod.QARZ`، با یه اپِ مرجعِ decompile‌شده تطبیق داده شده -
  **تغییرش نده مگر با تستِ دقیق** (`LoanCalculatorRegressionTest.kt`) رو چند مثالِ واقعی.

### Compose - قواعدی که نقضشون باگ داده
- **توکن‌های رنگ (`AppPrimary`، `AppText`…) `@Composable`ان** - صداکردنشون داخلِ بلوکِ `Canvas`
  (که `DrawScope`ه) خطای کامپایل می‌ده. همیشه قبل از `Canvas` تو یه `val` محلی بخونشون.
- **هیچ کارتی رو دستی با `clip/background/border` نساز - همیشه `AppCard`.** رنگِ برجسته لازم داری؟
  پارامترِ `accentGradient`. `AppCard` داخلاً `fillMaxWidth()` می‌زنه ولی `Modifier.width(X)`
  قبلش constraint رو دقیق می‌کنه، پس عرضِ ثابت کار می‌کنه.
- `AppCard` محتوا رو تو `CompositionLocalProvider(LocalContentColor provides AppText)` می‌پیچه -
  **`Text`ِ بدونِ `color`ِ صریح که نامرئی شد، اول چک کن بیرونِ `AppCard` نباشه.**
- **`LazyVerticalGrid` داخلِ لیستِ تنبلِ دیگه** نمی‌تونه wrap-content باشه - به‌جاش
  `items.chunked(n).forEach { Row { ... } }` با `Spacer(weight)` برای ردیفِ ناقص.
- فیلدهای مبلغ: **هیچ‌وقت داخلِ `onValueChange` دوباره فرمت نکن** (cursor به‌هم می‌ریزه). state فقط
  رقمِ خام، فرمت فقط بصری با `ThousandsSeparatorTransformation`.
- **الگوی استانداردِ فیلدِ مبلغ**: `ThousandsSeparatorTransformation()` + `suffix = { Text("ریال") }`
  + کپشنِ `numberToWordsFa(amountRial / 10) + " تومان"` + `cleanNum` (نه `cleanNumDecimal`) + `KeyboardType.Number`.
- **🚨 واحد: دیتابیس همه‌جا ریال، نمایش همه‌جا تومان.** تبدیل **فقط** رو لبه‌ی UI با
  `tomanToRial`/`rialToToman`. چاپِ مستقیمِ مقدارِ خامِ دیتابیس با برچسبِ «تومان» یعنی عددِ
  ۱۰برابر - همین باگ تو `AccountDetailScreen` رخ داد و کاربر با اسکرین‌شات گزارشش کرد.
- **حالتِ خصوصی**: هر جای تازه‌ای که مبلغ نشون می‌ده باید از `LocalPrivacyMode.current` +
  `maskIfPrivate(...)` رد بشه (۱۸۱ جای مصرف - الگو رو از همون‌ها بردار، سیستمِ موازی نساز).
- **دو تابعِ هم‌نامِ `toFa`**: `core.toFa(x)` سراسریه (`toFa(x)`)، `ui.jibak.toFa()` اکستنشنه
  (`x.toFa()`). جابه‌جا نوشتنشون بیلد رو می‌شکنه (بیلدِ ۵۰۷). بررسیِ `callstyle.py` می‌گیردش.
- **RTL**: اپ globally RTLه؛ فیلدهای ذاتاً چپ‌به‌راست (موبایل، PIN، شماره‌چک، شناسه‌ی صیادی،
  شماره‌ی دسته‌چک، شناسه‌ی ملی، سرشماره‌ی پیامک) باید تو `ui/components/Ltr.kt` پیچیده بشن.
- **اسکرول‌بار تو RTL**: `contentPadding = PaddingValues(start = 10.dp)` وگرنه رو محتوا می‌افته.
- `SwipeToDeleteRow`: اگه بعدش دیالوگِ تایید باز می‌کنی حتماً `confirmDismiss = false`.
- `PrivacyCrossfade(privacyMode)` رو **کوچیک‌ترین جزئی** که مبلغ رو نشون می‌ده بپیچ؛ اگه تو
  `Row/Column` با `weight` می‌شینه، `weight` رو رو خودِ `PrivacyCrossfade` بذار.
- اسنپ‌شات از UI: `rememberGraphicsLayer()`، **نه `View.drawToBitmap()`** (رو بعضی گوشی‌ها خالی می‌ده).
- انتقالِ یه Composable به فایلِ جدید: **ایمپورت‌هاش خودکار نمیان** - چند بار CI رو شکسته.

### کامپوننت‌های مشترک (قبل از ساختنِ چیزِ جدید اول این‌ها رو ببین)
`AppCard` (+`accentGradient`) · `GradientButton` · `AppChip` · `SegmentedToggle` (کپسولِ لغزنده،
`Modifier.offset` پس RTL-آگاه) · `EmptyState(icon, title, description)` · `SwipeToDeleteRow` ·
`ConfirmDeleteDialog` · `AccountPickerDialog` · `BankBadge` · `TodayCard` (مشترکِ خانه/سررسید) ·
`SuccessCheckmarkOverlay` · `StaggerIn` · `Motion.kt` (`standard/snappy/heavy`) ·
`pressScaleClickable` · `Ltr.kt` · `ThousandsTransformation.kt` · `InAppBannerHost`/
`rememberInAppBanner` (بنرِ داخلی، نه Toast) · `FullScreenDialog` (پوششِ زیرصفحه‌های تنظیمات -
وگرنه تو عرضِ ۸۵٪ پنل گیر می‌کنن).

### تم و رنگ
- همه‌ی رنگ‌ها از `ui/theme/Color.kt` (`DarkAppColors`/`LightAppColors`)؛ `line`/`glass*` از primary
  مشتق می‌شن و باید هم‌قدم آپدیت بشن. شدتِ پس‌زمینه: `primaryAlpha`/`accentAlpha` تو `AuroraBackground.kt`.
- **طلایی (`AppAccent`) فقط نشانه‌ی پرمیوم/اشتراکه** (بج‌ها، `GoldSheenBox`) - رو حاشیه‌ی دکمه‌های
  معمولی نه (یه‌بار شد و کاربر گفت ناهماهنگه).
- پروژه عمداً **بلورِ واقعی (`RenderEffect`) نداره** - همه‌جا با گرادیانِ ثابت تقلید می‌شه.
- قفلِ اسکرین‌شات (`FLAG_SECURE`) عمداً حذف شده (تصمیمِ صریحِ کاربر).
- `ci-debug.keystore` یه کلیدِ امضای دیباگِ **ثابت** (نه سکرت) تو `app/build.gradle.kts`ه تا نصبِ
  هر بیلدِ جدید خطای «conflicts with an existing package» نده - **پاکش نکن**.
- **فونتِ سراسری Vazirmatn** (`app/src/main/res/font/`) - موقعِ ساختِ تصویر با PIL هم همین فایل.

### ساختِ تصویر با Python/PIL
- **متنِ فارسی**: `ImageFont.truetype(..., layout_engine=ImageFont.Layout.RAQM)` +
  `draw.text(..., direction="rtl", language="fa")`. (`arabic_reshaper`+`bidi` با `draw.text()`
  معمولی حروف رو نچسبیده/برعکس می‌ده.)
- **آیکون از تصویرِ رندرشده**: اول با اسکنِ پیکسل باکسِ سفتِ محتوا رو پیدا کن و کراپ کن - به چشم
  اعتماد نکن (یه‌بار ~۱۰٪ حاشیه‌ی مشکی جا موند و کاربر گزارشش داد).
- **JPEG در برابرِ PNG**: تصویرِ بزرگِ بدونِ شفافیت → JPEG (`quality=92, optimize=True`) حدودِ
  یک‌هفتمِ PNG. آیکون‌های لانچر (نیازمندِ شفافیت) PNG بمونن.

### گیت / CI
- **⛔ هر پوش یه بیلد نسازه - سهمیه‌ی ماهانه‌ی Actionsِ کاربر محدوده.** خواسته‌ی صریحِ کاربر
  (۲۰ مرداد): «هر بار بیلد نساز، همه‌ی تغییرات رو انجام بده در آخر بساز.» بحثِ **دقیقه‌ی ماهانه**ست،
  نه فضای انباشته - پس پاک‌کردنِ Releaseهای قدیمی **هیچ کمکی نمی‌کنه** (اشتباهِ اولیه‌ی من).
  هر رانِ اندروید ≈ **۱۵ دقیقه**.
- **🚨 فیلترِ `paths:` رو ایونتِ `pull_request` بی‌اثره.** گیت‌هاب برای PR کلِ دیفِ PR نسبت به base
  رو نگاه می‌کنه، نه فقط آخرین پوش. چون PR #67 صدها فایلِ `native-android/**` داره، **حتی یه کامیتِ
  فقط-CLAUDE.md هم یه بیلدِ ۱۵دقیقه‌ای راه می‌ندازه** (تاییدشده: رانِ ffd5fa7 و 91319c5 هر دو
  فقط مستند بودن ولی کامل بیلد شدن). پس نمی‌شه به فیلترِ مسیر تکیه کرد.
- **راهِ درست: تو *همه‌ی* کامیت‌های میانی `[skip ci]` بذار** (رو `push` و `pull_request` هر دو کار
  می‌کنه)، و فقط آخرِ کار یه‌بار **دستی با `workflow_dispatch`** بیلد بگیر.
- **قبل از اینکه برنچی رو «زائد» فرض کنی، `git log origin/main..origin/<branch>` و PRهای بازش رو
  چک کن.** یه‌بار همین‌جا نوشته شد «فلان برنچ merge‌شده» در حالی که ۲۱ کامیتِ منحصربه‌فرد داشت.
- برنچِ جدیدِ داوطلبانه نساز؛ از `main` شروع کن - مگر هارنسِ CCR برنچِ مشخصی رو اجباری کرده باشه.
- APKها به‌عنوانِ **GitHub Release** (تگِ `build-<شماره‌ران>`) منتشر می‌شن، نه Artifact (سهمیه پر می‌شد).
- versionCode/versionName خودکار از `github.run_number`.
- تو `reactivecircus/android-emulator-runner`، **هر خطِ `script:` با یه `sh -c` جدا اجرا می‌شه** -
  `if/then/fi` چندخطی از هم می‌پاشه.
- تستِ اسکرین‌شاتِ Paparazzi (`app/src/test/.../ScreenshotTest.kt`) رو JVMه؛ برای تستِ جدید یه
  Composableِ **بدونِ Hilt** انتخاب کن (مثلِ `AppCard`).
- **`:core` فقط `kotlin("test")` رو کلاس‌پثِ تستش داره، نه JUnit4** - تو تست‌های `:core` از
  `kotlin.test.*` ایمپورت کن نه `org.junit.*` (یه‌بار CI رو شکست).
- **قبل از فرضِ رفتارِ یه کتابخونه‌ی خارجی بر اساسِ شباهت با کتابخونه‌ی مشابه، سورسِ واقعیش رو چک
  کن** - خصوصاً قبل از دست‌زدن به مسیرِ پرداخت (رجوع کن به حادثه‌ی مایکت پایین‌تر).

### هشدار درباره‌ی لوگوی بانک‌ها
موقعِ بررسیِ یه اپِ مرجعِ decompile‌شده معلوم شد کدِ پایه‌ی خیلی اولیه‌ی این پروژه حداقل یه لوگوی
اشتباه داشته (چیزی که «بانک سامان» نامیده می‌شد لوگوی بانکِ دیگه‌ای بود). داده تو `data/Banks.kt`
و لوگوها تو `res/drawable` (پکِ رسمیِ tejiran.ir). **یکی‌بودنِ بایت با یه فایلِ قدیمی ملاکِ درستیِ
اسم نیست** - همیشه با شکلِ واقعی و شناخته‌شده‌ی همون بانک تطبیق بده.

---

## استورها و اشتراک

### دو فلیور
`native-android/app/build.gradle.kts`: `cafebazaar` (Poolakey) و `myket` (کتابخونه‌ی رسمیِ
`com.github.myketstore:myket-billing-client`، الگوی کلاسیکِ IAB v3). هر فلیور
`SubscriptionManager`ِ خودش رو داره (`app/src/{cafebazaar,myket}/...`)؛ بقیه‌ی اپ فرقی نمی‌بینه.
مقادیرِ مانیفستِ کتابخونه‌ی مایکت تو `manifestPlaceholders` همون فلیور ست شدن.
- ۴ محصولِ اشتراک (`unlimited_loans_1m/3m/6m/1y`؛ ۳۰۰۰۰۰/۸۱۰۰۰۰/۱۴۴۰۰۰۰/۲۵۲۰۰۰۰ ریال) رو هر دو پنل.
- `MYKET_IAB_PUBLIC_KEY` مقدارِ واقعیه (تو `app/src/myket/.../SubscriptionManager.kt`).
- صحت‌سنجیِ سمتِ سرورِ مایکت: `server/.../Myket.kt` (`validateMyketPurchase`) - توکنِ ثابت
  `MYKET_ACCESS_TOKEN` تو `.env`. کلاینت `store` (`BuildConfig.FLAVOR`) رو تو
  `POST /api/subscription/verify` می‌فرسته؛ نسخه‌های قدیمی پیش‌فرض `cafebazaar`.
  ⚠️ فرمتِ ریسپانسِ موفقِ مایکت مستند نبود - فعلاً فقط بر اساسِ کدِ HTTP (۲xx). بعدِ اولین خریدِ
  واقعی لاگِ سرور رو چک کن.
- **مایکت `ITEM_TYPE_INAPP` می‌خواد نه `ITEM_TYPE_SUBS`** (پنلش اصلاً «نوعِ محصول» نداره).
- Play Protect: اولین نصبِ APKِ مایکت (کلیدِ امضای تازه) «Blocked by Play Protect» می‌ده - طبیعیه.
  فرمِ Google «Play Protect Appeals» تو ۲۷ تیر ۱۴۰۵ برای رانِ ۲۵۹ ارسال شد
  (SHA256 `d23a8ef52ee330ad18dca043e42bdda4477923ff7b8507f0a1e83c5311d2d4ad`). **تا وقتی نتیجه
  معلوم نشده applicationId و کلیدِ امضای release رو عوض نکن.**

### 🚨 حادثه‌ی خریدِ گیرکرده‌ی مایکت (مرداد ۱۴۰۵) - درسِ مهم
بازبینِ مایکت گفت «بعدِ پرداختِ موفق، محصول فعال نمی‌شه».
- **تشخیصِ اولیه اشتباه بود** (کامیتِ `b00d146`، برگردونده شد با `8a7d71d`): فرض شد IabHelperِ
  مایکت مثلِ IabHelperِ کلاسیکِ گوگل `handleActivityResult` داره - **نداره و اصلاً کامپایل نشد**.
- **مکانیزمِ واقعی** (تاییدشده از سورس): `launchPurchaseFlow` نتیجه رو از طریقِ
  `ProxyBillingActivity`ِ داخلیِ خودِ کتابخونه می‌گیره - مستقل از `MainActivity.onActivityResult`.
  یعنی نسخه‌ی اصلیِ no-op درست بوده.
- **علتِ محتمل**: کشته‌شدنِ پروسه‌ی اپ تو پس‌زمینه حینِ پرداخت → از بین رفتنِ listener. محدودیتِ
  شناخته‌شده‌ی همه‌ی الگوهای «پرداخت با ریدایرکت» رو اندروید.
- **رفعِ عملی (پابرجا، کامیتِ `c5725c9`)**: `SubscriptionManager.restorePurchases()` (فلیورِ myket)
  که هر بار `SubscriptionScreen` باز می‌شه از رو inventory چک می‌کنه کاربر مالکِ چه SKUهاییه و
  بی‌صدا برای تاییدِ سرور می‌فرسته. مستقل از علتِ دقیقِ گم‌شدنِ callback کار می‌کنه.
  فلیورِ cafebazaar یه `restorePurchases()` no-op گرفت (Poolakey از ActivityResultRegistry
  استفاده می‌کنه و این ریسک رو کمتر داره).
- **⏳ باقی‌مونده**: یه خریدِ واقعیِ تازه رو گوشیِ واقعی تست بشه - هم مسیرِ اصلی هم restorePurchases.

---

## 🎨 قاعده‌های طراحی (از `design/README.md` و `design/PROMPT-for-claude-code.md`)

**فایلِ طراحی حالا داخلِ خودِ مخزنه: پوشه‌ی `design/`.** دیگه لازم نیست کاربر هر نشست
دوباره بفرستدش. `design/TOKENS.md` نگاشتِ هر مقدارِ طرح به توکنِ `ui/theme`ه.

- مقادیر را از attributeهای `style` در فایلِ HTML بردار. **حدس نزن، گرد نکن، «بهتر» نکن.**
- هیچ رنگی که در `design/سیستمِ طراحی جیبک.dc.html` نیست استفاده نشود.
- سایه‌ها همه سخت‌اند: `0 4px 0` هم‌رنگِ تیره‌ترِ خودش. هیچ blur، هیچ سایه‌ی رنگی، هیچ
  گرادیانِ تزئینی.
- تمِ تیره فقط جابه‌جاییِ توکن است. هیچ صفحه‌ای دو بار چیده نمی‌شود.
- عددها فارسی با جداکننده‌ی `٬` ؛ منفی با `−` نه پرانتز.
- راست‌به‌چپ: عنصرِ اول همیشه راست است. فلشِ «قبلی» سمتِ راست، «بعدی» سمتِ چپ.
- کوچک‌ترین هدفِ لمسی ۴۴dp.
- آیکون‌های داخلِ ماک‌آپ placeholder هستند — از آیکونِ Materialِ خودِ برنامه استفاده کن.
- **🚨 قبل از اعمالِ هر بسته‌ی طراح، `git diff` رو فایل‌های مشترک بخون.** بسته‌ی `home-final`
  یه دیالوگِ تاییدِ پرداخت رو که همون روز اضافه شده بود بی‌صدا پاک کرده بود - یعنی درست همون
  چیزی که کاربر شکایتش رو کرده بود برگشت. کپیِ کورِ فایل ممنوع.
- **بسته‌ی طراح می‌تونه دو فایلِ هم‌محتوا داشته باشه** (`CoinEconomy.kt` + `CoinRules.kt` هر دو
  یه تایپ رو اعلان می‌کردن → ~۴۰ خطا، بیلدِ ۵۱۲). خودِ READMEِ بسته منبعِ حقیقته.
- **هر تغییرِ صفحه در یک commit جدا؛ اسمِ commit شماره‌ی فریم را داشته باشد:**
  `redesign(15a): home normal state`

### 🚨 درسِ گران‌قیمتِ ۳ شهریور - چرا دورِ اول شکست خورد
سیستمِ طراحی (رنگ/کارت/دکمه/سایه) کامل پیاده شد، ولی **فریم‌ها کارت‌به‌کارت باز نشدن**.
نتیجه: اسکلتِ اپِ قدیمی سرِ جاش موند و فقط لباسِ نو پوشید - کاربر گفت «طراحی صفحات مثل
برنامه‌ی قبلیه، فقط چهارتا آیتم اضافه شده» و **حق داشت**.

**جوابِ ابهام‌ها**: شش ابهامی که سرِ راه پیدا شد، تو `design/ANSWERS-section-37.md` جواب گرفتن
(درِ ورودیِ تنظیمات، قرصِ تبِ فعال، جای کلیدِ حالتِ خصوصی، رنگِ ریلِ بودجه، قاعده‌ی کارتِ کشف،
و هدفِ پس‌انداز). فریم‌های `37a`..`37d` هم برای همین‌ها اضافه شدن. **قبل از زدنِ دارایی/گزارش
اون فایل رو بخون** - سه تا از جواب‌ها دقیقاً به همون دو صفحه می‌خورن.

**قاعده‌ی جایگزین:** قبل از دست‌زدن به هر صفحه، اول با
`python3 native-android/tools/static-checks/design-frames.py "design/Duolingo Redesign.dc.html" 15a`
لیستِ کارت‌های همون فریم رو دربیار، بعد کد بزن. یک‌جا «همه‌ی برنامه رو بازطراحی کن» ممنوعه.

---

## 🔄 تصمیم‌های خودم که کاربر **می‌تونه بخواد عوض بشه** (خواسته‌ی صریحش: «یادم بنداز»)

خواسته‌ی صریحِ کاربر (۹ شهریور): «این تصمیماتی که گفتی یادت بمونه، اگه خواستم اصلاح کنم
یادآوری کن.» یعنی این‌ها **قفل نیستن** - برخلافِ بخشِ بعدی. هر وقت کاربر گفت «بریم مرور کنیم»
یا سراغِ همون صفحه رفت، **خودت این‌ها رو یادش بنداز** و بپرس بمونه یا عوض بشه.

| # | تصمیم | چرا این‌جوری شد | برگردوندنش |
|---|---|---|---|
| ۱ | **پرداختِ قسط فقط تو نمای کاملِ اقساط** - پیش‌نمایشِ سه‌ردیفه‌ی `27b` دکمه‌ی پرداخت نداره | تصمیمِ صریحِ خودِ فریم (`36i`): تپِ اشتباه رو ردیفِ ریز راحت رخ می‌ده و پس‌گرفتنِ «پرداخت شد» سخته | `InstallmentsPreviewCard` - ردیف‌ها رو کلیک‌پذیر کن |
| ۲ | **نوارِ قسطِ عقب‌افتاده خودش پرداخت نمی‌کنه**، فقط می‌بره رو همون ردیف | استثنای خودِ `36i` | همون‌جا، `onSeeAll` رو به پرداختِ مستقیم عوض کن |
| ۳ | **متنِ «تسویه‌ی زودتر» تخمینیه** («حدودِ …») | مبلغِ تکِ قسط‌ها دستی قابلِ‌ویرایشه ولی مانده‌ی اصل از فرمولِ اولیه میاد | `EarlySettlementCard` - اگه دقتِ بیشتر خواستی باید تفکیکِ اصل/سود هم تو `loan_rows` ذخیره بشه |
| ۴ | **کارتِ تسویه‌ی زودتر دکمه نداره** | تسویه‌ی واقعی کارِ بانکه نه اپ | افزودنِ دکمه‌ی «تماس با بانک»/«یادداشت» |
| ۵ | **ردیفِ «سود» و کلِ کارتِ تسویه برای وامِ بی‌نرخ نشون داده نمی‌شن** | «۰٪» گمراه‌کننده‌ست | شرطِ `ratePct > 0` رو بردار |
| ۶ | **قیمتِ دارایی = `sell_price`** (نه `buy_price`) | یعنی «الان می‌تونی به این قیمت بفروشی» - همون چیزی که ارزشِ داراییِ کاربره | `PriceService.parseRaw` |
| ۷ | **تازه‌سازیِ قیمت هر ۲۹ دقیقه** (۴۹ بار در روز از سقفِ **۵۰**) | ۱۴۴۰÷۵۰=۲۸٫۸ → ۲۹ دقیقه، یکی ذخیره؛ با `staleThresholdMinutes: 30`ِ خودِ servix هم جوره | `REFRESH_INTERVAL` تو `PriceService` |
| ۸ | **نگاشتِ نمادها سمتِ سروره نه تو اپ** | اصلاحش با یه دیپلویِ سرور می‌شه، بدونِ آپدیتِ اپ | `SYMBOL_ALIASES` |
| ۹ | **تاریخچه‌ی قیمت از اسنپ‌شاتِ خودمونه، نه endpointِ تاریخچه‌ی سرویس** | سهمیه‌ی اضافه نمی‌خواد و ساختارش تاییدشده‌ست · ⚠️ **عیبش**: گذشته نداریم، از روزِ راه‌اندازی پر می‌شه | اگه گذشته لازم شد، `GET /api/prices/history`ِ خودِ سرویس رو هم بزن |
| ۱۰ | **ترمیمِ زنجیر: ۱۰۰ سکه · فقط تا ۴۸ ساعت · ماهی یک‌بار** | تا «جبرانِ یه لغزش» بمونه نه «خریدنِ گذشته» | `Reward.STREAK_REPAIR` و `ActiveStreak.repairable` |
| ۱۱ | **نشانِ «هدفِ پس‌انداز» = «به‌زودی»** | خودِ قابلیتِ هدفِ پس‌انداز تو اپ وجود نداره | بعدِ ساختِ اون قابلیت، `Badge.comingSoon` رو بردار |
| ۱۲ | **نشان‌های گذشته صامت باز می‌شن + یه شیتِ جمع‌بندی** | نُه جشنِ پشت‌سرهم موقعِ اولین اجرا آزاردهنده‌ست | `syncBadges()` تو `GamificationViewModel` |

## تصمیماتِ محصولی که نباید دوباره بازشون کنی
- **حسابِ تست**: فقط `TEST_ACCOUNT_PHONE` کدِ ثابت می‌گیره؛ شماره‌ی واقعیِ کاربر همیشه کدِ تصادفی.
  کاربر ریسکش رو شنید و **صریحاً طراحیِ فعلی رو تایید کرد** - دوباره نپرس.
- **پشتیبان‌گیریِ دراپ‌باکس**: رد شد (اپ سیستمِ بکاپِ خودش رو داره).
- **خریدِ درون‌برنامه‌ای از سایت** (دورزدنِ استور): رد شد - ناقضِ قوانینِ استوره.
- **همگام‌سازیِ گذشته‌نگرِ وام/چک با حسابداری**: ساخته و بعد **کاملاً حذف شد** (`MIGRATION_17_18`
  تراکنش‌های `sourceType IN ('loan','cheque')` رو پاک می‌کنه). دلیل: «موجودیِ اولیه» یعنی موجودیِ
  *الان*، پس کسرِ دوباره‌ی قسط‌های قدیمی دوباره‌حسابیه (کاربر موجودیِ منفیِ ۵ میلیاردی دید).
  سینکِ **رو-به-جلو** هست و می‌مونه.
- **کارتِ خریدِ اشتراک عمداً به کاربرِ ازقبل‌مشترک نشون داده نمی‌شه** - باگ نیست (یه‌بار بازبینِ
  مایکت به همین خاطر گفت «بخشِ خرید پیدا نشد»).
- **دکمه‌ی «+» برجسته‌ی وسطِ نوارِ ناوبری**: اضافه شد و به‌خواستِ کاربر برداشته شد.
- **نمودارِ روندِ ۶ماهه + شبکه‌ی «دسترسیِ سریع» تو تبِ خانه**: اضافه شد و به‌خواستِ کاربر برگردونده شد.
- **مسکات/شخصیتِ کارتونی: نه، هیچ‌جا** - نه بالای صفحه، نه تو حالتِ خالی، نه هیچ‌جای دیگه.
  بریفِ اولیه‌ی بازطراحی هم همین بود؛ ۱۸ شهریور بسته‌ی «پنج فریمِ متفرقه»ی ClickUp Brain یه
  کیفِ پولِ خندان به هر پنج صفحه اضافه کرده بود و کاربر صریحاً رد کرد: «نه نمی‌خوام کلا».
  (راهِ میانیِ «فقط تو حالت‌های خالی» هم پیشنهاد شد و **رد شد** - دوباره پیشنهادش نکن.)
- **«دیوار»** فقط یه اشتباهِ تایپی/گفتاری کاربر بود - بسته‌ست.

---

## مجوزهای حساس (استورها توضیح می‌خوان)
- **`READ_CALENDAR`/`WRITE_CALENDAR`**: مالِ «افزودنِ سررسیدها به تقویمِ گوشی» تو
  `LoanDetailScreen.kt` (`runCalendarExport`). متنِ توضیح به بازبین: قابلیت کاملاً اختیاریه، مجوز
  فقط لحظه‌ی لمسِ همون دکمه گرفته می‌شه، اپ هیچ داده‌ای از تقویم نمی‌خونه/نمی‌فرسته.
- **`RECEIVE_SMS`/`READ_SMS`**: مالِ «خوندنِ خودکارِ پیامکِ بانکی» و «انتخابِ سرشماره از صندوقِ
  پیامک». متنِ توضیح: قابلیت اختیاریه و پیش‌فرض خاموش، مجوز runtime و فقط لحظه‌ی روشن‌کردن/زدنِ
  همون دکمه گرفته می‌شه، پردازش کاملاً روی خودِ دستگاهه و هیچ داده‌ای به هیچ سروری نمی‌ره.

---

## وضعیتِ فعلی (۲۹ مرداد ۱۴۰۵ / ۲۰۲۶-۰۸-۲۰)

### برنچ
کارِ فعلی رو **`claude/bank-9fiumm`** (برنچِ اجباریِ هارنسِ CCR). `main` عقبه و این برنچ رو نداره.
برنچِ قدیمی‌ترِ `claude/loan-chat-program-continue-4vpy1r` (با PR بازِ #66) هم هست؛ برنچِ فعلی از
رو همون ساخته شده و شاملِ همه‌ی کارِ اونه. **مرج به `main` رو خودسرانه انجام نده - فقط با تاییدِ صریح.**
(استثنا: تعمیراتِ امنیتیِ سرور جدا و با تاییدِ صریحِ کاربر cherry-pick شدن رو `main` - رجوع کن پایین‌تر.)

### 🔖 ادامه‌ی نشست - آخرین وضعیت (۱۳ شهریور ۱۴۰۵ / ۲۰۲۶-۰۹-۰۴)
**اگه از یه چتِ جدید ادامه دادی، فقط همین بخش رو بخون؛ بقیه‌ی فایل زمینه‌ست.**
کاربر تاریخچه‌ی چت رو پاک می‌کنه تا توکن کمتر مصرف بشه - پس **هرچی لازمه اینجاست**.

#### ✅ بخشِ ۶۲ و ۶۳ طراح - پیاده شد (۲۵ شهریور، بیلد نگرفته)
بسته‌ی تازه‌ی طراح حالا **۱۲ فایلِ جدا** است، نه یک فایل - در `design/frames/` کامیت شد
(`05-Asset.dc.html` بخشِ ۶۲ · `02-Home.dc.html` بخشِ ۶۳ و ۵۶ و ۵۷).

- **بخشِ ۶۲ - بازچیدمانِ «قیمتِ روز»** (`MarketPricesScreen.kt` بازنویسی شد): جست‌وجو +
  قرصِ «دارایی‌های من» (**پیش‌فرض روشن**) + قرصِ «فقط قیمتِ زنده» + چهار سرگروهِ جمع‌شونده.
  رمزارز **بسته** باز می‌شود و دو ردیفِ اولش بیرون می‌ماند. در حالتِ جست‌وجو سرگروه‌ها
  حذف می‌شوند (فهرستِ تخت). نقطه‌ی سبزِ کنارِ نام یعنی «داری».
- 🚨 **«قیمتِ دستی» سرگروهِ خودش شد** - این **قاعده‌ی ۴ِ بخشِ ۵۴ را باطل می‌کند** (آن‌جا
  گفته بود نیم‌سکه زیرِ «طلا» بماند). طراح خودش پس گرفت: آن هشت نماد یک **جنسِ** متفاوت‌اند
  نه یک صفتِ متفاوت.
- 🚨 **بجِ بنفشِ «تازه» برداشته شد.** ستونِ تغییر یک واژگانِ رنگی دارد (سبز/قرمز/خاکستری) و
  رنگِ پنجم یک معنیِ پنجم خوانده می‌شود؛ بنفش رنگِ «جدید/تبلیغ» است. جایش متنِ خاکستریِ
  «تازه — هنوز داده نداریم» در همان جای درصد است، بی بج و بی فلش. بجِ «قیمتِ دستی» خاکستری می‌مانَد.
- **بخشِ ۶۳b/c - پیش‌بینیِ کسری**: کارت از **قرمز به طلایی** رفت. قرمز در این برنامه معنیِ
  ثبت‌شده دارد (پولی که واقعاً دیر شده)؛ پیش‌بینی واقعیت نیست. **عددِ دومِ راه‌حل اضافه شد**
  («روزی X تا آخرِ ماه می‌رسانَد») - `MonthForecast` سه مشتقِ تازه گرفت: `perDayRial`،
  `safePerDayRial`، `runsOutOnDay`. حالتِ **سالم** کارت نمی‌گیرد، فقط یک خطِ آرام.
  ⚠️ انحراف: آن خط طبقِ طرح باید **داخلِ هیرو** باشد؛ فعلاً در جای خودِ کارت رندر می‌شود.
- **بخشِ ۶۳a - اشتراک‌یاب**: هیرو «N خرجِ تکراری پیدا شد» + ماهانه/سالانه · ستونِ چپ
  **مبلغِ ماهانه** با برچسبِ «ماهانه» · «N ماهِ پیاپی» جای تاریخِ شروع · دکمه‌ی رد شد
  **«این اشتراک نیست»** و **دائمی** است (شرطِ ±۱۵٪ اجاره و قسط را هم می‌گیرد).
- ✅ **بخشِ ۵۶ و ۵۷ هر دو از قبل پیاده شده بودند** (۲۵ شهریور دوباره چک شد، فایل‌به‌فایل با
  بسته‌ی طراح مقایسه شد): `CoinWalletScreen` ردیفِ «فعال»، کارتِ ترمیم و
  `LaunchedEffect { refreshRepairable() }` را دارد؛ `LoanWidget` هم بازطراحی‌شده و
  `updateAll()` وصل است. **نسخه‌ی ما جلوترِ فایلِ بسته است** (به‌جای `jalaliDayIndex`ِ
  تقریبی از `JalaliCalendar.daysBetween` استفاده می‌کند). کاری لازم نبود.

#### ✅ بخشِ ۷۲ - فروشگاهِ کامل (دورِ ۸) - پیاده شد (۲۶ شهریور، بیلد نگرفته)
فریم‌ها `72a`/`72b`/`72c` در `design/frames/10-Coin.dc.html`. تشخیصِ مرکزیِ طراح:
**فروشگاه با تنوعِ نوعِ قلم کامل می‌شود، نه با تعدادِ ردیف** - ۱۴ تم و ۳ آیکون در واقع
دو نوع‌اند، و تمِ پانزدهم ویترین را بلندتر می‌کند بی این‌که چیزی حل کند.
- **سه قلمِ تازه، به ترتیبِ طراح**: ۱) **ستِ نمادِ دسته** - تنها قلمی که هر روز دیده
  می‌شود (فرمِ ثبت، دونات، هر ردیفِ تراکنش). ۲) **قابِ آواتار** - تنها جای شخصیِ برنامه،
  و در هدرِ خانه دیده می‌شود پس خرید همان‌جا نتیجه می‌دهد. ۳) **تمِ مناسبتی** - تنها
  قلمِ مهلت‌دار؛ سکه‌ای که فوریت نداشته باشد جمع می‌شود و خرج نمی‌شود.
- 🚨 **ستِ نماد با `mutableStateOf`ِ سراسری پیاده شد (`SymbolTheme.style`)، نه
  `CompositionLocal`**: نمادها از فهرست‌های **ثابتِ** `Category.kt` می‌آیند که composable
  نیستند و ۳۸ جای مصرف نباید امضا عوض کنند. `CategoryEntry.icon` از `ImageVector`ِ ثابت
  به `get() = iconForKey(iconKey)` تبدیل شد، پس خرید روی دسته‌های **ثابت** هم می‌نشیند نه
  فقط دلخواه‌ها. تنها نقطه‌ی نوشتنش `ThemeViewModel.init` است.
- **قاب** (`ui/components/AvatarFrame.kt`): پنج شکلِ وکتوری، همه از توکن‌های موجود
  (`AppPrimary`/`AppAccent`/`AppLine`) پس با تمِ خریداری‌شده هم‌قدم می‌شوند. **فقط روی
  آواتارِ خودِ کاربر** (خانه + تنظیمات)؛ آواتارِ طرفِ‌حساب قاب نمی‌گیرد وگرنه معنیِ
  «مالِ من» را از دست می‌دهد. قطرِ بیرونی ثابت می‌مانَد و خودِ آدمک کوچک می‌شود.
- **ویترین: تبِ افقی** (`72b`) - «همه» پیش‌فرض، بعد پنج تب. سرگروه **داخلِ** هر تب
  می‌مانَد. قلمِ کمیاب **بالای همه‌ی تب‌ها**، بیرونِ تب‌بندی، با شمارشِ روز.
- 🚨 **قیمت کارِ ساخت را می‌گوید، نه میزانِ دیده‌شدن** (`72c`). منطقِ «هرچه بیشتر دیده
  شود گران‌تر» منطقِ فروشنده است: با سقفِ ماهانه‌ی ~۹۰۰ سکه، کاربرِ تازه تنها چیزی را که
  می‌خواهد آخر می‌خرد و بین راه دلسرد می‌شود. پس ستِ نماد **۵۰۰** (کارش از آیکون بیشتر
  است) و قاب **۱۰۰** (پنج‌شش شکلِ ساده).
- **قلمِ نشان‌قفل باید با هیچ قیمتی خریدنی نباشد** - وگرنه نشان فقط «مسیرِ کندتر» به همان
  چیز است. سه‌تا واقعی شد: `theme:gold` (ماهِ منظم) · `coinskin:aged_gold` (وامِ بسته) ·
  `frame:laurel` (زیرِ بودجه). ⏳ دو نوعِ دیگر (آیکون و جایزه) هنوز قلمِ نشان‌قفل ندارند -
  آیکون فایلِ هنریِ تازه می‌خواهد.
- 🐛 **باگِ واقعی که سرِ همین کار پیدا شد**: `coinskin:aged_gold` با
  `unlockBadge = "YEAR_COMPLETE"` نوشته شده بود و **هیچ نشانی با این کد وجود ندارد** -
  یعنی ردیف برای همیشه قفل بود و دیالوگ کدِ خام را نشان می‌داد (کدهای واقعی کوچک‌اند:
  `loan_closed`…).
- **تمِ مناسبتی**: `ShopItem.window` تازه است و شناسه **سال دارد** (`theme:nowruz1405`).
  خارج از بازه از ویترین می‌رود مگر کاربر خریده باشد - خریدار ردیفش را نگه می‌دارد تا
  بتواند دوباره فعالش کند.
- **«سبزآبی» تیره‌تر و به «سبزآبیِ تیره» تغییرِ نام داد** - در ۳۸px از «فیروزه» قابلِ
  تفکیک نبود (ایرادِ طراح).
- **دکمه‌ی گروهیِ پیامک به نوارِ چسبانِ پایین رفت** (همان الگوی `64`): کاربر اول فهرست را
  می‌خوانَد و بعد تصمیم می‌گیرد؛ عددِ N روی خودِ دکمه.
- **مقصدِ FABِ تبِ وام تصمیمِ کاربر است و بسته**: محاسبه‌گر، نه فرمِ دستی. طراح پذیرفت و
  دو اصلاحِ جبرانی داد که هر دو پیاده شدند:
  1. **نماد دیگر «+»ِ خالی نیست** - «+» وعده‌ی فرم می‌دهد و ماشین‌حساب باز می‌کند؛ ولی
     ماشین‌حسابِ تنها هم یک‌دستیِ FAB را در پنج تبِ دیگر می‌شکند. حالا `Icons.Filled.Calculate`
     با یک دایره‌ی سفیدِ ریزِ «+» در گوشه (`AppFab(plusBadge = true)`ِ تازه).
     ⚠️ انتخابِ اولِ طراح `PostAdd` بود؛ **عمداً نرفتیم** چون معلوم نبود در `filled` است یا
     `automirrored.filled` و سندباکس نمی‌تواند کامپایل کند - دو بیلد پشتِ‌سرهم سرِ همین
     جنس خطا رفته بود. `Calculate` قطعی است و همان فال‌بکِ خودِ طراح.
  2. **ردیفِ «افزودنِ وامِ دستی» از تهِ محاسبه‌گر به بالای آن رفت** - کسی که «+» زده ممکن
     است فرم می‌خواسته، پس اولین چیزی که می‌بیند باید راهِ رسیدن به فرم باشد نه آخرین.
     این ایرادِ کشف را بیشتر از خودِ نماد کم می‌کند.

#### 🧪 شکستِ بیلدِ ۵۴۵ و بررسیِ ایستای بیستم
`ModalBottomSheet`/`rememberModalBottomSheetState` آزمایشی‌اند و بی
`@OptIn(ExperimentalMaterial3Api::class)` **کامپایل نمی‌شوند**. شیتِ تقویمِ بخشِ ۶۷ آن را
نداشت. بسته‌های طراح این انوتیشن را همراه نمی‌آورند، پس `experimental.py` ساخته شد.

#### ✅ بخشِ ۷۱ - صفحه‌ی نقدی · خرجِ امروز · تاریخچه‌ی اعلان - پیاده شد (۲۵ شهریور، بیلد نگرفته)
فریم‌ها `71a`..`71e` در `design/frames/02-Home.dc.html`. این همان بازخوردِ دورِ ۷ بود.
- 🚨 **ستونِ تازه + مهاجرت**: `AccountTransactionEntity.originLabel` + `MIGRATION_30_31`
  (دیتابیس **نسخه‌ی ۳۱**). منبع تا حالا فقط در مرکزِ پیام‌ها می‌نشست و روی تراکنش یک
  `description`ِ عمومی می‌رفت، پس بعدِ تایید گم می‌شد. `null` یعنی **دستی** و همان‌طور هم
  نوشته می‌شود (قاعده‌ی ۳ِ `71c`: خطِ خالی یعنی «نمی‌دانم»).
- **`CompactTransactionRow`ِ تازه** (`ui/account/TransactionRow.kt`) مشترکِ صفحه‌ی نقدی و
  شیتِ خرجِ امروز: مبلغِ باعلامتِ چپ · «منبع · ساعت» خطِ دوم · یکی از سه آیکون (زنگ/حباب/+)
  سمتِ راست با خانه‌ی **هم‌رنگِ جهت** برای خودکارها و خنثی برای دستی. ردیف از ~۹۰px به ~۵۰.
  لوگوی بانک عمداً نمی‌آید. ساعت از `createdAt` خوانده می‌شود و اگر خوانا نبود **جا می‌مانَد**،
  «۰۰:۰۰»ِ دروغ نمی‌گذارد.
- **صفحه‌ی نقدی**: هیرو + دو قرصِ «واریزِ ماه/برداشتِ ماه» (بی این، انتخابِ ماه فقط فهرست را
  کوتاه می‌کند) · نوارِ ماه (فلشِ ماهِ بعد بی‌داده **خاموش** است نه غایب؛ در RTL «قبلی» راست
  است) · سرگروهِ روز جای تاریخِ هر ردیف · اشاره‌ی «بزن/بکش» یک‌بار زیرِ فهرست نه زیرِ هر ردیف.
- **دو توکنِ تازه `txIn`/`txOut`** (جدولِ `71b`): در شب `#4FD49A`/`#FF8E8E` - جوهرِ عادی روی
  ردیفِ ریزِ ۵۰px خوانده نمی‌شود. خانه‌ی آیکون از `primaryPill`/`dangerPill`ِ موجود می‌آید.
- **`71d` تپ روی «خرجِ امروز»**: `ModalBottomSheet`، نه صفحه - یک نگاهِ دوثانیه‌ای و زمینه
  حفظ می‌شود. **واریزها نمی‌آیند** (عددِ هیرو خرج است نه تراز) و نامِ حساب پیشِ منبع می‌آید.
  ⚠️ مقصدِ قبلیِ این تپ «تبِ دارایی» بود و عوض شد.
- **`71e` تاریخچه‌ی اعلان**: صفحه‌ی تازه ساخته **نشد** (همان `40`). `DueDateReminderWorker` و
  `ComeBackWorker` حالا `InboxRepository` می‌گیرند و هر اعلانی که می‌فرستند یک ردیف هم
  می‌نویسد. ⚠️ گونه‌شان عمداً **خبر** است نه اقدام‌دار، وگرنه شمارنده‌ی زنگ برای همیشه باز
  می‌مانَد. سرگروه‌ها «در انتظارِ تو»/«پیشینه»؛ نگه‌داری ۳۰ → **۹۰ روز** و سقفِ ردیف ۲۰۰ → ۶۰۰.
- ⏳ **باقی‌مانده‌ی همین بخش**: `AutoTxNotifier` از قبل ردیف دارد، ولی اعلانِ آزمایشیِ
  تنظیمات عمداً ردیف نمی‌نویسد (آزمایش است، نه رویداد).

#### ✅ بخشِ ۷۰ - سه صفحه‌ی آخرِ مسیرِ وام - پیاده شد (۲۵ شهریور، بیلد نگرفته)
فریم‌ها `70a`/`70b`/`70c`؛ سند: `design/README-section70.md`. **مسیرِ وام تمام شد** (هفت
صفحه از `MyLoansScreen` تا `DepositScreen`). چهار فایل با هم اعمال شدند چون امضای
`BankLoanScreen` عوض شد.
- 🚨 **رگرسیونی که خودِ بخشِ ۶۹ ساخته بود**: `lastInstallment` فقط در `onCalculated` پر
  می‌شد، یعنی فقط با تپِ دکمه. بخشِ ۶۹ هیروِ زنده آورد و متنِ دکمه را به «جدولِ اقساط را
  ببین» برد، پس کاربری که فقط قسط می‌خواست دیگر دکمه را نمی‌زد و **کارتِ سبزِ «از عهده‌اش
  برمی‌آیم؟» هیچ‌وقت ظاهر نمی‌شد**. رفع: `onInputChanged` (۱۳ فراخوان) حذف شد و
  `onLiveInstallment: (Double?) -> Unit` جایش آمد که هیرو با `LaunchedEffect` صدا می‌زند -
  یک منبعِ حقیقت جای دو تا. **درس: هر جا دکمه‌ای از «تنها راهِ دیدنِ نتیجه» به یک کنشِ
  اختیاری تبدیل می‌شود، هر state‌ای که از کال‌بکِ همان دکمه پر می‌شد مرده است.**
- 🚨 `AffordScreen` (سه مبلغ) و `DepositScreen` (چهار مبلغ) **هیچ‌کدام از `maskIfPrivate`
  رد نمی‌شدند** - هر دو `LocalPrivacyMode` گرفتند.
- `remember` → `rememberSaveable` در هر دو (چرخشِ گوشی فرم را پاک می‌کرد)؛ `payText`ِ
  `AffordScreen` کلیدِ `initialInstallment` گرفت وگرنه قسطِ کارتِ پل فقط بارِ اول اثر داشت.
- هیروِ زنده در هر دو؛ هیرویِ سپرده **سودِ ماهانه** است نه مبلغِ نهایی. متنِ هر دو دکمه
  **«ثبت در تاریخچه»** شد (نتیجه از قبل در هیرو دیده می‌شود).
- **نرخ‌یاب عمداً هیروِ زنده نگرفت** - `findRate` جست‌وجوی عددی است و روی هر تپِ صفحه‌کلید گران.
- ⚠️ اگر روی تایپِ سریع عددِ هیرو پرش‌دار دیده شد، `countUpDouble` را از همان هیرو بردار.

#### ✅ بخشِ ۶۹ - ورودیِ «قسط و سود» - پیاده شد (۲۵ شهریور، بیلد نگرفته)
فریم‌ها `69a`/`69b`؛ سند: `design/README-section69.md`. **فرمول دست نخورد.**
- 🚨 **پنجمین و ششمین موردِ باگِ `return`**، هر دو در همین فایل: تقویم و «امور چک».
  دومی بدتر بود - فیلدها `rememberSaveable`ند پس داده نمی‌رفت، ولی `listState` می‌رفت و
  کاربر بعدِ برگشت **سرِ بالای فرمِ یازده‌فیلدی** بود و فکر می‌کرد پاک شده. هر دو حالا
  پوششِ تمام‌صفحه روی همان `LazyColumn`اند.
- 🚨 **اسلایدرِ نرخ بی‌اثر بود نه غایب**: در حالتِ «سرویسِ اعتباری» رندر می‌شد و
  `onValueChange` بی‌صدا دورش می‌انداخت. حالا شرط دورِ **خودِ اسلایدر** است.
- **هیروِ زنده‌ی قسط** (`remember` روی شش ورودی + `runCatching`؛ روی ترکیبِ نامعتبرِ
  قرض‌الحسنه `compute` تقسیم‌بر‌صفر می‌کند پس هیرو **ساخته نمی‌شود** و خطا زیرِ دکمه می‌ماند).
  `LocalPrivacyMode` تازه به این صفحه آمد - تا امروز مبلغِ محاسبه‌شده نداشت.
- **«امور چک» از وسطِ فرم به تهِ فهرست** رفت · **یازده کارت → هفت** («فاصله‌ی قسط» و
  «تنفس» زیرِ «تنظیماتِ پیشرفته») · دکمه: «محاسبه کن» → **«جدولِ اقساط را ببین»**.
- ✅ **`rateSourceLabel` وصل شد** (طراح گفت بسازیمش و خودش نساخت چون فکر می‌کرد
  `BankLoanOutcome` جای دیگری است - **همان فایل بود**): فیلدِ تازه‌ی `BankLoanOutcome` +
  `rateSource.label` در `onCalculated` + `MainActivity` همان را به `sourceLabel`ِ
  `ResultScreen` می‌دهد. `RateSource` از `private` درآمد.
- ⚠️ `AutoShrinkText` پارامترِ `fontWeight` نداشت (بررسیِ `apisurface` گرفت) - یک
  پارامترِ اختیاری گرفت، بقیه‌ی مصرف‌ها بی‌تغییر.
- `HeroMuted` واقعاً در `ui/components/AppHeroCard.kt` بود و `Color.White` روی گرادیانِ
  سبزِ ثابتِ هیرو درست است (همان کاری که `AccountsTotalHero` می‌کند).
- ⏳ باقی‌مانده: `CalculatorHostScreen` · `AffordScreen` · `DepositScreen`.

#### ✅ بخشِ ۶۸ - صفحه‌ی «نتیجه‌ی محاسبه» - پیاده شد (۲۵ شهریور، بیلد نگرفته)
فریم‌ها `68a`/`68b`؛ سند: `design/README-section68.md`. **فرمول دست نخورد.**
- **هفت `StatBox`ِ هم‌اندازه → یک کارت با ردیف‌های برچسب/مقدار**، ردیفِ اول (کلِ بازپرداخت)
  درشت‌تر. کارمزدِ قرض‌الحسنه هم ردیف شد نه جعبه‌ی شرطیِ تمام‌عرض (وقتی صفر بود حفره می‌ساخت).
- **افسانه‌ی حلقه**: `LoanRing` دورنگ بود ولی نمی‌گفت کدام رنگ چیست؛ دو نقطه‌ی هم‌رنگِ کمان
  با درصد زیرش آمد. ⚠️ درصدِ افسانه **سهمِ سود از کل** است ولی ردیفِ فهرست `interestPct`
  (**سود نسبت به اصل**) - دو عددِ متفاوت با دو برچسبِ متفاوت، عمدی.
- **جدولِ اقساط خلاصه و سرستون گرفت** («۱۲ قسط · جمعِ … · تا …» + سه برچسبِ ستون).
- **قرصِ منبع**: پارامترِ تازه‌ی `sourceLabel: String? = null`؛ ما `"قسط و سود"` را از
  `MainActivity` پاس دادیم (تنها حالتی که به نتیجه می‌رسد؛ `AffordScreen` خروجی نمی‌دهد).
- نگاشتِ توکن: `AppGold` وجود ندارد → `AppAccent` (همان رنگی که کمانِ سودِ حلقه با آن کشیده
  می‌شود، پس افسانه واقعاً هم‌رنگِ کمان است).
- **ترتیبِ بعدی طبقِ نظرِ طراح**: `BankLoanScreen` → `CalculatorHostScreen` → `AffordScreen`
  → `DepositScreen`.

#### ✅ بخشِ ۶۷ - فرمِ «افزودنِ وامِ دستی» - پیاده شد (۲۵ شهریور، بیلد نگرفته)
فریم‌ها `67a`/`67b`/`67c`؛ سند: `design/README-section67.md`.
- 🚨 **چهارمین موردِ باگِ `return` وسطِ کامپوزبل**: بلوکِ `if (showCalendarPicker) { … return }`
  کلِ فرم را از کامپوزیشن برمی‌داشت → اسکرول صفر می‌شد. حالا `ModalBottomSheet`
  (`skipPartiallyExpanded = true`) روی فرم می‌آید. **همان الگوی `AssetTradeSheet` و
  `AccountDetailScreen` - اگر جای پنجمی دیدی همین‌طور رفعش کن.**
- **کارتِ خلاصه‌ی زنده بالای فرم** (`AppHeroCard`): کلِ بازپرداخت + آخرین سررسید. تا وقتی
  قسط و تعداد نیامده **اصلاً نمی‌آید** (نه «—»).
- **نوارِ طلاییِ سقفِ تعدادِ قسط** در حالتِ ویرایش، بالای فرم؛ خطای بعدِ ذخیره هم می‌مانَد.
- **شش کارتِ تک‌فیلدی → سه کارت** (شناسه/عدد/وضعیت) + `FieldLabel`ِ تازه.
- **چرخِ تاریخِ سه‌ستونه حذف شد** - دو نما می‌ماند: `InlineJalaliDateRow` (درجا) و
  `CalendarPickerScreen`. یعنی فریمِ `29i` کارِ تازه‌ای نمی‌خواهد جز همین حذف.
- ⚠️ **تصحیحِ طراح روی READMEِ خودمان**: پسوندِ فیلدهای این بخش **تومان** است نه ریال؛
  بندِ «همیشه ریال» کهنه بود (بسته‌ی وام همه را تومان کرد).
- دو نگاشتِ توکن: `AppGoldPill` وجود ندارد → `AppGoldPillSoft` · `AppHeroCard` لامبدای
  محتوا می‌گیرد پس `AppHeroInk = Color.White`ِ خودِ فایل درست است.
- ⏳ **پنج فایلِ دیگرِ مسیر هنوز قابِ بصری نگرفته‌اند** (`CalculatorHost`/`BankLoan`/
  `Result`/`Afford`/`Deposit`). طراح گفت **`ResultScreen` بیشترین اثر را دارد** چون
  خروجیِ هر سه محاسبه‌گر است - بعدی همان است.

#### 📝 بازخوردِ دورِ ۷ کاربر (۲۵ شهریور، رو بیلدِ ۵۴۳) - ثبت شد، کد زده نشده
متنِ فرستاده‌شده به طراح: `design/MESSAGE-round7-to-design.md`.
۱. **بازطراحیِ صفحه‌ی نقدی** (`AccountDetailScreen`): انتخابِ ماه · منبعِ هر تراکنش
   (کدام برنامه/سرشماره، دستی یا خودکار) · **ساعتِ دقیق** · سبز/قرمزِ زیباتر · حالتِ شب.
   ⚠️ **منبع روی تراکنش ذخیره نمی‌شود**: `appLabelOf(packageName)` فقط در مرکزِ پیام‌ها
   می‌نشیند و روی تراکنش یک `description`ِ عمومی («خودکار از اعلانِ بانکی») می‌رود →
   **ستونِ تازه + migration لازم است**. `createdAt` هست ولی نمایش داده نمی‌شود.
۲. **تپ روی «خرجِ امروز»ِ هیرویِ خانه** → فهرستِ خرج‌های همان روز (الان مقصد ندارد).
۳. **تاریخچه‌ی اعلان‌ها بماند** - احتمالاً همان مرکزِ پیام‌ها (`40`) با سرگروهِ «رسیدگی‌شده».
۴. ~~**«وام‌های من» مثلِ اپِ مرجعِ «وام من» شود**~~ - ❌ **کاربر پس گرفت (۲۵ شهریور):
   «اینو نمی‌خوام دیگه، بعدا بهت میگم کجاشو باید درست کرد.»** یعنی طرحِ فعلیِ `27a` سرِ
   جایش می‌مانَد و بعداً فقط **نقطه‌های مشخصی** را که خودش می‌گوید اصلاح می‌کنیم.
   **دوباره این را پیشنهاد نکن و بازطراحیِ کاملِ آن صفحه را شروع نکن.**

#### ✅ بخشِ ۶۶ - چهار موردِ بازِ ردیفِ قسط - پیاده شد (۲۵ شهریور، بیلد نگرفته)
- **بجِ وضعیت قرص شد** (طراح `64a`ِ خودش را پس گرفت): چهار حالت با جوهر+زمینه‌ی توکنی،
  حاشیه فقط برای دو حالتِ فوری. `AppGoldPill` وجود ندارد → `AppGoldPillSoft`.
- **لمس‌پذیری**: سایه‌ی ۱dp روی ردیف (زبانِ `GradientButton`)؛ ردیفِ پرداخت‌شده سایه
  نمی‌گیرد و زمینه‌اش `AppChipBg` است - تو-رفته، چون کارش برگشتی است.
- 🚨 **طراح بندِ ۳ خودش را پس گرفت** (`design/CORRECTION-section66-item3.md`): ادعا کرده بود
  تپ روی ردیفِ پرداخت‌شده از قبل بی‌صدا برش می‌دارد - **غلط بود**، آن را از نسخه‌ی قدیم خوانده
  بود؛ گاردِ `else if (!paid)` در **جای فراخوان** است نه در `InstallmentRow`. پس **تپ روی
  ردیفِ پرداخت‌شده هیچ کاری نمی‌کند و همین می‌ماند** (`64c`). شکایتِ کاربر مسئله‌ی **کشف** بود
  نه مسیر: «برداشتنِ پرداخت» اولِ منو رفت و `ConfirmDialog(DESTRUCTIVE)` گرفت.
  ⚠️ `setRowUnpaid` تراکنشِ حسابداری را برنمی‌گرداند، پس جمله‌ی `consequence` کوتاه شد.
- «پرداخت شد» سبز ماند و طراح تاییدش کرد؛ «با تأخیر» عمداً قرمز است.

#### ✅ بخشِ ۶۴ - فهرستِ کاملِ اقساط - پیاده شد (۲۵ شهریور، بیلد نگرفته)
فریم در `design/frames/03-Loan.dc.html`. این آخرین جای بخشِ وام بود که طرحِ نو نداشت -
کاربر گزارش داد «دیدنِ همه‌ی ۱۲ قسط» به صفحه‌ی سبکِ قدیمی می‌رفت.
- **حاشیه‌ی سبزِ همه‌ی ردیف‌ها رفت**؛ فقط دو حالتِ فوری حاشیه‌ی رنگی دارند (عقب‌افتاده قرمز،
  بعدی طلایی) و بقیه خطِ خنثی - قاعده‌ی `51a`.
- **بجِ «پرداخت نشده» و پالسش حذف شد**؛ جایش **مهلت** نشست («۳ روز گذشته»/«۵ روز مانده»/
  «امروز»/«بعدی»). آینده‌ی دور عمداً هیچ برچسبی نمی‌گیرد.
- ردیفِ پرداخت‌شده **جوهرش** خاکستری می‌شود، `alpha` نمی‌خورد (محوشدن مبلغ و تاریخ را هم
  ناخوانا می‌کرد). تنها استثنا حالتِ گروهی است که همان `alpha 0.5` را نگه داشت.
- **دوربین و مدادِ ردیف با یک سه‌نقطه‌ی ۴۴ جایگزین شدند** (`DropdownMenu` با سرِ «قسط N ·
  تاریخ»). گیره‌ی ریز کنارِ شماره یعنی «رسید دارد».
- 🚨 **تپ روی ردیف دیگر پرداخت را برنمی‌دارد، فقط می‌زند** (`64c` + تفکیکِ `46c`).
  برداشتن رفت به همان منویِ سه‌نقطه. قبلاً یک تپِ اشتباه یک قسطِ پرداخت‌شده را بی هیچ تاییدی
  از دست می‌داد.
- **نوارِ چسبانِ پایین در حالتِ گروهی**: جمعِ مبلغ + «پرداختِ N قسط». دیالوگِ تایید هم حالا
  جمعِ مبلغ را تکرار می‌کند.
- **دو باگِ واقعی که سرِ همین کار پیدا شدند**:
  1. `ThousandsSeparatorTransformation.transformedToOriginal` کاماي **لاتین** می‌شمرد در حالی
     که خروجی `٬`ِ فارسی است → همیشه صفر، پس مکان‌نما به‌اندازه‌ی جداکننده‌ها جلو می‌افتاد و
     ویرایشِ وسطِ عدد رقم را جای غلط درج می‌کرد. (این همان کلاسِ باگی است که خودِ این
     ترنسفورمیشن برای رفعش ساخته شده بود.)
  2. دیالوگِ «ویرایشِ مبلغِ قسط» برچسبِ **تومان** داشت ولی مقدارِ **ریالِ خام** را نشان و
     ذخیره می‌کرد - همان باگِ ۱۰برابر. حالا `rialToToman` در ورود و `tomanToRial` در ذخیره.
- ⏳ بخشِ ۵۱ (بازطراحیِ تبِ سررسید، چهار گروهِ زمانی) هم در همین فایل آمده - **هنوز پیاده نشده**.

#### ✅ بخشِ ۵۱ - بازطراحیِ تبِ سررسید - پیاده شد (۲۵ شهریور، بیلد نگرفته)
تنها تبی که از بازطراحی جا مانده بود. فریم در `design/frames/03-Loan.dc.html`.
- **تاگلِ سه‌تاییِ «اقساط/چک/طلب‌وبدهی» حذف شد.** حالا یک فهرستِ یکپارچه با گروه‌بندی روی
  **فوریت**: عقب‌افتاده / همین هفته / بعد از این. کاربر اولِ ماه یک سوال دارد نه سه.
  گروهِ خالی اصلاً نشان داده نمی‌شود.
- **پنج قرصِ فیلتر**: همه · قسط · چک · تکراری · قرض. فیلترند، نه تب.
- **پرداختِ تکراری منبعِ تازه‌ی این تب شد** (`AccountRepository.observeRecurringPayments`).
  سررسیدِ ذخیره‌شده ندارد، فقط روزِ ماه - پس سررسیدِ پیشِ‌رو حساب می‌شود و **هیچ‌وقت
  عقب‌افتاده نمی‌شود** (نمی‌دانیم پرداخت شده یا نه).
- **هیرو**: «تا آخرِ ماه باید بدهی» + دو قرصِ شمارشیِ غیرکلیک‌پذیر. `HeroTone.RED` تازه ساخته
  شد و **فقط وقتی موردِ عقب‌افتاده هست** قرمز می‌شود؛ در نبودش سبز - وگرنه کاربری که
  همه‌چیز را پرداخت کرده هم حسِ بدهی می‌گیرد.
- 🚨 **رنگ حاملِ جهت است**: «۹ روز» در گروهِ عقب‌افتاده یعنی نه روز **گذشته** (قرمز) و همان
  عدد در پیشِ‌رو یعنی نه روز **مانده** (نارنجی). بی این قاعده یک عدد دو معنی می‌دهد.
- تفاوتِ نوعِ تعهد از **آیکون و سطرِ دوم** می‌آید، نه از رنگ (همان تصمیمِ نشان‌ها).
- تپ روی ردیف به مقصدِ خودش می‌رود؛ **دکمه‌ی «پرداخت شد» ندارد** - این تب صفحه‌ی دیدن است
  نه کنش، پرداخت در مقصد ثبت می‌شود تا عکسِ رسید و تاریخِ واقعی هم بگیرد.
- ⚠️ `DueTab` و چهار کامپوزبلِ قدیمی (`DueTabBar`/`PendingHero`/`OverdueRow`/`PlainRow`)
  حذف شدند. سه استریمِ `installments`/`cheques`/`debts` در ViewModel **ماندند** (مصرفِ
  دیگری دارند) و کنارشان `all` تازه آمد.
- ⚠️ enumِ تازه `DueSource` نام گرفت نه `DueKind` - `DueKind` از قبل در
  `ui/calendar/FinancialCalendarViewModel.kt` با مقادیرِ متفاوت وجود داشت. بررسیِ
  `apisurface` همین را گرفت.

#### ✅ بخشِ ۶۵ - نسخه‌ی شبِ تبِ سررسید - پیاده شد (۲۵ شهریور، بیلد نگرفته)
- **شش توکنِ تازه در `AppColorPalette`** (نه هاردکد در صفحه - همان دامِ تبِ دارایی):
  `dueOverduePill/Border` · `dueNextPill/Border` · `dueLaterPill/Border`. مقادیرِ تیره
  عیناً جدولِ `65b`: `#3A1F1E`/`#5A2E2C` · `#332A12`/`#4A3D22` · `#233029`/`#2A3640`.
  مقادیرِ روشن از توکن‌های روشنِ موجود مشتق شدند.
- 🚨 **قاعده‌ی مرکزی**: در شب فوریت با **جوهر** گفته می‌شود نه پس‌زمینه. قرمزِ روز روی
  سطحِ تیره به ۲٫۱:۱ می‌رسد (ناخوانا) و طلایی برعکس چنان می‌درخشد که از هشدارِ قرمز
  بلندتر حرف می‌زند - یعنی سلسله‌مراتب **وارونه** می‌شود.
- **آیکونِ نوعِ منبع هم‌رنگِ جوهرِ فوریت شد** نه خنثی (بندِ آخرِ ۶۵) - تنها المانِ ردیف
  که هر دو بُعد (نوع + فوریت) را با هم می‌گوید.
- بجِ روز حالا **حاشیه** هم دارد؛ سه حالت: عقب‌افتاده/امروز · تا ۷ روز · دورتر.

#### 🪙 جوابِ طراح به ایرادِ آیکونِ سکه (۲۵ شهریور) - **ایراد وارد بود**
جدولِ قبلی روی **شمارشِ اجزا** بود (چهار جزءِ رنگی) و غلط. جدولِ تازه روی **نقش**
می‌چرخد: `rim` (بیرونی‌ترین حلقه) · `body` (قرصِ اصلی) · `detail` (هرچه روی قرص).
- نگاشتِ فایلِ واقعیِ ما: `rim` = حلقه‌ی `#0B3A2A` · `detail` = حرفِ «ج» + نقطه ·
  `body` = **ندارد** (طلا در پس‌زمینه است).
- **فرمول**: `rim` با `bodySaturate` می‌پژمرد، `immune` (حرف + نقطه) با `markSaturate`.
  ⚠️ **حلقه مصون نشود** - اگر هم حلقه هم حرف مصون باشند کلِ پیش‌زمینه مصون است و طرحِ ب
  همان حالتی می‌شود که اصلاحِ قبلی بستش. شرطِ «دستِ‌کم یک چیزِ رنگی بماند» را خودِ حرف
  برآورده می‌کند.
- **طراح بندِ ۲ی `58c` خودش را پس گرفت** («پژمردگی فقط روی پیش‌زمینه») - پس‌زمینه هم
  می‌پژمرد و حرفِ ما درست بود. یعنی طرحِ ب دو چیزِ پژمرنده دارد (طلای پس‌زمینه + حلقه)
  و یک چیزِ مصون - سالم‌ترین از هر چهار طرح.
- **`CoinIcon`ِ داخلِ برنامه پژمردگی نمی‌گیرد و نباید بگیرد** - اگر سکه‌ی کیف کم‌رنگ شود
  کاربر فکر می‌کند موجودی‌اش مشکل دارد.
- ✅ **پیاده‌سازیِ فعلی با این نگاشت جور است** (`body` = حلقه، `immune` = حرف) - کاری
  لازم نبود.

#### 🚨 درسِ ۲۵ شهریور - لیستِ «باقی‌مانده»ی همین فایل کهنه بود
به کاربر گفتم «صفحه‌ی سکه، ترمیمِ زنجیر و ویجت ساخته نشده‌اند» - **هر سه از قبل کامل
بودند**. منبعِ اشتباه همین فایل بود، نه کد. `CoinWalletScreen` (سکه‌ی بزرگ + معادلِ
تومانی + تاریخچه + ردیفِ «فعال» با نوارِ هفته) و کارتِ ترمیم با دیالوگِ تاییدش کامل‌اند،
و ویجت هم در بیلدِ ۵۳۶ بازطراحی شده («N روز مانده» + `FLAG_ACTIVITY_NEW_TASK` + مقصدِ
تبِ سررسید). **قاعده‌ی خودِ همین فایل را رعایت نکردم: قبل از گفتنِ «پیاده نشده» کدِ
واقعی را چک کن.**

**چیزی که واقعاً ساخته نشده** (و طراح هم گفت آخر بمانند چون لایه‌ی داده‌ی تازه می‌خواهند):
هدفِ پس‌انداز · قدرتِ خرید · آرشیوِ سالانه · حسابِ خانواده.

**جوابِ طراح به چهار سوالِ باز (۲۵ شهریور):**
- دسته‌بندیِ فایل‌هایش (بودجه زیرِ گزارش، سررسید زیرِ وام) **درست است، دست نخورد**.
- **نسخه‌ی شبِ تبِ سررسید را می‌سازد** - منتظرش بمان.
- **نسخه‌ی شبِ `27f` لازم نیست** - محاسبه‌گر صفحه‌ی گذراست و رنگِ خاصی ندارد؛ موقعِ شک بپرس.
- **ایرادِ آیکونِ سکه** هنوز جواب نگرفته - جدولِ طراح چهار جزءِ رنگی فرض کرده، فایلِ
  واقعیِ ما دو مسیرِ `#0B3A2A` در `immune` دارد و طلا در پس‌زمینه است.

#### 🎯 نظرِ طراح درباره‌ی چهار قابلیتِ ساخته‌نشده (۲۵ شهریور) - **دوتاشان را نساز**
هیچ‌کدام فریم ندارند (فقط در فهرستِ بازخوردها بودند)، و طراح موافق است که فعلاً ساخته نشوند.
ولی صریح گفت **دوتاشان احتمالاً اصلاً نباید ساخته شوند**:
- **قدرتِ خرید** - از قبل به‌عنوانِ بجِ گزارش وجود دارد؛ صفحه‌ی مستقلش همان **دوگانگی**ای
  است که سرِ «آمارِ وام» بستیم.
- **حسابِ خانواده** - لایه‌ی همگام‌سازیِ **چندکاربره** می‌خواهد، یعنی بزرگ‌ترین کارِ کلِ
  پروژه. تصمیمِ **محصولی** است نه طراحی - قبل از هر کاری باید کاربر تکلیفش را روشن کند.
- دو تای واقعی: **هدفِ پس‌انداز** (نشانِ `GOAL_REACHED` مقصد ندارد) و **آرشیوِ سالانه**.

#### 🔍 فهرستِ «بعداً همه را با هم چک می‌کنیم» (خواسته‌ی صریحِ کاربر، ۲۵ شهریور)
کاربر گفت **«این باگ‌ها یادت باشه، بعداً همه رو چک کنیم»**. یعنی الان دنبالشان نرو، ولی
وقتی گفت «بریم چک کنیم» **این فهرست را یکجا بهش بده** - مثلِ چک‌لیستِ تست پایین‌تر.

| # | مورد | وضعیت |
|---|---|---|
| ۱ | کارتِ کشیده‌شده‌ی خانه | کارت از خانه برداشته شد؛ **باید در کیفِ سکه چک شود** که همان‌جا کشیده نباشد - اگر بود، ریشه در خودِ کارت است |
| ۲ | صفحه‌ی سفیدِ تبِ خانه (نبودِ حالتِ لودینگ) | `FEEDBACK-round2` · اسکلتِ لودینگ اضافه شد ولی رو گوشی تایید نشده |
| ۳ | کرشِ «آزمایشِ تشخیص» | همان الگوی اسکرولِ تودرتو؛ **بررسی نشده که واقعاً رفع شده باشد** |
| ۴ | نوتیفیکیشنِ بلوبانک | گزارشِ کاربر، هنوز ریشه‌یابی نشده |
| ۵ | ~۳۰ موردِ `design/FEEDBACK-round2.md` | به‌خواستِ خودِ کاربر تا پایانِ بازطراحی معلق |
| ۶ | ویجت: تپ اپ را باز نمی‌کرد | کد رفع شده (بیلدِ ۵۳۶) ولی **رو گوشی تایید نشده** |
| ۷ | همه‌ی کارهای بعدِ بیلدِ ۵۱۳ | هیچ‌کدام رو گوشیِ واقعی دیده نشده‌اند |

#### وضعیتِ کلی
- **برنچ**: `claude/bank-9fiumm` (اجباریِ CCR). همه‌چیز پوش‌شده، دیسک تمیز.
  سرِ برنچ: `910726b`.
- **آخرین بیلدِ موفق: ۵۴۲** (تگِ `build-542`) - فروشگاهِ سکه، تشخیصِ سخت‌گیرترِ پیامک،
  دکمه‌ی «پرداخت»ِ بریده، دیالوگِ امتیاز، فهرستِ دوطبقه‌ی پیامک‌ها.
  ⚠️ بیلدِ **۵۴۱ شکست خورد** (دو خطای کامپایل) و بررسیِ `trailinglambda.py` از دلش درآمد.
  **بعدِ ۵۴۲ پوش شدند و در هیچ بیلدی نیستند**: خطِ «N وام عقب‌افتاده» بالای فهرستِ وام‌ها،
  و پژمردگیِ ۳۲پله‌ی هر چهار آیکون.
- بیلدِ ۵۴۰ هم موفق بود (تگِ `build-540`) - رفعِ کرشِ «افزودن از پیامک‌ها».
- بیلدِ ۵۳۹ هم موفق بود (تگِ `build-539`) - بندِ ایمنیِ کارتِ کشیده‌شده‌ی خانه.
  بیلدِ ۵۳۸ هم موفق بود: رفعِ پیامکِ آینده‌نگر (بیمه)، اعلانِ تک‌سوالی با منبع + دکمه‌ی «نه»،
  شش تمِ تازه و کاتالوگِ فروشگاه، و آیکونِ قلک.
- بیلدِ ۵۳۷ هم موفق بود (تگِ `build-537`) - شاملِ منبعِ پیام‌ها، ردِ پیامکِ قبض، و صفحه‌ی
  «افزودنِ دستی از پیامک‌ها». ⚠️ شش تمِ تازه و کاتالوگِ فروشگاه **در این بیلد نیستند** (بعدش کامیت شدند).
- بیلدِ ۵۳۶ هم موفق بود (تگِ `build-536`) - شاملِ ویجتِ بازطراحی‌شده + `updateAll`، چهار طرحِ
  آیکون و هشت پله‌ی پژمردگی، آمارِ وام کنارِ وام‌ها، و خطِ منبعِ قیمتِ Servix.
- بیلدِ ۵۱۳ هم موفق بود (`build-513`). قبلش ۵۰۱ موفق، و ۵۰۳/۵۰۷/۵۱۰/۵۱۲ شکست خوردن
  (هر چهارتا خطای کامپایل، هر چهارتا بعدش یه بررسیِ ایستای تازه گرفت - پایین‌تر).
  ⚠️ **هیچ‌کدوم از کارهای بعدِ ۵۰۱ رو گوشیِ واقعی دیده نشدن.**
- **سهمیه ریست شده** ولی همچنان **بدونِ درخواستِ صریح بیلد نگیر**. هر ران ≈۱۵ دقیقه.
- سرورِ زنده به‌روزه. **قیمت‌ها الان از servix.cc با ۵۶ نماد میان** (پایین‌تر).
- **🚨 درسِ محیطی (۴ شهریور)**: کانتینرِ این نشست ری‌استارت شد و ورکینگ‌کپی رو `main` نشسته بود،
  نه رو برنچِ کاری - ۳۵۷ کامیت عقب، بدونِ هیچ خطایی. **اولِ هر نشستِ ادامه‌دار
  `git log --oneline -1` و `git status` رو چک کن و اگه لازم بود
  `git reset --hard origin/claude/bank-9fiumm` بزن**، وگرنه رو یه پایه‌ی غلط کد می‌زنی
  (همون اشتباهی که یه‌بار کارِ سه subagent رو دور ریخت).

#### ✅ کارهای نشستِ ۱۲-۱۳ شهریور (همه پوش‌شده، تو بیلدِ ۵۱۳)

**بسته‌های کلاد دیزاین که اعمال شدن** (به‌ترتیب): بسته‌ی ۱۵فایلیِ دارایی/گزارش/خانه ·
`account-final` (فرم و فهرستِ حساب) · بسته‌ی سه‌فایلی (شیتِ تراکنش، تقویم، شیتِ نشان) ·
`badges-final` (نمادِ متمایز برای هر نُه نشان، به‌جای یه مدالِ یکسان) · `onboarding-final`
(بازبینیِ مسیرِ ورودِ اول) · `theme-shop` + `nested-categories` · `round6-kt`.

**🚨 درسِ مهم - بسته‌ی طراح می‌تونه کارِ قبلی رو بی‌صدا پاک کنه**: بسته‌ی `home-final` دیالوگِ
تاییدِ پرداختی رو که همون نشست به `HomeScreen` اضافه شده بود **حذف کرده بود** - دقیقاً همون
چیزی که کاربر شکایتش رو کرده بود («الان هیچی نمی‌پرسه»). برگردونده شد + یه کامنتِ هشدار
بالاش. **از این به بعد قبل از اعمالِ هر بسته، `git diff` رو فایل‌های مشترک رو بخون، نه فقط
فایل رو کپی کن.**

**تاییدِ کنش همه‌جا** (خواسته‌ی صریح: «رو هرچیزی که می‌زنم - چه حذف چه افزودن چه پرداخت - بپرسه»):
- `ui/components/ConfirmDialog.kt` با قالبِ واحدِ طراح جایگزین شد:
  `ConfirmTone { DESTRUCTIVE, PAYMENT, HEAVY_CHANGE }` + `UndoBar`.
- نسخه‌ی طراح یه پارامترِ `background` داشت که تو `GradientButton` وجود نداره - به گونه‌ها نگاشت شد.
- **پوششِ عقب‌رو**: `ConfirmDeleteDialog`/`ConfirmPayDialog` به‌عنوانِ wrapper موندن تا هر
  ۱۳ جای مصرفِ قبلی بدونِ تغییر کار کنن.

**تبِ دارایی - قیمت‌ها**:
- **۵۶ نماد** (از ۲۰ تا). `CATALOG_TO_CODE` تو `server/.../PriceService.kt` کامل شد از رو PDFِ
  خودِ servix که کاربر فرستاد (با `pdftotext -layout` خونده شد).
  **`TRX`/`SOL`/`DOGE` باگِ واقعی بودن** (سرویس داشتشون، نگاشت نداشت). شش نماد به کدِ ناموجود
  نگاشت شده بودن. هشت نماد رو خودِ servix نداره → تو `data/AssetCatalog.kt` با
  `hasLivePrice = false` علامت خوردن و `MarketPricesScreen` فیلترشون می‌کنه (تصمیمِ «ب»ِ کاربر).
- **تازه‌سازی هر ۲۹ دقیقه** (`REFRESH_INTERVAL` تو `PriceService`): سقفِ کاربر ۵۰ فراخوان در
  ۲۴ ساعته؛ ۱۴۴۰÷۵۰ = ۲۸٫۸ → ۲۹ دقیقه یعنی ۴۹ فراخوان در روز، یکی ذخیره. با
  `staleThresholdMinutes: 30`ِ خودِ servix هم جوره.
- **ساعتِ واقعی تو هدر**: `pricesUpdatedMinutesAgo` (که تیک نمی‌خورد و کهنه می‌شد) با
  `pricesUpdatedClock` (`toFaTime`) عوض شد - «۱۲:۳۰ به‌روز شد».
- ⏳ **فلشِ تغییرِ قیمت رو همه‌ی ردیف‌ها** هنوز نه - تاریخچه‌ی ۳۱ نمادِ تازه هنوز رو سرور جمع
  نشده. طراح باید حالتِ سومِ «هنوز داده نداریم» رو بکشه.
- ⏳ **چیدمانِ صفحه برای ۵۶ نماد** (گروه‌بندی/جستجو) کارِ طراحه، هنوز نیومده.

**صفحه‌ی حساب نقدی** (`ui/account/AccountDetailScreen.kt`):
- تراکنش‌ها **قابلِ ویرایش** شدن (تپ → `EditTransactionDialog`؛ فقط مبلغ و توضیح -
  نوع/حساب/تاریخ عمداً نه). `AccountViewModel.updateTransaction` تازه‌ست.
- **باگِ ۱۰برابر**: ردیف‌ها مقدارِ خامِ ریال رو با برچسبِ «تومان» چاپ می‌کردن. رفع شد با
  `rialToToman(...)`. (رجوع کن به قاعده‌ی «دیتابیس ریال، نمایش تومان».)
- اشاره‌ی کشفِ ژست: «برای ویرایش بزن · برای حذف بکش».

**شیتِ ثبتِ تراکنش**: ریشه‌ی `NewTransactionSheet` هیچ پس‌زمینه‌ای نداشت، پس صفحه‌ی زیرش
دیده می‌شد (گزارشِ کاربر با اسکرین‌شات). `.background(AppBg)` + یه `clickable`ِ بی‌اثر برای
گرفتنِ تپِ عبوری اضافه شد.

**کیفِ سکه از تبِ خانه**: `CoinChip` پارامترِ اختیاریِ `onClick` گرفت (هدفِ لمسی فقط وقتی
مقصد داره ساخته می‌شه)؛ تپ روش `CoinWalletScreen` رو به‌عنوانِ اورلی باز می‌کنه
(`BackHandler` + `.background(AppBg)` بهش اضافه شد).

**دسته‌بندیِ تودرتو** (`CategoryDao`/`CategoryRepository`/`CategoryManagementScreen`):
`parentName` **نام** است نه کلیدِ خارجی، پس Room هیچ cascadeای نداره - دستی پیاده شد:
`renameParentOfChildren` (تغییرِ نام: اول بچه‌ها بعد خودِ ردیف) و `promoteChildrenOf`
(حذف: بچه‌ها به سطحِ بالا میان، `parentName = NULL`) و `childCountOf` (برای نشون‌دادنِ
تعدادِ متاثر تو دیالوگ).

**فروشگاهِ تم**: بررسی شد و **از قبل کامل بود** - `ColorTheme` با چهار تم، قیمت، گیتِ نشانِ
`steady_month` برای طلایی، `applyTo(AppColorPalette)`، مسیرِ تنظیمات و `ThemeShopScreen`.
هیچ کدی لازم نبود؛ گزارشِ «چیزی برای انجام نیست» داده شد به‌جای دوباره‌سازی.

#### 🪙 بریفِ اکوسیستمِ سکه (خواسته‌ی بزرگِ کاربر، ۱۲ شهریور) - **هنوز پیاده نشده**
کاربر با ۸ اسکرین‌شات یه بریفِ کامل داد: «یک اکوسیستمِ کامل برای سکه … می‌خوام خیلی خیلی
سورپرایزم کنی و یک چیزِ بی‌نقص درست کنی». اجزاش:
- **~۱۰ تم** (طراح فعلاً ۴ تا داده؛ شش‌تای دیگه رو باید بفرسته - فقط سه‌تایی هگز لازمه).
- **آیکونِ اپِ خریدنی** + **دسته‌نمادهای خریدنی**.
- **اشتراکِ جایزه‌ای**: ۴ تا ۷ روز، ماهی یک‌بار، رسیدن بهش عمداً سخت.
- چیزهایی که **فقط با سکه** به‌دست بیان، و قاعده‌های کسبِ سکه دقیق و سقف‌دار.
- **پژمردگیِ آیکون**: اگه ۷ روز به اپ سر نزنی، آیکونِ اپ عوض می‌شه.
  ⚠️ **تصحیحِ صریحِ کاربر**: «نزدیک هشتا طراحیه و دوتا نیست» - یعنی **۸ پله، از روزِ دوم شروع**،
  نه دو حالت. (جوابِ کامل تو `design/ANSWERS-icon-count.md`.)
- **زیرساختِ سکه از قبل هست**: `CoinEventEntity` (جدولِ `coin_events`، ایندکسِ یکتا رو
  `(type, dateKey)`)، و موجودی همیشه **مشتق** می‌شه (`SELECT COALESCE(SUM(amount),0)`) -
  هیچ‌وقت یه شمارنده‌ی ذخیره‌شده. جدولِ اقتصادِ سکه تو `design/CoinEconomy.md`.
- ⛔ **چیزی که هنوز نیست و بلوکه‌ست**: `CoinViewModel`/`CoinRepository` وجود ندارن، پس
  `CoinScreen.kt` (`45a`) و `ShopScreen.kt` (`45b`) عمداً پیاده نشدن. `IconWither.kt` (`45c`)
  هم منتظرِ ۲۷ فایلِ SVGِ طراحه.
- **⚠️ عیبِ فنیِ اعلام‌شده به طراح**: فرمولِ `saturate = 1 − 0.10·n` رو آیکونِ لانچر
  **اجراشدنی نیست** - آیکونِ لانچر یه ریسورسِ ایستاست و `VectorDrawable` فیلترِ اشباع نداره.
  تنها راهِ واقعی: طراح خودش پالتِ «پله‌ی ۸»ِ کم‌اشباع رو بده تا بینِ دو پالت درون‌یابی بشه.
  (`activity-alias` مکانیزمِ عوض‌کردنِ آیکونِ لانچره.)

#### 🚨 دو قاعده‌ی آیکون که موقعِ ساختِ فروشگاهِ سکه باید رعایت بشن (طراح، ۲۱ شهریور)
- **الیاسِ بی‌مقصد فعال نشه.** هر چهار طرح (الف کیفِ پول · ب سکه · پ حرفِ ج · ت قلک) فایل
  دارن، ولی فقط طرحِ الف مقصد داره. تا وقتی `ShopScreen` (`45b`) ساخته نشده، فقط **نُه
  الیاسِ طرحِ الف** (پایه + هشت پله) تو مانیفست بمونن - وضعیتِ فعلی درسته. اگه ۳۲ الیاسِ
  چهار طرح فعال بشن، اندروید آیکونِ اپ رو چندبار تو فهرستِ برنامه‌ها نشون می‌ده و کاربر
  نسخه‌ی تکراری می‌بینه.
- **ویترینِ ده‌تاییِ تم دو سرگروه می‌خواد** (طراح، ۲۱ شهریور): چهار تمِ قدیمی زیرِ «تمِ پایه»
  و شش تمِ کاتالوگ زیرِ «تمِ رنگی» - وگرنه ده ردیفِ پشتِ‌هم و کاربر نمی‌فهمه کدوم از قبل مالِ اوست.
- **مبنای ترجمه‌ی مالکیتِ قدیمی، شناسه‌ی ذخیره‌شده‌ست نه تمِ فعال.** طراح نگران بود که ردیفِ
  `spend:THEME_PALETTE` نمی‌گه کدوم تم. **چک شد و این نگرانی اینجا موضوعیت نداره**: مالکیتِ
  تم هیچ‌وقت تو دفترِ سکه ننشسته - `UiPrefs.owned_themes` از اول شناسه‌ی خودِ تم رو
  (`blue`/`purple`) نگه می‌داشته، پس ترجمه به `theme:blue` بی‌ابهامه و تمِ فعال هیچ نقشی نداره.
- **ویترینِ فروشگاه همیشه پله‌ی صفره** (`49d`). ردیفِ فروشگاه نباید آیکونِ پژمرده نشون بده،
  وگرنه کاربر فکر می‌کنه جنسِ خراب می‌خره.

#### 🧪 بررسیِ ایستا - الان **۱۹ بررسی**
- `trailinglambda.py` (تازه، از شکستِ **۵۴۱**): لامبدای انتهایی در کاتلین همیشه به
  **آخرین** پارامتر می‌چسبد. `ShopRow(a, b, c, d) { … }` لامبدا را به `leading`ِ
  اختیاری داد و `onConfirm`ِ اجباری بی‌مقدار ماند. سه تله موقعِ نوشتنِ خودِ بررسی:
  `->` عمقِ جنریک را خراب می‌کرد · آفست یکی جلوتر از پرانتزِ بسته بود · و خودِ
  **اعلانِ** تابع هم با الگوی فراخوانی می‌خورد (`{`ِ بعدش بدنه است).
- ⚠️ الگوی دومِ شکستِ ۵۴۱ (`when` غیرِجامع بعد از حذفِ یک شاخه) بررسی **ندارد**.

#### 🧪 بررسیِ ایستا - سابقه (۱۷ بررسیِ قبلی) (سه‌تای تازه از شکستِ بیلدهای همین نشست)
`bash native-android/tools/static-checks/run-all.sh` **قبل از هر بیلد باید سبز باشه.**
- `callstyle.py` (تازه، از شکستِ **۵۰۷**): تو پروژه دو تابعِ هم‌نامِ `toFa` هست -
  `core.toFa(x)` سراسریه (`toFa(x)`) و `ui.jibak.toFa()` اکستنشنه (`x.toFa()`).
  ۲۱ جا تو ۴ فایل اکستنشن رو تابعی صدا زده بودن. این بررسی همین رو می‌گیره.
- `receivers.py` (تازه، از شکستِ **۵۱۰**): `AccountViewModel` نوشته بود `repository.` ولی
  فیلد `accountRepository`ه. هر گیرنده‌ی کوچک‌حرفِ فایل‌های ViewModel/Repository باید تو همون
  فایل اعلان شده باشه. (نسخه‌ی اولش ۳ مثبتِ کاذب از پارامترِ لامبدا داشت - رفع شد.)
- `redeclare.py` (تازه، از شکستِ **۵۱۲**): طراح هم `CoinEconomy.kt` هم `CoinRules.kt` رو
  فرستاده بود و هر دو `CoinSpend`/`CoinEvent`/`BuyResult` رو اعلان می‌کردن (~۴۰ خطا).
  `CoinRules.kt` حذف شد (خودِ README طراح فقط `CoinEconomy` رو لیست کرده بود). نسخه‌ی اولِ
  این بررسی فلیورها رو (`SubscriptionManager`، `FlavorAdManager`) تکراری می‌دید - با
  گنجاندنِ source-set تو کلید رفع شد.
- `moneytypes.py` برای `/data/coin/` استثنا خورد (سکه `Int`ه نه ریالِ `Double`).
- ⚠️ سه اسکریپت (`typography.py`، `opacity.py`، `oldstyle.py`) تو پوشه هستن ولی **تو
  `run-all.sh` صدا زده نمی‌شن** - این‌ها بررسیِ وفاداری به سیستمِ طراحی‌ان و پرسروصدان؛
  اگه لازم شد دستی بزنشون، خودکار سبز/قرمز نمی‌کنن.

#### 📤 بسته‌های تحویل‌شده به کلاد دیزاین (فایل، نه کد)
- **کدِ ورودِ اول** (اسپلش/خوش‌آمد/تم/مجوز/ورود/OTP/شیت‌های بعدِ ورود).
- **`BadgesScreen.kt` + `BadgeRetroSheet.kt`** + امضای مدلِ `Badge`.
- **بخشِ وام - ۹ صفحه** (۱۳ شهریور، آخرین تحویل): `MyLoansScreen` · `LoanDetailScreen`
  (۱۹۴۸ خط) · `InstallmentDetailScreen` · `AddManualLoanScreen` · `CalculatorHostScreen` ·
  `BankLoanScreen` · `ResultScreen` · `AffordScreen` · `DepositScreen`، به‌علاوه‌ی
  `MyLoansViewModel` و چهار فایلِ کمکی (`PresetCard`, `BankTile`, `ProgressRing`, `LoanPresets`).
  README همراهش هشت قاعده‌ی نشکستنی + سه نکته‌ی خاصِ همین بخش رو داشت.
- اسنادِ جواب که تو `design/` کامیت شدن: `SERVIX-symbols.md` · `MESSAGE-round6-to-design.md` ·
  `ANSWERS-themeshop-nested.md` · `ANSWERS-icon-count.md` · `ANSWERS-round6.md`.
- **جوابِ سوالِ بلوکه‌کننده‌ی طراح**: `AppPrimary` از قبل `@Composable get() =
  LocalAppColors.current.primary`ه و هر ۱۹۹ مصرفش داخلِ composableه؛ الگوی
  `val x = AppPrimary` قبل از `Canvas` **قاعده‌ی مستندِ پروژه‌ست، نه استثنا**.

#### ⏸ چیزی که عمداً اعمال **نشد**
`AmountText.kt`ِ طراح - سیستمِ `LocalPrivacyMode`ِ موجود (۱۸۱ جای مصرف) رو دوباره‌کاری می‌کرد.
به‌جاش پیشنهاد شد همون لایه‌ی موجود دو چیز کم داشت: **تپ برای نمایشِ ۴ثانیه‌ای** و
**ماسکِ نقطه‌ایِ هم‌عرض**. منتظرِ نظرِ کاربر/طراح.

#### ✅ دو قابلیتِ متمایزکننده (۱۱ شهریور) - خواسته‌ی صریحِ کاربر «همه رو اضافه کن»
هر دو عمداً چیزی رو نشون می‌دن که کاربر **نمی‌دونه** یا **هنوز نشده** - نه گزارشِ گذشته:
- **اشتراک‌یاب** (`core/RecurringDetector.kt` + `ui/accounting/SubscriptionFinderScreen.kt`):
  خرجی که تو ≥۳ ماهِ **متمایز** با مبلغِ ±۱۵٪ ثابت تکرار شده رو از رو تاریخچه کشف می‌کنه.
  مبلغِ اعلامی **میانه**ست نه میانگین. درِ ورودی: `DiscoveryCard`ِ تبِ گزارش (تنها کارتِ کشفی
  که تپ‌پذیره). دو اقدام: افزودن به پرداخت‌های تکراری، یا نادیده‌گرفتنِ دائمی
  (`UiPrefs.ignoredSubscriptions`). ⚠️ با کارتِ «اشتراکِ تکراری»ِ قدیمی فرق داره - اون
  اعلامِ **دستیِ** کاربره؛ موردِ مشترک از لیستِ کشف حذف می‌شه.
- **پیش‌بینیِ کسری** (`core/MonthForecast.kt` + `ShortfallForecastCard` تو خانه): سرعتِ خرجِ
  روزانه × روزهای مونده، در برابرِ موجودیِ همه‌ی حساب‌ها. **زیرِ ۷ روزِ گذشته از ماه عمداً
  هیچی نمی‌گه** (یه خریدِ بزرگ کلِ تخمین رو منفجر می‌کنه).
- ⏳ **هیچ‌کدوم فریمِ طراحی ندارن** - `design/MESSAGE-round5-to-design.md` نوشته شد با سه سوالِ
  باز (چیدمانِ ردیفِ اشتراک، جای کارتِ هشدار و اینکه قرمزِ «فوری» درسته یا نارنجیِ نرم، و
  نسبت/امضای برندِ تصویرِ رسیدِ دنگ). **قبل از قطعی‌کردنِ ظاهر، جوابِ طراح رو بگیر.**

**🔑 درسِ تازه: تستِ واحد یه باگِ واقعی گرفت، نه انتظارِ غلطِ تست.** `normalizeLabel` علائم رو
پاک نمی‌کرد، پس «اسنپ -» و «اسنپ» دو گروهِ جدا می‌شدن و هیچ‌کدوم به حدنصابِ سه ماه نمی‌رسیدن -
یعنی اشتراکِ واقعی **اصلاً کشف نمی‌شد** و کاربر فقط می‌دید «چیزی پیدا نشد». بیلدِ ۵۰۰ رو همین
شکست. **درس: برای هر تابعِ نرمال‌سازی، حالتِ «با علامت» و «بدونِ علامت» رو هم تست کن.**

#### ✅ جوابِ کلاد دیزاین رسید (۱۰ شهریور) - بسته‌ی به‌روز داخلِ `design/`
کاربر بسته‌ی کامل رو فرستاد و **هر دو سوالِ ما جواب گرفت**. `design/REPLY-loan-27a-27e.md` حذف شد
(دیگه موضوعیت نداره) و این فایل‌ها به‌روز شدن: `Duolingo Redesign.dc.html` (همگام، شاملِ `27f`)،
`support.js`، `Loan-27a-27f-handoff.md`، `Icon-Splash-38b-39-handoff.md`، `DARK-MAP-and-MEDAL.md`.

- **🚨 اصلاحِ شماره‌ی فریم: محاسبه‌گرِ ادغام‌شده `27f`ه نه `27e`.** اشتباه از خودِ طراح بود، نه از
  نسخه‌ی ما - `27e` تو همه‌ی نسخه‌ها «مودال‌ها و شیت‌ها» بوده. (تاییدِ محلی: نسخه‌ی قدیمیِ ما صفر بار
  `27f` داشت، نسخه‌ی تازه دو بار.)
- **`settledAt`**: تایید شد که **لازم نیست** - تسویه‌شده = `paidCount == totalCount`، و تاریخِ
  تسویه = تاریخِ پرداختِ آخرین قسط.
- **جدولِ نرخِ بانک‌ها**: تایید شد که **از طرح بیرونه**؛ `banks.json` با `rateAnnualPercent` منتفی.
  «تصمیمِ ۱٫۵»ِ هندآف عیناً همون پیشنهادِ خودمونه (پایین‌تر پیاده شد).
- **`27f` نسخه‌ی تیره نداره** - طراح گفت اگه لازم شد می‌سازه، وگرنه نقشه‌ی تیره از
  `design/DARK-MAP-and-MEDAL.md` برداشته بشه. (فعلاً ازش استفاده نکردیم چون همه‌ی رنگ‌های `27f`
  از توکن‌های موجود درمیان و خودشون تمِ تیره دارن.)

**متنِ دورِ قبلی که هنوز جوابش کامل نیومده**: `design/MESSAGE-round3-to-design.md`
(ده موردِ بی‌فریم: مرکزِ پیام‌ها · شخصی‌سازیِ نوارِ پایین · آیکونِ اپ · اسپلشِ انیمیشن‌دار ·
تم‌های بیشتر · سررسید · آیکونِ خوکِ بودجه · کارت‌های کشفِ گزارش · کشوی میان‌بر · جای آواتار).
**از این ده تا، پنج موردِ «تازه» جواب گرفت** (مرکزِ پیام‌ها `40` · آیکون+اسپلش `38b`/`39` ·
تم‌ها `18b` · **نوارِ پایین `41`**). **پنج موردِ «اصلاح» هنوز مانده**: سررسید · آیکونِ خوکِ بودجه ·
کارت‌های کشفِ گزارش · کشوی میان‌بر · جای آواتار/چرخ‌دنده - خودِ طراح گفته منتظرِ همین پنج‌تاست.

#### ✅ بخشِ ۴۱ - شخصی‌سازیِ نوارِ پایین - **پیاده شد** (۱۱ شهریور، بیلد گرفته نشده)
فایل‌های تازه: `core/NavSuggestion.kt` (+ `NavSuggestionTest.kt`، ۹ تست) · `ui/nav/NavDestinations.kt` ·
`ui/nav/NavSlotsViewModel.kt` · `ui/nav/NavSuggestionCard.kt` (`41a`) · `ui/nav/NavEditorSheet.kt` (`41b`).
- **نوار دیگه `BottomTab.entries` نیست** - از `navSlots`ِ DataStore میاد. `NavDestination.sanitize`
  تضمین می‌کنه همیشه ۵ خانه‌ست و اسلاتِ ۰ خانه‌ست (قفلِ `41c` تو **داده** اعمال می‌شه نه فقط UI).
- **مخزن فعلاً ۷ مقصده نه ۱۴**: خانه/دارایی/گزارش/بودجه/سررسید/وام/چک. بقیه‌ی چیزهایی که طراح
  شمرده (پیام‌ها، تنظیمات، هدف، تقویم) **مقصدِ `NavHost` نیستن**؛ با اضافه‌شدنِ هر route، فقط یه
  ردیف به `NavDestination` اضافه می‌شه.
- **شمارش تو `navigateTo()`ه، نه `onClick`ِ نوار.** (اول اشتباه تو نوار گذاشته شد: مقصدی که
  نامزدِ **آمدن** به نواره اصلاً از نوار باز نمی‌شه، پس هیچ‌وقت هیچ پیشنهادی ساخته نمی‌شد.)
  `navigateTo` تنها قیفِ ناوبریِ رو-به-جلوئه، پس دقیقاً همون «رویدادِ ورودِ صفحه»ی `41c`ه.
- ⚠️ **دو انحراف از سندِ طراح، هر دو عمدی:**
  1. **کشیدن راهِ اصلیه** (خواسته‌ی صریحِ کاربر، ۱۱ شهریور) - طراح گفته بود «با کشیدن نیست».
     ضربه **حذف نشد**، راهِ دوم موند، تا نگرانیِ دسترس‌پذیریِ طراح هم جواب بگیره.
  2. **فشارِ طولانی رو نوار** ویرایشگر رو باز می‌کنه - تو سندِ طراح نیست، ولی تنها درِ ورودیِ
     طرح (کارتِ `41a`) تا ۲۱ روز داده جمع نشه اصلاً نمیاد؛ بدونش قابلیت سه هفته دسترس‌ناپذیر بود.
- ویرایشگر عمداً **`FullScreenDialog` نیست** - داخلِ محتوای `Scaffold` رندر می‌شه تا نوارِ واقعی
  دیده بشه و همون لحظه عوض بشه (قاعده‌ی مرکزیِ `41b`).
- ⏳ **باقی‌مونده**: `TourTarget` اگه کاربر تبِ اسپاتلایت‌شده رو از نوار برداره، مختصاتِ کهنه تو
  `tourBounds` می‌مونه. ریسکش ناچیزه (تور فقط اولین اجراست، نوار اون موقع پیش‌فرضه).

#### 📦 بسته‌ی ۴۱ - چیزی که رسید (۱۱ شهریور)
سند: `design/NavBar-41-handoff.md`؛ فریم‌های `41a` (کارتِ پیشنهاد) · `41b` (ویرایشگر) · `41c` (قاعده‌ها)
داخلِ `Duolingo Redesign.dc.html` (این فایل **فقط اضافه شده، هیچ چیزی حذف نشده**).
- **ایده‌ی مرکزی**: راهِ اصلی صفحه‌ی تنظیمات **نیست** - اپ خودش بازشدنِ صفحه‌ها رو می‌شمره و تو
  خانه یه کارتِ پیشنهاد میاره («۴۱ بار وام، ۲ بار بودجه - جابه‌جا کنم؟») با نوارِ قبل/بعد و سه جواب.
  ویرایشگر دکمه‌ی دومِ همون کارته.
- ⚠️ **تناقض با تصمیمِ قبلیِ خودمون**: تو `MESSAGE-round3` نوشته بودیم «فقط خانه و سررسید ثابت‌اند»؛
  طراح گفته **فقط اسلاتِ ۰ (خانه) قفل**، بقیه آزاد - قاعده‌ش: هیچ مقصدی نباید تنها راهش نوار باشه.
- دیتای لازم: `navSlots` (۵تایی) · `destinationOpenCount(destId, weekKey)` محلی · `navSuggestionState`.
  مخزنِ مقصدها **همون ۱۴تای `Shortcut`ِ کشوی میان‌بره**، دو نمایشِ مستقل.
- شرطِ نشون‌دادنِ پیشنهاد (هر سه با هم): ≥۲۱ روز داده · نسبتِ ≥۵:۱ · ≥۶۰ روز از پیشنهادِ قبلی.
- جابه‌جایی **با ضربه** (اسلات ← مقصد)، نه کشیدن. نوارِ واقعی همون لحظه عوض می‌شه.
- تعدادِ خانه‌ها و آیکونِ سفارشی **عمداً نیومدن** و طراح گفته پیشنهادش «نه»ست.
- همراهش سه فایلِ تازه هم اومد: `design/JibakComponents.kt` (کامپوننت‌های مرجع)،
  `design/JibakFormat.kt` (ارقامِ فارسی)، و `design/ic_launcher_{background,foreground}.xml`.
- ⚠️ `TOKENS.md` و `Color-values.kt.txt`ِ این زیپ **عقب‌تر از نسخه‌ی ماست** (`#A8E0C4`/`ChartBarMuted`
  رو حذف کرده) - عمداً جایگزین **نشدن**.

#### ✅ کارهای این نشست (همه پوش‌شده، هیچ‌کدوم تو بیلدِ ۴۹۲ نیستن)

**قیمتِ زنده‌ی دارایی‌ها - سرور و اپ هر دو آماده**
- منبع عوض شد به **gheymat.online**: `GET https://backend.gheymat.online/api/prices/all`
  با هدرهای `x-api-token` و `X-Country-Code: IR`. جوابش `{"data":[{symbol, sell_price, ...}]}`.
- `server/.../PriceService.kt`: تازه‌سازیِ **ساعتی** (۲۴ بار در روز از سقفِ ۲۵۰ در روز)،
  کش تو `price_snapshot`، و **نگاشتِ نمادها (`SYMBOL_ALIASES`) عمداً سمتِ سروره** تا اصلاحش
  آپدیتِ اپ نخواد. قیمت‌ها **به ریال** تبدیل می‌شن (نمادهای `-TMN` ×۱۰).
- **تاریخچه**: جدولِ `price_history` (کلیدِ مرکبِ symbol+date) از رو همون fetchهای ساعتی پر
  می‌شه - سهمیه‌ی اضافه نمی‌خواد. `GET /api/prices/history?symbol=BTC&days=30`.
  ⚠️ **گذشته نداریم**، از روزِ راه‌اندازی به بعد پر می‌شه.
- اپ: `AssetRepository.refreshPrices()` + `priceHistory()` + `priceChangePercent()`،
  `AssetViewModel.monthChange`، و `ApiService.getPrices()/getPriceHistory()`.
- ⏳ **هنوز چک نشده**: بعدِ اولین fetchِ واقعی، `raw` تو جوابِ `/api/prices` رو ببین تا اسمِ
  دقیقِ نمادها معلوم بشه و `SYMBOL_ALIASES` اصلاح بشه.

**صفحه‌ی وام - کامل شد (`27a` · `27b` · `27e` · `29p`)**
- `27b` جزئیاتِ وام: کارتِ خلاصه (حلقه‌ی درصد + مانده/قسط/اقساط/سود) · **پیش‌نمایشِ سه‌ردیفه‌ی
  اقساط بدونِ دکمه‌ی پرداخت** (تصمیمِ `36i`) + «دیدنِ همه‌ی N قسط» · نوارِ قرمزِ قسطِ عقب‌افتاده
  که به همون ردیف می‌بره · کارتِ «تسویه‌ی زودتر».
- `29p`: کارتِ **مشخصات** (بانک/مبلغ/نرخ/تعدادِ قسط/پایان/ضامن) + کارتِ **قسطِ بعدی** با دکمه‌ی
  پرداخت (وقتی قسطِ عقب‌افتاده هست، جاش رو به نوارِ قرمز می‌ده).
- `27a`: ترتیبِ تب‌ها، قرصِ سبزِ تبِ فعال، **ردیفِ پشتیبان‌گیری** به‌جای دو دکمه، و
  **فیلترِ «فعال / تسویه‌شده»** (پیش‌فرض فعال) جای دکمه‌ی متنیِ قبلی.
- `27f` **(تصمیمِ کلاد دیزاین، ۹ شهریور - شماره‌ش `27e` نیست)**: تبِ چهارمِ «بانکی» **ادغام شد** تو
  «محاسبه‌گر». حالا سه تب: وام‌های من · سپرده · محاسبه‌گر. داخلِ محاسبه‌گر سگمنتِ دوحالته:
  `INSTALLMENT` («قسط و سود»، پیش‌فرض) / `AFFORDABILITY`. فایلِ `ui/CalculatorHostScreen.kt`.
  **حالتِ آخر ذخیره نمی‌شه** (خواسته‌ی صریحِ طرح). کارتِ سبزِ «از عهده‌اش برمی‌آیم؟» زیرِ نتیجه‌ی
  حالتِ اول، قسطِ محاسبه‌شده رو به حالتِ دوم می‌بره (`AffordScreen(initialInstallment)`).
  **دکمه‌ی + هدر عمداً اضافه نشد** - افزودن فقط با FAB (تاییدِ صریحِ طراح).
- `LoanRepository.getRatePct()` و `earlySettlementSaving()` تازه‌ان.
- **«تصمیمِ ۱٫۵» - منبعِ نرخ (۱۰ شهریور، تاییدشده‌ی طراح)**: داخلِ حالتِ «قسط و سود» یه سگمنتِ
  دومِ کوچیک‌تر: **سرویسِ اعتباری** (نرخ از سرور، فیلد **خواندنی**، اسلایدرش هم بی‌اثر) /
  **وامِ بانکی** (نرخ **دستی**، با چهار قرصِ نوعِ وام که فقط فیلد رو پر می‌کنن و قابلِ ویرایش
  می‌مونه: ازدواج ۴٪ · مسکن ۱۸٪ · قرض‌الحسنه ۰٪ · سایر). تو حالتِ دوم انتخابِ بانک **اختیاری**ه
  و فقط اسم/لوگو/رنگ می‌ده. `loanTypePresets` عمداً **محلیه نه سرور** (سالی یه‌بار عوض می‌شه و
  آفلاین هم باید کار کنه) - تو `ui/BankLoanScreen.kt`.

**`17a` شیتِ ثبتِ تراکنش**
عنوانِ پویا (خرجِ تازه/دخلِ تازه/جابجایی) + آیکونِ هم‌رنگ · سه **قرصِ مستقل** جای تاگلِ لغزنده ·
کارتِ مبلغِ **وسط‌چینِ ۳۴یِ بدونِ کادر** (`BasicTextField`) · دکمه‌ی «**ثبت · ۱۰ سکه**» ·
دکمه‌ی «**+ باز**» که ثبت می‌کنه ولی شیت رو باز نگه می‌داره (فقط مبلغ و توضیح پاک می‌شن).

**دارایی**: تپ رو طلا/رمزارز حالا `AssetDetailScreen` رو باز می‌کنه (`onOpenAsset` خالی مونده بود).

**بررسیِ ایستا (وضعیتِ اون نشست: ۱۲ بررسی؛ الان ۱۷ - رجوع کن به بخشِ «بررسیِ ایستا» بالا).**
سه خلأ تو همون نشست پیدا و بسته شد:
1. `tools/static-checks/icons.py` **(تازه)** - `Icons.Filled.X` بدونِ ایمپورت + `roundToInt`.
2. `verify.py`: `BasicTextField`/`SolidColor`/`TextAlign`/`TextStyle`/`KeyboardOptions` اضافه شدن -
   بلافاصله یه `TextAlign`ِ بی‌ایمپورتِ **واقعی** تو `LoanDetailScreen` گرفت که بیلد رو می‌شکست.
3. قبل‌ترش `Spacer/Box/Row/Column`.
**قبل از هر بیلد `bash tools/static-checks/run-all.sh` باید سبز باشه.**

#### ✅ چک + طلب‌وبدهی/دنگ - کامل شد (۱۱ شهریور، طبقِ `design/ANSWERS-chequecounterpartydang.md`)
هر ۷ فریمِ چک و هر ۴ فریمِ طلب‌وبدهی/دنگ پیاده شدن (کامیت‌های `d2b2b7e` تا `7338af0` رو
`claude/bank-9fiumm`، **بیلد گرفته نشده**):
- **چک**: `7b` (بخشِ جمع‌شونده‌ی «اطلاعاتِ بیشتر» با شناسه‌ی صیادی/دسته‌چک/بابت) · `8a` (از قبل کامل
  بود، بدونِ تغییر) · `29k` (شناسه‌ی صیادی/۴رقمِ آخرِ حساب/بستنِ دسته‌چک) · `29l` (کارتِ طرفِ‌حساب +
  امتیازِ ریسکِ برگشت تو جزئیاتِ چک) · `29m` (از قبل کامل بود) · `29a` (CategoryDonut به‌جای
  ProgressRing تو هیرویِ گزارش) · `21g` (اشاره به امتیازِ ریسک تو حالتِ خالی).
- **طلب‌وبدهی/دنگ**: `22a`/`22b` (آدمک+موبایل تو لیست/جزئیاتِ طرف‌حساب) · `22c` (دنگ - `DangScreen.kt`/
  `DangViewModel.kt`/`DangRepository.kt`، هر ۴ روش مساوی/درصدی/دلخواه/قلم‌به‌قلم + حالتِ مهمانی +
  صورت‌حسابِ عکسی با `rememberGraphicsLayer`) · `22d` (دیالوگِ ویرایشِ اسم/موبایل/شکلِ آدمک، رنگ
  عمداً غیرقابل‌ویرایش چون قطعی از رو نامه).
- **زیرساخت**: `MIGRATION_28_29` (نسخه‌ی ۲۸→۲۹، جدولِ `dang_events`/`dang_participants`/
  `dang_items`/`dang_item_shares` + ستون‌های تازه‌ی `ChequeBookEntity`/`CounterpartyEntity`/
  `ChequeEntity.counterpartyId`/`LoanEntity.counterpartyId`) · `core/NameMatch.kt` (نرمال‌سازیِ
  اسمِ فارسی، تازه نوشته شد چون تابعِ «۱۷b» تو کد پیدا نشد) · `core/ChequeRiskScore.kt` (فرمولِ
  **موقت** - فرمولِ دقیق بعداً میاد) · `CounterpartyPickerDialog.kt` (مشترکِ چک/دنگ).
- **حدسِ خودکارِ طرفِ‌حساب رو دیتای قدیمی** (سوالِ ۶): `ChequeViewModel`/`MyLoansViewModel` هر دو
  یه‌بار تو `init` چک/وامِ بدونِ `counterpartyId` رو با `DebtRepository.guessOrCreateCounterparty`
  وصل می‌کنن (هم‌الگو با `LoanRepository.repairPaidCounts`).
- ⚠️ **انتخابِ اجباریِ طرفِ‌حساب موقعِ ثبت** فقط رو فرمِ چک (`AddEditChequeScreen`) پیاده شد، نه رو
  فرمِ وام - چون وام هیچ فریمِ طراحیِ اختصاصی برای این نداشت و دست‌زدن به `BankLoanScreen`/
  `AddManualLoanScreen` (مسیرِ حساس، رجوع کن به بخشِ فرمول‌های حساس) بدونِ طرحِ تاییدشده ریسکِ
  بی‌مورد داشت.
- ⏳ **بیلد گرفته نشده** - قبل از اولین بیلد `bash tools/static-checks/run-all.sh` (سبز بود) رو
  دوباره بزن و رو گوشیِ واقعی تستِ ثبتِ چک/دنگ رو انجام بده.

#### 📋 فریم‌های باقی‌مانده (۲۰ تا)
⚠️ «پیاده نشده» یعنی **طرحِ جدید نگرفته**، نه اینکه قابلیتش نباشه. و **چندتاشون احتمالاً از قبل
زیرِ شماره‌ی دیگه‌ای انجام شدن** (`19a`/`19b`/`19c` تو `27d`، `20d` کیفِ سکه، `29h` مجوزها) -
**قبل از شروعِ هرکدوم اول کدِ واقعی رو چک کن.**

| گروه | فریم‌ها |
|---|---|
| حسابداری (۳) | `17b` مدیریتِ دسته‌ها · `21b` افزودنِ حساب · `29f` جزئیاتِ حساب‌کتاب |
| وام/سپرده (۶) | `11c` شیتِ جزئیاتِ وام · `29b` آمارِ وام · `29e` تاریخچه‌ی محاسبات · `29j` محاسبه‌گرِ سقفِ وام · `29n` سپرده · `21f` وامِ خالی |
| اشتراک/سکه (۳) | `20a` معرفیِ امکانات · `20c` اشتراکِ چهارپلنه · `20d` کیفِ سکه |
| تنظیمات (۳) | `19a` پشتیبان‌گیری · `19b` امنیت · `19c` درباره |
| متفرقه (۵) | `9a` تقویمِ مالی · `29g` جزئیاتِ دارایی · `29h` گیتِ مجوزها · `29i` انتخابِ تاریخ · `29o` یادداشت |

**ترتیبِ توافق‌شده‌ی بعدی**: چک (۷ فریمِ یکجا) ← طلب و بدهی ← بقیه.

### 📋 جواب‌های کاربر به ابهاماتِ چک + طلب‌وبدهی/دنگ (۱۱ شهریور) - فقط ذخیره شد، کد نزده
سندِ کامل: `design/ANSWERS-chequecounterpartydang.md`. **قبل از شروعِ چک/طلب‌وبدهی این ۱۱ مورد رو بخون:**

**چک:**
۱. فیلدهای اضافه (شناسه‌ی صیادی، دسته‌چک، یادداشت) حذف نشن - زیرِ فرمِ `7b` یه بخشِ «بیشتر»ِ
   جمع‌شونده (پیش‌فرض بسته) بساز و این سه رو اونجا بذار.
۲. **امتیازِ ریسکِ برگشت واقعیه، نه تبلیغاتی** - امتیازدهی به صادرکننده از رو تاریخچه‌ی پاس/برگشتِ
   چک‌هایی که به همون صادرکننده لینکن. فرمولِ دقیق جداگانه مطرح می‌شه؛ فعلاً فقط بدون دیتامدل +
   محاسبه لازمه، نه فقط UI.
۳. `ChequeBookEntity`: شناسه‌ی صیادی + ۴رقمِ آخرِ حساب + `closedAt` اضافه بشن. Migration لازمه.
۴. یادآورِ چک: هر چک تنظیمِ مستقلِ خودش رو می‌گیره (طبقِ `29m`). تنظیمِ سراسریِ فعلی **حذف نمی‌شه** -
   پیش‌فرضِ چک‌های تازه می‌مونه؛ کاربر بعداً هر چک رو جدا می‌تونه عوض کنه. **اقساطِ وام همچنان از
   همون تنظیمِ سراسری استفاده می‌کنن** - این تغییر فقط رو چکه.
۵. نمودارِ گزارشِ چک (`29a`): همون کامپوننتِ میله‌ای/دوناتیِ گزارشِ حسابداریِ فعلی کپی بشه؛ توکنِ
   رنگ و اندازه از همونجا بیاد.

**طلب‌وبدهی + دنگ:**
۶. **لینک‌شدنِ چک/وام به طرفِ‌حساب اجباری می‌شه** - موقعِ ثبتِ چک/وامِ جدید باید طرفِ‌حساب
   انتخاب/ساخته بشه. چک/وامِ قدیمی (`counterpartyId = null`) باید خودکار/حدسی وصل بشه: نامِ
   صادرکننده رو با نامِ طرف‌حساب‌های موجود - با **همون تابعِ نرمال‌سازی/شباهتِ ادغامِ دسته‌ها
   (`17b`)** - مقایسه کن؛ تطبیقِ قطعی وصل شه، مبهم/بی‌نام یه طرفِ‌حسابِ موقتِ «نامشخص» بگیره که
   کاربر بعداً دستی اصلاح می‌کنه. **تابعِ نرمال‌سازی رو تو کد پیدا کن و دوباره استفاده کن، از نو
   ننویس** مگر واقعاً قابلِ‌استفاده‌ی مجدد نبود.
۷. «دنگ» - نسخه‌ی اول هر ۴ روش (مساوی/درصدی/دلخواه/قلم‌به‌قلم). قلم‌به‌قلم با احتیاطِ بیشتر چون هر
   قلمِ فاکتور Entityِ جداست که به چند نفر نسبت داده می‌شه - اگه پیچیدگی/زمان مشکل شد همون‌جا خبر
   بده تا اولویت‌بندی دوباره بشه.
۸. حالتِ مهمانی/مناسبت - نسخه‌ی اول: عنوانِ رویداد + «پاکتِ موقت» تا بسته‌شدن + حذف از میانگینِ
   ماهانه‌ی گزارش، همه تو همین راند.
۹. صورت‌حسابِ عکسی: طرحِ دقیقِ ظاهری لازم نیست، یه چیزِ سادهٔ هم‌رنگِ اپ (لوگو/رنگ/فونتِ همون
   سیستمِ طراحیِ فعلی) کافیه. `rememberGraphicsLayer()` طبقِ استانداردِ پروژه.
۱۰. تشخیصِ خودکار/ادغامِ اسمِ تکراریِ طرفِ‌حساب: همون الگوریتمِ نرمال‌سازی/شباهتِ `17b` - **پیدا کن
    و دوباره استفاده کن**، از نو ننویس مگر واقعاً نشد.
۱۱. `CounterpartyEntity` فیلدِ `phone` تازه می‌گیره. رنگِ آواتار از همون پالتِ آواتارِ موجودِ اپ
    (`AvatarPalette-dark.kt.txt`) - نه تصادفی، نه دستی.

#### صفِ کارهای دیگه
0. ✅ **کرشِ «افزودن از پیامک‌ها» (۲۱ شهریور) - رفع شد.** صفحه داخلِ
   `SettingsSubPageScaffold` رندر می‌شد که یک `Column(verticalScroll)` است، و خودش
   فهرستِ تنبل دارد → **اسکرولِ عمودیِ تودرتو با ارتفاعِ بی‌نهایت = کرش**. دقیقاً همان
   الگوی کرشِ «آزمایشِ تشخیص». حالا هدر و اسکرولِ خودش را دارد و مستقیم در دیالوگِ
   تمام‌صفحه می‌نشیند. **قاعده: هر صفحه‌ای که `LazyColumn` دارد هرگز داخلِ
   `SettingsSubPageScaffold` نرود.**
0. ✅ **باگِ «کارتِ کشیده‌شده‌ی خانه» - با برداشتنِ خودِ کارت حل شد (۲۵ شهریور).**
   `StreakRepairCard` **دو جا** بود: تبِ خانه و کیفِ سکه. سندِ طراح (`56b`) صریح است که
   جایش کیفِ سکه است - «آنچه نیست فقط صفحه‌ای برای دیده‌شدنشان است» - و آن صفحه از قبل
   ساخته شده بود. نسخه‌ی خانه حذف شد (کامپوزبل + `repairable` + `refreshRepairable`).
   ⚠️ **علتِ کشیده‌شدن هیچ‌وقت پیدا نشد**؛ اگر همان کارت در کیفِ سکه هم کشیده دیده شد،
   یعنی ریشه در خودِ کارت است نه در چیدمانِ خانه.
1. **بازخوردِ دورِ دومِ کاربر** (`design/FEEDBACK-round2.md`، ~۳۰ مورد) - کاربر گفت **فقط ذخیره
   کن، بعدِ تمومِ بازطراحی همه رو با هم مرور و رفع می‌کنیم**. سه تا از بقیه مهم‌ترن:
   باگِ صفحه‌ی سفیدِ خانه (نبودِ حالتِ `loading`) · کرشِ «آزمایشِ تشخیص» · نوتیفیکیشنِ بلوبانک.
2. صفِ بی‌تاریخ: ارقامِ فارسی (لایه‌ی سراسری) · `AmountText` + ضربه برای نمایشِ موقت ·
   چیدمانِ دسته‌ی تودرتو (`36b`) · ستونِ `manuallyCategorized`.
3. **منتظرِ خودِ کاربر**: جی‌میلِ پشتیبانی + آیدیِ تلگرام · تصمیم‌های هدیه‌ی اشتراک ·
   تاییدِ دسته‌ی «سفر» · فازِ وب‌اپ.


### 🔴 نشستِ ۲۹ مرداد - کارهای بازِ فوری (اول این‌ها رو بخون)

**۱. دیپلویِ امنیتیِ سرور - ✅ حل شد (۲۰ مرداد)**
کامیتِ امنیتی (`83417f2` رو `claude/bank-9fiumm`) با `git cherry-pick` رو `main` هم اعمال شد
(کامیتِ `7d06a473`). ورک‌فلوی `build-server.yml` (رانِ #116) تو مرحله‌ی «بارگذاری jar سرور»
**۴ بار پشتِ‌سرهم** با `Failed to CreateArtifact: Artifact storage quota has been hit` شکست
خورد - برخلافِ پیامِ خودِ گیت‌هاب («هر ۶-۱۲ ساعت دوباره حساب می‌شه»)، بعدِ بیش از ۱۵ ساعت هم حل نشد.
**رفعِ ریشه‌ای**: `build-server.yml` بازنویسی شد - جای اینکه jar بینِ دو jobِ جدا (`build`→
`upload-artifact`→`deploy`→`download-artifact`) دست‌به‌دست بشه، حالا **یه job واحد**ه که مستقیم
از همون مرحله‌ی ساخت با scp به VPS می‌فرسته - دیگه اصلاً به فضای Artifact نیازی نیست. رانِ #117
(کامیتِ `e120d3d` رو `main`) با این روش **موفق شد**. سرور الان با رفعِ حفره‌ها بالاست.
⚠️ اگه بازم بیلدهای سرور به مشکلِ مشابه خوردن، این الگو (build+deploy تو یه job، بدونِ
upload/download-artifact) رو برای بقیه‌ی ورک‌فلوها هم به کار ببر.

**۲. کلیدِ API قیمتِ طلا/ارز - سمتِ سرور آماده شد، منتظرِ اجرای دستیِ ورک‌فلو**
کاربر (۶ شهریور) یه کلیدِ **واقعیِ رایگانِ نوسان** فرستاد (مقدارش عمداً این‌جا نیست - **کلیدِ واقعی هیچ‌وقت تو مخزن نوشته نشه**، فقط GitHub Secrets؛ این یکی چون یه‌بار این‌جا کامیت شده بود باید باطل شود)
(سقفِ ۱۲۰ درخواست در ماه، یعنی حدودِ ۴ بار در روز). سمتِ سرور کامل نوشته و تست شد:
- `server/.../PriceService.kt` - تازه‌سازی **زمان‌محور** نه درخواست‌محور: هر بار `/api/prices`
  صدا زده می‌شه فقط اگه بیشتر از ۶ ساعت از آخرین fetch گذشته باشه دوباره از نوسان می‌گیره (۴
  بار در روز = ۱۲۰ در ماه، سرِ حدِ مجاز). نتیجه تو جدولِ `price_snapshot` (ردیفِ تکی، مثلِ
  `app_version`) کش می‌شه.
  ⚠️ **ساختارِ دقیقِ پاسخِ نوسان تاییدنشده** - سندباکس نتونست `api.navasan.tech` یا لینکِ
  مستنداتِ کاربر (`bit.ly/34l72qi`) رو fetch کنه (egress بسته). پس پارس **عمومیه**: هر کلیدِ
  JSONِ ریشه (چه مقدارِ خام چه `{"value": ...}`) به Double تبدیل و برگردونده می‌شه - بعدِ اولین
  فراخوانیِ واقعی رو سرور، لاگ رو چک کن ببین اسمِ نمادها (`usd_sell`, `gol18`, ...) دقیقاً چیه.
- `server/.../routes/PricesRoutes.kt` - `GET /api/prices` عمومی و بدونِ auth (مثلِ
  credit-rates)؛ اگه سکرت هنوز ست نشده `{"prices": {}}` خالی برمی‌گردونه، خطا نمی‌ده.
- `.github/workflows/set-price-env.yml` - مثلِ الگوی ملی‌پیامک، سکرتِ `PRICE_API_KEY` رو تو
  `.env` رو VPS می‌نویسه و pm2 رو ری‌استارت می‌کنه.
- `./gradlew compileKotlin`/`test` سبز؛ اجرای محلی تستی هم شد (بدونِ کلید، جوابِ خالیِ صحیح داد).

**۲.۰ وضعیتِ قیمت - ✅ کاملاً حل شد (۱ شهریور، منبعِ نهایی: servix.cc)**

بعدِ سه‌بار عوض‌کردنِ منبع (نوسان ۴۰۱ → gheymat.online کلید رد شد → **servix.cc**)، الان
قیمت‌های واقعی از سرور میان و تاییدشده. کاربر تو `servix.cc/auth/register` ثبت‌نام کرد،
کلید گرفت، تو GitHub Secrets گذاشت. جوابِ زنده‌ی `/api/prices` (۱ شهریور):
دلار ۲٬۱۴۰٬۳۰۸ ریال، طلای ۱۸عیار ۲۲۲٬۰۹۷٬۵۰۷ ریال، سکه‌ی امامی ۲٬۲۱۰٬۶۳۷٬۵۰۰ ریال، بیت‌کوین
۱۶۷٬۴۲۳٬۵۸۶٬۰۸۲ ریال - همه مستقیم به ریال (نه تومن)، بدونِ نیازِ تبدیل.

**چرا servix بهتر از دو تای قبلی بود:**
- کلید تو **هدر** می‌ره (`X-API-Key`)، نه URL - امن‌تر.
- `GET /api/v1/assets` همه‌ی نمادها رو یه‌جا می‌ده، فقط ۱ واحد از سهمیه کم می‌کنه.
- مقادیرِ `..._RLS` مستقیم ریالن - نیازی به انتخابِ منبع/ضربِ ۱۰ (مشکلِ gheymat) نیست.
- **`/history` و `/ohlc` واقعی داره** - دیگه لازم نیست فقط از اسنپ‌شاتِ روزانه‌ی خودمون
  استفاده کنیم؛ می‌شه بعداً تاریخچه‌ی واقعی رو هم وصل کرد (فعلاً وصل نشده، کارِ بعدیه).
- **`TRX`, `SOL`, `DOGE` حالا پشتیبانی می‌شن** (تو gheymat نبودن) - فقط `TON` و
  `SILVER_999` هنوز نگاشتِ ریالی ندارن («—» درسته، نه باگ).

⚠️ **نکته‌ی نگه‌داری‌شده از تاریخچه‌ی قبلی این بخش** (چرا هر دو منبعِ قبلی رد شدن) پایین‌تر
مونده - برای درسِ «رمزِ پنل ≠ توکنِ وب‌سرویس» و «همیشه بدنه‌ی خطا رو لاگ کن، نه فقط کدِ وضعیت».

⏳ **باقی‌مونده**: پیامِ `attributionRequired: true` تو جوابِ `/api/v1/access` یعنی سرویکس
یه اعتبار/نشون‌دادنِ منبع تو اپ می‌خواد - هنوز جایی تو UI اضافه نشده. قبل از انتشارِ نسخه‌ی
بعدی باید یه خطِ کوچیک («داده‌ی قیمت از Servix.cc») جایی تو صفحه‌ی دارایی/تنظیمات اضافه بشه.

---
### 📜 تاریخچه‌ی منابعِ قبلی (بایگانی - فقط برای درس، دیگه لازم نیست دنبالش بری)

سه چیز درست شد و رو `main` دیپلوی شدن (`f7143e4`, `c2c2b6c`, `43a30e6`)، ولی **قیمت هنوز
نمیاد** چون خودِ سرویس کلید رو قبول نمی‌کنه:

1. **باگِ نوشتنِ کلید (رفع شد)**: هر سه ورک‌فلوی `set-*-env` مقدارِ سکرت رو مستقیم تو الگوی
   `sed -i "s|^KEY=.*|KEY=$VALUE|"` می‌ذاشتن. جداکننده خودش `|` بود و کلیدِ تازه‌ی gheymat
   یه `|` داشت → `sed: unterminated 's' command` و کلید **هیچ‌وقت رو سرور نمی‌نشست**.
   دو رانِ اول (`#2`, `#3`) دقیقاً به همین دلیل شکست خورده بودن، نه به‌خاطرِ خودِ کلید.
   حالا با `grep -v` + `printf` نوشته می‌شه (امن در برابرِ `| & / " $`) + `chmod 600`.
2. **پارسِ درستِ gheymat** (کامیتِ `ad20486`) رو `main` رفت و سرور سالم بالا اومد.
3. **چکِ سلامت** حالا قیمت‌ها رو **آخرِ** خروجی چاپ می‌کنه و استکِ کرش رو می‌بره، وگرنه
   بخشِ قیمت زیرِ صدها خط استکِ Compose گم می‌شد.

**🔴 مشکلِ باقی‌مونده - کلید معتبر نیست.** لاگِ خودِ سرور (۱۱ شهریور، ۱۰:۲۲):
```
price_fetch_failed | status=401
body={"success":false,"message":"A valid account token is required to access this endpoint.","error_code":"HTTP_ERROR"}
```
یعنی کلید سالم به سرور می‌رسه ولی gheymat ردش می‌کنه. سه احتمال، به‌ترتیبِ احتمال:
- **کلیدِ اشتباه کپی شده** - دقیقاً همون درسِ ملی‌پیامک: «رمزِ ورودِ پنل» ≠ «توکنِ وب‌سرویس».
  باید توکنی باشه که تو پنلِ gheymat زیرِ بخشِ API/توکن اومده.
- **اشتراک/پلنِ حسابِ gheymat فعال نیست** یا سهمیه‌ش تموم شده.
- کلید موقعِ کپی فاصله/نیم‌فاصله‌ی اضافه گرفته.

⚠️ **قبل از هر کارِ دیگه رو قیمت‌ها، اول این ۴۰۱ باید حل بشه** - کدِ سمتِ ما آماده‌ست و
با اولین کلیدِ معتبر بدونِ تغییر کار می‌کنه. برای تستِ سریع بعدِ عوض‌کردنِ سکرت:
`set-price-env.yml` رو دستی بزن، بعد `check-server-health.yml` (قیمت‌ها ته لاگ).

⏳ نگاشتِ پنج سکه (`SEKE/SEKB/SEKEN/SEKER/SEKG`) هنوز تاییدنشده‌ست - بعدِ اولین fetchِ
موفق، `raw` تو جوابِ `/api/prices` نامِ فارسیِ هر نماد رو می‌ده.

**۲.۱ وضعیت (۶ شهریور، همون نشست) - انجام شد:**
1. ✅ کاربر `PRICE_API_KEY` رو تو گیت‌هاب Secrets گذاشت.
2. ✅ کاربر صریحاً گفت «برو سریع انجام بده» - چون `set-price-env.yml`/کدِ سرور فقط رو
   `claude/bank-9fiumm` بودن (نه `main`) و گیت‌هاب workflow_dispatch رو فقط از رو `main`
   قبول می‌کنه، فقط همین قابلیت (نه کلِ برنچِ کاری) با PR #68 مرج شد رو `main`
   (کامیتِ `ad47715`). سرور با پوش به `main` خودکار دیپلوی شد.
3. ✅ `set-price-env.yml` دستی اجرا شد - کلید رفت رو سرورِ زنده.
4. ✅ (این‌ها مالِ منبعِ gheymatِ منسوخ‌شده بودن؛ منبعِ نهایی الان servixه - رجوع کن به بخشِ ۲.۰ بالا.)
5. ✅ `AssetRepository.refreshPrices`/`priceHistory` کاملاً وصلن (دیگه placeholder نیست) -
   رجوع کن به `native-android/data/.../AssetRepository.kt`. هنوز رو گوشیِ واقعی دیده نشده.

**۲.۱ هدیه‌ی اشتراکِ تشویقی - ثبت‌شده، هنوز پیاده نشده (۲۲ مرداد، آخرین‌بار دقیق‌شده)**
**نسخه‌ی نهاییِ خواسته‌ی کاربر** (نسخه‌ی قبلیِ «۲ ماه/۱ ماه» با این جایگزین شد):
- **همه‌ی کاربرانی که از قبل با شماره‌شون وارد شدن** (چه خریدار چه نه) → **۱ ماه هدیه**.
- **کسایی که تا الان اشتراک خریدن** → **۴۵ روز هدیه**.
- باید **موقعِ اولین ورودِ بعدی‌شون به کاربر اعلام بشه** (یه پیامِ توی اپ، نه فقط سایلنت).
- کاربر گفت **دوشنبه که سراغم اومد (بعدِ رفعِ محدودیتِ سهمیه‌ی هفتگی)، ازش لیستِ دقیقِ کارهایی که
  باید انجام بده رو بخوام** - یعنی اول ازش بپرس/بگو باید چیکار کنه، بعد شروع کن.

**۲.۲ تشخیصِ دسته‌بندیِ خودکارِ تراکنشِ بانکی - تصمیمِ کاربر گرفته شد، هنوز پیاده نشده**
وضعیتِ فعلیِ کد (`BankSmsReceiver.kt` خط ۷۱): تراکنشِ خودکارِ برداشت‌شده از پیامک/نوتیفیکیشنِ
بانکی همیشه می‌ره تو دسته‌ی کورِ «سایر هزینه»/«سایر درآمد» - هیچ حدسی نمی‌زنه، از کاربر هم چیزی
نمی‌پرسه. **کاربر گزینه‌ی «حدسِ هوشمند + پرسیدن فقط وقتی نامطمئنه» رو تایید کرد**: اگه از رو
کلیدواژه‌ی متنِ پیامک (اسمِ فروشگاه/سرویس) مطمئن بود خودش دسته‌بندی کنه، ولی وقتی نامطمئنه یه
نوتیفیکیشنِ کوچیک بده و از کاربر بپرسه دسته‌ش چیه. **هنوز کدنویسی نشده** - جزوِ همون لیستِ
دوشنبه‌ست.

**۲.۳ وضعیتِ کلاد دیزاین (بخشِ نوتیفیکیشن/کشوی میان‌بر)**: کلاد دیزاین هم داره رو بندِ نوتیفیکیشن
(هم دسترسیِ نوارِ اعلان، هم کشوی میان‌برِ کشیدنی) کار می‌کنه؛ کاربر گفت خودش هم به محدودیتِ سهمیه
خورده، وقتی کلاد دیزاین تمومش کرد کاربر فایل رو میاره اینجا. **روالِ کاری که کاربر خواست**: اول
با هم تستش کنیم، اگه کم‌وکسری نداشت تازه پیاده‌سازی رو شروع کنیم، و پیش‌رفتن **قدم‌به‌قدم** باشه

**۲.۴ ایمیل/کانالِ پشتیبانیِ جدید - منتظرِ کاربر**
ایمیلِ فعلیِ تنظیماتِ پشتیبانی هنوز اسمِ قدیمی («وام من») داره، باید عوض بشه (کاربر خودش باید
جی‌میلِ جدید بسازه، من نمی‌تونم اکانتِ گوگل بسازم). علاوه بر ایمیل، یه راهِ ارتباطیِ دومِ رسمی هم
قراره اضافه بشه - **پیشنهادِ من تلگرام بود، کاربر پرسید نظرِ من چیه، پیشنهاد دادم تلگرام**ه (رایج‌تره
بینِ اپ‌های ایرانی، کاربرهای اپ هم با شماره‌موبایلِ ایرانی وارد می‌شن). **منتظرِ آیدیِ تلگرام و
ایمیلِ جدید از کاربرم** - وقتی رسید، صفحه‌ی «پشتیبانی» تو تنظیمات آپدیت بشه.

**۲.۵ وب‌اپ (فازِ بزرگِ آینده، تایید شد ولی اولویتش بعدِ اندرویده)**
کاربر می‌خواد **دقیقاً همین برنامه رو تو مرورگر** (مثلِ سایتِ بانک‌ها) بسازه تا **رو iOS هم قابلِ
استفاده باشه** (به‌جای ساختِ اپِ نیتیوِ Swift که پروژه‌ی کاملاً جداست). از همون بک‌اندِ Ktورِ فعلی
استفاده می‌کنه. **محدودیتِ مهم**: خوندنِ خودکارِ پیامک/نوتیفیکیشنِ بانکی تو مرورگر اصلاً امکان‌پذیر
نیست - کاربرِ وب‌اپ باید تراکنش‌ها رو دستی وارد کنه. **تصمیم**: این فاز بعدِ تمومِ بازطراحیِ
Duolingoِ اندروید شروع بشه، نه هم‌زمان. هنوز هیچ کدی براش نوشته نشده.
⚠️ **کاربر خواستِ صریح**: برای همین محدودیتِ اتوماسیون تو وب‌اپ، **باید یه راه‌حل فکر بشه** - قبل
از شروعِ وب‌اپ باید گزینه‌ها رو بررسی کنیم (مثلاً: کپی‌کردنِ متنِ پیامک و پیست تو یه فیلد که خودش
پارسش کنه با همون منطقِ `BankSmsParser`، یا اتصال به ایمیلِ تراکنشیِ بانک‌ها اگه بانکی همچین چیزی
داره، یا هر ایده‌ی دیگه). **هنوز جواب نداده، فقط ثبت شد که موقعِ شروعِ فازِ وب‌اپ حتماً باید روش کار بشه.**
(نه همه‌چیز یه‌جا).

**۲.۶ چهار اصلاحِ کوچیکِ دیگه برای بریفِ کلاد دیزاین - ثبت‌شده، هنوز اعلام نشده**
- **واژه**: به‌جای «استریک» از «فعال» استفاده بشه (تو کلِ متن‌ها/طرح‌ها).
- **آواتارِ پروفایل**: یه آیکونِ کوچیکِ آواتار برای پروفایل با انتخابِ دختر/پسر - **فقط یه
  نمادکِ شخصی‌سازیه، نه مسکاتِ کارتونیِ سراسری** (تناقض با «بدونِ آدمک/مسکات» نیست - اون تصمیم
  سرِ جاشه، این یه آیکونِ کوچیکِ جداست).
- **مجوزِ باتری**: تو صفحه‌ی مجوزها (کنارِ مجوزهای پیامک/نوتیفیکیشن) یه آیکون/مرحله برای مجوزِ
  معافیت از بهینه‌سازیِ باتری هم اضافه بشه.
- **یادداشت‌ها**: امکانِ افزودنِ عکس به یادداشت‌ها هم اضافه بشه.

**۲.۷ هشدارِ افزایشِ قیمتِ ارزِ دیجیتال (تو تبِ دارایی) - ✅ پیاده شد (کامیتِ `04f32b9`)**
کاربر خواست: برای ارزِ دیجیتالِ تو دارایی، اگه قیمتش **نسبت به ماهِ قبل بالاتر رفته باشه** یه
هشدار/نشونه به کاربر نشون داده بشه (درصدِ تغییر). `AssetSection` حالا از
`AssetViewModel.monthChange` می‌خونه و یه بجِ کوچیکِ هشداررنگ کنارِ دارایی‌های رمزارزیِ صعودی
نشون می‌ده؛ دارایی‌های ارز/طلا/دلخواه بی‌اثرن. **هنوز رو گوشیِ واقعی دیده نشده** (بیلد لازم داره).

**۲.۸ دسته‌بندیِ جدیدِ «سفر» - ثبت‌شده (برداشتِ من از پیامِ گنگِ کاربر، تاییدِ صریح نگرفتم)**
کاربر دو پیامِ نامفهوم فرستاد («یکه شبیهِ خورشید هست» + «تو دسته سفر باشه») و دو بار به سوالِ
دقیق‌سازی «بدونِ نظر» جواب داد. برداشتِ منطقی‌ترین: یه **دسته‌بندیِ جدیدِ «سفر»** با **آیکونِ
شبیهِ خورشید** اضافه بشه. ⚠️ **این تاییدِ صریح نگرفته - دفعه‌ی بعد که کاربر اومد حتماً یه‌بار
شفاهی تاییدش کن قبل از اجرا** (احتمالِ برداشتِ غلط از پیامِ گنگ هست).

**۲.۹ آیکونِ ۷روزه‌یِ نشانِ «فعال» - ثبت‌شده**
وقتی نشانِ «فعال» (همون استریک، رجوع کن به بندِ ۲.۶) به ۷ روزِ پشتِ‌سرهم یا بیشتر برسه، آیکونش
باید **تارِ عنکبوت با عنکبوت** باشه (نه شعله یا هر نمادِ دیگه).

**نکاتِ بازِ مهم که قبل از پیاده‌سازی باید با کاربر دقیق بشه (نقدِ من، هنوز جواب نگرفتم):**
1. **تعریفِ «خریدن» روشن نیست** - یعنی کسی که *الان* اشتراکِ فعال داره، یا هرکسی که *تا حالا حتی
   یه‌بار* خریده (حتی اگه الان اشتراکش تموم شده)؟ فرقِ زیادی تو هزینه و منطقِ سرور داره.
2. **تداخلِ عددی با #35**: تسکِ #35 (پیاده نشده) قبلاً همین عددِ «۴۵ روز» رو برای «کاربرِ قدیمیِ
   سرور، موقعِ اولین ثبت‌نامِ رسمی» تعریف کرده بود. این یکی یه هدیه‌ی *جدا* با همون عدده برای گروهِ
   «خریدارها». از نظرِ فنی مشکلی نداره (جمع می‌شن)، ولی متنِ نمایش‌داده‌شده به کاربر باید این دو رو
   از هم جدا کنه وگرنه کاربر فکر می‌کنه دوباره همون هدیه رو گرفته.
3. **جمع‌شدن رو تاریخِ انقضای فعلی، نه جایگزینیش** - اگه یکی مثلاً اشتراکِ یک‌ساله داره، این هدیه
   نباید تاریخِ انقضاش رو کوتاه/بازنویسی کنه، فقط بهش اضافه بشه.
4. **ستونِ ضدِتکرار** - دقیقاً مثلِ #35، تا با هر لاگینِ بعدی دوباره هدیه ندن.
5. **هزینه‌ی این کار دقیقاً رو همون ۵-۶ نفرِ پولیِ فعلی می‌شینه** - ۴۵ روز رایگان برای دقیقاً
   کسایی که الان تنها منبعِ درآمدن. منطقیه به‌عنوانِ تشکر/وفاداری، ولی درآمدِ فعلی رو عقب می‌ندازه.

**نظرِ من (نقد، نه تایید):** ایده‌ی هدیه‌دادن برای تشکر از اولین خریدارها منطقیه، مخصوصاً چون هنوز
اولِ راهیم و رضایتِ همین چندنفر برای تبلیغِ دهان‌به‌دهان مهمه. ولی موردِ ۱ و ۲ بالا حتماً باید قبل
از اجرا با کاربر روشن بشه.

**درباره‌ی طراحی:** به نظرم نیازی به کارِ جدید از کلاد دیزاین نیست. `PostLoginSheets.kt` (تسکِ
#30) از قبل دقیقاً همین الگو رو داره: عددِ روز از سرور میاد + یه پیامِ اضافه‌ی جداگانه برای حالتِ
خاص («چون از قبل وارد برنامه شده بودی...»). این ساختار می‌تونه عیناً برای هر دو حالتِ جدید (خریدار
/ واردشده‌ی قدیمی) استفاده بشه - فقط متن‌ها عوض می‌شه. این یه کارِ منطق+سرور هست، نه طراحیِ جدید،
مگه اینکه بخوایم شکلِ ظاهریِ همون صفحه رو هم تو بازطراحیِ Duolingo عوض کنیم (که تو لیستِ کلی هست).

شبیهِ مکانیزمِ #35 - باید با **مهاجرتِ خودکار تو `Db.kt`** انجام بشه (نه SSHِ دستی)، با ستونِ
ضدِتکرارِ خودش (جدا از ستونِ #35). سرور باید از رو `subscription_purchases`/تاریخِ اولین‌ورود
تشخیص بده کدوم کاربر خریداره و کدوم فقط قبلاً واردشده.

**۳. بازطراحیِ دومِ ظاهری - سبکِ Duolingo (فعلاً فقط «ذخیره کن»، کد نزن)**
بعدِ بازطراحیِ اول («Liquid Glass»، پایین توضیح داده شده)، کاربر یه دورِ کاملاً جدید خواسته: کاربر
گفت رفتارِ کلاد دیزاینِ اولی رو نگاه کرد و دید ظاهرِ فعلیِ اپ (که از قبل «شیشه‌ای/تیره» بود) خیلی
فرقی با طرحِ اول نکرد - چون طرحِ اول اتفاقاً همون سبک رو پیشنهاد داده بود. این‌بار بریفِ زیر داده شد
به کلاد دیزاین (متنِ کامل قابلِ کپی، برای رجوعِ بعدی):

> برنامه‌ی اندروید فارسی «جیبک»... الان یه ظاهرِ «شیشه‌ای/تیره» داره و می‌خوام کاملاً عوض بشه به
> سبکِ Duolingo: رنگی، شاد، غیررسمی، پرانرژی - **بدونِ شخصیت/موجودِ کارتونی (بدونِ آدمک/مسکات)**.
> پس‌زمینه‌ی روشن، رنگ‌های پررنگ/solid (نه نیمه‌شفاف)، گیمیفیکیشن (استریک/بج/نوارِ پیشرفت)، آیکونِ
> گردتر، فونتِ درشت‌تر. نوارِ پایین دقیقاً ۵ تبِ فعلی بدونِ دکمه‌ی وسطِ برجسته بمونه، همه‌ی قابلیت‌های
> فعلی دست‌نخورده، فقط ظاهر عوض بشه.

کلاد دیزاین یه پیش‌نمایشِ ۳ صفحه (خانه/گزارش/دارایی) فرستاد - کاربر تاییدش کرد («فعلا اینطوری هست»)
و الان منتظرِ **فایلِ کاملِ همه‌ی ۱۶ صفحه**‌ست. **تا اون فایل نرسه، شروعِ پیاده‌سازی نکن.**

⚠️ **دو تا فیچرِ جدید تو این پیش‌نمایش دیدم که تو کدِ فعلی وجود ندارن** (نه صرفاً تغییرِ ظاهری):
- **استریکِ روزانه**: شمارشِ روزهای پشت‌سرهمی که کاربر تراکنش ثبت کرده («۷ روز پشت‌سرهم»). نیازِ
  به دیتای جدید (تاریخِ آخرین ثبت + شمارنده، احتمالاً تو `UiPrefs` یا یه Entityِ جدید) داره.
- **بج/دستاورد**: کارت‌های دستاورد («۳۰ روز بی‌وقفه»، «بودجه رعایت شد»). نیازِ به منطقِ تشخیصِ
  شرط‌ها و احتمالاً یه جدولِ جدید برای ذخیره‌ی دستاوردهای بازشده.
از کاربر پرسیدم می‌خواد این دو تا رو هم بسازم، هنوز جواب نداده (گفت «کلا تموم شد بهت میدم فایل رو»
- یعنی بعد از رسیدنِ فایلِ کامل باید دوباره بپرسم یا تصمیم بگیرم).

**⚠️ گسترشِ دامنه (بعدِ دیدنِ اولین پیش‌نمایش):** کاربر گفت فقط ۱۶ صفحه کافی نیست - **همه‌ی
صفحات/شیت‌های برنامه** (حدود ۳۶ فایل، نه فقط ۱۶ تا) باید بازطراحی بشن، و به کلاد دیزاین گفتیم
یه «سورپرایز» بسازه که برنامه رو از رقبا متمایز کنه و کاربرِ جدید رو معتاد نگه داره - رو تجربه‌ی
اولین‌ورود/گیمیفیکیشن هم تمرکز کنه. کلاد دیزاین یه پیش‌نمایشِ گسترده‌تر فرستاد (روشن+تیره برای
هر صفحه، یادداشت‌های پیاده‌سازیِ دقیق به فایل‌های واقعی، پالتِ رنگیِ جدید، و یه لیستِ بلندِ
«امضای بصریِ منحصربه‌فرد» شاملِ طرحِ سکه/زنجیرِ استریک/ریبونِ رسید) - تاییدشده، **همون طرحِ
پایه‌ی Duolingoی قبلی رو گسترش داده، نه از نو**. الان با **اوپوس + افورتِ max** داره دورِ
اصلاحاتِ نهایی رو می‌زنه.

**⚠️ فیچرِ سومِ جدید - سیستمِ «سکه» (اقتصادِ داخلیِ اپ):** کاربر خواستِ یه سیستمِ کاملِ امتیاز/سکه:
- **کسب:** با انجامِ دستاورد/استریک + استفاده‌ی روزانه از برنامه + کارهای مشخص (اولین تراکنش،
  تکمیلِ پروفایل، وصل‌کردنِ پیامک/نوتیفیکیشنِ بانکی به حساب).
- **خرج:** تخفیفِ خریدِ اشتراک + بازکردنِ قابلیتِ اضافه + فقط نمایش/امتیاز (هر سه‌تا، نه فقط یکی).
- باید یه جای مشخص (پروفایل یا تبِ جدید) موجودی+تاریخچه‌ی سکه دیده بشه؛ طرحِ خودِ سکه بخشی از
  همون «امضای بصریِ منحصربه‌فرد» باشه. این متنِ دقیق به کلاد دیزاین هم داده شد. **فیچرِ کاملاً
  جدیده - نیازِ به جدولِ دیتابیسِ جدید (تراکنش‌های سکه: مقدار، دلیل، تاریخ) + منطقِ کسب/خرج داره،
  نه صرفاً UI.**

**یادآوریِ قابلیت‌هایی که از قبل هست و نباید تو بازطراحی فراموش/بازسازی بشن:**
- خوندنِ خودکارِ پیامکِ بانکی (سرشماره وصل به حساب) - از قبل کامل کار می‌کنه.
- خوندنِ نوتیفیکیشنِ بانکی (برای بانک‌هایی مثل بلوبانک که پیامک نمی‌دن) - از قبل کامل کار می‌کنه.

### 📋 بازبینیِ فایلِ نسخه‌ی ۴ کلاد دیزاین (۲۵ مرداد) + روالِ تاییدشده‌ی پیاده‌سازی
زیپِ `design_handoff_bank_redesign/` (۴ فایلِ `.dc.html` + README + `support.js`) کامل اسکن شد.
**وضعیت: ۱۴۷ کارتِ یکتا، صفر تکراری** (مشکلِ ۱۳ صفحه‌ی تکراریِ دورِ قبل حل شده).
بخش‌های تاییدشده: ۲۹ (۱۶ صفحه‌ی بی‌طرحِ قبلی) · ۳۰ (۶۰ فریمِ تیره) · ۳۱ (کشوی میان‌بر، **پروتوتایپِ
واقعاً کارکن داخلِ فایل**، ۱۰ ثانیه) · ۳۲ (آواتارِ دختر/پسر) · ۳۳ (حالتِ خالی/لودینگ/خطا) · ۳۴
(امضای بصری: زنجیرِ «فعال» + ریبونِ رسید) · ۳۵ (مجوزِ اعلان + راهنمای تصویری + مجوزِ باتری).
«سفر»، تارِعنکبوتِ ۷روزه، عکس تو یادداشت، ۵ تبِ بدونِ FAB - همه ✅.

**ایرادهای بازِ اعلام‌شده به کلاد دیزاین (متنِ اصلاحیه فرستاده شد، منتظرِ فایلِ نهایی):**
- «شش رقم» تو کارتِ `28c` (۵ جای دیگه درست «پنج‌رقمی»ه) · «استریک» تو ۳ فایلِ دیگه (۶ بار؛
  فایلِ اصلی تمیزه).
- **۱۰ قابلیتِ طراحی‌نشده**: بایگانیِ چک · زیرمجموعه‌ی دسته · پرداختِ گروهیِ اقساط · یادداشت/عکسِ
  رسید/شماره‌ی پیگیریِ قسط · اندازه‌ی فونت · حذفِ حسابِ کاربری · نوعِ حساب+موجودیِ اولیه · بازه‌ی
  دلخواهِ گزارش · ویرایش/حذفِ **وام** (فقط برای چک طراحی شده، کارتِ `29l`) · افزودنِ دستیِ وام.
- کارتِ `27b` (جزئیاتِ وام) دکمه‌ی پرداخت رو هر ردیفِ قسط نداره - «پرداخت شد» فقط تو `29p`ه.

**🔴 روالِ اجراییِ تاییدشده‌ی کاربر (۲۵ مرداد) - این رو جدی بگیر:**
1. **تا فایلِ کاملِ اصلاح‌شده نرسیده، پیاده‌سازی رو شروع نکن.** (تصمیمِ صریحِ کاربر؛ من پیشنهاد
   داده بودم می‌شه از بخشِ ظاهریِ قطعی‌شده شروع کرد، کاربر ترجیح داد صبر کنه - محترمه و کم‌ریسک‌تره.)
2. وقتی فایل رسید، **قدم‌به‌قدم** پیش برو، نه یه‌جا همه‌ی ۱۴۷ صفحه:
   اول سیستمِ طراحی (`theme/Color.kt` + توکن‌ها) + ۵ تبِ اصلی → **یه بیلد** → کاربر رو گوشیِ
   واقعی ببینه و تایید کنه → بعد دسته‌ی بعدیِ صفحات. اگه چیزی به دلش نچسبید وسطِ کار معلوم بشه،
   نه آخرِ کار.
3. ⛔ یادآوری: هر رانِ اندروید ≈۱۵ دقیقه از سهمیه‌ی ماهانه‌ی کاربر - کامیت‌های میانی `[skip ci]`
   بخورن و بیلد فقط دستی با `workflow_dispatch` گرفته بشه.

### 🚧 قدمِ ۱ پیاده شد (۲۵ مرداد) - سیستمِ طراحی + ۵ تبِ اصلی، **بیلد هنوز گرفته نشده**
فایلِ نهاییِ کلاد دیزاین (زیپِ دومِ ۲۵ مرداد) رسید و هر ۱۲ ایرادِ اعلام‌شده حل شده بود:
«شش رقم» صفر شد، «استریک» تو هر ۴ فایل صفر شد، و **بخشِ ۳۶ «کنترل‌های گمشده»** با ۱۷ کارت برای
همون ۱۰ قابلیت اضافه شد (+ کارتِ `36i` که تصمیمِ ساختاریِ «پرداخت فقط از صفحه‌ی کاملِ اقساط» رو
با یه استثنای هوشمند برای قسطِ عقب‌افتاده مستند می‌کنه). ۳۷ بخش، ۱۸۹ فریم، صفر تکراری.

**چی پیاده شد** (سه کامیتِ `53875e0`، `8ae7df6`، `9b43bc5`):
- `theme/Color.kt` کاملاً بازنویسی شد + `theme/Dimens.kt`ِ تازه (شعاع/فاصله/ارتفاع/ضخامت +
  `Modifier.hardShadow`). سبکِ «Liquid Glass» **کاملاً حذف شد**؛ توکن‌های `glass*` عمداً نگه
  داشته شدن ولی `@Deprecated`ن و به معادلِ تخت اشاره می‌کنن.
- `AppCard` چهار گونه گرفت (پیش‌فرض/طلایی/فوری/تمام‌شده)، `GradientButton` پنج گونه + فشرده‌شدنِ
  «کلیدِ فیزیکی»، و `AppFab`/`AppHeroCard`/`HeroTone` تازه ساخته شدن.
- نوارِ پایین از «شناورِ شیشه‌ای با نشانگرِ لغزنده» به نوارِ ماتِ چسبیده با خطِ بالاییِ ۲ پیکسلی.
  `AuroraBackground` از پشتِ تب‌ها برداشته شد (خودِ فایلش پاک نشد).
- هر ۵ تب + آمار + گزارشِ چک به کارتِ قهرمانِ مشترک وصل شدن. **رنگ‌ها همه سبز نیستن**:
  خانه/دارایی/بودجه سبز، گزارش/آمار/گزارشِ چک بنفش (طبقِ توکنِ «بنفش = بودجه و آمار»).

**⚠️ دو موردی که باید از کاربر تایید بگیری (نگفته، من طبقِ طرح جلو رفتم):**
1. **تمِ تیره دیگه مشکیِ خالص نیست** - `#10181F` شد. کاربر قبلاً صریحاً «پشت کامل مشکی» خواسته
   بود، ولی سیستمِ طراحیِ جدید صریحاً می‌گه «هرگز مشکیِ خالص» (رو مشکیِ خالص لبه‌ی کارت‌ها گم می‌شه).
   اگه کاربر پس‌گرفت، فقط `DarkAppColors.bg` رو برگردون.
2. **کارتِ قهرمانِ خانه هنوز «مانده‌ی کل» رو نشون می‌ده**، ولی طرح (کارتِ `15a`) عددِ قهرمان رو
   «خرجِ امروز» گذاشته. من مانده رو نگه داشتم (تصمیمِ محصولیِ تاییدشده‌ی قبلی + تپ روش می‌بره
   دارایی) و خرجِ امروز رو به‌عنوانِ قرصِ دوم اضافه کردم. اگه کاربر جابه‌جایی خواست، ساده‌ست.

**باقی‌مونده‌ی همین بازطراحی (قدمِ ۲ به بعد، بعدِ تاییدِ کاربر رو گوشی):** ۱۰ کنترلِ بخشِ ۳۶ ·
گیمیفیکیشن (فعال/سکه/بج - نیازِ جدولِ دیتابیسِ جدید) · کشوی میان‌بر (بخشِ ۳۱) · آواتارِ
دختر/پسر (۳۲) · حالت‌های خالی/لودینگ/خطا (۳۳) · امضای بصری (۳۴) · مسیرِ مجوزِ اعلان (۳۵) ·
`ThemeMode.SYSTEM` · بقیه‌ی ~۳۰ صفحه‌ی پوش‌شده.

### 🔴 خواسته‌ی «کپیِ برابرِ اصل» (۲۵ مرداد) - این جایگزینِ هر تصمیمِ سلیقه‌ایِ منه
کاربر صریحاً گفت: **«دقیقا می‌خوام بشه مثل طراحی کلاد دیزاین، مو نزنه، کپی برابر اصل».**
یعنی هر جا من عمداً از طرح فاصله گرفته بودم (چون «تصمیمِ محصولیِ قبلی» بود) باید برگرده رو طرح.
موردهایی که همین‌جوری اصلاح شدن:
- عددِ قهرمانِ **خانه**: «مانده‌ی کل» → **«خرجِ امروز»** + نمودارِ ۷ روزه + قرصِ درصدِ تغییر.
- عددِ قهرمانِ **بودجه**: «خرجِ ماه» → **«امروز می‌تونی خرج کنی»** (سهمِ روزانه) + ۷ نقطه‌ی
  زیر/بالای سهم + کارتِ سفیدِ جدای «کلِ ماه».
- **نوارِ پایین**: اشتباهاً فرض کرده بودم طرح نشانگر نداره و حذفش کردم؛ **داره** - قرصِ ثابتِ
  `#E9F7EF` پشتِ آیکونِ تبِ فعال (۴۲×۲۸، گوشه‌ی ۱۱). برگشت.
- **رنگِ تبِ دارایی**: اشتباهاً بنفشش کرده بودم چون `MainSection` رو تبِ گزارش فرض کردم؛
  در واقع نمای «حساب‌کتاب‌ها»ی تبِ داراییه → سبز شد، و گزارش کارتِ بنفشِ واقعیِ خودش رو گرفت.

**درسِ ماندگار**: قبل از اینکه یه کارتِ `AppHeroCard` رو رنگ کنی، **مطمئن شو کدوم تابع تو کدوم
تبه** - `AccountingScreen.kt` سه تا `Screen` (دارایی/بودجه/گزارش) داره که همه از `Section`های
داخلِ همون فایل ساخته می‌شن و اسم‌هاشون گمراه‌کننده‌ست.

### 🧪 بیلدِ ۴۶۵ شکست خورد - دو الگوی خطا که بررسیِ ایستا نمی‌گرفت
`:core:test` سبز بود ولی `:app:compileCafebazaarDebugKotlin` با ۶ خطا شکست:
1. `Theme.kt` هنوز `palette.accent` صدا می‌زد در حالی که فیلدِ `accent` از پالت حذف شده بود
   (جاش `goldInk`). → اسکریپتِ `palette.py` ساخته شد: هر `palette.X`/`LocalAppColors.current.X`
   باید فیلدِ واقعیِ `AppColorPalette` باشه.
2. پارامترهای پول رو `Long` تعریف کرده بودم ولی **کلِ پروژه مبلغ‌ها رو `Double` نگه می‌داره**
   (`AccountTransactionEntity.amount`، `balanceOf()`، `fmt()`، `countUpAmount()`).
   → اسکریپتِ `moneytypes.py` ساخته شد.

⚠️ **بررسیِ ایمپورت‌محور کافی نیست** - این دو کلاس خطا (نوعِ داده و دسترسیِ فیلد) رو نمی‌گیره.
قبل از هر بیلد **هر ۱۷ بررسی** رو با `bash native-android/tools/static-checks/run-all.sh` بزن -
نه فقط چهارتای اولیه.

**عکس‌های مرجعِ Paparazzi** بعدِ تغییرِ `AppCard` بی‌اعتبار شدن و با
`record-paparazzi-snapshots.yml` بازسازی شدن (کامیتِ `c4fde1c`) - همیشه بعدِ تغییرِ ظاهرِ
`AppCard`/کامپوننتِ تستی این ورک‌فلو رو دستی بزن، وگرنه استپِ اسکرین‌شاتِ بیلدِ اصلی می‌شکنه.

**⏳ ویجتِ گوشی هم قراره طراحیِ جداگانه بگیره** (کاربر گفت طراحیِ برنامه که تموم شد، طراحیِ ویجت
هم می‌ده). یادآوری: ویجتِ فعلی باگ داره (تپ‌کردن روش برنامه رو باز نمی‌کنه/هنگ می‌کنه) - احتمالاً
`Intent.FLAG_ACTIVITY_NEW_TASK` تو `LoanWidget.kt` (خط ~۸۹) کمه. وقتی طرحِ ویجت رسید، هم‌زمان با
پیاده‌سازیِ ظاهرِ جدید این باگ رو هم رفع کن.

### ✅ بازطراحیِ اولِ ظاهری - «Liquid Glass» طبقِ کلاد دیزاین - انجام و تاییدشده (۱۹-۲۰ مرداد)
فایلِ طراحی: `design_handoff_bank_redesign/Home Redesign.dc.html` (تو `/tmp`، از یه zip آپلودشده -
**رو دیسکِ ریپو ذخیره نشده**، اگه نشستِ بعدی بهش نیاز داشت باید کاربر دوباره بفرسته یا از
`design_handoff/` قدیمی‌تر که تو ریپو هست استفاده بشه، هرچند اون یکی برای برنچِ خیلی عقب‌تر بود).
لوگوی جدید (کیفِ پول + نمودار، از یه عکسِ AI-generatedِ کاربر) هم رو لانچر و هم رو اسپلش گذاشته شد
(اسپلش: لوگوی قدیمی پاک و با گرادیانِ نرمِ سبز جایگزین شد، رجوع کن به کامیتِ اسپلش).
**درسِ مهم از این دور**: سه‌بار تلاشِ subagent برای پیاده‌سازیِ باقیِ صفحات **رو یه پایه‌ی قدیمیِ
غلط** (حدودِ ۱۵۰ کامیت عقب‌تر) کار کرد و نتیجه‌ش دور ریخته شد - چون worktreeاش رو با
`git reset --hard origin/claude/bank-9fiumm` هماهنگ نکرده بود. **از این به بعد هر ایجنتی که رو
worktreeِ جدا کار می‌کنه، اول باید `git fetch` + `git reset --hard origin/<branch>` بزنه و با
`git log --oneline -3` تاییدِ commitِ سرِ برنچ رو بگیره، وگرنه متوقف بشه** - این قاعده رو تو هر
promptِ Agent بذار.
بعدِ چند دورِ بازبینیِ دقیق (نه فقط رنگ، چیدمان/فاصله/اندازه هم مو‌به‌مو با HTML تطبیق داده شد)
معلوم شد **اکثرِ ۱۶ صفحه از قبل دقیقاً مطابقِ طرح بودن** (چون بازطراحیِ ۱۶-۱۷ مردادِ قبلی همون سبکِ
شیشه‌ای رو پیاده کرده بود) - فقط این‌ها واقعاً تغییر خواستن: عددِ موجودیِ خانه (۳۶.sp)، شکلِ
آیکونِ ردیفِ تراکنش (گردگوشه نه دایره)، FAB گردگوشه‌تر، فونتِ مبلغِ شیتِ تراکنشِ جدید (۲۶.sp).
تنظیمات عمداً به نسخه‌ی HTML برنگشت چون یه بازطراحیِ جدیدتر و تاییدشده (۱۸ مرداد، بر اساسِ اپِ
مرجعِ دیگه) داره که رو همون HTML اولویت داره.

### ✅ بسته‌ی «بازطراحیِ کاملِ ورود + پولکی‌سازی» - **انجام شد** (بیلدِ ۴۶۳ موفق)
#28..#37 همه پیاده شدن. نکاتِ ماندگارش:
- `BenefitsScreen` **حذف شد**؛ جاش `ui/onboarding/OnboardingFlow.kt` (۴ مرحله: خوش‌آمد → تم →
  یادآور+مجوزِ اعلان → اولین حساب‌کتابِ «نقدی») + مرحله‌ی پنجم = همون گیتِ `LoginScreen`.
  کلیدِ DataStore عمداً همون `benefits_seen` مونده (اسمِ پراپرتی شد `onboardingDone`) تا کاربرِ
  قدیمی دوباره آنبورد نشه.
- `ui/onboarding/PostLoginSheets.kt`: دو صفحه‌ی بعد از اولین ورودِ موفق (هدیه‌ی اشتراک با عددِ
  سرور + `legacyGift`، و «نگرانِ داده‌هات نباش»). اسمِ اختیاریِ کاربر همین‌جا **یه‌بار** پرسیده
  می‌شه. کلیدِ `post_login_sheets_seen`. قبلش `refreshStatus()` صدا زده می‌شه چون `legacyGift`
  فقط از `GET /api/auth/me` میاد.
- صفحه‌ی اشتراک: تعرفه‌ها دیگه نمای جایگزین نیست، **زیرِ همون ردیف باز می‌شه** (مرورِ اشتراک
  همیشه رندر می‌شه، داخلِ یه `run { }` تو LazyListScope).
- زیرمجموعه‌ی دسته‌بندی: `CustomCategoryEntity.parentName` (نام، نه id - چون والد می‌تونه یه
  دسته‌ی **ثابت** باشه که ردیفِ دیتابیس نداره) + `MIGRATION_22_23`. دیتابیس **نسخه‌ی ۲۳**.
- «طرف حساب» از قبل همون `DebtScreen`ه؛ صفحه‌ی جدایی وجود نداشت، کاری لازم نبود.

### 📦 بسته‌ی قدیمیِ ثبت‌شده (بایگانی - انجام شده)
کاربر ۱۹ مرداد پشتِ‌هم فرستاد و گفت **«فقط ذخیره کن»** + **«همه رو با هم انجام بدی»**. تا وقتی
صریح نگفته «انجام بده»، کد نزن. جزئیاتِ کاملِ هر کدوم تو تسک‌های #28-#35.
- **#28 مسیرِ اولین ورود عیناً مثلِ پولکی** (۵ مرحله: خوش‌آمد → انتخابِ تم → یادآور+مجوزِ اعلان →
  افزودنِ حساب‌کتاب با «نقدی» → ورود). **`BenefitsScreen` کاملاً حذف می‌شه.**
- **زیرکارِ #28: «نقدی»** - چک شد **وجود نداره**؛ `AccountEntity` فقط `name`+`bankName` داره.
  باید به‌عنوانِ حسابِ غیربانکی اضافه و **تو کلِ اپ** یک‌دست بشه (فرمِ حساب، تبِ دارایی،
  `AccountPickerDialog`، `BankBadge`).
- **#29 صفحه‌ی ورود + کدِ تایید** عیناً (ویرایشِ شماره، دکمه‌ی غیرفعال تا کاملِ‌شدنِ کد، شمارشِ معکوس).
- **#30 دو شیتِ بعد از ورود**: هدیه‌ی اشتراک، و «نگران ازدست‌رفتنِ داده‌ها نباش».
- **#31 بازطراحیِ صفحه‌ی اشتراک** طبقِ «پولکی‌پلاس» + لیستِ امکانات. **تعرفه‌ها رو *همون صفحه*
  باز بشه، نه صفحه‌ی جدا.** دراپ‌باکس عمداً تو لیست نیست (قبلاً رد شده).
- **#32 زیرمجموعه‌ی دسته‌بندی** - `CustomCategoryEntity` هیچ `parentId` نداره، دسته‌ها تخت‌ان.
- **#33 دارایی‌های غیرنقدی** (طلا/سکه/ارز/رمزارز با قیمتِ روز) - اصلاً وجود نداره، بزرگ‌ترین کارِ
  این بسته. **قیمت رو سرورِ خودمون بگیره و کش کنه** (مثلِ `credit_rates`) تا کلیدِ API تو اپ لو نره.
  ⛔ وابسته به کلیدِ APIِ سرویسِ قیمت که **کاربر باید خودش بگیره**.
- **#34 تبلیغات** - «دیوار» **اشتباهِ گفتاری بود، منظور بازار**: یعنی **کافه‌بازار ادز + مایکت ادز**،
  هر فلیور مالِ خودش (همون الگوی `SubscriptionManager`). **فقط `!subscribed`** - SDK برای مشترک
  اصلاً بارگذاری نشه. AdMob نه. کاربر خودش حساب/Zone ID می‌گیره.
  **زیرساخت الان ساخته بشه، شناسه‌ها placeholder بمونن** («به‌موقعش اونا رو اضافه می‌کنم» - ۲۱ مرداد
  دوباره تایید کرد: «شناسه تبلیغات هم بعدا اضافه میکنم») - اگه شناسه خالی بود SDK بارگذاری نشه و
  هیچ جای خالی تو UI دیده نشه.
- **#35 اشتراکِ هدیه**: پایه **۳۰ روز** برای همه (آزمایشیِ ۷روزه‌ی فعلی می‌شه ۳۰)، و **۱۵ روزِ
  اضافه (جمعاً ۴۵) برای هر شماره‌ای که *الان* تو دیتابیسِ سروره**. با **مهاجرتِ خودکار تو `Db.kt`**
  + ستونِ ضدِتکرار، **نه SSHِ دستی**. سرور باید «قدیمی بودن» رو تو `/api/auth/me` برگردونه تا اپ
  جمله‌ی اضافه‌ی «چون از قبل وارد برنامه شده بودی، ۱۵ روز بیشتر گرفتی» رو نشون بده. عددها از سرور،
  نه هاردکد.
- **#36 اعلانِ برگشتِ کاربر** - مثلِ پولکی («۲ روزه رفتی، وقتشه برگردی 💔») ولی **روزانه**، با عددِ
  واقعیِ روزهای بی‌فعالیتی از آخرین تراکنش. تپ → مستقیم صفحه‌ی ثبتِ تراکنش. **با «یادآورِ روزانه»ی
  موجود فرق داره** (اون ساعتِ ثابت برای همه، این فقط برای کاربرِ غایب) - نباید یه روز هر دو بزنن.
- **#37 «اسم» تو حساب کاربری** - اختیاری، همیشه قابلِ اسکیپ، **هیچ‌وقت اولِ برنامه پرسیده نشه**.
  فیلد تو زیرصفحه‌ی حساب کاربری + یه‌بار پرسیدنِ ملایم تو شیتِ هدیه‌ی #30 (رد کرد = دیگه نپرس).
  جای مصرفش: **سربرگِ خروجیِ PDF/اکسل**. ⚠️ برای پیامِ خوش‌آمد استفاده نشه - قبلاً ساخته و
  به‌خواستِ صریحِ کاربر حذف شد.

### 📦 بسته‌ی دومِ «پولکی‌سازی» - ثبت‌شده با ۴۸ اسکرین‌شاتِ مرجع (۲۰ مرداد)
تصمیم‌های تاییدشده‌ی کاربر: **«حساب» → «حساب‌کتاب»** (اسمِ پولکی) · **«طرف حساب» با «طلب و بدهی»
یکی می‌شه** · خرید/فروشِ دارایی **رو دخل‌وخرج اثر نداره** ولی بعدش پیشنهادِ ثبتِ تراکنش می‌ده ·
**همه‌چیزِ دارایی ساخته بشه و قیمت‌ها فعلاً جای خالی بمونن** تا کلیدِ APIِ قیمت برسه.
- **#38 تراکنشِ جدید** - شیتِ سه‌تبی **خرج(قرمز)/دخل(آبی)/جابجایی(سبز)**: مبلغ، حساب‌کتاب، تاریخ،
  توضیحات، دسته‌بندی + ۳ دسته‌ی پیشنهادی. تو «جابجایی» به‌جای حساب‌کتاب، **مبدا ← مقصد**.
- **#39 حساب‌کتاب** - دو نوع: **کارت بانکی** (شماره‌ی ۱۶رقمی، نامِ دلخواه، سوییچِ خواندنِ پیامک،
  موجودیِ اولیه) و **منبع دیگر** (آیکونِ انتخابی، نام، موجودیِ اولیه) ← همون «نقدی»/کارتِ اعتباری.
- **#40 دسته‌بندی‌ها** - صفحه‌ی مدیریت با تبِ خرج/دخل (رنگِ متفاوت)، **جابه‌جایی با کشیدن**،
  دکمه‌ی + بالای صفحه، ساختِ دسته با انتخابِ آیکون.
- **#41 دارایی** - نمودار + ارزشِ کل + درصد؛ خرید(سبز)/فروش(قرمز)؛ ۴ دسته: ارزِ دیجیتال، ارزِ
  فیزیکی، طلا/سکه/نقره، عنوانِ دلخواه. صفحه‌ی جزئیات با نمودار و بازه‌های ۱هفته/۱ماه/۶ماه/۱سال.
  **خرید و فروشِ یه دارایی تو یک ردیف جمع می‌شن، نه دو ردیف.**
- **#42 گزارش** - فقط یه **+** پایینِ صفحه که همون شیتِ #38 رو باز کنه.
- **#43 بودجه** - شیتِ «بودجه‌ی جدید»: مقدار، انتخابِ حساب (+«همه حساب‌کتاب‌ها»)، انتخابِ دسته‌بندی
  (+«همه دسته‌بندی‌ها»).
- **#44 سررسید** - **بجِ عددی** کنارِ کاشی‌ها (مثلِ «۲» کنارِ طلب و بدهی).
- **#45 خانه** - کارتِ «هنوز سرشماره اضافه نکردی» با میان‌برِ تنظیمات + دکمه‌ی + که #38 رو باز کنه.

### کارهای معلق / منتظرِ کاربر
- **⏸ پنهان‌شدنِ نوارِ پایین برای فضای بیشتر - ثبت شد، کاربر گفت «فعلا ولش کن ولی یادت باشه».**
  ایده‌ی خودِ کاربر: بعدِ عوض‌کردنِ تب دو سه ثانیه بمونه بعد بره پایین؛ نشونه‌ای باشه که بشه
  دوباره بالا آوردش؛ اگه کاربر خودش بالا آورد ۷ ثانیه بمونه.
  ⚠️ **نقدِ من (گفته شد)**: تایمر با **کشوی میان‌بُر** تصادف می‌کنه - هر دو رو لبه‌ی پایین و با
  کشیدن به بالان. ضمناً ناوبریِ اصلی رو قایم‌کردن یعنی هر تعویضِ تب دو حرکت می‌شه، و بندِ ۹
  سیستمِ طراحی صریحاً می‌گه نوار به لبه چسبیده و همیشه پیداست.
  **جایگزینِ پیشنهادیِ من**: پنهان‌شدن **با اسکرول** (پایین → مخفی، بالا → برگرد). زیرساختش از
  قبل هست (`rememberIsScrollingUp` + `bottomBarVisible`)، فقط الان به «وام‌های من» وصله و باید
  به هر پنج تب وصل بشه. کاربر هنوز بینِ این دو انتخاب نکرده.
- **⏳ بنچمارک/تستِ باره - درخواستِ کاربر، هنوز شروع نشده** («۱۰۰۰ تا ۱۰ هزار نفر هم‌زمان چی می‌شه،
  سرور می‌کشه، برنامه کند نمی‌شه؟»). **هیچ‌وقت این تست رو مستقیم رو سرورِ زنده/VPS اجرا نکن** -
  ریسکِ خرابی/سوختنِ اعتبارِ پیامک/داون‌شدنِ واقعی برای کاربرهای واقعی داره. روشِ درست: یه نمونه‌ی
  محلی/جداگانه از همون کدِ سرور بالا بیار (نه VPS) و رو همون بارگذاری کن. برای شبیه‌سازیِ لاگین
  از `TEST_ACCOUNT_PHONE`/کدِ ثابت استفاده کن (اگه رو نمونه‌ی محلی env ست بشه) تا پیامکِ واقعی
  نره؛ برای بقیه‌ی endpointها (`/api/loans`, `/api/auth/me`) با JWTِ ساختگیِ امضاشده با همون
  سکرت تست کن. نتیجه رو با اعدادِ ساده (چند درخواست در ثانیه، کجا گلوگاهه - SQLite تکاننده‌س
  چون یه‌نویسنده‌ست، یا CPU/RAMِ خودِ VPS) به زبونِ ساده گزارش بده. **هنوز شروع نشده، فقط ثبت شد.**
- **❌ ایمپورتِ وام از PDF - کنسل شد** (۲۱ مرداد، صریحاً: «pdf رو بیخیال»). ۵ نمونه PDF بررسی شده
  بود و طرحِ تاییدشده‌ای هم داشت (تاییدِ ردیف‌به‌ردیفِ درجا + تشخیصِ کلیدواژه‌محور رو خودِ گوشی، بدون
  AI/سرور). **شروعش نکن مگه خودش دوباره مطرح کنه.**
- **⚠️ ناهماهنگیِ بازِ فرمولِ قرض‌الحسنه**: برای وامِ ازدواج (۳ میلیارد ریال، ۱۲۰ ماهه، ۴٪) بانک
  ۲۸٬۱۵۵٬۰۰۰ اعلام کرده، اپ ۲۷٬۲۷۲٬۷۲۷. با اجرای دستیِ فرمول تایید شد **اجرای کد درسته** - هیچ‌کدوم
  از سه الگوی موجود (QARZ=۲۷٬۲۷۲٬۷۲۷، STANDARD=۳۰٬۲۹۵٬۴۷۸، FLAT=۳۰٬۰۴۱٬۶۶۷) به عددِ بانک نمی‌رسه.
  **کاربر فعلاً منصرف شد** («ولش کن»). اگه دوباره مطرح شد: اول **عکسِ جدولِ کاملِ ۱۲۰ ردیف** رو
  بخواه (نه یه عدد) و **قبل از هر تغییری چند تستِ رگرسیونِ جدید اضافه کن**.
- **ویجت (`ui/widget/LoanWidget.kt`)**: کاربر گفته ساده‌ست و با تپ اپ باز نمی‌شه/هنگ می‌کنه.
  احتمالِ زیاد نبودِ `Intent.FLAG_ACTIVITY_NEW_TASK` (خط ~۸۹، context اکتیویتی نیست) - باید واقعاً
  تست/دیباگ بشه نه حدس. **کاربر گفته بعدِ انتشارِ اپ یادآوری کنم** - تا خودش نگفته شروع نشه.
- **رتبه‌ی اعتباری (فاز بعد)**: خوندنِ رتبه از یه سرویسِ بیرونی (مثلاً فینوتک). «الان نمی‌خوام».
- **تبلیغ تو نسخه‌ی رایگان (فاز بعد)**: تپسل یا آدیوری/صباویژن (نه AdMob)، فقط برای `!subscribed`.
  کاربر: «فعلاً نه، اول منتشر کنم و نصب جمع کنم». وقتی خواست، اول خودش باید حساب/Zone ID بگیره.
- **⏳ سینکِ خودکارِ سررسیدها با اپِ «تقویم من»** (ریپوی `ebrahimpersianh/Calendar`، فعلاً متوقف):
  تصمیم شد از **همون سرورِ فعلیِ VameMan** استفاده بشه (ظرفیتش کافیه، تاییدشده). طرح: (۱) جدول/مسیرِ
  `calendar_events` روی همین سرور، (۲) «تقویم من» اول باید لاگینِ شماره‌موبایلی بگیره (فعلاً هیچ
  سرور/حسابی نداره)، (۳) «اتومات» = هر بار باز شدنِ اون اپ رویدادها رو می‌گیره (نه پوش، فعلاً لازم
  نیست). **گزینه‌ی نشون‌دادنِ تقویمِ سیستمِ گوشی صریحاً رد شد.** شروعش بعدِ برگشتنِ کاربر سراغِ اون پروژه.
- **تاییدنشده**: نصبِ `sqlite3` رو VPS + اجرای `UPDATE users SET subscribed=1...` برای دو حسابِ تست/دائمی.

### تاییدهای گرفته‌شده
OTP end-to-end با شماره‌ی واقعی ✅ · بازطراحیِ ظاهر/انیمیشن (بیلدِ ۳۲۹) ✅ · اسپلش/لوگو/تبِ بودجه/تبِ
دارایی ✅ · رفعِ کرشِ migrationِ ۱۱→۱۲ (بیلد داده شد، تاییدِ نهایی رو گوشی نگرفتیم).
**تاییدنشده رو گوشیِ واقعی**: یک‌دست‌سازیِ کارت‌ها (`0c0de5b`) و کلِ کارِ ۱۸ مرداد.

---

## ✅ چک‌لیستِ تستِ کاربر - **هر وقت گفت «می‌خوایم تست کنیم» این رو کامل بهش یادآوری کن**
خواسته‌ی صریحِ کاربر (۶ شهریور): «همه رو موقعی که گفتم می‌خوایم تست کنیم یادم بیار».
یعنی وقتی کاربر گفت تست، **این فهرست رو یکجا و ساده بهش بده** - نه اینکه منتظرِ سوالش بمونی.
هر چیزِ تازه‌ای که ساخته می‌شه، همین‌جا اضافه بشه.

**رفعِ باگ‌هایی که باید رو گوشی تایید بشن:**
- صفحه‌ی اجازه‌ی باتری/اعلان دیگه هر بار بعدِ اسپلش فلش نزنه.
- حالتِ خصوصی (چشم) هم روشن بشه هم **خاموش** (قبلاً فقط روشن می‌شد).
- دکمه‌ی چرخ‌دنده‌ی تنظیمات بالای تبِ خانه باشه.
- تپ رو **ردیف‌های تبِ سررسید** جزئیاتِ وام رو باز کنه (نه فقط دکمه‌ی «پرداخت کن»).
- حذفِ یه دسته‌بندیِ باتراکنش → باید بپرسه به کدوم دسته منتقل بشن.
- آیکونِ اپ رو صفحه‌ی گوشی **لبه‌به‌لبه‌ی سبز** باشه، نه کیفِ کوچیک وسطِ مربعِ سفید.

**قابلیت‌های تازه‌ای که باید امتحان بشن:**
- **فشارِ طولانی رو آیکونِ اپ** → سه میان‌بر: تراکنش · سررسید · گزارش.
- **تنظیمات ← پیامکِ بانکی**: کارتِ وضعیت، کلیدِ جدا برای هر بانک، و «آزمایشِ تشخیص» (متنِ پیامک رو پیست کن).
- **تنظیمات ← قاعده‌های تشخیص**: ساختِ قاعده + پیش‌نمایشِ زنده («با ۱۲ تراکنش می‌خوره»).
- **تنظیمات ← ظاهر و تم**: سه‌حالتیِ روشن/تیره/سیستم · اندازه‌ی متن · انیمیشنِ کم.
- **تنظیمات ← تمِ رنگی**: پیش‌نمایش با یه تپ، خرید با تپِ دوم. ⚠️ موجودیِ سکه لازمه.
- **تنظیمات ← امنیت**: قفل که خاموشه ردیف‌های زیرش پنهان بشن.
- **تنظیمات ← درباره**: فشارِ طولانی رو شماره‌ی نسخه → کپیِ اطلاعاتِ فنی.
- **حالتِ شب** رو همه‌ی صفحه‌ها.
- **صفحه‌ی وام و چک**: ردیف‌های سه‌حالته، مدالِ وامِ تسویه‌شده، تاگلِ سه‌حالته‌ی چک، چیپِ «برگردان» تو بایگانی.

**تازه‌های بیلدِ ۵۳۱ که باید تست بشن (نصب‌شده ولی هنوز تایید نشده):**
- **تقویمِ مالی** (تنظیمات ← ابزارها): علاوه بر قسطِ وام باید **چک** و **پرداختِ تکراری** هم
  بیان، هرکدوم با آیکونِ خودش. رنگِ نقطه فقط پرداخت‌شدن رو می‌گه نه نوع.
- **تبِ بودجه**: عددها **تومان** باشن (باگِ ۱۰برابرِ ریال/جداکننده‌ی لاتین) · درصدِ «کلِ ماه»
  بالای ۱۰۰ باید **قرمز** بشه · پیشنهادِ «بودجه رو قرض بدم؟» بعدِ «بله» نوارِ **واگرد** بده.
- **تبِ گزارش**: حداکثر **دو** کارتِ کشف · ضربدرشون بعدِ بستن و بازکردنِ صفحه **برنگرده** ·
  اگه هیچ کشفی نبود کارتِ «بررسی شد، چیزی نبود» بیاد نه غیب‌شدنِ کلِ بخش.
- **کشوی میان‌بر** (کشیدن از پایین): دکمه‌ی **ویرایش**، انتخابِ آیکون، شمارنده‌ی «۶ از ۸»،
  «ثبتِ خرج» که قفله · ترتیبی که جابه‌جا می‌کنی دیگه گم نشه.
- **اعلانِ «رفتی، برگرد»**: باید حدودِ **۸ شب** بیاد نه نیمه‌شب (چند روز طول می‌کشه تا معلوم بشه).
- سه باگِ گزارش‌شده: صفحه‌ی سفیدِ خانه · کرشِ «آزمایشِ تشخیص» · نوتیفیکیشنِ بلوبانک.

**فروشگاهِ سکه ساخته شد (۲۳ شهریور، بخشِ ۶۰ طراح) - بیلد نگرفته:**
- `ui/shop/ShopScreen.kt` + `ShopViewModel.kt` تازه‌اند. مسیرش: تنظیمات ← **«فروشگاهِ سکه»**
  (همان ردیفِ «تمِ رنگی»ِ قبلی). `ThemeShopScreen.kt` **حذف شد** چون فروشگاه جایش را گرفت.
- ⚠️ **بیرونِ `SettingsSubPageScaffold` رندر می‌شود** (فهرستِ تنبل دارد) - همان قاعده‌ی کرش.
- **ده تم در دو سرگروه** («تمِ پایه» چهارتا · «تمِ رنگی» شش‌تا) + **سه آیکونِ اپ**، اینها
  تنها قلم‌هایی‌اند که واقعاً مقصد دارند. نماد/سکه‌پوست/اشتراکِ جایزه‌ای ردیف دارند ولی
  `comingSoon = true` گرفتند - **عمداً خریدنی نیستند تا کدِ واقعی‌شان ساخته شود**، وگرنه
  کاربر سکه می‌داد و هیچ اتفاقی نمی‌افتاد.
- **آیکونِ خریدنی**: سه الیاسِ تازه (`LauncherAlias9..11` = سکه/حرفِ ج/قلک) + سه فایلِ
  `mipmap-anydpi-v26`. `IconWither.apply(context, step, activeIcon)` حالا اگر آیکونِ
  خریداری‌شده فعال باشد **پله را نادیده می‌گیرد** (هشت پله فقط برای طرحِ پیش‌فرض ساخته شده).
  `UiPrefs.activeIcon` تازه است و ردیفِ «بازگشت به آیکونِ پیش‌فرض» فقط وقتی می‌آید که
  آیکونِ غیرپیش‌فرضی فعال باشد.
- **خرید** از `ShopViewModel.buy` می‌گذرد و `BuyResult` می‌دهد (سکه کم · نشان لازم · از
  قبل داری) که در **بنرِ داخلی** دیده می‌شود. موجودی لحظه‌ی خرید دوباره از دفتر خوانده
  می‌شود، و `refId` همان شناسه‌ی قلم است پس دوبار زدن دوبار خرج نمی‌کند.
- سه جوابِ سوالِ طراح: `BASE_THEME_IDS` درست بود (`green/blue/purple/gold`) و به
  `ShopCatalog.kt` منتقل شد · `AppRadius.pill` وجود ندارد، `AppRadius.button` (۹۹۹) است ·
  `AppGoldPill` هم نیست، `AppGoldPillSoft` است · `ConfirmTone.NEUTRAL` هم نیست،
  `HEAVY_CHANGE` جایش نشست · `ShopCategory` چهار مقدار دارد (`THEME/ICON/SYMBOL/REWARD`)
  نه `APP_ICON`/`SUBSCRIPTION`.
- `moneytypes.py` برای `/ui/shop/` استثنا خورد (سکه `Int` است نه ریالِ `Double`).

**پژمردگیِ هر چهار آیکون - کامل شد (۲۴ شهریور، بخشِ ۶۱ طراح):**
- `generate-wither.py` بازنویسی شد و حالا **۳۲ پله** می‌سازد (۴ طرح × ۸)، نه ۸ تا.
- **دو فرمول، نه یکی**: اشباع (`1 − 0.10·n`) برای کیف و سکه · **درخشندگی**
  (`1 − 0.055·n`) برای حرفِ ج و قلک. یافته‌ی طراح و درست بود: پس‌زمینه‌ی آن دو از اول
  بی‌فام است و اشباع‌زدایی رویشان **هیچ اثری ندارد** - یعنی خریدارِ ۴۰۰سکه‌ای پژمردگی
  را اصلاً نمی‌دید. سقفِ `immune`: اشباع ۰٫۶۵ · درخشندگی ۰٫۷۲.
- ⚠️ اشباع‌زدایی حالا با **درخشندگیِ Rec.709** است نه HLS، تا با جدولِ طراح مو‌به‌مو یکی
  باشد. یعنی هشت پله‌ی طرحِ کیف هم **دوباره ساخته شدند** و کمی با قبل فرق دارند.
- **لایه‌ی رویه**: `ic_wither_overlay_{dark,light}.xml` - چهار گروهِ
  `dust`/`web`/`spider`/`crack` با آستانه‌ی ۱/۳/۵/۷. دو فایل نه چهار، چون تقسیم روی
  **روشناییِ زمینه** است نه طرح. «تار» پرده‌ی کم‌رنگ است نه بلور (وکتور فیلترِ بلور ندارد).
- مانیفست حالا **۳۶ الیاس** دارد: ۰..۸ کیف · ۹/۱۰/۱۱ پله‌ی صفرِ سه طرحِ خریدنی ·
  ۱۲..۳۵ پله‌های ۱..۸ آن‌ها. نگاشت در `IconWither.aliasFor` و **دوتکه** است.
- ⚠️ پس‌زمینه‌ی طرحِ **کیف** عمداً دست‌نخورده می‌ماند (بندِ افقی هم‌رنگش است)؛ بقیه
  پس‌زمینه‌شان هم می‌پژمرد.
- ⚠️ **ناهماهنگیِ کوچک**: جدولِ طراح برای «سکه» چهار جزء دارد (بدنه/حلقه/حرف) ولی
  فایلِ واقعیِ ما فقط دو مسیرِ `#0B3A2A` در `immune` دارد و طلا در **پس‌زمینه** است.
  نتیجه درست درمی‌آید (پس‌زمینه می‌پژمرد + رویه) ولی «بدنه»ی رنگی ندارد.

**تازه‌های ۲۳ شهریور (پوش‌شده، بیلد نگرفته):**
- **تشخیصِ پیامک سخت‌گیرتر شد**: تبلیغِ همراه اول/ایرانسل و پیامکِ **رمزِ پویا** دیگر
  تراکنش حساب نمی‌شوند. منطق برعکس شد - پیامک باید **دلیلِ پذیرفته‌شدن** داشته باشد
  (مانده/موجودی، یا نامِ کارت/حساب/شبا، یا فعلِ گذشته مثل «کسر شد»)، نه صرفاً در فهرستِ
  ممنوعه نباشد (`reportEvidence` + `adKeywords` در `BankSmsParser`، با ۳ تستِ تازه).
- دکمه‌ی «پرداخت»ِ فهرستِ وام‌ها «پردا» دیده می‌شد - قابِ `Modifier.size(44.dp)` متن را
  می‌برید؛ حالا `defaultMinSize`.
- خطِ «داده‌ی قیمت از Servix.cc» از صفحه‌ی جزئیاتِ دارایی و شیتِ انتخاب برداشته شد؛
  **فقط روی صفحه‌ی «قیمتِ روز» ماند** - شرطِ `attributionRequired`ِ سرویس باید یک جا
  برآورده بماند وگرنه استفاده از داده‌شان تخلف است.
- دیالوگِ «لذت بردی از جیبک؟» بازطراحی شد: سطحِ سیستمِ طراحی به‌جای `AlertDialog`ِ خام،
  و **پنج ستاره خودشان دکمه‌اند**.

**پوش‌شده ولی هنوز تو هیچ بیلدی نیست (بیلدِ بعدی):**
- **صفحه‌ی قیمتِ روز**: هشت نمادِ بی‌سرویس (نیم‌سکه، ربع‌سکه، پوند، لیر…) باید **برگردن** با
  بجِ خاکستریِ «دستی» و عددِ خودِ کاربر · بجِ بنفشِ «تازه» رو نمادهایی که تاریخچه ندارن ·
  اون ~۲۰ ردیفِ «—» **نباید** برگرده.

**تازه‌های بیلدِ ۵۱۳ که باید تست بشن:**
- **تاییدِ کنش**: پرداختِ قسط از تبِ خانه، حذفِ تراکنش، حذف/تغییرِ نامِ دسته - همه باید **بپرسن**.
- **تبِ دارایی ← قیمت‌ها**: ۵۶ نماد بیاد، هدر ساعتِ واقعی نشون بده («۱۲:۳۰ به‌روز شد»).
- **حسابِ نقدی**: تپ رو تراکنش → ویرایشِ مبلغ/توضیح؛ کشیدن → حذف؛ عددها **تومان** باشن نه ریال.
- **شیتِ ثبتِ تراکنش**: پس‌زمینه‌ش کاملاً مات باشه، صفحه‌ی زیرش دیده نشه.
- **تپ رو سکه‌ی تبِ خانه** → کیفِ سکه باز بشه، دکمه‌ی برگشتِ گوشی هم ببندتش.
- **تغییرِ نامِ دسته‌ای که زیرمجموعه داره** → زیرمجموعه‌ها هم بیان؛ **حذفش** → زیرمجموعه‌ها
  به سطحِ بالا بیان، نه اینکه ناپدید بشن.
- **نُه نشان** هرکدوم نمادِ خودش رو داشته باشه (نه یه مدالِ یکسان).

**چیزهایی که هنوز پیاده نشدن و نباید انتظارشون رو داشته باشه:**
صفحه‌ی سکه (`45a`) · اشتراکِ جایزه‌ای و دو قلمِ نماد (ردیف دارند ولی `comingSoon`) ·
فلشِ تغییرِ قیمت رو همه‌ی ردیف‌های دارایی · ترمیمِ زنجیر · ضربه برای نمایشِ موقتِ مبلغ.
✅ **فروشگاه (`45b`) و پژمردگیِ هر چهار آیکون (`45c`) دیگر انجام شده‌اند** - ۲۳ و ۲۴ شهریور.
ده تمِ فروشگاه هم کامل است (چهار پایه + شش رنگی).

---
## تاریخچه‌ی فشرده‌ی نشست‌ها (فقط «چی شد»؛ درس‌های فنی‌شون بالاست)
- **۲۷ تیر** - versionCode خودکار؛ مایکت کامل شد (کلیدِ IAB، ۴ محصول، `Myket.kt`، فیلدِ `store`)؛
  رفعِ باگِ کارتِ خرید تو دوره‌ی آزمایشی؛ پکیجِ انیمیشن (Aurora, ProgressRing, CoinCelebration,
  GoldSheen, count-up)؛ ارسالِ فرمِ Play Protect.
- **۲۹ تیر** (نشستِ بزرگ) - رفعِ سررسیدِ ماهانه؛ رفعِ پرشِ رقمِ فیلدهای مبلغ (۷ جا)؛ بازطراحیِ لیستِ
  اقساط؛ حسابِ تستِ دائمی + رفعِ بجِ گمراه‌کننده؛ `subscription_tier`؛ اسپلشِ آتیشی؛ رفعِ سیوِ
  تکراریِ وام؛ ویرایشِ مشخصاتِ وام؛ تورِ اولین ورود؛ شیشه‌ای‌شدنِ باکس‌ها؛ بازطراحیِ ورود/OTP +
  SMS Retriever؛ رفعِ متنِ نامرئیِ دارک‌مود؛ فول‌اسکرین‌شدنِ زیرصفحه‌های تنظیمات.
- **۳۰ تیر** - Liquid Glass رو `AppCard`؛ رفعِ bidi صفحه‌ی ورود؛ `Ltr.kt`؛ آماده‌سازیِ عکس‌های استور.
- **۳۱ تیر** - دیپلویِ خودکارِ سرور؛ تستِ Paparazzi؛ جداکردنِ ردیف‌های قسط به `loan_rows` (۱۱→۱۲).
- **۲ مرداد** - انتشارِ APK به GitHub Release؛ رفعِ کرشِ ۱.۰.۳۱۵؛ تغییرِ معنیِ «تاریخ دریافت وام»؛
  تستِ اینسترومنتدِ migration + اسموک‌تست رو امولاتور. (اصلاحیه: PR #64 از قبل merge شده بود.)
- **۳ مرداد** - بررسیِ ردِ مایکت/کافه‌بازار: کارتِ خریدِ مخفی (طراحیِ درست)، envِ کافه‌بازار رو سرور
  (رفع شد)، نوعِ اشتباهِ محصولِ مایکت (`SUBS`→`INAPP`).
- **۴ مرداد** - افزودنِ ماژولِ حسابداریِ شخصی (دسته‌بندی + بودجه + گزارش + جستجو + پرداختِ تکراری،
  ۱۲→۱۳)؛ تثبیتِ نامِ «حسابدار من» (بعداً شد «جیبک»)؛ فعال‌شدنِ OTP.
- **۵ مرداد** (۸۵ کامیت) - نوارِ پایین به ۵ تب رسید؛ طلب و بدهی؛ یادداشت؛ سینکِ رو-به-جلوی
  وام/چک↔حسابداری؛ تشخیصِ بانک از شماره‌کارت؛ مدیریتِ دسته‌بندی‌ها؛ خروجیِ PDF/اکسل؛ خوندنِ
  خودکارِ پیامکِ بانکی؛ تمِ مشکی+سبز؛ لوگوی جدید.
- **۶ مرداد** - سینکِ گذشته‌نگر ساخته و حذف شد (۱۶→۱۷، ۱۷→۱۸)؛ یکدست‌کردنِ فیلدهای مبلغ؛ تاییدِ
  حذفِ تراکنش؛ رفعِ باگِ `addDays` منفی + بازطراحیِ نوارِ تاریخ.
- **۸-۹ مرداد** - حادثه‌ی خریدِ مایکت (بالا).
- **۱۶-۱۷ مرداد** - بازطراحیِ ظاهریِ بزرگ: اسپلشِ تصویری (`assets/splash/splash.jpg`، کراپِ
  `(23,23,626,1503)` از ۱۰۲۴×۱۵۳۶، JPEG عمدی؛ متنِ اسم بخشی از خودِ عکسه نه `Text`؛ ۲۱ مرداد با بازسازیِ گرادیانِ زیرِ متن به «جیبک» عوض شد)؛
  لوگوی جدیدِ لانچر (کراپِ `(134,124,1121,1135)` از ۱۲۵۴×۱۲۵۴ برای رفعِ حاشیه‌ی مشکی)؛ نوارِ
  ناوبریِ شناورِ شیشه‌ای؛ تبِ بودجه فقط دسته‌های بودجه‌دار + FAB؛ بازطراحیِ تبِ دارایی (کارتِ
  مانده‌ی کل + ردیفِ افقیِ حساب‌ها)؛ **یک‌دست‌سازیِ همه‌ی کارت‌ها با `AppCard.accentGradient`**؛
  `SegmentedToggle`.
- **۱۸ مرداد** - سه کار:
  1. **وصل‌کردنِ سرشماره‌ی پیامکِ بانک به حساب**: ستونِ `AccountEntity.smsSender` + `MIGRATION_18_19`
     (فقط `ALTER TABLE accounts ADD COLUMN smsSender TEXT`)؛ فیلد + دکمه‌ی **«انتخاب از پیامک‌های
     گوشی»** تو `AddEditAccountScreen` (`ui/account/SmsSenderPicker.kt` - فرستنده‌های یکتای
     `Telephony.Sms.Inbox` با نمونه‌ی متن، اونایی که با `BankSmsParser` پارس می‌شن اولِ لیست، مجوزِ
     `READ_SMS` فقط لحظه‌ی زدنِ دکمه)؛ تطبیق با `smsSenderMatches`/`normalizeSmsSender` تو
     `core/BankSmsParser.kt` (+ `BankSmsParserTest.kt`).
     **تغییرِ رفتارِ مهم تو `BankSmsReceiver`**: ترتیب = سرشماره → ۴ رقمِ آخرِ کارت → حسابِ اول؛ ولی
     فال‌بکِ «حسابِ اول» فقط وقتی **هیچ** حسابی سرشماره نداره. وگرنه پیامکِ فرستنده‌ی ناشناس نادیده
     گرفته می‌شه (قبلاً پیامکِ بانکِ B بی‌صدا رو حسابِ بانکِ A می‌نشست).
  2. **بازطراحیِ تنظیمات** طبقِ اپِ مرجعِ کاربر: ریشه = ردیف‌های آیکون+عنوان+مقدار+فلش که زیرصفحه‌ی
     تمام‌صفحه باز می‌کنن (حساب کاربری / ظاهر برنامه / ویبره (سوییچِ درجا) / یادآورها / مدیریت
     داده‌های من / پیامک‌های بانکی / ابزارها / امنیت / درباره‌ی برنامه با نسخه + امتیازدهی +
     پشتیبانی + حریم خصوصی). «امور چک» و «حساب‌های بانکی» از تنظیمات **حذف شدن** (از تب‌ها در
     دسترسن). «تقویم مالی/آمار/تاریخچه‌ی محاسبات» **حذف نشدن** چون هیچ نقطه‌ی ورودیِ دیگه‌ای
     ندارن - زیرِ ردیفِ «ابزارها» جمع شدن. قدم‌های تورِ `SETTINGS_CALENDAR`/`SETTINGS_CHEQUE`
     (که ردیف‌های حذف‌شده رو اسپاتلایت می‌کردن) حذف و تو توضیحِ قدمِ `SETTINGS` ادغام شدن.
  3. **اسپلشِ سریع‌تر**: ۲۱۵۰ → ۱۴۵۰ میلی‌ثانیه + خروجِ محو/بزرگ‌شونده، و `AnimatedAppEntrance`
     از «swoosh از بالا-چپ» به fade+scale (۰.۹۶→۱، ۳۰۰ms) عوض شد تا ادامه‌ی همون حرکت باشه.
- **۱۹-۲۰ مرداد** - بازطراحیِ اولِ ظاهریِ کاملِ ۱۶ صفحه طبقِ HTMLِ کلاد دیزاین («Liquid Glass») +
  لوگوی جدیدِ لانچر/اسپلش (رجوع کن به بخشِ «وضعیتِ فعلی» بالا برای جزئیات).
- **۲۱ مرداد** - اسمِ نمایشی از «حسابدار من» به «جیبک» (applicationId دست‌نخورده)؛ رفعِ باگِ تکرارِ
  رسیدِ خرید (n برابر اشتراک با ارسالِ چندبارهٔ یه رسید) + rate limit رو request-otp/verify-otp/crash
  + `Log.kt` با ماسک‌کردنِ شماره/توکن (کامیت `83417f2` رو برنچ، `7d06a473` رو `main` - وضعیتِ
  دیپلوی تو بخشِ «وضعیتِ فعلی» بالا). ایمپورتِ PDF کنسل شد؛ شناسه‌های تبلیغات به بعد موکول شد.
  کلیدِ نمونه‌ی قیمتِ طلا/ارز گرفته شد ولی تستِ سندباکس شکست خورد (egress بسته).
- **۲۹ مرداد** (نشستِ فعلی) - رفعِ حفره‌ی امنیتیِ تکرارِ رسید فرستاده شد رو main، دیپلویش گیرِ
  سهمیه‌ی storage افتاد (بالا)؛ بازبینیِ دقیقِ پیکسلیِ همه‌ی ۱۶ صفحه‌ی بازطراحی‌شده (اکثراً تاییدِ
  «از قبل درست بود»، فقط چهار جزئیاتِ کوچیک اصلاح شد)؛ درخواستِ بازطراحیِ دومِ سبکِ Duolingo داده
  شد و منتظرِ فایلِ کاملِ کلاد دیزاینه (بالا توضیح داده شده).
- **۱۲-۱۳ شهریور** - هفت بسته‌ی کلاد دیزاین اعمال شد؛ تاییدِ کنش با قالبِ واحد (`ConfirmTone`)؛
  ۵۶ نمادِ قیمت + تازه‌سازیِ ۲۹دقیقه‌ای + ساعتِ واقعی؛ ویرایشِ تراکنشِ حسابِ نقدی + رفعِ باگِ
  ۱۰برابرِ ریال/تومان؛ رفعِ پس‌زمینه‌ی شفافِ شیتِ تراکنش؛ کیفِ سکه از تبِ خانه؛ دسته‌ی تودرتو
  (تغییرِ نام/ترفیعِ بچه‌ها)؛ سه بررسیِ ایستای تازه (`callstyle`/`receivers`/`redeclare`) از
  شکستِ بیلدهای ۵۰۷/۵۱۰/۵۱۲؛ **بیلدِ ۵۱۳ موفق**؛ بریفِ اکوسیستمِ سکه ثبت شد؛ کدِ ۹ صفحه‌ی
  بخشِ وام برای طراح بسته‌بندی و تحویل شد.
