## پروژه
اپلیکیشن اندروید محاسبه‌گر اقساط وام فارسی (اسم برند/نمایشی: «وام من») برای کافه‌بازار، تحت نام تیم «Sad Team».

**⚠️ وضعیت فعلی (خیلی مهم): اپ وب دیگه قابل بیلد نیست.** طبق خواسته‌ی صریح کاربر برای این‌که کل ریپو
عملاً Kotlin حساب بشه، فایل‌های زیرساخت بیلد اپ وب/Capacitor (`package.json`، `package-lock.json`،
`capacitor.config.json`، `scripts/`، `resources/`، و workflow `build-apk.yml`) **کاملاً حذف شدن**
(نه فقط مخفی از آمار زبان). خودِ پوشه‌ی `www/` (کد HTML+JS اپ وب) هنوز تو ریپو هست ولی دیگه هیچ راهی
برای تبدیلش به APK جدید وجود نداره. یعنی **اپی که الان رو کافه‌بازار منتشره، تا وقتی `native-android/`
جایگزینش نشه، دیگه قابل آپدیت نیست** — این یه تصمیم آگاهانه و تاییدشده‌ی کاربر بود (نه یه اشتباه)، پس
این‌جوری نگهش دار، سعی نکن اپ وب رو دوباره قابل بیلد کنی مگر صریح خواسته بشه. اولویت فوری الان
publish کردن `native-android/` رو کافه‌بازاره تا این وقفه تموم بشه.

استک کلاینت (بایگانی، دیگه بیلد نمی‌شه): یه فایل HTML+CSS+JS تک‌فایلی تو `www/index.html`.
بک‌اند (برای ورود اجباری با موبایل + همگام‌سازی ابری + اشتراک): پوشه‌ی server/ — Kotlin + Ktor + SQLite (JDBC/sqlite-jdbc)، جدا از کلاینت. جزئیات کامل تو server/README.md.

## سرور از Node.js به Kotlin/Ktor بازنویسی شد، و اپ وب کلاً از حالت قابل‌بیلد در اومد
کاربر خواست کل ریپو از نظر آمار زبان گیت‌هاب عملاً Kotlin حساب بشه. مرحله‌ی اول: اپ وب (`www/`) با
`.gitattributes` (`www/** linguist-vendored`) از شمارش آمار زبان کنار گذاشته شد، بدون حذف فایل — ولی
کاربر بعداً صریحاً گفت باقیِ زیرساخت بیلدش (`package.json`, `capacitor.config.json`, `scripts/`,
`resources/`) رو هم **واقعاً پاک کن**، حتی بعد از این‌که بهش هشدار دادم این یعنی اپ وب دیگه هیچ‌وقت
قابل بیلد نیست و اپ Kotlin هنوز رو کافه‌بازار منتشر نشده — کاربر دوبار تایید کرد. پس این پاک‌سازی
عمدیه، برنگردونش. بک‌اند (`server/`) هم واقعاً از Node.js+Express به
Kotlin+Ktor (Gradle، Kotlin ۲.۰.۲۱ هماهنگ با `native-android/`) بازنویسی و **جایگزین** شد — کد Node.js
قدیمی (`server/src/*.js`، `package.json`) کاملاً حذف شده، نه کنارش نگه داشته شده.
- رفتار عیناً پورت شده، نه فقط شبیه‌سازی‌شده: همون اسکیمای SQLite (یه فایل `data.sqlite` قدیمیِ Node
  مستقیم با نسخه‌ی Kotlin هم کار می‌کنه)، همون مسیرهای API، همون کدهای خطای JSON دقیق (کلاینت‌های
  `www/index.html` و `native-android` رو همین رشته‌های خطا مثل `no_token`/`wrong_code`/`subscription_required`
  match می‌کنن، پس عمداً عوض نشدن)، همون فرمول دوره‌ی آزمایشی ۷روزه و منطق اشتراک.
- ساختار کد تو `server/src/main/kotlin/ir/sadteam/loancalc/server/`: `Application.kt` (main + ماژول Ktor)،
  `Db.kt` (اسکیمای SQLite + مایگریشن، با `withConnection` یه کانکشن جدا به‌ازای هر کوئری چون JDBC
  Connection بر خلاف better-sqlite3 thread-safe نیست)، `Auth.kt` (JWT با java-jwt + `requireAuth`)، `Sms.kt`،
  `SubscriptionStatus.kt`، `Cafebazaar.kt`، و `routes/{AuthRoutes,LoansRoutes,SubscriptionRoutes,CrashRoutes}.kt`.
  دیتای وام‌ها (`loans`) به‌صورت `JsonElement` خام رد و بدل می‌شه (نه یه data class مشخص) چون شکل دقیق هر
  وام کاملاً دست کلاینته.
- `build.gradle.kts` یه تسک دستی `fatJar` داره (بدون پلاگین Shadow/Ktor خارجی، فقط `Jar` + `zipTree`) که یه
  jar واحد runnable می‌سازه؛ دیپلوی رو VPS از `pm2 start src/index.js` به
  `pm2 start java --interpreter none -- -jar build/libs/loan-calculator-server-1.0.0-all.jar` عوض شده
  (جزئیات کامل تو `server/README.md`).
- کامپایل/تست واقعاً تو همین سندباکس تایید شد (بر خلاف `native-android` که `dl.google.com` مسدوده):
  Maven Central از پراکسی این محیط در دسترسه، پس `gradle compileKotlin`/`gradle test`/`gradle fatJar` همه
  اجرا شدن، و کل فلوی OTP→JWT→loans GET/PUT→crash→subscription با curl واقعی رو یه سرور در حال اجرا تست شد.
- سرویس پیامک هنوز پیام‌رسانه (تصمیم ملی‌پیامک زیر هنوز معلقه) — فقط فایلش از `server/src/sms.js` به
  `Sms.kt` منتقل شده، منطقش عوض نشده.
- یه workflow جدید (`.github/workflows/build-server.yml`) اضافه شده که رو هر push/PR که `server/**` رو
  عوض کنه `gradle test` + `gradle fatJar` رو اجرا و jar خروجی رو آپلود می‌کنه — مثل الگوی
  `build-native-android.yml`، ولی چون این پروژه به Maven Central نیاز داره نه `dl.google.com`،
  محدودیت شبکه‌ی سندباکس این یکی رو نداره و از قبل تو همین محیط build/test شده.
- تو یه راند بعدی، کاربر خواست زیرساخت بیلد اپ وب هم **کاملاً حذف** بشه (نه فقط مخفی از آمار زبان):
  `package.json`, `package-lock.json`, `capacitor.config.json`, `scripts/backup-entry.js`,
  `scripts/poolakey-entry.js`, `resources/{icon,splash}.png`, و `.github/workflows/build-apk.yml`
  (که بدون این فایل‌ها دیگه اصلاً کار نمی‌کرد) همه حذف شدن. قبل از اجرا صریحاً بهش هشدار داده شد که این
  یعنی اپ وب هیچ‌وقت دیگه قابل بیلد نیست و اپ زنده‌ی کافه‌بازار تا publish شدنِ `native-android/` قابل
  آپدیت نمی‌مونه — کاربر با علم به این، دوبار تایید کرد. پوشه‌ی `www/` خودش (کد HTML+JS) دست‌نخورده و
  هنوز تو ریپو هست (فقط با `.gitattributes` از آمار زبان مخفیه)، صرفاً دیگه هیچ ابزاری برای بیلدش نمونده.

## بازنویسی Kotlin: دوباره فعال شده (تصمیم قبلی برگشت خورد)
یه تلاش موازی برای بازنویسی کامل اپ به Kotlin/Jetpack Compose (پوشه‌ی `native-android/`) شروع شده بود؛ تب «وام بانکی» کامل ساخته و رو CI (گیت‌هاب اکشن) هم build/test موفق شد. کاربر یه بار تصمیم گرفت این مسیر کلاً کنار گذاشته بشه (پوشه و workflow حذف شدن)، **ولی بعداً صریحاً همین تصمیم رو برگردوند و خواست ادامه پیدا کنه** — پس `native-android/` و `.github/workflows/build-native-android.yml` دوباره برگردونده شدن و کار روش ادامه داره. **به‌روزرسانیِ مهم بعدتر**: اولش این یه بازنویسی موازی بود (اپ وب `www/` کنارش دست‌نخورده باقی می‌موند)، ولی کاربر بعداً تصمیم گرفت زیرساخت بیلد اپ وب کلاً حذف بشه (رجوع کن به هشدار بالای فایل) — یعنی الان دیگه واقعاً موازی نیست، `native-android/` تنها مسیر باقی‌مونده برای ساختن نسخه‌ی جدید اپه، فقط هنوز رو کافه‌بازار منتشر نشده.
- وضعیت فعلی: **کل چک‌لیست فاز ۱ اصلی + همه‌ی پالایش‌های بعدش، از جمله یادآوری سررسید، تکمیل شده.** هر ۴ تب اصلی محتوای واقعی دارن (وام بانکی، محاسبه‌گر، سود سپرده، وام‌های من)؛ ورود OTP اجباری + حالت مهمان؛ محدودیت «۱ وام رایگان» + خرید واقعی اشتراک کافه‌بازار (SDK بومی Poolakey، نه پلاگین Capacitor وب)؛ سینک ابری واقعی (پوش بعد از هر تغییر + چک تعارض بعد از ورود)؛ صفحه‌ی تنظیمات + خروج از حساب + اندازه فونت (کوچک/متوسط/بزرگ، با `LocalDensity` override)؛ تم روشن (یه `CompositionLocal` برای رنگ‌ها، بدون نیاز به تغییر تک‌تک صفحه‌ها)؛ وضعیت پرداخت مستقل هر قسط با تاریخ سررسید واقعی، ویرایش دستی مبلغ (+ گزینه‌ی «همین مبلغ رو بقیه‌ی اقساط هم بگیرن»)، ثبت تاخیر پرداخت (`paidLate`/`paidDate`)، یادآوری سررسید (نوتیفیکیشن واقعی با WorkManager + یه تبدیل جلالی↔میلادی دقیق نجومی تازه‌نوشته‌شده تو `core/JalaliCalendar.kt` که با کتابخونه‌ی پایتون jdatetime تایید شده)، گزارش خطا (کرش ریپورت، بدون سرویس ثالث)، و درباره‌برنامه/حریم‌خصوصی تو تنظیمات - رجوع کن به `native-android/README.md`. معماری چندماژوله‌ست: `:data` (Room، DataStore، Retrofit مطابق دقیق قرارداد API سرور)، Hilt برای DI، Navigation-Compose.
- **پرکردن شکاف با اپ رقیب (VAMMAN) هم تکمیل شده**: کاربر فایل APK واقعی اپ رقیب رو فرستاد؛ با `androguard` (بدون decompile/کپی کد - فقط manifest/permissions/لیست کامپوننت‌ها/رشته‌های فارسی استخراج‌شده از dex) تحلیل شد که چی داره که این پروژه نداره، بعد کاربر گفت «هرچی داره من نداریم اضافه کن». کل لیست اضافه شد: ماژول «امور چک» (دریافتی/پرداختی + دسته‌چک + استعلام صیادی placeholder)، ماژول «حساب بانکی» (چند حساب + دفترچه‌ی تراکنش واریز/برداشت، موجودی همیشه محاسبه‌شده)، چند منبع درآمد مستقل (ثابت/متغیر، جایگزین `IncomePrefs` قبلی) + تصحیح آستانه‌ی «منطقه‌ی امن» داشبورد از حدسِ قبلی ۳۰٪/۵۰٪ به عدد واقعی ۶۵٪ (از رشته‌های dex رقیب)، خروجی اکسل (.xlsx، دستی با `ZipOutputStream`، بدون کتابخونه‌ی جدید) کنار PDF، نمودار خطی «تاریخچه پرداخت» (Canvas، کنار دونات موجود)، قفل خودکار با تاخیر قابل‌تنظیم (بی‌درنگ/۱/۵/۱۵ دقیقه) + قفل موقت ۳۰ ثانیه‌ای بعد از ۵ تلاش ناموفق PIN، پشتیبان‌گیری خودکار روزانه (WorkManager، فضای داخلی اپ، نه SAF) + دکمه‌ی بازیابی، سرچ تو منوی تنظیمات، و پیوست عکس رسید رو وام/چک (Photo Picker مدرن + Coil). هر آیتم PR جدا داشت و رو CI واقعی (`build-native-android.yml`) سبز شد قبل از merge.
- مونده: چیز مشخصی از فاز ۱، پالایش‌های شناخته‌شده، یا لیست پرکردن شکاف بالا باقی نمونده. قفل اثر انگشت **عمداً** پورت نمی‌شه (تصمیم قبلی کاربر رو وب، همینجا هم صدق می‌کنه).
- **دورهای پالایش (رجوع کن به `native-android/README.md` برای جزئیات کامل هر دور)**: تمِ روشن/تاریک (فقط دوحالته - یه تلاشِ اولیه برای تمِ طلاییِ جداگانه بعداً برگشت خورد؛ کاربر توضیح داد منظورش رنگِ طلایی به‌عنوانِ لهجه‌ی ظریف تو کلِ اپه، نه یه Theme.Mode جدا - تاریک ویژگیِ اشتراکیه)، ویبره‌ی واقعی (نه فقط هپتیکِ ظریفِ Compose - ویژگی اشتراکیه)، عکس رسیدِ مخصوصِ هر قسط (نه یه عکس کلی رو کل وام)، دوره‌ی آزمایشیِ رایگانِ ۷روزه‌ی سرور-محورِ کلید‌خورده به شماره‌تلفن (`server/src/main/kotlin/.../SubscriptionStatus.kt`، رجوع کن به `server/README.md`)، تقویمِ اینلاینِ چرخونه‌ای (بدونِ ناوبری)، امورِ چک مستقیم تو تبِ «وام بانکی»، حذفِ کاملِ `Toast` سیستمی از کلِ اپ (به‌جاش بنرِ داخلِ اپ - چون Android 12+/بعضی رام‌ها آیکونِ اپ رو کنارِ Toast می‌چسبونن)، و رفعِ چندتا باگِ واقعی (نصب رو تقویمِ گوشی فقط رو یه تقویم، `GET /api/auth/me` هیچ‌وقت واقعاً صدا زده نمی‌شد، صفحه‌ی ورود پس‌زمینه‌ی صریح نداشت، منوی تنظیمات اسکرول نداشت). یه فایلِ APK واقعیِ اپِ Digipay هم برای الهامِ گرافیکی/انیمیشن گرفته شده - تحلیلِ کاملش (رجوع کن به README) برای دورِ بعد مونده.
- این پروژه تو سندباکس فعلی build نمی‌شه (`dl.google.com` مسدوده)؛ تنها راه تأیید واقعی، ران‌کردن `.github/workflows/build-native-android.yml` (فقط با `workflow_dispatch` یا push به main/master تریگر می‌شه، نه push به یه فیچر برنچ معمولی) و چک‌کردن نتیجه‌ی واقعیشه، نه حدس‌زدن از رو خوندن کد.
- جزئیات کامل و به‌روز پلن/وضعیت تو `native-android/README.md` نگه داشته می‌شه — قبل از ادامه‌ی کار، اونجا رو هم بخون.

## معماری
> این بخش معماری/کد اپ وب (`www/index.html`) رو مستند می‌کنه — همه‌ی جزئیات کد هنوز درسته، ولی
> زیرساخت بیلدش (`package.json`, `capacitor.config.json`, `scripts/`, `resources/`,
> `build-apk.yml`) حذف شده (رجوع کن به هشدار بالای فایل)، پس هر بخشی که به «workflow»، «Capacitor
> plugin نصب/آپگرید»، یا ساختن APK جدید اشاره می‌کنه، دیگه واقعاً اجرا نمی‌شه — فقط رکورد تاریخیه.
- چهار تب پایین (به ترتیب): وام بانکی (تب پیش‌فرض/اول، فرم مبلغ/نرخ/ماه + لیست بانک‌ها)، محاسبه‌گر (سقف وام بر اساس درآمد)، سود سپرده، وام‌های من
- `.app` و `.modal-sheet` عمداً `max-width` ندارن (قبلاً ۴۶۰px بود که رو گوشی‌های با ویوپورت CSS بزرگ‌تر حاشیه‌ی خالی دو طرف صفحه ایجاد می‌کرد)؛ این یه اپ فقط-موبایلِ Capacitor هست، نه یه سایت ریسپانسیو، پس نیازی به max-width نیست — دوباره اضافه‌ش نکن
- لوگوی بانک‌ها و خدمات اعتباری به‌صورت فایل جدا هستن، نه base64 توی کد (تو icons/banks/ و icons/services/) — چون لوگوهای رسمی بانک‌ها کپی‌رایت دارن و نباید مستقیم embed بشن؛ اگه فایل عکس نبود، fallback به بج رنگی با حروف اول اسم میره (این رفتار عمدیه، خرابش نکن)
- بج لوگوی بانک‌ها (`.bank-badge`) عمداً پس‌زمینه‌ی سفید یکسان داره (نه رنگ اختصاصی هر بانک) — این استاندارد مطابق اپ‌های مشابه هست؛ رنگ اختصاصی هر بانک (`b.c`) فقط زمانی به‌عنوان پس‌زمینه استفاده می‌شه که لوگو نداشته باشه یا لود عکس fail بشه (fallback به حروف اول اسم)
- اعداد فارسی/عربی که کاربر تایپ می‌کنه باید با تابع cleanNum/cleanNumDecimal به انگلیسی تبدیل بشن قبل از parse — این یه باگ قبلی بوده، حواست باشه دوباره نسازیش
- کارت‌های «وام‌های من» (`renderLoansList`) لوگوی بانک رو با `findBankMeta(l.bank)` از رو اسم پیدا می‌کنن (جست‌وجو تو `banks`/`creditServices`)؛ برای وام‌های دستی که اسم بانک/فروشنده با هیچ‌کدوم مچ نشه، fallback به حروف اول اسم (`initials()`) رو یه بج رنگ ثابت (`--primary-dim`) می‌ره. اسم بانک تو فرم افزودن دستی وام (`saveManualLoan`) اجباریه، دیگه مقدار پیش‌فرض «مشخص‌نشده» نداره
- پشتیبان‌گیری («وام‌های من» → پشتیبان‌گیری) از پلاگین‌های `@capacitor/filesystem` + `@capacitor/share` استفاده می‌کنه (بسته‌بندی‌شده با esbuild تو `www/vendor/backup.js`) — چون دانلود مستقیم با `<a download>`+`blob:` تو WebView واقعیِ اندروید کار نمی‌کنه (فقط تو مرورگر معمولی، که برای تست لوکال بهش برمی‌گرده). اگه پکیج رو عوض کردی، esbuild رو دوباره طبق کامنت بالای scripts/backup-entry.js بزن
- عکس‌های icons/banks و icons/services حداکثر ۱۶۰×۱۶۰ نگه داشته می‌شن (چون فقط تو بج ۵۴px نمایش داده می‌شن)؛ عکس با رزولوشن بالاتر از این فقط حجم اضافه می‌کنه، قبل از commit با Pillow (resize + quantize فقط برای PNG با روش Fast Octree تا آلفا خراب نشه) کوچیک/فشرده‌شون کن
- لیست بانک‌ها/خدمات اعتباری (`#bankGrid`) دو تا ردیف اسکرول افقی جداست (`.bank-scroll`، هر کدوم با یه `.bank-group-label` بالاش: «بانک‌ها» و «خدمات اعتباری»)، نه گرید عمودی که چندین ردیف بشکنه — هر کارت (`.bank-item`) عرض ثابت ۷۸px داره (آیکون ۵۴px + اسم با line-clamp دو خطی) طوری که دقیقاً ۴ تا کامل کنار هم دیده بشه و بقیه با اسکرول افقی (مثل الگوی `.chips`/`.preset-scroll`) دیده بشن. اگه بانک/گروه جدیدی اضافه کردی همین الگوی افقی رو نگه دار، به گرید wrap-شونده برنگردون
- دوره‌ی تنفس (grace period) فقط مبلغ اصل رو (با سود ساده‌ی روزشمار) بزرگ نمی‌کنه؛ باید تاریخ سررسید قسط اول (و در نتیجه تاریخ پایان وام) هم به‌اندازه‌ی graceMonths به‌عقب بیفته — این تو renderTable و محاسبه‌ی statEndDate پیاده شده، اگه گریس رو دست‌کاری کردی هر دو رو با هم چک کن
- مبلغ هر قسط از صفحه‌ی جزئیات قسط قابل ویرایش دستیه (چون بعضی بانک‌ها کارمزد/جریمه‌ی اضافه‌ای روی محاسبه‌ی خودمون می‌ذارن که ما نمی‌تونیم حدس بزنیم)؛ ویرایش با `confirmEditInstallment` ثبت می‌شه، `syncTotalsFromRows` جمع کل/سود رو از رو خودِ اقساط (نه فرمول اولیه) دوباره حساب می‌کنه، و اگه وام از قبل تو «وام‌های من» ذخیره شده بود (`currentLoanId` ست شده)، `persistCurrentLoanIfSaved` کل آرایه‌ی rows رو تو localStorage می‌نویسه. `openSavedLoan` هم برای وام‌های ذخیره‌شده، اگه rows ذخیره‌شده موجود باشه، به‌جای محاسبه‌ی دوباره از صفر همون rows (با ویرایش‌ها/وضعیت پرداخت‌شده‌هاش) رو استفاده می‌کنه — این مکانیزم رو نشکن، وگرنه ویرایش قسط و ثبت پرداخت با بستن/بازکردن دوباره‌ی وام از بین می‌ره. بعد از ثبت ویرایش یه‌دونه قسط، همیشه (از طریق `openConfirmModal` با `danger:false`) پرسیده می‌شه که همین مبلغ رو بقیه‌ی اقساط هم بگیرن یا نه؛ `openConfirmModal` حالا یه `yesLabel`/`danger` اختیاری هم می‌گیره تا برای این‌جور موارد غیرمخرب دکمه‌ش قرمز/«حذف کن» نباشه
- `.chips` (چیپ‌های تعداد اقساط، فاصله‌ی زمانی، ...) عمداً `overflow-x:auto` هستن نه `flex-wrap` — تا رو گوشی هر چقدر گزینه باشه، اسکرول افقی بشه نه اینکه به چند خط بشکنه؛ اگه گزینه‌ی جدیدی به یه chips اضافه کردی و به‌جاش خواستی wrap بشه، این رفتار رو عمدی نشکن
- ورود با شماره موبایل (نه گوگل — عمداً حذف شد) از طریق بک‌اند تو server/ انجام می‌شه: OTP پیامکی با پیام‌رسان (payam-resan.com، وب‌سرویس sms-webservice.com — قبلاً کاوه‌نگار پیش‌فرض بود، عوض شد؛ یا هر سرویس دیگه، `Sms.kt`)، JWT برای نگه‌داشتن سشن. آدرس واقعی سرور باید تو `API_BASE_URL` (بالای تگ `<script>` اصلی) ست بشه
- **ورود اختیاریه، با حالت «مهمان»**: اولین باری که اپ باز می‌شه (و هنوز نه لاگین شده نه «مهمان» رو انتخاب کرده)، `checkLoginGateOnStart()` (بعد از `hideSplashOnce`) مودال `#phoneLoginModal` رو با کلاس `mandatory` باز نگه می‌داره (دکمه‌ی «انصراف»، `.login-cancel-btn`، مخفیه)؛ ولی یه دکمه‌ی `.guest-btn` («ادامه به‌عنوان مهمان») هم هست که با `continueAsGuest()` یه فلگ `guest_mode` تو localStorage ست می‌کنه و می‌بنده — از اون به بعد دیگه این گیت دوباره نشون داده نمی‌شه (نه لاگین، نه مهمان مجدد از کاربر پرسیده نمی‌شه). `handleLogout` هم بعد از خروج همین فلگ `guest_mode` رو ست می‌کنه (نه این‌که گیت رو دوباره باز کنه) تا کاربر بعد از خروج آزادانه به‌عنوان مهمان ادامه بده
- **قفل اثر انگشت کاملاً حذف شده** (بنا به تصمیم کاربر، چون ورود/مهمان‌بودن الان به‌جاش مدیریت می‌شه) — پکیج `@aparajita/capacitor-biometric-auth`، فایل `www/vendor/biometric-auth.js`، منبعش `scripts/biometric-entry.js`، و `#biometricLock` از کد حذف شدن. اگه دوباره خواسته شد، باید از صفر با یه پکیج بایومتریک جدید پیاده بشه، کدهای قبلی برنگردون (توی تاریخچه‌ی گیت هست اگه لازم شد)
- **اشتراک (محدودیت یک وام رایگان، حتی برای مهمون)**: `canSaveAnotherLoan()` (تو `saveLoan`/`saveManualLoan` چک می‌شه) اگه از قبل حداقل یه وام ذخیره شده باشه: وقتی `account` نداریم (مهمون) `handleLoanLimitReached()` مودال ورود رو باز می‌کنه («اول باید وارد بشی»)؛ وقتی لاگین هستیم ولی `subscribed` فالسه، مودال `#subscriptionModal` باز می‌شه. یعنی مسیر همیشه «مهمون → (۱ وام رایگان) → باید وارد بشی → (بازم می‌خواد بیشتر) → باید اشتراک بخره»ست. `subscribed` سمت کلاینت از `localStorage` (کلید `subscribed`) و بعد از لاگین/باز شدن اپ از `GET /api/auth/me` تازه می‌شه (`refreshSubscriptionStatus`). سمت سرور هم `routes/LoansRoutes.kt` PUT یه چک دفاعی داره (۴۰۳ اگه `subscribed=0` و آرایه بیش از ۱ تا). ستون `subscribed` تو جدول `users` هست (پیش‌فرض ۰)
  - **خرید اشتراک واقعیه، وصل به پرداخت درون‌برنامه‌ای کافه‌بازار، با ۴ تا پلن پلکانی (۱/۳/۶/۱۲ ماهه)** — با پلاگین Capacitor آماده‌ی `@salarizadi/capacitor-cafebazaar-poolakey` (بسته‌بندی‌شده با esbuild تو `www/vendor/poolakey.js`، منبعش `scripts/poolakey-entry.js`). **این پلاگین فقط `purchaseProduct`/`consumeProduct` داره، نه `subscribeProduct`** — پس هرکدوم از ۴ پلن یه خرید یک‌باره‌ی غیرقابل‌مصرفِ جداگانه‌ست (`consumeProduct` عمداً صدا زده نمی‌شه)، نه اشتراک واقعاً تمدیدشونده؛ یعنی بعد از انقضا کاربر باید خودش دوباره بخره، تمدید خودکار نیست. تو پنل کافه‌بازار هم هر ۴ تا باید از نوع «غیرقابل‌مصرف» تعریف بشن، نه «اشتراک». شناسه‌های محصول (`CAFEBAZAAR_SUBSCRIPTION_TIERS` تو `www/index.html`، باید دقیقاً با پنل و با `TIER_DURATION_DAYS` تو `routes/SubscriptionRoutes.kt` یکی باشن): `unlimited_loans_1m`, `unlimited_loans_3m`, `unlimited_loans_6m`, `unlimited_loans_1y`. مودال `#subscriptionModal` این ۴ گزینه رو با قیمت واقعی (گرفته‌شده از `getProducts()` پلاگین) نشون می‌ده؛ `purchaseSubscription(productId)` پلاگین رو initialize و purchaseProduct می‌کنه، بعد `purchase.purchaseToken`/`productId` رو به `POST /api/subscription/verify` می‌فرسته. سرور (`Cafebazaar.kt` + `routes/SubscriptionRoutes.kt`) با API رسمی کافه‌بازار (`pardakht.cafebazaar.ir/devapi/v2`، با `CAFEBAZAAR_CLIENT_ID`/`SECRET`/`REFRESH_TOKEN` تو `.env`) خرید رو مستقل تایید می‌کنه (به حرف کلاینت اعتماد نمی‌شه)، و اگه معتبر بود بر اساس مدت پلن (`TIER_DURATION_DAYS`) به `subscribed_until` کاربر روز اضافه می‌کنه (اگه اشتراک قبلی هنوز فعال بود، از رو همون تاریخ انقضا جلو می‌ره، نه از الان). `isSubscribed()` (تو `SubscriptionStatus.kt`، مشترک بین `routes/AuthRoutes.kt` و `routes/LoansRoutes.kt`) یه کاربر رو مشترک می‌دونه اگه یا `subscribed=1` دستی ست شده باشه (برای پشتیبانی/تست) یا `subscribed_until` هنوز نگذشته باشه. **همه‌چی فعاله**: کلید RSA (`CAFEBAZAAR_RSA_PUBLIC_KEY` تو `www/index.html`)، هر ۴ محصول تو پنل کافه‌بازار (با شناسه‌های بالا، نوع «غیرقابل‌مصرف»)، و `CAFEBAZAAR_CLIENT_ID`/`SECRET`/`REFRESH_TOKEN` تو `.env` سرور رو VPS — همه ست شدن و با یه درخواست تستی (`{"error":"no_token"}` به‌جای `cafebazaar_not_configured`) تایید شد که سرور مقادیر رو درست می‌خونه. اپ فعلاً برای بررسی به کافه‌بازار فرستاده شده (حالت «انتشار دستی» انتخاب شده، یعنی بعد از تایید هم خودکار عمومی نمی‌شه). مراحل کامل گرفتن این مقادیر تو server/README.md مستند شده
  - **این تغییر Capacitor رو از نسخه‌ی ۶ به ۸ آپگرید کرد** (چون این پلاگین فقط `@capacitor/core>=7` رو پشتیبانی می‌کنه) — همه‌ی پکیج‌های `@capacitor/*` (android, app, core, filesystem, share, splash-screen, cli) با هم به v8 رفتن. تمپلیت جدید CLI خودش minSdk/AGP/Kotlin درست رو تو `android/` تازه‌ساخته‌شده می‌سازه، ولی خودِ `.github/workflows/build-apk.yml` به چندتا تغییر دستی نیاز داشت (هر کدوم با چندین رانِ CI واقعی پیدا و تایید شد، نه حدسی):
    - Node.js تو workflow باید حداقل ۲۲ باشه (Capacitor CLI 8 اجباری‌ش می‌کنه)، نه ۲۰
    - Java تو workflow باید ۲۱ باشه (ماژول‌های اندرویدِ Capacitor 8 با Java 21 کامپایل می‌شن)، نه ۱۷ — وگرنه خطای مبهم `invalid source release: 21` می‌ده (که چون یکی از سکرت‌های `RELEASE_KEY_*` مقدارش به‌طور تصادفی برابر رشته‌ی «release» بود، تو لاگ گیت‌هاب به `***` ماسک می‌شد و اول گمراه‌کننده بود)
    - قدم «پایین‌آوردن minSdk به ۲۲» که قبلاً برای پشتیبانی از گوشی‌های قدیمی‌تر بود باید کلاً حذف بشه — چاچوب Cordova باندل‌شده با Capacitor 8 (`org.apache.cordova:framework:14.0.1`) به minSdk حداقل ۲۴ نیاز داره و merge مانیفست با ۲۲ fail می‌شه
    - پلاگین Poolakey از **JitPack** میاد (`com.github.cafebazaar.Poolakey:poolakey`)، نه از Maven Central/Google — چون مخزن JitPack تو `android/build.gradle` تازه‌ساخته‌شده پیش‌فرض نیست، یه استپ جدید تو workflow این رو اضافه می‌کنه: `sed -i "s#mavenCentral()#mavenCentral()\n        maven { url 'https://jitpack.io' }#" android/build.gradle`
- همگام‌سازی «وام‌های من» به سبک «کل آرایه رو بگیر/بده» هست (نه per-loan) — `persistLoans()` بعد از هر تغییر هم لوکال ذخیره می‌کنه هم (اگه لاگین باشیم) `syncLoansToServer()` رو صدا می‌زنه. فقط دقیقاً یه‌بار، بلافاصله بعد از ورود موفق (`syncAfterLogin`)، اگه سرور و گوشی هر دو داده‌ی متفاوت داشته باشن از کاربر می‌پرسه کدوم بمونه؛ در بقیه‌ی مواقع «آخرین نوشته برنده‌ست» بدون سوال
- فونت Vazirmatn لوکاله (`www/fonts/Vazirmatn[wght].woff2`، فایل variable-weight ~۱۰۸KB + مجوز OFL.txt کنارش) نه از CDN — قبلاً از cdnjs.cloudflare.com می‌اومد که یه وابستگی شبکه‌ی غیرضروری بود؛ اگه خواستی نسخه‌ی فونت رو عوض کنی، از پکیج npm `vazirmatn` (فایل `Vazirmatn-Variable-font-face.css` و `fonts/webfonts/Vazirmatn[wght].woff2` داخلش) بگیر، نه فایل‌های static-weight (حجم‌شون خیلی بیشتره)
- `versionCode`/`versionName` اندروید تو workflow (نه تو کد کلاینت) هر بار از `github.run_number` ساخته می‌شه (`sed` روی `android/app/build.gradle` بعد از `cap add android`) — چون بازار (کافه‌بازار) آپلود نسخه‌ای با versionCode تکراری یا کمتر از قبلی رو رد می‌کنه
- گزارش خطا (کرش ریپورت) بدون سرویس ثالث (نه Sentry، نه Firebase Crashlytics) پیاده شده: `window.onerror`/`unhandledrejection` تو کلاینت به `POST /api/crash` (تو server/، `routes/CrashRoutes.kt`) می‌فرستن، حداکثر ۵ گزارش در هر بار باز شدن اپ (که لوپ خطا سرور رو بمباران نکنه)، و اگه `API_BASE_URL` خالی باشه کلاً کاری نمی‌کنه. پنل مدیریتی جدا نداره — گزارش‌ها مستقیم با sqlite3 رو خودِ سرور کوئری می‌شن (دستورش تو server/README.md هست)
- صفحه‌ی «حریم خصوصی» تو تنظیمات (`togglePrivacy`/`#privacyBox`) اضافه شده، درست کنار «درباره برنامه»؛ اگه رفتار جمع‌آوری داده (لوکال/سینک ابری/کرش ریپورت) عوض شد، این متن رو هم به‌روز کن که واقعیت رو منعکس کنه

## فرمول‌های حساسِ محاسبه (خیلی مهم، تست‌شده و تاییدشده)
- وام‌های عادی (نرخ سود بالای ۴٪): فرمول اقساط مساوی نزولی، i = نرخ سالانه/100 × (فاصله هر قسط به روز/365)
- وام‌های قرض‌الحسنه (ازدواج، فرزندآوری، قرض‌الحسنه — نرخ ۴٪ یا کمتر): این‌ها اصلاً سود مرکب ندارن! قسط‌های ۱، ۱۳، ۲۵، ۳۷... (ابتدای هر سال) کاملاً و فقط مخصوص کارمزد سالانه‌ان (روی مانده‌ی همون لحظه) و هیچ اصل وامی توشون نیست؛ اصل وام فقط بین بقیه‌ی اقساط (یعنی n − ⌈n/12⌉ قسط غیرکارمزدی) مساوی تقسیم میشه. این تو تابع computeLoan با method='qarz' پیاده شده و با فرمول واقعیِ یه اپ محاسبه‌گر وام مرجع (decompile شده) تطبیق و تایید شده — تغییرش نده مگر با تست دقیق روی چند مثال واقعی.

## انیمیشن/پولیش ظاهری
- عدد قسط ماهانه (`#installmentAmt`) و کل بازپرداخت (`#statTotal`) با تابع `animateNumber` به‌صورت شمارشی (count-up، ease-out cubic، ۵۰۰ میلی‌ثانیه) نمایش داده می‌شن، نه این‌که یهو ظاهر بشن؛ مقدار قبلی رو رو `el.dataset.animVal` نگه می‌داره تا حساب کردن دوباره از همون‌جا شروع بشه نه از صفر
- المان‌های لمسی (`.chip`, `.preset-card`, `.bank-item`, `.loan-card`, `.cta`) همه یه transform:scale خفیف رو `:active` دارن + transition نرم رو رنگ/border؛ اگه چیز لمسی جدیدی اضافه کردی همین الگو رو رعایت کن
- فلش‌های آکاردئونی تنظیمات (`.si-arrow`) دیگه با تعویض متن ▾/▴ کار نمی‌کنن، با کلاس `open` و `transform:rotate(180deg)` روی همون گلیف چرخش نرم می‌کنن — اگه آکاردئون جدیدی اضافه کردی از همین الگو (نه textContent swap) استفاده کن
- `.toggle` (سوییچ‌ها) و `.nav-item .ic svg` (آیکون تب فعال) از easing فنری `cubic-bezier(.34,1.56,.64,1)` استفاده می‌کنن برای حس «فنری»؛ مودال‌ها (`.modal-backdrop`/`.modal-sheet`) هم fade+slide نرم دارن (`backdropFade`/`slideUp`)
- سوییچ بین ۴ تب اصلی پایین صفحه (`tabOrder` تو `switchTab`) اسلاید افقی داره: جهتش از رو فاصله‌ی ایندکس تب قبلی (`currentTabName`) و جدید تو `tabOrder` حساب می‌شه (`showView(id, 'left'|'right')` کلاس `slide-l`/`slide-r` رو اضافه می‌کنه). این اسلاید فقط بین خودِ این ۴ تب اعمال می‌شه، نه بین صفحه‌های برگشتی/جزئیات (نتیجه، تنظیمات، افزودن دستی) که همچنان fade عمودی ساده (`viewEnter`) دارن — چون `switchTab('settings')` تو `tabOrder` نیست و `currentTabName` رو آپدیت نمی‌کنه

## هشدار درباره‌ی منبع لوگوها
موقع بررسی یه اپ محاسبه‌گر وام مرجع دیگه (decompile شده) معلوم شد که کد پایه‌ی اولیه‌ی همین پروژه (قبل از استخراج به فایل) حداقل یه لوگوی اشتباه داشته (مثلاً چیزی که اسمش «بانک سامان» بود در واقع لوگوی بانک دیگه‌ای بود). لوگوهای فعلی از یه پک رسمی (tejiran.ir) بر اساس نام پوشه دانلود شدن و با علم عمومی (شکل واقعی لوگوی هر بانک) چک شدن — **صرفاً یکی‌بودن بایت با یه فایل قدیمی یا اپ دیگه رو ملاک درستی اسم بانک نگیر**؛ همیشه لوگوی جدید رو با شکل واقعی و شناخته‌شده‌ی همون بانک تطبیق بده.

## کارهای درحال انجام
- فایل‌های لوگوی بانک‌ها/خدمات اعتباری از base64 قبلی استخراج و به icons/banks/ و icons/services/ اضافه شدن (اغلب با آرم رسمی واقعی هر بانک)؛ اگه بانک/سرویس جدیدی اضافه شد، لوگوش رو هم به‌همین شکل (فایل جدا، نه base64، ترجیحاً PNG با پس‌زمینه شفاف تا رو بج سفید درست دیده بشه) اضافه کن
- ~~resources/icon.png منبع آیکون اپ وب بود که `build-apk.yml` ازش آیکون/اسپلش می‌ساخت~~ — هر دو
  (`resources/` و `build-apk.yml`) حذف شدن وقتی زیرساخت بیلد اپ وب پاک شد (رجوع کن به هشدار بالای فایل).
  اگه اپ Kotlin به آیکون/اسپلش نیاز داشت، منبعش رو تو `native-android/app/src/main/res/` جدا مدیریت کن.
- ci-debug.keystore یه کلید امضای دیباگ ثابته (نه یه راز/سکرت — دقیقاً مثل کلید دیباگ پیش‌فرض اندروید با پسورد عمومی «android»، alias=`androiddebugkey`، که خودِ گوگل هم استفاده می‌کنه) تا نصب هر بیلد جدید رو بیلد قبلی با ارور «conflicts with an existing package»/«App not installed» رد نشه (چون امضاها فرق داشتن). این فایل رو پاک نکن، وگرنه این باگ برمی‌گرده. **الان فقط اپ بومی ازش استفاده می‌کنه** (اپ وب/Capacitor و `build-apk.yml`ش که قبلاً استفاده می‌کردن حذف شدن):
  - اپ بومی (`native-android/`، `build-native-android.yml`): چون `android/` اینجا کامیت‌شده و ثابته، امضای دیباگ **مستقیم تو `native-android/app/build.gradle.kts`** به همین فایل (`rootProject.file("../ci-debug.keystore")`) پین شده — قطعی‌تر از `~/.android/debug.keystore` که بین رانرها فرق می‌کرد. همین باگ «نصب رو نسخه‌ی قبلی» رو داشت که با این پین حل شد. `versionCode` هم تو workflow از `github.run_number` تزریق می‌شه (`sed` رو `versionCode = 1`) تا بیلد جدید نسبت به قبلی downgrade حساب نشه. **بیلد release اپ بومی هم با همین کلید امضا می‌شه** و workflow هر دو آرتیفکت رو آپلود می‌کنه — برای نصب رو گوشی همیشه `loan-calculator-native-release-apk` رو بده (بیلد debug به‌خاطر `debuggable` ذاتاً لگ داره و فقط برای توسعه‌ست)؛ `minify` تو release عمداً خاموشه (ریسک R8 با reflection های Gson، بدون امکان تست runtime).
- applicationId نهایی `ir.sadteam.loancalc` (بدون beta) — قبلاً `ir.loancalc.beta` بود، با تایید کاربر عوض شد. برچسب نسخه هم از «بتا ۲» به «نسخه ۱» تغییر کرد (`.badge-version` تو index.html، عنوان تب، و about box؛ این فایل دیگه بیلد نمی‌شه ولی مقادیرش به‌عنوان مرجع تاریخی درسته)
- ~~ساخت release APK امضاشده تو `.github/workflows/build-apk.yml`~~ — این workflow با بقیه‌ی زیرساخت
  بیلد اپ وب حذف شد؛ ساخت release APK امضاشده الان فقط از طریق `build-native-android.yml` (اپ بومی) انجام می‌شه.
- سرور بک‌اند دیپلوی و روشنه: یه VPS آروان‌کلاود (اوبونتو، IP `37.152.187.44`)، با pm2 (`pm2 start src/index.js --name loan-server` + `pm2 save` + `pm2 startup`) به‌صورت دائمی اجرا می‌شه و با `systemctl enable pm2-ubuntu` بعد از ری‌استارت سرور هم خودکار بالا میاد. دامنه‌ش ساب‌دامین رایگان خودِ آروان‌کلاوده: `server-8a887fd3-2459-4e6d-ace8-884dc36ca3db.ir-thr-fr1.arvancompute.ir` (از تنظیمات همون سرور تو پنل آروان، نه از یه ریجستراری جدا). nginx به‌عنوان reverse proxy رو پورت ۸۰/۴۴۳ به `localhost:3000` وصله (فایل تنظیماتش `/etc/nginx/sites-available/loan-api`؛ چون اسم دامنه طولانیه، `server_names_hash_bucket_size 128;` تو `/etc/nginx/nginx.conf` هم لازم بود اضافه بشه وگرنه nginx تست کانفیگ fail می‌ده). گواهی HTTPS با certbot (Let's Encrypt، `sudo certbot --nginx -d <دامنه>`) گرفته شده و خودکار تمدید می‌شه. `API_BASE_URL` تو `www/index.html` با همین آدرس HTTPS ست شده. **مهم: این VPS فعلاً هنوز همون سرور Node.js قدیمی رو اجرا می‌کنه** — بازنویسی Kotlon/Ktor فقط تو این ریپو انجام شده (کامپایل/تست محلی شده)، ولی چون این سندباکس SSH به VPS نداره، دیپلوی واقعی (`git pull` + `./gradlew fatJar` + عوض کردن دستور pm2 طبق `server/README.md`) هنوز رو سرور واقعی انجام نشده — کاربر باید خودش این مرحله رو دستی رو VPS انجام بده.
- **هنوز حل‌نشده / منتظر تصمیم کاربر:**
  - سرویس پیامک OTP: از کاوه‌نگار به پیام‌رسان (payam-resan.com) عوض شده بود، ولی کاربر اون رو کنسل کرد و الان می‌خواد **ملی‌پیامک** (melipayamak.com) رو راه بندازه. مدارک حساب ملی‌پیامک کاربر تایید شده. کد فعلی (`Sms.kt`، بعد از بازنویسی سرور به Kotlin) هنوز برای پیام‌رسانه.
    - ملی‌پیامک دو تا API متفاوت داره: یکی قدیمی (`rest.payamak-panel.com`، مبتنی بر یوزرنیم/پسورد، نه توکن) و یکی جدید — **کنسول ملی‌پیامک** (کاملاً REST + Auth Token، بدون نیاز به یوزر/پس). باید از کنسول جدید استفاده کنیم. ورود به کنسول: `https://console.melipayamak.com/auth/login`؛ بعد از لاگین، توکن واقعی و نمونه‌کدهای آماده (با توکن خودِ کاربر پرشده) تو بخش «Auth Token» / «قطعه کد آماده ویژه برنامه‌نویسان» هست.
    - طبق FAQ خودِ ملی‌پیامک، برای پیامک OTP بهتره از **«ارسال براساس پترن (الگو)»** استفاده بشه نه «ارسال ساده» — چون پیامک ساده از خط مشترک بیشتر در معرض بلاک/فیلتر شدن قرار می‌گیره، ولی پترن (یه بدنه‌ی از قبل تاییدشده تو پنل با جای‌خالی برای کد) قابل‌اعتمادتره. یعنی احتمالاً قبل از نوشتن `sms.js` نهایی، کاربر باید یه پترن/بدنه برای «کد تایید» تو پنل ملی‌پیامک بسازه و تاییدش بگیره (نه فقط از ارسال ساده استفاده کنیم).
    - هنوز منتظریم کاربر بعد از لاگین به کنسول، اسکرین‌شات صفحه‌ی توکن/نمونه‌کد رو بفرسته تا `Sms.kt`/`.env.example`/`server/README.md` رو به API واقعی (توکن + endpoint دقیق ارسال ساده یا پترن) عوض کنیم.
  - «دیوار» فقط یه اشتباه تایپی/گفتاری کاربر بود (منظورش چیز دیگه‌ای نبود) — این مورد بسته‌ست، دیگه نیازی به پیگیری نداره
