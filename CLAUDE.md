## پروژه
اپلیکیشن اندروید محاسبه‌گر اقساط وام فارسی («محاسبه‌گر اقساط») برای کافه‌بازار، تحت نام تیم «Sad Team».
استک: یه فایل HTML+CSS+JS (تک‌فایلی، بدون build step) که با Capacitor برای اندروید پکیج میشه.
فایل اصلی: www/index.html

## معماری
- چهار تب پایین: محاسبه‌گر (وام بانکی معمولی)، وام بانکی، سود سپرده، وام‌های من
- لوگوی بانک‌ها و خدمات اعتباری به‌صورت فایل جدا هستن، نه base64 توی کد (تو icons/banks/ و icons/services/) — چون لوگوهای رسمی بانک‌ها کپی‌رایت دارن و نباید مستقیم embed بشن؛ اگه فایل عکس نبود، fallback به بج رنگی با حروف اول اسم میره (این رفتار عمدیه، خرابش نکن)
- اعداد فارسی/عربی که کاربر تایپ می‌کنه باید با تابع cleanNum/cleanNumDecimal به انگلیسی تبدیل بشن قبل از parse — این یه باگ قبلی بوده، حواست باشه دوباره نسازیش

## فرمول‌های حساسِ محاسبه (خیلی مهم، تست‌شده و تاییدشده)
- وام‌های عادی (نرخ سود بالای ۴٪): فرمول اقساط مساوی نزولی، i = نرخ سالانه/100 × (فاصله هر قسط به روز/365)
- وام‌های قرض‌الحسنه (ازدواج، فرزندآوری، قرض‌الحسنه — نرخ ۴٪ یا کمتر): این‌ها اصلاً سود مرکب ندارن! فقط کارمزد سالانه که در قسط‌های ۱، ۱۳، ۲۵، ۳۷... (ابتدای هر سال) جدا از قسط اصل گرفته میشه، رو مانده‌ی همون لحظه. این تو تابع computeLoan با method='qarz' پیاده شده و با اعداد واقعی بانک مرکزی تست و تایید شده — تغییرش نده مگر با تست دقیق.

## کارهای درحال انجام
- فایل‌های لوگوی بانک‌ها/خدمات اعتباری از base64 قبلی استخراج و به icons/banks/ و icons/services/ اضافه شدن؛ اگه بانک/سرویس جدیدی اضافه شد، لوگوش رو هم به‌همین شکل (فایل جدا، نه base64) اضافه کن
- مرحله بعدی: ساخت نسخه‌ی امضاشده (release APK) برای کافه‌بازار
