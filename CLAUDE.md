## ⚠️ دستورالعملِ ثابت - کاربر فنی نیست، همیشه ساده توضیح بده
کاربرِ این پروژه پس‌زمینه‌ی فنی/برنامه‌نویسی نداره («هیچ اطلاعات مهمی ندارم فقط امیدوارم باگ نخوره»).
**این یعنی تو هر چتی، بعدِ هر تغییرِ فنی (خصوصاً چیزهایی که رو سرور/دیتابیس/زیرساخت اثر می‌ذارن، نه
فقط ظاهرِ اپ)، خودت - بدونِ اینکه کاربر بپرسه - یه خلاصه‌ی خیلی ساده و مبتدی‌پسند هم بده**: چی عوض
شد، چرا مهمه، آیا خطری داره یا نه، و آیا کاربر باید کاری بکنه یا نه. از اصطلاحاتِ فنی (migration،
artifact، CI، deploy، Room و…) یا خودداری کن یا در حدِ یه جمله‌ی خیلی ساده معنیش رو بگو. جزئیاتِ فنیِ
کامل (که تو بقیه‌ی این فایل هست) برای خودِ توئه، نه چیزی که باید به کاربر تحویل بدی مگر خودش بخواد.
این دستورالعمل خودش نتیجه‌ی یه بازخوردِ صریح کاربره («چرا قبلاً بهم نگفتی») - یعنی جدی بگیرش، نه فقط
وقتی یادت اومد.

## کارآمدترکردنِ مصرفِ توکن (بدونِ تناقض با قانونِ بالا)
کاربر یه فایلِ راهنمای عمومی (انگلیسی، برای کاهشِ مصرفِ توکن) پیدا کرد و پرسید اضافه بشه یا نه.
تصمیم: فقط بخش‌هایی که واقعاً به هدررفتنِ توکن مربوطن و **هیچ تناقضی با قانونِ بالا (توضیحِ ساده‌ی
فارسی بعدِ هر تغییرِ فنی) ندارن** اضافه شدن. بخشِ «فقط انگلیسی جواب بده»/«توضیح نده» عمداً رد شد -
مستقیم با خواسته‌ی صریحِ کاربر تناقض داشت.
- قبل از نوشتن، فایلِ موجود رو بخون؛ اگه عوض نشده، دوباره نخونش.
- فایل‌های بزرگ‌تر از ۱۰۰ کیلوبایت رو فقط اگه واقعاً لازمه بخون.
- کوچیک‌ترین تغییرِ درست رو انجام بده، نه بازنویسیِ اضافی؛ ویرایشِ کدِ موجود رو به بازنویسی از صفر
  ترجیح بده.
- تو کد کامنتِ اضافی/بدیهی ننویس (این پروژه از قبل همینو رعایت می‌کرد - رجوع کن به بخشِ «کارِ
  عمومیِ کدنویسی»/سبکِ کامیت‌های قبلی).
- فایل‌های گزارش/خلاصه/چنج‌لاگ/یادداشت (مثلِ SUMMARY.md، NOTES.md، TODO.md) رو خودسرانه نساز -
  فقط اگه صریح خواسته شد.
- **این‌ها هیچ‌کدوم به «کوتاه‌ترکردنِ توضیحاتِ ساده‌ی فارسی برای کاربر» ربطی ندارن** - قانونِ بالا
  (بخشِ اول همین فایل) همیشه اولویت داره، حتی اگه یعنی مصرفِ توکنِ بیشتر.


## پروژه
اپلیکیشنِ اندرویدِ فارسی برای کافه‌بازار/مایکت، تیمِ «Sad Team». **اسمِ نمایشی: «حسابدار من»**
(تاییدشده و تو کد اعمال‌شده). `applicationId` همیشه `ir.sadteam.loancalc` می‌مونه - **هیچ‌وقت
عوضش نکن**: از دیدِ استورها یعنی یه اپِ کاملاً جدید، همه‌ی نصب‌ها/نظرها از دست می‌رن. عنوانِ
صفحه‌ی خودِ استور مستقله؛ کاربر باید دستی تو پنلِ دولوپر عوضش کنه.

اپ اولش محاسبه‌گرِ اقساطِ وام بود، الان یه اپِ حسابداریِ شخصیِ کامله (دخل/خرج، بودجه، دارایی،
گزارش، طلب‌وبدهی)؛ وام/چک هم به‌عنوانِ بخشی از همون اپ موندن.

**ریپو یه مجموعه‌ی واحد و ۱۰۰٪ Kotlinه.** اپِ وبِ قدیمی (`www/`) کاملاً حذف شد (تو تاریخچه‌ی گیت
هست). دو بخشِ اصلی:
- **کلاینت**: اندرویدِ بومی Kotlin + Jetpack Compose تو `native-android/` - تنها کلاینتِ موجود.
  جزئیاتِ معماری/فیچرها تو `native-android/README.md`.
- **بک‌اند**: Kotlin + Ktor + SQLite تو `server/` - ورودِ اجباری با موبایل (OTP)، همگام‌سازیِ
  ابری، اشتراکِ کافه‌بازار/مایکت. جزئیات تو `server/README.md`.

**سندباکس نمی‌تونه `native-android` رو بیلد کنه** (`dl.google.com` مسدوده) - تنها راهِ تاییدِ
واقعی، ران‌کردنِ `.github/workflows/build-native-android.yml` و چک‌کردنِ نتیجه‌ست، نه حدس‌زدن از
رو خوندنِ کد. `server/` برعکس (فقط Maven Central لازم داره) همین‌جا واقعاً بیلد/تست می‌شه.

## سرور (VPS آروان‌کلاود - زنده و فعال)
- IP `37.152.187.44`، دامنه `server-8a887fd3-2459-4e6d-ace8-884dc36ca3db.ir-thr-fr1.arvancompute.ir`
  (HTTPS با certbot، خودکار تمدید می‌شه). nginx رو ۸۰/۴۴۳ به `localhost:3000` پراکسی می‌کنه
  (`/etc/nginx/sites-available/loan-api`).
- جارِ runnable تو `~/VameMan/` با pm2 اجرا می‌شه (`pm2 start java --name loan-calc-api ...`).
  `.env` کنارِ jar تو همون پوشه‌ست. (پوشه قبلاً `~/loan-server` بود؛ هرجا تو متن دیدیش یعنی مالِ
  قبل از تغییرِ نامه.)
- کد تو `server/src/main/kotlin/ir/sadteam/loancalc/server/`: `Application.kt`، `Db.kt` (اسکیما +
  مایگریشن)، `Auth.kt`، `Sms.kt`، `SubscriptionStatus.kt`، `Cafebazaar.kt`، `Myket.kt`، و
  `routes/{Auth,Loans,Subscription,Crash,Backup,CreditRates,AppVersion}Routes.kt`.
- **دیپلویِ خودکار**: `build-server.yml` رو هر پوشِ `main` که `server/**` رو عوض کنه، jar رو
  می‌سازه، به VPS می‌فرسته، سلامتش رو چک می‌کنه، جایگزین می‌کنه (با بکاپِ `.bak`)، `pm2 restart`
  می‌زنه، و اگه سرویس بالا نیومد خودکار برمی‌گردونه. **دیگه دستی jar آپلود نکن** - سه‌بار همین
  کار سرورِ زنده رو خوابونده.
- ورک‌فلوهای کمکی: `set-melipayamak-env.yml`، `set-cafebazaar-env.yml`، `check-server-health.yml`
  (این آخری ۵ کرشِ آخرِ `crash_reports` رو هم چاپ می‌کنه - برای دیباگِ کرشِ گوشیِ کاربر بدونِ
  نیاز به لاگِ خودش).

### OTP (پیامکِ کدِ تایید) — ✅ کاملاً فعاله، کارِ باقی‌مونده نداره
ملی‌پیامک (SOAP، `SendByBaseNumber`). پترن تاییدشده (`bodyId=496763`) و شاملِ هشِ SMS Retriever
(`FuPsBGlTa0q`) تو خطِ آخرشه تا پرکردنِ خودکارِ کد کار کنه. کاربر با شماره‌ی واقعی تست کرد و
پیامک رسید.

**مقادیرِ حساس (یوزرنیم/APIKey) عمداً تو این فایل نوشته نشدن** - تو GitHub Secrets و `~/VameMan/.env`
هستن. اگه لازم شد عوض بشن، سکرت‌ها رو آپدیت کن و `set-melipayamak-env.yml` رو دستی اجرا کن.

**درسِ مهم**: ملی‌پیامک (و سرویس‌های مشابه) «رمزِ ورودِ پنل» و «APIKeyِ وبسرویس» جدا دارن. همیشه
اونی که صریحاً برای **وبسرویس/API** نوشته شده رو تو `MELIPAYAMAK_PASSWORD` بذار، نه رمزِ ورود -
یه‌بار همین اشتباه باعثِ «ارسالِ کد ناموفق» شد.

**اگه خواستی سرویسِ پیامکی عوض بشه**: فقط `Sms.kt` (تابعِ `sendOtpSms`) - بقیه‌ی کد کاری نداره
پیامک از کجا می‌ره.

## اپ بومی (native-android/)
معماریِ چندماژوله: `:app` (Compose UI)، `:data` (Room + SQLCipher، DataStore، Retrofit)، `:core`
(محاسباتِ خالص، بدونِ وابستگیِ اندرویدی)، Hilt برای DI، Navigation-Compose.

**۵ تبِ نوارِ پایین**: خانه / دارایی / گزارش / بودجه / سررسید. «وام» و «چک» دیگه تبِ نوارِ پایین
نیستن - از تبِ «سررسید»/«خانه» به‌عنوانِ صفحه‌ی پوش‌شده باز می‌شن. تبِ «وام» خودش زیرناوبریِ
داخلی داره (بانکی/محاسبه‌گر/سپرده/وام‌های من).

جزئیاتِ کاملِ فیچرها تو `native-android/README.md` - قبل از کارِ جدی اونجا رو هم بخون.

قفلِ امنیتی PIN+اثرِ انگشت پیاده‌ست (`ui/security/LockScreen.kt`، `AppLockViewModel.kt`، با
`BiometricPrompt` - به همین خاطر `MainActivity` از `ComponentActivity` به `FragmentActivity` عوض
شده). `ci-debug.keystore` یه کلیدِ امضای دیباگِ **ثابت** (نه سکرت) تو `app/build.gradle.kts`ه تا
نصبِ هر بیلدِ جدید رو بیلدِ قبلی خطای «conflicts with an existing package» نده - **پاکش نکن**.

قفلِ اسکرین‌شات (`FLAG_SECURE`) عمداً حذف شده (تصمیمِ صریحِ کاربر - مانعِ عکس‌گرفتن می‌شد).

**ماژولِ «امور چک» کامله** - پیوستِ عکس، اطلاعاتِ بیشتر (شناسه‌ی ملی/مانده/واریزی)، دسته‌چک +
شناسه‌ی صیادی، ساعتِ زنده، صفحه‌ی استعلامِ صیادی، منوی کشویی (گزارش‌دهی/دسته‌چک/استعلام/یادآوری/
بایگانی/پشتیبان‌گیری/بازیابی). **درسِ مهم: قبل از شروعِ هر فیچرِ «باقی‌مونده»، اول کدِ واقعی رو چک
کن - این لیست یه‌بار اشتباهاً «پیاده‌نشده» علامت خورده بود در حالی که کامل پیاده بود.**
## فرمول‌های حساسِ محاسبه (خیلی مهم، تست‌شده و تاییدشده)
پیاده‌سازی مرجع الان `native-android/.../core/LoanCalculator.kt`ه (قبلاً تو `www/index.html` بود که
حذف شده؛ منطق عیناً پورت شد، فرمول‌ها عوض نشدن):
- وام‌های عادی (نرخ سود بالای ۴٪): فرمول اقساط مساوی نزولی، i = نرخ سالانه/100 × (فاصله هر قسط به روز/365)
- وام‌های قرض‌الحسنه (ازدواج، فرزندآوری، قرض‌الحسنه — نرخ ۴٪ یا کمتر): این‌ها اصلاً سود مرکب ندارن! قسط‌های ۱، ۱۳، ۲۵، ۳۷... (ابتدای هر سال) کاملاً و فقط مخصوص کارمزد سالانه‌ان (روی مانده‌ی همون لحظه) و هیچ اصل وامی توشون نیست؛ اصل وام فقط بین بقیه‌ی اقساط (یعنی n − ⌈n/12⌉ قسط غیرکارمزدی) مساوی تقسیم میشه. این تو `LoanMethod.QARZ` پیاده شده و با فرمول واقعیِ یه اپ محاسبه‌گر وام مرجع (decompile شده) تطبیق و تایید شده — تغییرش نده مگر با تست دقیق (`LoanCalculatorRegressionTest.kt`) روی چند مثال واقعی.

## هشدار درباره‌ی منبع لوگوها
موقع بررسی یه اپ محاسبه‌گر وام مرجع دیگه (decompile شده) معلوم شد که کد پایه‌ی خیلی اولیه‌ی همین پروژه
حداقل یه لوگوی اشتباه داشته (مثلاً چیزی که اسمش «بانک سامان» بود در واقع لوگوی بانک دیگه‌ای بود). داده‌ی
بانک‌ها/خدمات اعتباری الان تو `native-android/.../data/Banks.kt` هست و لوگوها منابع اندرویدی
(`res/drawable`) هستن، از یه پک رسمی (tejiran.ir) بر اساس نام دانلود و با علم عمومی (شکل واقعی لوگوی هر
بانک) چک شدن — **صرفاً یکی‌بودن بایت با یه فایل قدیمی یا اپ دیگه رو ملاک درستی اسم بانک نگیر**؛ همیشه
لوگوی جدید رو با شکل واقعی و شناخته‌شده‌ی همون بانک تطبیق بده.


## 🔑 درس‌ها و قواعدِ فنیِ ماندگار (چکیده‌ی همه‌ی نشست‌ها - مهم‌ترین بخشِ این فایل)
این‌ها از دلِ نشست‌های قبلی استخراج شدن. **گزارشِ روایتیِ اون نشست‌ها فشرده شد، ولی این قواعد
عیناً موندن** - قبل از هر کارِ مرتبط، همین بخش رو بخون.

### دیتابیس و Migration (پرریسک‌ترین بخشِ پروژه)
- **هر `CREATE INDEX` تو یه migrationِ دستی، باید عیناً تو `indices = [...]`ِ همون `@Entity` هم
  اعلام بشه.** یه‌بار نشد و نسخه‌ی ۱.۰.۳۱۵ برای هر کسی که آپدیت می‌کرد بلافاصله کرش می‌کرد
  (`IllegalStateException: Migration didn't properly handle`). فقط مسیرِ آپگرید تاثیر می‌گیره، نصبِ
  تازه نه - پس تستِ نصبِ تازه این باگ رو نمی‌گیره.
- **هر migrationِ جدید باید یه تستِ `migrateN` تو `data/src/androidTest/.../MigrationTest.kt`
  بگیره** (الگو تو کامنتِ همون فایل). ورک‌فلوی `instrumented-tests.yml` یه امولاتورِ واقعی بالا
  میاره. اگه با «Not enough space» شکست خورد، عددِ `-partition-size` رو تو همون فایل بیشتر کن (نه
  دنبالِ باگِ KVM بگرد).
- دیتابیس از `net.sqlcipher` استفاده می‌کنه، نه SQLite استانداردِ اندروید - پس تستِ migration رو
  JVM (ارزون) ممکن نیست، حتماً امولاتور لازمه.
- `exportSchema=true` روشنه؛ اسکیمای هر نسخه تو `native-android/data/schemas/` کامیت می‌شه.
- ردیف‌های قسط تو جدولِ جدای `loan_rows` (نه دیگه داخلِ `dataJson`). `LoanRepository.getRows()`
  **suspend**ه (کوئریِ واقعیِ دیتابیس) - هیچ‌وقت مستقیم تو `remember{}` صداش نکن، وگرنه رو thread
  اصلی کرش می‌ده؛ الگوی درست: `var x by remember { mutableStateOf(...) }` + `LaunchedEffect(key) { x = ... }`.
  `getOrMigrateRows` یه فال‌بکِ خودترمیم‌شونده‌ست: اگه وامی تو `loan_rows` خالی بود، از `dataJson`
  قدیمی یا پیش‌فرض بازسازی می‌کنه.
- `AccountRepository.addTransaction` قبلاً `id = System.currentTimeMillis()` می‌ساخت؛ تو یه حلقه‌ی
  سریع چند تراکنش id یکسان می‌گرفتن و `@Upsert` بی‌صدا یکی رو جایگزینِ قبلی می‌کرد. الان `id` یه
  پارامترِ اختیاریه - **هر جا تو حلقه تراکنش می‌سازی، شمارنده‌ی صریح پاس بده.**
- `toUserRow()` سمتِ سرور ستون‌ها رو با اسم می‌خونه: **هر `SELECT ... FROM users` باید همه‌ی
  ستون‌ها (از جمله `subscription_tier`) رو داشته باشه** وگرنه `SQLException`.

### تاریخِ فارسی (`core/PersianDate.kt`)
- `addDays`/`addMonths` **باید مسیرِ جداگانه برای عددِ منفی داشته باشن**. قبلاً با `repeat(n)`
  پیاده شده بودن و `repeat` تو Kotlin برای nِ منفی بی‌صدا هیچ‌کاری نمی‌کنه (نه خطا) - نتیجه: فلشِ
  «روزِ قبل» تو گزارش/خانه/سررسید کار نمی‌کرد و نمودارِ ۷روزه هر ۷ ستونش یه روز بود. تستِ رگرسیون
  داره (`persianDateAddDaysHandlesNegativeOffsets`).
- برای سررسیدِ اقساط، اگه `intervalDays % 30 == 0` باشه از `addMonths` استفاده کن نه `addDays` -
  تقویمِ فارسی ماهِ یکسانِ ۳۰روزه نداره (۶ ماه ۳۱روزه‌ان)، وگرنه روزِ سررسید به‌مرور می‌لغزه.
- `addMonths` موقعِ clampِ روز از `JalaliCalendar.daysInMonth` استفاده می‌کنه (اسفندِ کبیسه رو درست
  حساب می‌کنه)، نه `monthLength()`ِ ساده.
- **معنیِ «تاریخ دریافت وام»**: تاریخی که کاربر وارد می‌کنه **خودش سررسیدِ قسطِ اوله** (نه یه دوره
  بعدش) - تصمیمِ صریحِ کاربر. استثنا: وام‌های دارای دوره‌ی تنفس، که قسطِ اول به‌اندازه‌ی تنفس بعدتره
  و یه توضیحِ دینامیک زیرِ فیلد نشون داده می‌شه. تاریخ‌ها ذخیره نمی‌شن، پویا حساب می‌شن - پس این
  تغییر رو وام‌های ازقبل‌ذخیره‌شده هم اثر گذاشت (عمدی، تاییدشده).

### Compose - قواعدی که نقضشون باگ/کرش داده
- **توکن‌های رنگ (`AppPrimary`، `AppText`، ...) پراپرتیِ `@Composable`ان.** صداکردنشون داخلِ بلوکِ
  رسمِ `Canvas` (که `DrawScope`ه نه `@Composable`) خطای کامپایل می‌ده. همیشه قبل از `Canvas` تو یه
  `val` محلی بخونشون.
- **هیچ کارتی رو دستی با `clip/background/border` نساز - همیشه `AppCard`.** اگه رنگِ برجسته لازم
  داری، پارامترِ `accentGradient` بده. (کارت‌های دستی گوشه/سایه/هایلایتِ متفاوت می‌گیرن و صفحه
  ناهماهنگ می‌شه - یه‌بار کاربر گزارشش داد.)
- `AppCard` داخلاً `fillMaxWidth()` می‌زنه، ولی `Modifier.width(X)` قبلش constraint رو دقیق می‌کنه،
  پس عرضِ ثابت درست کار می‌کنه.
- `AppCard` محتوا رو تو `CompositionLocalProvider(LocalContentColor provides AppText)` می‌پیچه -
  این جایگزینِ دستیِ کاریه که `Surface` خودکار می‌کرد. **اگه یه `Text` بدونِ `color` صریح جایی
  نامرئی شد، اول چک کن بیرونِ هر `AppCard`ی نباشه.**
- **گریدِ تنبل (`LazyVerticalGrid`) داخلِ لیستِ تنبلِ دیگه نمی‌تونه wrap-content باشه** و ارتفاعِ
  ثابت می‌خواد (که همیشه غلط درمیاد). به‌جاش از `items.chunked(n).forEach { Row { ... } }` با
  `Spacer(weight)` برای پرکردنِ ردیفِ ناقص استفاده کن.
- فیلدهای مبلغ: **هیچ‌وقت داخلِ `onValueChange` مقدارِ state رو دوباره فرمت نکن** (کاما اضافه کن) -
  cursor به‌هم می‌ریزه. الگوی درست: state فقط رقمِ خام، فرمت فقط بصری با
  `ThousandsSeparatorTransformation` (`ui/components/ThousandsTransformation.kt`).
- **الگوی استانداردِ فیلدِ مبلغ تو کلِ اپ**: `ThousandsSeparatorTransformation()` +
  `suffix = { Text("ریال", ...) }` + یه کپشنِ `numberToWordsFa(amountRial / 10) + " تومان"` زیرش +
  `cleanNum` (نه `cleanNumDecimal` - ریال اعشار نداره) + `KeyboardType.Number`.
- **RTL**: اپ globally RTLه؛ فیلدهایی که ذاتاً چپ‌به‌راستن (شماره‌موبایل، PIN، شماره‌چک، شناسه‌ی
  صیادی، شماره‌ی دسته‌چک، شناسه‌ی ملی) باید تو کامپوننتِ مشترکِ `ui/components/Ltr.kt` پیچیده بشن،
  وگرنه رقم از سمتِ راست وارد می‌شه.
- **اسکرول‌بار تو RTL**: `contentPadding = PaddingValues(start = 10.dp)` لازمه وگرنه خطِ اسکرول‌بار
  رو محتوا می‌افته (`start` تو RTL یعنی سمتِ راست، همون‌جا که اسکرول‌بار رسم می‌شه).
- `SwipeToDeleteRow` پارامترِ `confirmDismiss` داره: `true` یعنی خودِ ژست حذفِ نهاییه؛ **اگه بعدش
  دیالوگِ تایید باز می‌کنی حتماً `confirmDismiss = false` بده**، وگرنه ردیف قبل از تایید بصری حذف
  می‌شه.
- `PrivacyCrossfade(privacyMode) { masked -> ... }` رو **کوچیک‌ترین جزئی** که مبلغ رو نشون می‌ده
  بپیچ. اگه تو `Row/Column` با `weight` می‌شینه، `weight` رو رو خودِ `PrivacyCrossfade` بذار نه
  محتوای داخلش.
- گرفتنِ اسنپ‌شات از UI: از `rememberGraphicsLayer()`ِ Compose استفاده کن، **نه `View.drawToBitmap()`**
  (رو بعضی گوشی‌ها بی‌صدا خروجیِ خالی می‌ده).
- وقتی یه Composable رو از یه فایل به فایلِ جدید منتقل می‌کنی، **ایمپورت‌هاش خودکار نمیان** - چند
  بار همین باعثِ شکستِ CI شده.

### کامپوننت‌های مشترکِ اپ (قبل از ساختنِ چیزِ جدید، اول این‌ها رو ببین)
`AppCard` (کارتِ شیشه‌ای، `accentGradient` اختیاری) · `GradientButton` (CTA اصلی) · `AppChip` ·
`SegmentedToggle` (کپسولِ لغزنده‌ی دوگزینه‌ای) · `EmptyState` · `SwipeToDeleteRow` ·
`ConfirmDeleteDialog` · `AccountPickerDialog` · `BankBadge` (لوگوی بانک از رو اسم) ·
`TodayCard` (مشترکِ خانه/سررسید - تغییرش هر دو تب رو عوض می‌کنه) · `SuccessCheckmarkOverlay` ·
`StaggerIn` (ورودِ پلکانی) · `Motion.kt` (توکن‌های فنر: `standard/snappy/heavy`) ·
`pressScaleClickable` (فشردگی + هپتیک) · `Ltr.kt` · `ThousandsTransformation.kt` ·
`InAppBannerHost`/`rememberInAppBanner` (بنرِ داخلی، نه Toast سیستمی) · `FullScreenDialog`
(پوششِ مشترکِ زیرصفحه‌های تنظیمات - وگرنه تو عرضِ ۸۵٪ پنل گیر می‌کنن).

### تم و رنگ
- همه‌ی رنگ‌ها از `ui/theme/Color.kt` (`DarkAppColors`/`LightAppColors`). برای تغییرِ رنگِ اپ فقط
  همین دو تا رو عوض کن - `line`/`glass*` از همون primary مشتق می‌شن و باید هم‌قدم آپدیت بشن.
- شدتِ پس‌زمینه‌ی متحرک: `primaryAlpha`/`accentAlpha` تو `AuroraBackground.kt`.
- یه رنگِ هاردکدِ جدا تو `SplashIntroScreen.kt` هست (`RingTeal`) - موقعِ تغییرِ رنگِ اصلی یادت باشه.
- **طلایی (`AppAccent`) فقط نشانه‌ی پرمیوم/اشتراکه** (بج‌ها، `GoldSheenBox`) - رو حاشیه‌ی دکمه‌های
  معمولی استفاده نشه (یه‌بار شد و کاربر گفت ناهماهنگه).
- پروژه عمداً **بلورِ واقعی (`RenderEffect`) نداره** - همه‌جا با گرادیانِ ثابت تقلید می‌شه، تا رو
  گوشیِ ضعیف روان بمونه.

### ساختِ تصویر با Python/PIL
- **متنِ فارسی**: `arabic_reshaper`+`bidi` با `draw.text()` معمولی حروف رو نچسبیده و برعکس رندر
  می‌کنه. راهِ درست: `ImageFont.truetype(..., layout_engine=ImageFont.Layout.RAQM)` +
  `draw.text(..., direction="rtl", language="fa")`.
- **ساختِ آیکون از یه تصویرِ رندرشده**: اول با اسکنِ پیکسل باکسِ سفتِ محتوای واقعی رو پیدا کن و
  کراپ کن - به چشم اعتماد نکن (یه‌بار ~۱۰٪ حاشیه‌ی مشکی جا موند و کاربر گزارشش داد).
- **JPEG در برابرِ PNG**: برای تصویرِ بزرگِ بدونِ شفافیت، JPEG (`quality=92, optimize=True`) حدودِ
  یک‌هفتمِ PNG حجم می‌گیره. برای آیکون‌های لانچر (که شفافیت لازم دارن) PNG بمونه.

### گیت / CI
- **قبل از اینکه یه برنچ رو «زائد» فرض کنی، حتماً `git log origin/main..origin/<branch>` و PRهای
  بازش رو چک کن.** یه‌بار تو همین فایل نوشته شد «فلان برنچ merge‌شده و بی‌ضرره» در حالی که ۲۱
  کامیتِ منحصربه‌فرد و یه PR باز داشت.
- برنچِ جدیدِ داوطلبانه نساز؛ از `main` شروع کن - مگر هارنسِ CCR برنچِ مشخصی رو اجباری کرده باشه.
- APKها به‌عنوانِ **GitHub Release** (تگِ `build-<شماره‌ران>`) منتشر می‌شن، نه Artifact - سهمیه‌ی
  Artifact چندبار پر شد و بیلدِ سالم رو غیرقابل‌دانلود کرد.
- تو `reactivecircus/android-emulator-runner`، **هر خطِ `script:` با یه `sh -c` جدا اجرا می‌شه** -
  `if/then/fi` چندخطی از هم می‌پاشه. هر خط باید یه دستورِ کاملِ مستقل باشه.
- تستِ اسکرین‌شاتِ Compose با Paparazzi (`app/src/test/.../ScreenshotTest.kt`) رو JVM اجرا می‌شه.
  برای تستِ جدید یه Composableِ **بدونِ Hilt** انتخاب کن (مثلِ `AppCard`)، نه یه صفحه‌ی کامل.

### تصمیماتِ محصولی که نباید دوباره بازشون کنی
- **حسابِ تست**: فقط `TEST_ACCOUNT_PHONE` کدِ ثابت می‌گیره. شماره‌ی واقعیِ کاربر همیشه کدِ تصادفیِ
  واقعی می‌خواد. کاربر اول خواست هر دو کدِ ثابت بگیرن، ریسکش توضیح داده شد، و **صریحاً پذیرفت که
  طراحیِ فعلی درسته** - دوباره دربارش سوال نپرس.
- **پشتیبان‌گیریِ دراپ‌باکس**: صریحاً رد شد (اپ سیستمِ بکاپِ خودش رو داره).
- **خریدِ درون‌برنامه‌ای از سایت** (دورزدنِ استور): بررسی و رد شد - ناقضِ قوانینِ استوره.
- **همگام‌سازیِ گذشته‌نگرِ وام/چک با حسابداری**: ساخته شد، بعد **کاملاً حذف شد** (`MIGRATION_17_18`
  تراکنش‌های `sourceType IN ('loan','cheque')` رو پاک می‌کنه). دلیل: «موجودیِ اولیه» یعنی موجودیِ
  *الان*، پس کسرِ دوباره‌ی قسط‌های قدیمی دوباره‌حسابیه (کاربر موجودیِ منفیِ ۵ میلیاردی دید). سینکِ
  **رو-به-جلو** (از این به بعد) هست و می‌مونه.
- **کارتِ خریدِ اشتراک عمداً به کاربرِ از قبل مشترک نشون داده نمی‌شه** - این باگ نیست. یه‌بار
  بازبینِ مایکت به همین خاطر گفت «بخشِ خرید پیدا نشد».
- **مایکت `ITEM_TYPE_INAPP` می‌خواد نه `ITEM_TYPE_SUBS`** - پنلِ مایکت اصلاً «نوعِ محصول» نداره،
  پس همه‌چی محصولِ درون‌برنامه‌ایِ معمولیه. با `SUBS` خودِ SDK خرید رو رد می‌کرد.

### نکته‌های پراکنده‌ی مهم
- **فونتِ سراسری Vazirmatn**ه (`app/src/main/res/font/`) - موقعِ ساختِ تصویر/عکسِ استور با PIL هم
  از همین فایلِ فونت استفاده کن تا با خودِ اپ یکی باشه.
- **مجوزهای `READ_CALENDAR`/`WRITE_CALENDAR`**: کافه‌بازار/مایکت این‌ها رو «دسترسیِ خطرناک»
  علامت می‌زنن و توضیح می‌خوان (تا الان دوبار پرسیدن). مالِ قابلیتِ «افزودنِ سررسیدها به تقویمِ
  گوشی» تو `LoanDetailScreen.kt` (`runCalendarExport`)ه. متنِ توضیحی که باید به بازبین داد:
  قابلیت کاملاً اختیاریه، مجوز فقط لحظه‌ی لمسِ همون دکمه (Runtime Permission) گرفته می‌شه، و اپ
  هیچ داده‌ای از تقویمِ کاربر نمی‌خونه/نمی‌فرسته.
- **هشِ SMS Retriever** به بسته‌نام + کلیدِ امضا بستگی داره (دیباگ و ریلیز دو هشِ متفاوت).
  `ui/auth/SmsRetrieverHash.kt` موقعِ باز شدنِ اپ هشِ واقعی رو تو لاگ چاپ می‌کنه
  (`adb logcat -s SmsRetrieverHash`). **اگه کلیدِ امضای ریلیز عوض شد، هشِ جدید باید تو خطِ آخرِ
  پترنِ ملی‌پیامک هم آپدیت بشه** وگرنه پرکردنِ خودکارِ کد بی‌صدا از کار می‌افته (تایپِ دستی همیشه
  به‌عنوانِ فال‌بک کار می‌کنه).

### الگوی کاریِ کاربر (رعایتش کن)
کاربر معمولاً چند بازخوردِ پشتِ‌سرهم می‌فرسته و می‌گه **«ذخیره کن»** - یعنی الان پیاده نکن، فقط
ثبت کن. بعد که لیستش تموم شد می‌گه **«همه رو انجام بده»**. وقتی گفت «ذخیره کن»، فقط تسک ثبت کن و
کدنویسی نکن.

**وقتی طرحِ مرجعِ کاربر یه رندرِ سه‌بعدی/نوریِ پیچیده‌ست** (نه چیدمانِ تخت از شکل‌های ساده)، از همون
اول بپرس آیا می‌شه خودِ تصویر رو مستقیم استفاده کرد - سه دورِ بازسازیِ دستیِ اسپلش شکست خورد تا
بالاخره خودِ عکس گذاشته شد.
## کارهای درحال انجام / تصمیم‌های معلق
- **🚨 حادثه‌ی خریدِ گیرکرده‌ی مایکت (۹-۱۲ مرداد ۱۴۰۵ / ۲۰۲۶-۰۷-۳۱ تا ۰۸-۰۳) - شاملِ یه تشخیصِ اشتباهِ
  خودم که یه‌بار کلِ بیلد رو خراب کرد، درسِ مهم برای چت‌های بعدی**: بازبینِ مایکت رو نسخه‌ی ۳۴۸ گزارش
  داد «بعدِ پرداختِ موفق و برگشت به اپ، محصول فعال نمی‌شه».
  **تشخیصِ اولیه (اشتباه، کامیتِ `b00d146`، بعداً برگردونده شد)**: با این فرض که IabHelperِ مایکت
  دقیقاً مثلِ IabHelperِ کلاسیکِ خودِ گوگل یه متدِ عمومیِ `handleActivityResult` داره،
  `SubscriptionManager.handleActivityResult` (فلیورِ `myket`) عوض شد تا `helper.handleActivityResult(...)`
  رو صدا بزنه. **این فرض غلط بود و اصلاً کامپایل نمی‌شد** (`Unresolved reference 'handleActivityResult'`)-
  وقتی بالاخره بعدِ حل‌شدنِ مشکلِ سهمیه‌ی گیت‌هاب یه بیلدِ واقعی گرفته شد، همینجا کشف شد. با چک‌کردنِ
  سورسِ واقعیِ کتابخونه (`github.com/myketstore/myket-billing-client`، فایلِ `IabHelper.java`) تایید
  شد این کلاس اصلاً چنین متدی نداره - **درسِ مهم: قبل از فرض‌کردنِ رفتارِ یه کتابخونه‌ی خارجی بر اساسِ
  شباهت با یه کتابخونه‌ی مشابهِ دیگه، اگه امکانش هست (اینترنت/WebFetch در دسترسه) حتماً سورسِ واقعیش
  رو چک کن، خصوصاً قبل از دست‌زدن به مسیرِ پرداخت.** این اشتباه با کامیتِ `8a7d71d` برگردونده شد
  (`handleActivityResult` دوباره no-op شد، دقیقاً مثلِ قبل).
  **مکانیزمِ واقعیِ کتابخونه** (تاییدشده از رو سورس): `launchPurchaseFlow` نتیجه رو از طریقِ یه
  `ProxyBillingActivity` داخلیِ خودِ کتابخونه (+ `ResultReceiver`) می‌گیره - کاملاً مستقل از
  `MainActivity.onActivityResult` ما. یعنی نسخه‌ی اصلیِ `handleActivityResult` (خالی/no-op، از همون
  اول) واقعاً درست بوده؛ اون قسمت از کد هیچ‌وقت باگ نداشته.
  **علتِ محتمل‌ترِ واقعیِ گزارشِ بازبین (هنوز صددرصد تاییدنشده)**: چون این مکانیزم یه Activity/Serviceِ
  جداگانه (اپِ مایکت) رو برای مدت‌زمانِ پرداخت (وارد کردنِ اطلاعاتِ کارت/رمز/OTP - می‌تونه ده‌ها ثانیه
  طول بکشه) باز نگه می‌داره، اندروید ممکنه پروسه‌ی اپِ ما رو تو پس‌زمینه به‌خاطرِ کمبودِ حافظه ببنده
  (خصوصاً رو گوشیِ میان‌رده‌ای مثلِ Galaxy A52 که بازبین باهاش تست کرده) - وقتی کاربر برمی‌گرده، یه
  پروسه‌ی کاملاً تازه بالا میاد و اون listenerِ اصلیِ `launchPurchaseFlow` (که تویِ حافظه بود) دیگه
  وجود نداره، پس هیچ callbackی اجرا نمی‌شه، با اینکه پرداخت واقعاً موفق بوده. این یه محدودیتِ
  شناخته‌شده‌ی همه‌ی الگوهای «پرداخت با ریدایرکت به اپِ دیگه» رو اندرویده، نه لزوماً یه باگِ خاصِ کدِ ما.
  **رفعِ عملیِ باقی‌مونده (کامیتِ `c5725c9`، هنوز پابرجا و صحیح)**: چون خریدِ مایکت از نوعِ غیرقابل‌مصرفه،
  پولِ کاربرهایی که تو این چند روز خریدن هنوز رو حسابشون رو مایکت مونده - یه تابعِ جدید
  `SubscriptionManager.restorePurchases()` (فلیورِ myket) به `SubscriptionScreen.kt` اضافه شد که هر
  بار این صفحه باز می‌شه (اگه کاربر لاگین باشه)، از رو inventoryِ IabHelper چک می‌کنه چه SKUهایی رو
  کاربر واقعاً مالکه و بی‌صدا دوباره برای تاییدِ سرور می‌فرسته - این مکانیزم مستقل از اینکه علتِ
  دقیقِ گم‌شدنِ callback چی بوده کار می‌کنه (چه process-death چه هر چیزِ دیگه‌ای)، پس همچنان راه‌حلِ
  درستیه و نگه داشته شد. فلیورِ cafebazaar یه `restorePurchases()` no-opِ بی‌ضرر گرفت.
  **کافه‌بازار از اولِ کار این ریسک رو نداشته** (Poolakey از ActivityResultRegistryِ مدرن استفاده
  می‌کنه که با تغییرِ کانفیگ/چرخه‌ی عمرِ اکتیویتی سازگارتره، هرچند تئوریاً کاملاً از ریسکِ کشته‌شدنِ
  پروسه مصون نیست).
  **⏳ کارِ باقی‌مونده**: بعدِ حل‌شدنِ مشکلِ سهمیه‌ی گیت‌هاب (۱ مرداد/۱ آگوست، ریست شد)، یه بیلدِ جدید
  باید گرفته و با یه خریدِ واقعیِ تازه رو گوشیِ واقعی تست بشه - هم مسیرِ اصلی هم مسیرِ ریکاوری
  (restorePurchases). اگه بازم گزارشِ مشابه اومد، این‌بار باید فرضِ «کشته‌شدنِ پروسه» رو با لاگ/تستِ
  واقعی (نه فقط استدلال) تایید یا رد کرد.
- **⚠️ ناهماهنگیِ حل‌نشده - فرمولِ قرض‌الحسنه با یه وامِ ازدواجِ واقعی نمی‌خونه**: کاربر گزارش داد
  برای وامِ ازدواج (۳۰۰ میلیون تومان = ۳ میلیارد ریال، ۱۲۰ ماهه، ۴٪)، بانکِ واقعی هر قسط رو
  ۲۸٬۱۵۵٬۰۰۰ ریال اعلام کرده، ولی اپ ۲۷٬۲۷۲٬۷۲۷ حساب می‌کنه. با اجرای دستیِ همون فرمولِ کد (پایتون،
  تو سندباکس) تایید شد: **۲۷٬۲۷۲٬۷۲۷ دقیقاً همون چیزیه که فرمولِ فعلیِ `QarzFormula` (تو
  `LoanCalculator.kt`) با این ورودی‌ها باید بده** - یعنی اجرای کد درسته، مشکل تو خودِ فرمول/باگِ
  دیگه‌ای نیست. ولی هیچ‌کدوم از سه فرمولِ موجودِ اپ (QARZ فعلی=۲۷٬۲۷۲٬۷۲۷، STANDARD/اقساطِ مساوی
  مرکب=۳۰٬۲۹۵٬۴۷۸، FLAT/سودِ ساده=۳۰٬۰۴۱٬۶۶۷) دقیقاً به ۲۸٬۱۵۵٬۰۰۰ نمی‌رسن - یعنی روشِ محاسبه‌ی این
  بانکِ خاص برای این نوع وام با هیچ‌کدوم از این سه الگو یکی نیست (نرخِ ماهانه‌ی معادل هم اگه محاسبه
  بشه یه عددِ غیرگردی درمیاد، نه یه چیزِ تمیزِ حدسی‌پذیر مثلِ ۴٪/۱۲). **کاربر فعلاً منصرف شد از
  پیگیری بیشتر** («نمی‌دونم ولش کن دستی هست دیگه») - نه فرمول عوض شد نه چیزی «رفع» شد، این یه
  ناهماهنگیِ باز و واقعیه. **اگه دوباره مطرح شد**: اول یه عکس از جدولِ کاملِ اقساطِ همون وام (همه‌ی
  ۱۲۰ ردیف، نه فقط یه عدد) از کاربر بخواه - با دیدنِ الگوی واقعی (آیا همه‌ی ماه‌ها دقیقاً یکسانن یا
  بعضی ماه‌ها فرق دارن، مثلاً سالِ اول با سال‌های بعد) می‌شه فهمید بانک دقیقاً چه فرمولی استفاده
  کرده. **قبل از هر تغییری تو `LoanCalculator.kt` (خصوصاً `QarzFormula`)، حتماً چندتا تستِ رگرسیونِ
  جدید رو `LoanCalculatorTest.kt`/`LoanCalculatorRegressionTest.kt` اضافه کن** - این فرمول قبلاً با
  یه اپِ رقیبِ decompile‌شده تطبیق داده شده بود (رجوع کن به بخشِ «فرمول‌های حساسِ محاسبه» بالا)، پس
  تغییرش می‌تونه مواردِ دیگه‌ای که درست بودن رو خراب کنه - نیاز به چندتا نمونه‌ی واقعیِ دیگه (نه فقط
  همین یکی) قبل از هر تغییرِ واقعی داره.
- **✅ تاییدِ بیلدِ بازطراحیِ ظاهر/انیمیشن گرفته شد (۲ مرداد ۱۴۰۵، چتِ جدید)**: کاربر بیلدِ ۳۲۹
  (کافه‌بازار) رو رو گوشیِ واقعی نصب/تست کرد - «آره خوبه» (شاملِ افکتِ دایره‌ایِ تعویضِ تم که تنها
  موردِ نگران‌کننده بود). دیگه نیازی به پیگیریِ این مورد نیست.
- **⏳ فیچرِ درحالِ‌انتظار - ایمپورتِ وام از PDF**: کاربر خواستِ یه قابلیتِ «ایمپورت وام از PDF» (خروجیِ
  اپ‌هایی مثلِ «دفترچه اقساط» - جدولِ ردیف/تاریخِ سررسید/مبلغِ قسط/مانده/تاریخِ پرداخت). ۵ نمونه PDF
  از ۴ وامِ متفاوت (بانک مهر ×۳، کارت طلایی، وام بانک سپه) بررسی شد - همه متنِ واقعیِ قابلِ‌خوندن
  دارن (نه اسکن)، ساختارِ جدولی‌شون یکسانه. کاربر گفت **صبر کن، چندتا نمونه‌ی دیگه هم می‌فرسته، بعد
  شروع کن** - یعنی هنوز نباید کدنویسیِ این فیچر شروع بشه، منتظرِ نمونه‌های بیشتر بمون.
  **دو تا خواسته‌ی صریحِ اضافه که حتماً موقعِ طراحی/پیاده‌سازیِ نهایی باید توش باشه**:
  1. حینِ ایمپورت، به‌ازای هر ردیف/قسطِ استخراج‌شده از PDF، از کاربر بپرسه «این درسته؟» - اگه تایید
     کرد بره سراغِ ردیفِ بعدی، اگه نه، همون‌جا درجا قابلِ‌ویرایش باشه (نه یه فرمِ جدا/صفحه‌ی جدا).
  2. تشخیصِ خودکارِ اینکه PDF مالِ کدوم فرمت/اپه باید **رو خودِ گوشی** (on-device) انجام بشه، نه با
     هوش‌مصنوعی/سرور - چون این کار فقط استخراجِ متنِ PDF (سبک، کتابخونه‌ی PDF معمولی، نه OCR/تصویر) +
     چک‌کردنِ اینکه متن شاملِ چندتا کلیدواژه/چیدمانِ ستونِ شناخته‌شده هست یا نه (مثلِ «مبلغ قسط»/«تاریخ
     سررسید»/«مانده وام») هست - این کار سبکه، برای گوشی سنگین نیست، نیازی به AI/سرور نداره. اگه بعداً
     چند فرمتِ مختلف (چند اپِ رقیب) پشتیبانی شد، فقط باید امتحان کنه متنِ PDF با کدوم الگوی
     شناخته‌شده جور درمیاد - بازم سبک می‌مونه، فقط چندتا الگوی متنیِ بیشتر برای چک‌کردن.

  **سوالِ کاربر (این نشست) و تصمیمِ نهایی درباره‌ی «هر PDFِ رندومی رو خودش تشخیص بده»**: کاربر پرسید
  چرا نمی‌تونه خودِ برنامه هر PDFی که فرستاد (حتی فرمتِ کاملاً ندیده) رو خودکار بفهمه. جواب/تصمیم:
  **صددرصدی نه** - فهمِ کاملِ هر فرمتِ رندوم نیاز به AIِ واقعی داره که یعنی وابسته‌به‌اینترنت/سرور و
  کندتر می‌شه، نه چیزی که رو خودِ گوشی سبک انجام بشه. راهِ حلِ تصمیم‌گرفته‌شده: تشخیص **کلیدواژه‌محورِ
  منعطف** باشه (دنبالِ کلمه‌های رایجِ فارسی مثلِ «تاریخ سررسید»/«مبلغ قسط»/«مانده» بگرده، نه فاصله‌ی
  دقیقِ کاراکترها) - این باعث می‌شه احتمالاً خیلی از فرمت‌های مشابه (نه فقط عینِ نمونه‌ها) هم شناخته
  بشن، ولی تضمینِ صددرصد نیست. **اگه یه PDF شناخته نشد، برنامه باید صریح بگه «نتونستم بخونمش» تا
  کاربر بفرستتش برای اضافه‌کردنِ الگوی جدید - نه اینکه ساکت/اشتباه چیزی ایمپورت کنه.** کاربر این
  جمع‌بندی رو تایید کرد و خواست تو همین لیست ثبت بشه.
- **✅ رفع شد - کرشِ migrationِ دیتابیس (۱۱→۱۲) رو گوشیِ واقعی**: دقیقاً همون ریسکی که اینجا نگرانش
  بودیم، تو نسخه‌ی ۱.۰.۳۱۵ رخ داد - کاربر گزارش داد اپ «اجرا می‌شه بعد کرش می‌کنه» (با اسکرین‌شاتِ
  دیالوگِ «keeps stopping» رو شیائومی). با اضافه‌کردنِ یه کوئریِ `crash_reports` به
  `check-server-health.yml`، دقیقِ خطا پیدا شد: `IllegalStateException: Migration didn't properly
  handle: loan_rows` - چون `MIGRATION_11_12` (تو `AppDatabase.kt`) صریحاً یه ایندکس رو
  `loan_rows.loanId` می‌ساخت (`CREATE INDEX index_loan_rows_loanId`)، ولی خودِ `LoanRowEntity`
  هیچ `indices` ای تو `@Entity` اعلام نکرده بود - Room موقعِ بازکردنِ دیتابیس این ناهماهنگی رو
  تشخیص می‌ده و همیشه کرش می‌کنه (فقط برای کسی که از نسخه‌ی قبلی آپدیت کنه، نه نصبِ تازه - نصبِ تازه
  چون migration اصلاً اجرا نمی‌شه این مشکل رو نداره). **رفع شد** با اضافه‌کردنِ
  `indices = [Index("loanId")]` به `@Entity` تو `LoanRowEntity.kt` (کامیتِ `5c4c29a`) - نیازی به
  migrationِ جدید/بامپِ ورژن نبود، چون خودِ ساختارِ فیزیکیِ دیتابیس از قبل درست بود، فقط اعلامِ
  کد باهاش یکی نبود. **درسِ مهم برای آینده**: هر وقت تو یه migrationِ دستی یه `CREATE INDEX` نوشتی،
  حتماً همون ایندکس رو تو `indices = [...]`ِ همون Entity هم اعلام کن، وگرنه دقیقاً همین کرش تکرار
  می‌شه. تاییدِ نهاییِ رفعِ کرش رو گوشیِ واقعی هنوز از کاربر نگرفتیم (بیلدِ جدید ساخته و لینکش داده
  شد) - اگه بازم گزارشِ کرش اومد، اول همینجا رو دوباره بخون.
- **✅ OTP فعال شد و تستِ end-to-end هم تاییدِ نهایی گرفت (۴ مرداد ۱۴۰۵ + تاییدِ ۲ مرداد‌ی چتِ جدید)**
  - رجوع کن به بخشِ OTP بالاتر برای جزئیاتِ کامل. پترنِ ملی‌پیامک تایید شد، سرویس با env جدید بالا
  اومد، و کاربر با شماره‌ی واقعیِ خودش امتحان کرد - پیامکِ کدِ تایید واقعاً می‌رسه. این موضوع کاملاً
  بسته‌ست، دیگه نیازی به پیگیری نیست.
- **✅ اصلاحیه‌ی ۲ مرداد ۱۴۰۵ - PR #64 قبلاً merge شده بود، یادداشتِ بالا اشتباه بود**: چک شد
  (`pull_request_read`) - PR #64 از `ccr-07146a93-ufo9vg` به `main` رو ۲۲ تیر merge شده. یعنی
  بخشِ زیادی از کارِ اون برنچ از قبل تو `main` بود؛ فقط ۳۲ کامیتِ *بعدیِ* اون (که رو
  `claude/ios-box-light-intensity-p7ew76` ادامه پیدا کرده بودن - OTP واقعی، بازطراحیِ ظاهر/انیمیشن،
  رفعِ کرشِ migration، و بقیه‌ی کارِ ثبت‌شده‌ی بالا) هنوز تو `main` نبودن. این نشست (چتِ جدید، برنچِ
  اجباریِ هارنس `claude/loan-chat-program-continue-4vpy1r`) اون ۳۲ کامیت رو با `git merge` (بدونِ
  conflict) گرفت و پوش کرد - الان `claude/loan-chat-program-continue-4vpy1r` شاملِ همه‌چیزه (کارِ
  `main` + کارِ `ios-box-light-intensity-p7ew76`). **`main` هنوز خودش این ۳۲ کامیت رو نداره** - اگه
  کاربر خواستِ merge نهایی به `main`، یه PR جدید از همین برنچ به `main` باز کن (فقط با تاییدِ صریحِ
  کاربر، خودسرانه merge نکن).
- **تاییدِ نصبِ `sqlite3` رو VPS + اجرای موفقِ دستورِ `UPDATE users SET subscribed=1...`** برای دو
  حسابِ تست/دائمی (رجوع کن به بخشِ «حسابِ تستِ دائمی» بالا) - هنوز از کاربر تاییدنشده.
- برنچ‌های زائدِ واقعاً بی‌ضرر رو گیت‌هاب (چندتا `claude/*` قدیمی که واقعاً تو `main` merge شدن، **نه**
  `ccr-07146a93-ufo9vg` که اصلاحیه‌ی بالا رو بخون) از سندباکس قابل پاک‌کردن نبودن (پراکسی گیت 403 داد،
  ابزار GitHub هم delete-branch نداره) — بی‌ضررن، فقط شلوغی. ولی طبقِ درسِ اصلاحیه‌ی بالا، قبل از اینکه
  یه برنچ رو «زائد» ببندی، حتماً `git log origin/main..origin/<branch>` رو چک کن، نه فقط حدس بزن. برنچِ
  جدیدی رو داوطلبانه نساز، همیشه از `main` شروع کن (به‌جز وقتی خودِ هارنسِ CCR یه برنچِ مشخص رو اجباری
  کرده، یا کاربر صریحاً خواستِ ادامه‌ی یه برنچِ دیگه رو - رجوع کن به بخشِ «وضعیتِ برنچ/CI» بالا).
- «دیوار» فقط یه اشتباه تایپی/گفتاری کاربر بود (منظورش چیز دیگه‌ای نبود) — این مورد بسته‌ست.
- **ویجت (`ui/widget/LoanWidget.kt`) باید دوباره کار بشه** — کاربر گفته الان خیلی ساده‌ست و وقتی روش
  می‌زنه، اپ باز نمی‌شه/هنگ می‌کنه. کلیک فعلی `clickable(actionStartActivity(Intent(context,
  MainActivity::class.java)))`ه (خط ۸۹) — چون context اینجا Activity نیست، به‌احتمال زیاد نبودِ
  `Intent.FLAG_ACTIVITY_NEW_TASK` علتشه؛ باید تست/دیباگ واقعی بشه، نه فقط حدس. کاربر گفته «چند روزه
  دیگه، وقتی اپ رو منتشر کردم» بهش یادآوری کنم که این رو درست کنیم — پس تا وقتی خودِ کاربر تو یه چتِ
  بعدی نگفته «الان وقتشه»، این کار شروع نشه، فقط رجوع کن به همین یادداشت.
- **رتبه اعتباری (فاز بعد)**: کاربر خواسته یه قابلیتِ آینده که از یه سایتِ بیرونی (مثلاً فینوتک) رتبه‌ی
  اعتباریِ کاربر رو بخونه و تو اپ (چه‌کسی ضامنِ چه‌کسی شده، چقدر وام گرفته، تو کدوم بانک‌ها می‌تونه وام
  بگیره) ثبت کنه. صراحتاً گفته «الان نمی‌خوام، برای نسخه‌ی بعد» — فقط یادآوری/ثبت شده، هنوز هیچ طراحی یا
  کدی براش نیست.
- **تبلیغ تو نسخه‌ی رایگان (فاز بعد)**: کاربر پرسید چجور می‌تونه تبلیغ بذاره؛ پیشنهاد شد **تپسل** یا
  **آدیوری/صباویژن** (سازگار با کافه‌بازار، برخلافِ AdMob که تو ایران مشکلِ پرداخت/دسترسی داره)، فقط
  برای کاربرهای غیرمشترک (`!subscribed`)، مثل بنر ثابت یا تبلیغ بینابینی. کاربر گفته **فعلاً نه** —
  می‌خواد اول اپ رو منتشر کنه و یه مدت نصب جمع کنه، بعداً برمی‌گرده رو این موضوع. تا وقتی خودش نگفته
  «الان وقتشه»، شروعش نکن؛ وقتی خواست، اول باید خودش تو تپسل/آدیوری حساب بسازه و Zone ID بگیره (این
  بخش رو کسی جز خودِ کاربر نمی‌تونه انجام بده).

- **⏳ تصمیمِ درحالِ‌انتظار - سینکِ خودکارِ سررسیدها با اپِ «تقویم من»**: کاربر یه اپِ کاملاً جداگانه
  (ریپوی `ebrahimpersianh/Calendar`، تقویمِ فارسی به‌اسمِ «تقویم من»، جزئیاتِ کامل تو CLAUDE.md همون
  ریپو) داره می‌سازه که فعلاً متوقفه (کاربر اول می‌خواد «وام من» رو تموم/منتشر کنه). خواسته: به‌محضِ
  ثبتِ یه وام/قسط تو «وام من»، **خودکار** (نه با گزینه‌ی «افزودن به تقویمِ گوشی» که از قبل هست) تو
  «تقویم من» هم دیده بشه.
  **گزینه‌ی اول (نشون‌دادنِ تقویمِ سیستمِ گوشی تو «تقویم من») رد شد** - کاربر صریح گفت نمی‌خواد
  این‌جوری، می‌خواد اتومات باشه.
  **تصمیمِ فعلی**: به‌جای ساختنِ یه سرورِ جدا برای «تقویم من»، از **همون سرورِ فعلیِ این ریپو (VameMan)**
  استفاده بشه - کاربر صراحتاً پرسید «همون سرور رو اجرا می‌کنیم فقط حافظه‌اش بس هست؟» و تاییدِ گرفت:
  آره، ظرفیتِ فعلیِ سرور (۲۷٪ دیسک اشغال، دیتابیسِ ۱۳۲ کیلوبایتی) برای این کارِ سبک (چندتا ردیفِ
  رابطه‌ای بیشتر) کاملاً کافیه، نیازی به سرورِ جدا/قوی‌تر نیست.
  **طرحِ کلی (هنوز پیاده‌سازی نشده، فقط تصمیم/طراحیه)**:
  1. رو همین سرور (`server/`) یه جدول/مسیرِ جدید برای «رویدادهای تقویم» اضافه بشه (لینک‌شده به شماره‌ی
     کاربر) - یا مستقیم از رو دیتای سررسیدِ وام‌های موجود بسازیمش، یا یه جدولِ عمومی‌ترِ
     `calendar_events` که «تقویم من» هم بتونه بعداً خودش رویداد اضافه کنه.
  2. «تقویم من» فعلاً **هیچ سرور/حسابِ کاربری‌ای نداره** (رجوع کن به CLAUDE.md خودش) - برای اینکه
     بدونه رویدادهای کدوم کاربر رو بگیره، باید اول یه سیستمِ لاگین/حساب (به همون شماره‌موبایلِ «وام
     من») بهش اضافه بشه - این خودش یه کارِ واقعیه، نه یه خط تنظیمات.
  3. «اتومات» یعنی «تقویم من» هر بار باز می‌شه (یا هر چند وقت یک‌بار) از همین سرور رویدادهای تازه رو
     می‌گیره - این یعنی عملاً بدونِ کارِ دستی/اکسپورت میاد، ولی دقیقاً «آنیِ لحظه‌ای» نیست مگر بعداً
     پوش‌نوتیفیکیشن هم اضافه بشه (فعلاً لازم نیست، همون «هر بار باز کردن» برای یه تقویمِ شخصی کافیه).
  **این طرح هنوز کدنویسی نشده** - چون خودِ پروژه‌ی «تقویم من» فعلاً متوقفه، این کار هم بعدِ برگشتنِ
  کاربر سراغِ اون پروژه شروع می‌شه، نه الان. جزئیاتِ متناظر تو CLAUDE.md ریپوی Calendar هم ثبت شد.

## مایکت (فلیورِ دوم، کنارِ کافه‌بازار) — کد آماده‌ست، فقط یه مقدار مونده
`native-android/app/build.gradle.kts` دو فلیور داره: `cafebazaar` (Poolakey، همونی که تا امروز بود)
و `myket` (کتابخونه‌ی رسمیِ `com.github.myketstore:myket-billing-client`، الگوی کلاسیکِ Android IAB
v3 - `IabHelper`/`Purchase`/`Inventory`). هر فلیور `SubscriptionManager` خودش رو داره
(`app/src/cafebazaar/.../subscription/` و `app/src/myket/.../subscription/`)، بقیه‌ی اپ (`main`
source set) هیچ فرقی بینِ دوتا نمی‌بینه. مانیفستِ کتابخونه‌ی مایکت از placeholder استفاده می‌کنه، نه
مقدارِ ثابت - مقادیرش (`ir.mservices.market` و ...) تو `manifestPlaceholders` همون فلیور تو
build.gradle.kts ست شدن، نه یه فایلِ AndroidManifest جدا.

**`MYKET_IAB_PUBLIC_KEY` دیگه placeholder نیست** - کاربر اپ رو تو پنلِ دولوپرِ مایکت ثبت کرد و کلیدِ
عمومیِ واقعی رو از اونجا گرفت؛ الان تو
`app/src/myket/kotlin/ir/sadteam/loancalc/subscription/SubscriptionManager.kt` مقدارِ واقعیه. همون
۴ شناسه‌ی محصولِ اشتراک (`unlimited_loans_1m/3m/6m/1y`، با همون قیمت‌های کافه‌بازار: ۳۰۰۰۰۰/۸۱۰۰۰۰/
۱۴۴۰۰۰۰/۲۵۲۰۰۰۰ ریال) هم تو پنلِ مایکت به‌عنوانِ محصولِ درون‌برنامه‌ای تعریف شدن.

**صحت‌سنجیِ خریدِ سمتِ سرور برای مایکت هم پیاده شد** (`server/.../Myket.kt`، تابع
`validateMyketPurchase`) - معادلِ `Cafebazaar.kt`، ولی ساده‌تر چون مایکت به‌جای فلوی OAuth یه
توکنِ دسترسیِ ثابت می‌ده (`MYKET_ACCESS_TOKEN` تو `.env`، هنوز رو VPS ست نشده - رجوع کن به
`server/README.md`). کلاینت الان `store` (`BuildConfig.FLAVOR`) رو هم تو
`POST /api/subscription/verify` می‌فرسته (`VerifySubscriptionRequest.store`) تا
`SubscriptionRoutes.kt` بدونه با کدوم API (کافه‌بازار یا مایکت) تاییدِ خرید کنه؛ نسخه‌های قدیمی‌تر
که این فیلد رو ندارن پیش‌فرض `cafebazaar` حساب می‌شن. ⚠️ فرمتِ دقیقِ ریسپانسِ موفقِ API مایکت تو
مستنداتشون نبود، پس فعلاً فقط بر اساسِ کدِ HTTP (۲xx) تصمیم می‌گیریم، نه یه فیلدِ خاص - بعدِ اولین
خریدِ واقعیِ تستی، لاگ سرور رو چک کن ببین این فرض درست بوده.

**اطلاعاتِ Play Protect**: اولین نصبِ APKِ مایکت (کلیدِ امضای تازه) رو گوشیِ واقعی «Blocked by Play
Protect» می‌ده (Unknown Developer - طبیعیه برای هر کلیدِ امضای جدید، نه باگِ کد). راه‌حلِ رسمیِ مایکت:
فرمِ Google «Play Protect Appeals» (لینکش تو myket.ir/kb/pages/blocked-by-play-protect-fa/ هست، فقط
با VPN باز می‌شه) با هشِ SHA256 از APK رو VirusTotal + اطلاعاتِ اپ.

**این فرم تو ۲۷ تیر ۱۴۰۵ (۲۰۲۶-۰۷-۱۸) واقعاً ارسال شد** - برای APKِ فلیورِ مایکت، رانِ CI شماره‌ی ۲۵۹
(versionCode=259)، با هشِ SHA256 برابرِ
`d23a8ef52ee330ad18dca043e42bdda4477923ff7b8507f0a1e83c5311d2d4ad` (صفر تشخیصِ مخرب رو VirusTotal).
گوگل گفته تا یه هفته (یعنی حدودِ ۳ مردادِ ۱۴۰۵، ۲۰۲۶-۰۷-۲۵) طول می‌کشه و هیچ پیامی درباره‌ی نتیجه
نمی‌ده - باید دستی دوباره نصب و تست بشه. **تا اون‌موقع نباید applicationId یا کلیدِ امضای release
عوض بشه**، وگرنه این بررسی از اول باید انجام بشه.

CI الان هر ۴ تا APK (کافه‌بازار دیباگ/ریلیز + مایکت دیباگ/ریلیز) رو می‌سازه و به‌عنوانِ آرتیفکتِ جدا
آپلود می‌کنه (`loan-calculator-native-myket-debug-apk` و `-myket-release-apk`).

## آپدیتِ خودکار
سرور یه جدولِ تک‌ردیفه‌ی `app_version` داره (`server/.../routes/AppVersionRoutes.kt`،
`GET /api/app-version`) که آخرین versionCode + لینکِ هر استور رو نگه می‌داره؛ اپ هر بار باز می‌شه
چک می‌کنه و اگه قدیمی بود یه بنر نشون می‌ده. **این جدول دستیه، خودکار آپدیت نمی‌شه** - بعدِ هر
انتشارِ واقعیِ نسخه‌ی جدید رو کافه‌بازار/مایکت، باید رو VPS این دستور زده بشه (دقیقش تو کامنتِ بالای
AppVersionRoutes.kt هم هست):
```bash
sqlite3 ~/VameMan/data.sqlite \
  "UPDATE app_version SET latest_version_code = <شماره‌نسخه>,
   cafebazaar_url = 'https://cafebazaar.ir/app/ir.sadteam.loancalc',
   myket_url = 'https://myket.ir/app/ir.sadteam.loancalc' WHERE id = 1;"
```


## تاریخچه‌ی فشرده‌ی نشست‌ها (فقط «چی شد»؛ درس‌های فنی‌شون بالا تو بخشِ «درس‌ها و قواعد» هست)
گزارشِ کاملِ روایتیِ این نشست‌ها قبلاً اینجا بود و برای کم‌کردنِ مصرفِ توکن فشرده شد (تاریخچه‌ی
کاملش تو گیت هست). **هیچ قاعده/تصمیم/درسِ فنی‌ای حذف نشد** - همه به بخشِ «درس‌ها و قواعد» منتقل شدن.

- **۲۷ تیر ۱۴۰۵** - versionCode/versionName خودکار از `github.run_number`. مایکت کامل شد (کلیدِ
  IAB واقعی، ۴ محصولِ اشتراک، صحت‌سنجیِ سمتِ سرور با `Myket.kt`، فیلدِ `store` تو درخواستِ تایید).
  رفعِ باگِ مخفی‌بودنِ کارتِ خرید تو دوره‌ی آزمایشی و هنگِ ظاهریِ صفحه‌ی ورود. پکیجِ انیمیشن
  (Aurora، ProgressRing، CoinCelebration، GoldSheen، count-up). فرمِ Play Protect Appeals ارسال شد.
- **۲۹ تیر ۱۴۰۵** (نشستِ بزرگ) - رفعِ باگِ سررسیدِ ماهانه (addMonths)، رفعِ پرشِ رقمِ فیلدهای مبلغ
  (۷ جا)، بازطراحیِ لیستِ اقساط، حسابِ تستِ دائمی + رفعِ بجِ گمراه‌کننده‌ی «آزمایشی»، ذخیره‌ی نوعِ
  اشتراک (`subscription_tier`)، اسپلشِ آتیشی، رفعِ سیوِ تکراریِ وام، ویرایشِ مشخصاتِ وام، تورِ
  اولین ورود (`AppTourOverlay`)، شیشه‌ای‌شدنِ باکس‌ها/دکمه‌ها، بازطراحیِ ورود/OTP + SMS Retriever،
  رفعِ متنِ نامرئیِ دارک‌مود، فول‌اسکرین‌شدنِ زیرصفحه‌های تنظیمات.
- **۳۰ تیر ۱۴۰۵** - کشفِ اشتباهِ این فایل درباره‌ی برنچ (درسش بالا ثبت شده). Liquid Glass رو
  `AppCard`. رفعِ باگ‌های bidi صفحه‌ی ورود. تنظیمِ Aurora. کامپوننتِ مشترکِ `Ltr.kt`. آماده‌سازیِ
  عکس‌های استور (اسکریپت‌هاش تو ریپو نیست).
- **۳۱ تیر ۱۴۰۵** - دیپلویِ خودکارِ سرور (CI/CD)، تستِ اسکرین‌شاتِ Paparazzi، و بزرگ‌ترین تغییرِ
  لایه‌ی داده: جداکردنِ ردیف‌های قسط به جدولِ `loan_rows` (migration ۱۱→۱۲).
- **۲ مرداد ۱۴۰۵** - انتشارِ APK به GitHub Release، رفعِ کرشِ ۱.۰.۳۱۵ (باگِ ایندکسِ migration)،
  تغییرِ معنیِ «تاریخ دریافت وام»، تستِ اینسترومنتدِ migration + اسموک‌تستِ اپ رو امولاتورِ واقعی.
- **۳ مرداد ۱۴۰۵** - بررسیِ ردِ مایکت/کافه‌بازار: کارتِ خریدِ مخفی (طراحیِ درست)، env نداشتنِ
  کافه‌بازار رو سرور (رفع شد با `set-cafebazaar-env.yml`)، و نوعِ اشتباهِ محصولِ مایکت (`SUBS`→`INAPP`).
- **۸ مرداد ۱۴۰۵** - بازطراحیِ ظاهر/انیمیشنِ کلِ اپ (۹+۵ مورد): افکتِ دایره‌ایِ تعویضِ تم،
  `EmptyState`، `Motion.kt`، `StaggerIn`، `animateItem`، ترنزیشنِ هیرو، pull-to-refresh، بازخوردِ
  لمس، انیمیشنِ نمودارها، کشیدن برای حذف، تیکِ تاییدِ ذخیره، جمع‌شدنِ نوارِ پایین، `PrivacyCrossfade`.
- **۴ مرداد ۱۴۰۵** - تصمیمِ افزودنِ ماژولِ حسابداریِ شخصی (به‌جای اپِ جدا)، پیاده‌سازیِ نسخه‌ی اول
  (دسته‌بندی + بودجه + گزارش + جستجو + پرداختِ تکراری، migration ۱۲→۱۳)، و یه رفت‌وبرگشتِ نامِ اپ
  که نهایتاً رو «حسابدار من» تثبیت شد.
- **۵ مرداد ۱۴۰۵** (بزرگ‌ترین بازسازیِ ساختاری، ۸۵ کامیت) - نوارِ پایین به ۵ تبِ فعلی رسید، طلب و
  بدهی، یادداشتِ مستقل، سینکِ رو-به-جلوی وام/چک↔حسابداری، تشخیصِ بانک از شماره‌کارت، مدیریتِ
  دسته‌بندی‌ها، خروجیِ PDF/اکسل، خوندنِ خودکارِ پیامکِ بانکی، تمِ مشکی+سبز، لوگوی جدید.
- **۶ مرداد ۱۴۰۵** - سینکِ گذشته‌نگر ساخته شد (migration ۱۶→۱۷) و بعد **کاملاً حذف شد**
  (migration ۱۷→۱۸) - دلیلش بالا تو «تصمیماتِ محصولی» هست. بعدش ۸ کارِ ذخیره‌شده یه‌جا پیاده شد:
  یکدست‌کردنِ فیلدهای مبلغ، تاییدِ حذفِ تراکنش، لوگو/رنگِ سبزِ جدید، و رفعِ باگِ بزرگِ `addDays`
  با عددِ منفی + بازطراحیِ نوارِ تاریخ.
## تغییراتِ نشستِ ۱۶-۱۷ مرداد ۱۴۰۵ (۲۰۲۶-۰۸-۰۶ تا ۰۸-۰۷) - بازطراحیِ ظاهریِ بزرگ: اسپلش، لوگو، نوارِ ناوبری، تبِ بودجه/دارایی، یک‌دست‌سازیِ کارت‌ها
همه رو برنچِ **`claude/bank-9fiumm`** (برنچِ اجباریِ هارنسِ CCR این نشست). ۱۶ کامیت، از `a12a847`
تا `0c0de5b`. **این برنچ هنوز تو `main` مرج نشده** - هیچ PRای هم براش باز نشده (کاربر درخواست نداد).

### ⚠️ وضعیتِ برنچ - اولین چیزی که تو چتِ بعدی باید بدونی
- برنچِ `claude/bank-9fiumm` سرِ کارِ فعلیه. `main` عقبه.
- برنچِ قدیمی‌ترِ `claude/loan-chat-program-continue-4vpy1r` (با PR بازِ #66) هم هنوز هست - این
  برنچِ جدید از رو همون ساخته شده، پس شاملِ همه‌ی کارِ اونه.
- **مرج به `main` رو خودسرانه انجام نده** - فقط با تاییدِ صریحِ کاربر.

### درسِ خیلی مهمِ این نشست: چه‌وقت باید دست از «بازسازیِ دستی» برداشت
کاربر یه طرحِ اسپلشِ رندرشده (استوری‌بردِ ۶مرحله‌ای، تصویرِ سه‌بعدیِ شیشه‌ای با حلقه‌ی نوریِ دورگرد،
بارشِ ذرات، بازتابِ زمین) داد و خواست تو اپ پیاده بشه. **سه بارِ پشتِ‌سرِ هم تلاش شد این با
شکل‌های خودِ Compose بازسازی بشه و هر سه بار کاربر با اسکرین‌شات گفت شبیه نشده**:
1. حلقه‌ی دوناتِ `Canvas` + هاله‌های رادیالی
2. پشته‌ی کارتِ دستی‌کشیده (سه `Box` چرخونده) + آیکونِ `Icons.Filled.TrendingUp`
3. یه بریدهٔ فقط-آیکون از تصویرِ مرجع (پس‌زمینه‌ی سیاهش کد شد به شفاف) رو یه هاله

**راه‌حلِ نهایی که جواب داد**: کلِ فریمِ نهاییِ خودِ طرح (شاملِ نوشته‌ی «حسابدار من» و شعارِ زیرش
که تو تصویر پخته شده) عیناً به‌عنوانِ یه تصویرِ تمام‌صفحه نمایش داده شد. یعنی هیچ بخشی از اسپلش
دیگه از نو کشیده نمی‌شه.

**قاعده‌ی کلی برای دفعه‌ی بعد**: وقتی طرحِ مرجعِ کاربر یه رندرِ سه‌بعدی/نوریِ پیچیده‌ست (نه یه
چیدمانِ تخت از شکل‌های ساده)، به‌جای تلاش برای بازسازیِ برنامه‌نویسی‌شده‌ش، **از همون اول** بپرس
آیا می‌شه خودِ تصویر رو مستقیم استفاده کرد. سه دور رفت‌وبرگشت و کلی توکن صرفه‌جویی می‌شه.

### اسپلشِ جدید (`ui/onboarding/SplashIntroScreen.kt` - کاملاً بازنویسی شد)
- `assets/splash/splash.jpg` (کراپِ پنلِ چپِ تصویرِ استوری‌بردِ کاربر، مختصاتِ کراپ: `(23, 23,
  626, 1503)` از تصویرِ ۱۰۲۴×۱۵۳۶) با `ContentScale.Crop` تمام‌صفحه نشون داده می‌شه.
- نسبتِ خودِ تصویر ۰.۴۱ ئه و گوشی‌های رایج ۰.۴۲–۰.۴۶، پس فقط چند ده پیکسل از بالا/پایین (که هر دو
  فضای خالیِ مشکی‌ان) بریده می‌شه - نه خودِ آیکون یا متن.
- **فرمت JPEG عمدیه نه PNG**: شفافیت لازم نیست و همین تصویر به‌صورتِ PNG ~۷۰۰KB می‌شد در برابرِ
  ~۹۵KB (با `quality=92, optimize=True`). رو حجمِ کلِ اپ اثرِ محسوس داشت.
- انیمیشن: یه ظهورِ نرم (اسکیل ۰.۹۴→۱ + محو) + یه «نفسِ» خیلی آروم (`rememberInfiniteTransition`،
  اسکیل تا ۱.۰۲۵) تا تصویرِ ثابت مُرده به‌نظر نیاد. بعدِ ~۲.۱ ثانیه `onDone`.
- **متنِ «حسابدار من» و شعارِ زیرش دیگه `Text` نیستن - بخشی از خودِ عکسن.** یعنی با فونت/تمِ
  سیستم عوض نمی‌شن. این پذیرفته‌شده‌ست، نه یه قلمِ جامونده.
- **زمینه‌ی اسپلش دیگه با تمِ روشن/تیره عوض نمی‌شه** (همیشه مشکیه، چون خودِ تصویر مشکیه). به همین
  خاطر پارامترِ `isDarkTheme` از `SplashIntroScreen` و پلامبینگِ `ThemeViewModel` از `AppRoot`
  (که فقط برای همین بود) حذف شدن.
- سه نقطه‌ی چشمک‌زنِ قبلی (`BlinkingDots`) حذف شد چون تو طرحِ مرجع نبود. «Powered By SadTeam»
  به‌عنوانِ تنها متنِ واقعیِ روی صفحه موند (تو فضای خالیِ پایین).

### لوگوی جدیدِ اپ + رفعِ حاشیه‌ی سیاه
- کاربر یه تصویرِ لوگوی شیشه‌ای (فلشِ صعودی + میله‌های نمودارِ سبزِ نئونی رو زمینه‌ی مشکی) داد.
  هر ۱۰ فایلِ لانچر (۵ چگالی × `ic_launcher`/`ic_launcher_round`) با Python/PIL ازش ساخته شد.
- **باگِ گزارش‌شده و رفع‌شده**: نسخه‌ی اول حاشیه‌ی سیاهِ اضافه داشت (کاربر: «کمی حاشیه مشکی داره»)
  چون عکسِ منبع ~۱۰٪ پدینگِ مشکی دورِ خودِ آیکون داشت. با اسکنِ پیکسل باکسِ سفتِ محتوای واقعی
  پیدا شد (`(134, 124, 1121, 1135)` از تصویرِ ۱۲۵۴×۱۲۵۴) و قبل از resize به همون کراپ شد.
  **درس: هر وقت از یه تصویرِ رندرشده آیکون می‌سازی، اول با اسکنِ پیکسل باکسِ محتوا رو پیدا کن،
  به چشم اعتماد نکن.**
- نسخه‌ی `_round` = همون کراپ + ماسکِ آلفای دایره‌ای (`ImageDraw.ellipse`).

### نوارِ ناوبریِ پایین - شناور و شیشه‌ای (`MainActivity.kt`)
قبلاً یه `Surface` تخت‌رنگِ چسبیده به لبه‌های صفحه بود. الان همون رسپیِ دقیقِ شیشه‌ایِ `AppCard`
(`shadow → clip → background(base) → background(gradient) → border`) با فاصله از لبه‌ها
(`padding(horizontal = 16.dp, vertical = 10.dp)`) و گوشه‌ی ۲۸dp - پس `AuroraBackground` از پشتش
دیده می‌شه.

**⚠️ یه دکمه‌ی «+» برجسته‌ی وسطِ نوار اضافه شد و بعد به‌خواستِ کاربر حذف شد** («اون به اضافه وسط
رو بردار»). اگه دوباره مطرح شد، یادت باشه یه‌بار امتحان شده و رد شده.

### تبِ «بودجه» - فقط دسته‌های بودجه‌دار (`ui/accounting/AccountingScreen.kt`)
کاربر با اسکرین‌شاتِ یه اپِ رفرنس گفت «الکی صفحه رو پر نکن».
- **باگِ ریشه‌ای**: `items(expenseCats)` همه‌ی دسته‌های هزینه رو رندر می‌کرد (حتی اونایی که سقف
  نداشتن، با متنِ «سقفی تعیین نشده»). به `items(budgetedCats)` تغییر کرد.
- چون با این تغییر دیگه راهی برای *افزودنِ* بودجه به یه دسته‌ی بی‌بودجه نبود (قبلاً با تپ رو
  همون ردیف انجام می‌شد)، یه **FAB «+»** اضافه شد که یه دیالوگِ دومرحله‌ای باز می‌کنه (انتخابِ
  دسته → واردکردنِ سقف). FAB فقط وقتی نشون داده می‌شه که حداقل یه دسته‌ی بی‌بودجه مونده باشه.
- `EmptyState` برای وقتی هیچ بودجه‌ای تعیین نشده.
- **باگِ جانبیِ رفع‌شده**: فرمِ ویرایشِ سقف، فیلدِ مبلغ رو تو یه `Row` کنارِ دکمه‌ی «ذخیره» با
  `weight(1f)` می‌ذاشت و عملاً جایی برای تایپ نمی‌موند (کاربر با اسکرین‌شات: «جا نداره برای وارد
  کردن سقف»). فیلد تمام‌عرض شد، دکمه‌ها رفتن تو یه ردیفِ جدا زیرش. یه دکمه‌ی «حذفِ سقف» هم اضافه شد.

### تبِ «دارایی» - بازطراحی طبقِ عکسِ مرجع
- کارتِ «گزارشِ ماه» از بالای صفحه رفت پایین‌تر؛ جاش یه **کارتِ بزرگِ «مانده‌ی کل»** (هم‌الگو با
  کارتِ تبِ خانه) با یه **آیکونِ جستجوی قابل‌تپ** نشست. فیلدِ جستجو حالا پیش‌فرض مخفیه و با تپِ
  همون آیکون باز/بسته می‌شه (قبلاً همیشه بازِ ثابت بود).
- ردیفِ چیپیِ سادهٔ حساب‌ها → **ردیفِ افقیِ اسکرول‌شونده از کارت**: هر کارت `BankBadge` (لوگوی
  بانک)، اسمِ حساب، و مانده‌ی رنگیِ خودِ همون حساب (قرمز اگه منفی). اولین کارت «+ حساب جدید»ه.
- کارتِ «مانده‌ی کل»ِ تبِ **خانه** هم قابل‌تپ شد و می‌بره تبِ «دارایی» (قبلاً اصلاً کلیک‌پذیر نبود).

### 🔑 یک‌دست‌سازیِ همه‌ی کارت‌ها - `AppCard.accentGradient` (مهم برای هر کارِ ظاهریِ بعدی)
کاربر با اسکرین‌شات گفت «یک دست بکن همه باکس ها رو». **ریشه‌ی مشکل**: چند کارت (موجودیِ خانه/
دارایی، کارت‌های حساب) دستی با `clip/background/border` ساخته شده بودن، نه با `AppCard` - پس
گوشه‌هاشون فرق داشت (۲۴dp/۱۸dp در برابرِ ۲۲dp)، سایه و هایلایتِ شیشه‌ای نداشتن، و دو کارتِ کنارِ
هم حتی پس‌زمینه‌ی متفاوت داشتن (`AppSurface2` در برابرِ `AppGlassBase`).

**رفع (الگویی که از این به بعد باید رعایت بشه)**:
- `AppCard` یه پارامترِ اختیاریِ جدیدِ **`accentGradient: Brush? = null`** گرفت - یه لایه‌ی رنگیِ
  نیمه‌شفاف که *روی* همون پایه‌ی شیشه‌ای و *زیرِ* هایلایت می‌شینه. یعنی کارت‌های «قهرمانِ» صفحه
  هم برجسته می‌مونن هم دقیقاً همون گوشه/سایه/حاشیه/هایلایتِ بقیه رو دارن.
- همه‌ی اون کارت‌های دستی به `AppCard` منتقل شدن.
- **قاعده: هیچ کارتِ جدیدی رو دستی با `clip/background/border` نساز. همیشه `AppCard` - اگه رنگِ
  برجسته لازم داری، `accentGradient` بده.**
- کارت‌های حسابِ ردیفِ افقی ارتفاعِ ثابتِ یکسان (`116.dp`) گرفتن (قبلاً بلندیِ متفاوت داشتن، چون
  محتواشون فرق داشت) و اسم/مبلغشون `maxLines = 1` + `TextOverflow.Ellipsis` شد.
  **نکته‌ی فنی**: `AppCard` داخلاً `fillMaxWidth()` می‌زنه، ولی چون `Modifier.width(158.dp)` قبلش
  میاد و constraint رو دقیق می‌کنه، عرضِ ثابت درست کار می‌کنه.
- **حاشیه‌ی `GradientButton` از طلایی (`AppAccent`) به `AppGlassBorder` عوض شد** - تو ستونی از
  کارت‌های سبز، تنها عنصرِ ناهماهنگ همون بود. طلایی همچنان نشانه‌ی پرمیوم/اشتراک می‌مونه (بج‌های
  اشتراک، `GoldSheenBox`) و نوارِ شیمرِ خیلی محوِ همین دکمه هم هنوز طلاییه.

### کامپوننتِ جدیدِ `ui/components/SegmentedToggle.kt`
کپسولِ لغزنده (پس‌زمینه‌ی رنگیِ متحرک با فنر زیرِ گزینه‌ی انتخاب‌شده) - جایگزینِ جفت‌`AppChip`های
کنارِ هم. اعمال شده رو تاگلِ «هزینه/درآمد» (`AddTransactionForm`، `AddRecurringForm`) و «دخل/خرج»
(`ReportSection`). از `Modifier.offset` استفاده می‌کنه (نه `absoluteOffset`) پس RTL-آگاهه.

### مواردی که اضافه و بعد به‌خواستِ کاربر برگردونده شدن (تکرارشون نکن)
1. **نمودارِ روندِ ۶ماهه + شبکه‌ی «دسترسیِ سریع» تو تبِ خانه** (کامیتِ `e535622`) - کاربر بعدِ
   دیدنش گفت «اون تب خانه رو مثل قبلا بکن». هر دو حذف شدن (کامیتِ `04b82a4`).
2. **دکمه‌ی «+» برجسته‌ی وسطِ نوارِ ناوبری** - بالاتر توضیح داده شد.

### باگِ کامپایلی که پیش اومد (درس برای دفعه‌ی بعد)
`AppPrimary`/`AppDanger` و بقیه‌ی توکن‌های رنگ **پراپرتیِ `@Composable`** ان (`@Composable get()`).
صداکردنشون داخلِ بلوکِ رسمِ `Canvas` (که یه `DrawScope` معمولیه، نه `@Composable`) خطای
`@Composable invocations can only happen from the context of a @Composable function` می‌ده.
**همیشه رنگ رو قبل از `Canvas` تو یه `val` محلی بخون و همون `val` رو داخلِ رسم استفاده کن.**

### وضعیتِ تست/تایید
- همه‌ی کامیت‌ها CI (`build-native-android.yml`) رو **سبز** رد کردن.
- کاربر اسپلش، لوگو، تبِ بودجه و تبِ دارایی رو رو گوشیِ واقعی دیده و بازخورد داده (که تو همین
  بخش اعمال شده).
- **آخرین کامیت (`0c0de5b` - یک‌دست‌سازیِ کارت‌ها) هنوز رو گوشیِ واقعی تایید نشده.**
