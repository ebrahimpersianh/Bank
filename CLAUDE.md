## پروژه
اپلیکیشن اندروید محاسبه‌گر اقساط وام فارسی (اسم برند/نمایشی: «وام من») برای کافه‌بازار، تحت نام تیم «Sad Team».

**این ریپو الان یه مجموعه‌ی واحد و ۱۰۰٪ Kotlin‌ـه، فقط رو یه برنچ (`main`).** اپ وب قدیمی (Capacitor/HTML+JS،
پوشه‌ی `www/`) که قبلاً حتی بعد از غیرقابل‌بیلد شدن هم تو ریپو نگه داشته می‌شد، به‌خواستِ صریح کاربر
**کاملاً حذف شد** (نه فقط از آمار زبان گیت‌هاب مخفی — واقعاً از ریپو پاک شد؛ اگه لازم شد تو تاریخچه‌ی
گیت هست). همچنین چندتا برنچ فرعی/تجربی (`claude/app-ux-round1` و چندتای دیگه) که یه مدت موازی با
`main` پیش می‌رفتن و باعث سردرگمی شده بودن (مثلاً یه‌بار سرور و اپ اشتباهی از رو یه برنچ قدیمی‌تر
دیپلوی/تست شدن)، همه merge/consolidate شدن تو `main`. **از این به بعد `main` تنها منبع حقیقته** — تو
هیچ چت دیگه‌ای دنبال نسخه‌ی موازی نگرد، فقط همینو بگیر/آپدیت کن.

دو تا بخش اصلی:
- **کلاینت**: اپ اندروید بومی Kotlin + Jetpack Compose تو `native-android/` — تنها کلاینتِ موجود و تنها
  مسیر ساختِ نسخه‌ی جدید اپ. جزئیات کامل معماری/فیچرها/تاریخچه‌ی توسعه تو `native-android/README.md`.
- **بک‌اند**: Kotlin + Ktor + SQLite (JDBC/sqlite-jdbc) تو `server/` — برای ورود اجباری با موبایل (OTP)،
  همگام‌سازی ابری «وام‌های من»/چک‌ها، و اشتراک کافه‌بازار. جزئیات کامل تو `server/README.md`.

این پروژه تو سندباکس فعلی **بیلد نمی‌شه** (`dl.google.com` مسدوده)؛ تنها راه تأیید واقعیِ `native-android/`،
ران‌کردن `.github/workflows/build-native-android.yml` (با `workflow_dispatch` یا push به `main`) و
چک‌کردن نتیجه‌ست، نه حدس‌زدن از رو خوندن کد. `server/` برعکس، چون فقط به Maven Central نیاز داره (نه
`dl.google.com`)، همین‌جا تو سندباکس هم `gradle compileKotlin`/`test`/`fatJar` واقعاً اجرا و تایید می‌شه.

## سرور: دیپلوی واقعی رو VPS انجام شد
سرور از Node.js+Express به Kotlin+Ktor بازنویسی شده بود (کد Node.js قدیمی کاملاً حذف شده، نه کنارش نگه
داشته شده) و حالا **رو VPS واقعی هم دیپلوی و روشنه**:
- VPS آروان‌کلاود (اوبونتو، IP `37.152.187.44`)، دامنه‌ش `server-8a887fd3-2459-4e6d-ace8-884dc36ca3db.ir-thr-fr1.arvancompute.ir`
  (HTTPS با certbot/Let's Encrypt، خودکار تمدید می‌شه). nginx رو پورت ۸۰/۴۴۳ به `localhost:3000` proxy می‌کنه
  (`/etc/nginx/sites-available/loan-api`، با `server_names_hash_bucket_size 128;` تو `nginx.conf`).
- جار runnable (`loan-calculator-server-1.0.0-all.jar`، از تسک دستی `fatJar` تو `build.gradle.kts`) تو
  `~/loan-server/` رو VPS با pm2 اجرا می‌شه: `pm2 start java --name loan-calc-api --interpreter none --cwd ~/loan-server -- -jar loan-calculator-server-1.0.0-all.jar` + `pm2 save`.
  `.env` (کنار jar، همون `~/loan-server/`) فقط `JWT_SECRET` و `PORT=3000` داره فعلاً — پیامک OTP واقعی
  هنوز فعال نیست، رجوع کن به بخش OTP پایین.
- ساختار کد تو `server/src/main/kotlin/ir/sadteam/loancalc/server/`: `Application.kt` (main + ماژول Ktor)،
  `Db.kt` (اسکیمای SQLite + مایگریشن، با `withConnection` یه کانکشن جدا به‌ازای هر کوئری)، `Auth.kt` (JWT)،
  `Sms.kt` (ارسال OTP)، `SubscriptionStatus.kt`، `Cafebazaar.kt`، و
  `routes/{AuthRoutes,LoansRoutes,SubscriptionRoutes,CrashRoutes,BackupRoutes,CreditRatesRoutes}.kt`.
- یه workflow (`.github/workflows/build-server.yml`) رو هر push/PR که `server/**` رو عوض کنه `gradle test` +
  `gradle fatJar` رو اجرا و jar خروجی رو آپلود می‌کنه.

### OTP (پیامک کد تایید) — هنوز فعال نیست، اینه که چیکار لازمه
سرویس فعلی تو `Sms.kt` **ملی‌پیامک** (melipayamak.com) هست، وب‌سرویس SOAP قدیمی
(`api.payamak-panel.com`، متد `SendByBaseNumber`، ارسال با خط اشتراکی/پایه). تا وقتی این سه متغیر تو
`.env` رو VPS (`~/loan-server/.env`) ست نشن، کد تایید فقط تو لاگ سرور (`pm2 logs loan-calc-api`) چاپ
می‌شه و پیامک واقعی نمی‌ره:
1. تو پنل ملی‌پیامک (پیامک > پترن‌ها) یه پترن متنی برای کد تایید بساز و منتظر تاییدش بمون (چیزی مثل
   «کد تایید شما: %code%») — بدون پترن تاییدشده، ارسال همیشه شکست می‌خوره. بعد شماره‌ی همون پترن
   (`bodyId`) رو تو `.env` بذار: `MELIPAYAMAK_BODY_ID=...`
   **مهم برای پرکردنِ خودکارِ کد تو اپ (SMS Retriever، رجوع کن به بخشِ «ورود/OTP: بازطراحی...»
   پایین‌تر)**: متنِ پترن باید یه خطِ جدیدِ آخر داشته باشه که دقیقاً هشِ ۱۱کاراکتریِ اپ رو شامل بشه
   (مثلاً «کد تایید شما: %code%\n<هش>») - وگرنه پرکردنِ خودکار هیچ‌وقت کار نمی‌کنه (نه خطا، فقط
   بی‌صدا هیچ‌وقت پیامک رو نمی‌گیره؛ تایپِ دستیِ کد همیشه به‌عنوانِ fallback کار می‌کنه). این هش رو
   از لاگِ خودِ اپِ نصب‌شده (`SmsRetrieverHash.kt`) بگیر - چون به بسته‌نام + کلیدِ امضا بستگی داره،
   برای بیلدِ دیباگ و بیلدِ ریلیزِ واقعی دو هشِ متفاوته.
2. `MELIPAYAMAK_USERNAME`/`MELIPAYAMAK_PASSWORD` رو از پنل ملی‌پیامک بگیر (اگه حساب جدید بهت Api Key
   داده به‌جای رمز عبور، همون رو تو `MELIPAYAMAK_PASSWORD` بذار).
3. رو VPS: `nano ~/loan-server/.env` (یا `cat >> ~/loan-server/.env`)، سه خط بالا رو اضافه کن، ذخیره کن.
4. `pm2 restart loan-calc-api` بزن تا `.env` جدید خونده بشه.
5. تست کن: یه درخواست OTP واقعی از اپ بزن، `pm2 logs loan-calc-api --lines 20 --nostream` رو چک کن که
   دیگه پیام `[SMS-DEV]` نیاد و پیامک واقعی به گوشی برسه.

اگه خواستی سرویس پیامکی رو عوض کنی، فقط کافیه `server/src/main/kotlin/ir/sadteam/loancalc/server/Sms.kt`
(تابع `sendOtpSms`) رو ویرایش کنی — بقیه‌ی کد (`routes/AuthRoutes.kt`) کاری نداره پیامک از کجا می‌ره.

**بروزرسانیِ ۲۷ تیر ۱۴۰۵ (۲۰۲۶-۰۷-۱۸)**: ملی‌پیامک برای تاییدِ پترن ظاهراً می‌خواد اپ رو یکی از استورها (کافه‌بازار
یا مایکت) آپلود/در دسترس باشه (اثباتِ واقعی‌بودنِ اپ) - کاربر برای همین دلیل الان اپ رو **هم رو
کافه‌بازار هم رو مایکت ارسال کرده و هر دو تو صفِ بررسی‌ان** (رجوع کن به بخشِ «تغییراتِ این نشست» پایین).
بعدِ اینکه حداقل یکی تایید شد، دوباره برو سراغِ تاییدِ پترنِ ملی‌پیامک.

**بروزرسانیِ ۳۱ تیر ۱۴۰۵ (۲۰۲۶-۰۷-۲۲)**: کاربر نسخه‌ی جدید (با همه‌ی تغییراتِ این چند نشست - فونتِ
Vazirmatn، Liquid Glass، بازطراحیِ ورود/OTP، رفعِ باگ‌های bidi/اسکرول/نوارِ وضعیت) رو دوباره هم رو
**کافه‌بازار هم مایکت فرستاده برای بررسی** (شاملِ توضیحِ دسترسی‌های «خطرناک»ِ READ_CALENDAR/
WRITE_CALENDAR برای بازار، و متنِ «چیزهای تازه»/حسابِ تستِ `09121111111`/کدِ `11111` برای هر دو
استور). **خواسته‌ی صریحِ کاربر: به‌محضِ تاییدِ حداقل یکی از این دو، بلافاصله برو سراغِ فعال‌سازیِ
واقعیِ پیامکِ OTP** (رجوع کن به مراحلِ کاملِ بالا - پترنِ ملی‌پیامک + یوزرنیم/پسورد) - این آخرین
مانعِ باقی‌مونده برای فعال‌شدنِ واقعیِ ورود با موبایله.

## اپ بومی (native-android/)
تنها کلاینت موجود، معماری چندماژوله: `:app` (Compose UI)، `:data` (Room، DataStore، Retrofit مطابق دقیق
قرارداد API سرور)، Hilt برای DI، Navigation-Compose. چهار تب اصلی (وام بانکی، محاسبه‌گر، سود سپرده،
وام‌های من) + ماژول‌های «امور چک» و «حساب بانکی» زیر تب وام بانکی. جزئیات کامل فیچرها، دورهای پالایش،
و تاریخچه‌ی تصمیم‌ها تو `native-android/README.md` نگه داشته می‌شه — قبل از ادامه‌ی کار حتماً اونجا رو
هم بخون، این فایل فقط خلاصه‌ی سطح‌بالا و نکات تازه‌ست.

قفل امنیتی PIN+اثر انگشت پیاده‌ست (`ui/security/LockScreen.kt`, `AppLockViewModel.kt`، با
`BiometricPrompt` — به همین خاطر `MainActivity` از `ComponentActivity` به `FragmentActivity` عوض شده).
این برخلافِ یه یادداشتِ خیلی قدیمی‌تر تو همین فایله («عمداً پیاده نشده») که با یه درخواستِ صریحِ بعدیِ
کاربر منسوخ شده بود؛ اگه جایی تو تاریخچه/کد کامنتی برخلاف این دیدی، همین خط رو ملاک بگیر، نه کامنتِ قدیمی.
ci-debug.keystore یه کلید امضای دیباگ ثابته (نه
یه راز/سکرت) که مستقیم تو `native-android/app/build.gradle.kts` پین شده تا نصب هر بیلد جدید رو بیلد
قبلی خطای «conflicts with an existing package» ندید — این فایل رو پاک نکن. applicationId نهایی
`ir.sadteam.loancalc`.

### تغییرات امروز (تمیزکاری تم طلایی + رفع باگ حالت خصوصی + اسپلش)
- **تم طلایی از رو فونت به پس‌زمینه/حاشیه‌ی باکس منتقل شد**: کامپوننت `AppCard` (لیبل بالای تقریباً
  همه‌ی باکس‌های اپ) قبلاً لیبل رو طلایی رنگ می‌کرد؛ برگشت به `AppMuted`. ۱۳ تا `Text`/`Button` دیگه هم
  (کپشن «X تومان»، نتیجه‌ی نرخ سود، CTA اشتراک و...) که فونتشون طلایی بود، به رنگ مناسب (خاکستری/رنگ
  اصلی اپ/متن معمولی) عوض شدن؛ دوتا مورد (شمارش‌معکوس دوره‌ی آزمایشی، لینک خرید اشتراک تو
  `BenefitsScreen`) به‌جاش یه بجِ پس‌زمینه/حاشیه‌ی طلایی گرفتن. رنگ طلایی خودش هم پررنگ‌تر شد
  (`#FFB020` تیره / `#B8860B` روشن، تو `ui/theme/Color.kt`). این یعنی طلایی هنوز تو اپ هست (حاشیه‌ی
  لمسیِ `PressScale`، رینگِ آواتار مشترکین، بج‌های بالا)، فقط دیگه رو فونت نیست — اگه جای جدیدی خواستی
  «لهجه‌ی طلایی» اضافه کنی، همین الگو (پس‌زمینه/حاشیه، نه `color=` رو `Text`) رو رعایت کن.
- **باگ حالت خصوصی (آیکون چشم)**: `LoanDetailScreen` اصلاً به `LocalPrivacyMode`/`maskIfPrivate` وصل
  نبود (برخلاف `MyLoansScreen`/`ResultScreen`)، برای همین مبلغ‌ها تو جزئیات وام حتی با حالت خصوصی روشن
  هم دیده می‌شدن. الان وصل شده. اگه صفحه‌ی جدیدی مبلغ نشون می‌ده، حتماً `LocalPrivacyMode.current` +
  `maskIfPrivate(...)` رو (`ui/privacy/PrivacyMode.kt`) روش اعمال کن.
- **قفل اسکرین‌شات (`FLAG_SECURE`) حذف شد** — `MainActivity.onCreate` دیگه اسکرین‌شات/نمایش تو App
  Switcher رو مسدود نمی‌کنه (تصمیم صریح کاربر، چون مانع تست/عکس گرفتن می‌شد).
- **اسپلش**: یه هاله‌ی گرم طلایی/کهربایی بیرونی (الهام از پرتال Doctor Strange) به هاله‌ی سبزآبی موجود
  اضافه شد، و نوار نورِ چرخان دورِ حلقه یه لایه‌ی محوتر/پهن‌تر پشتش گرفت (`ui/onboarding/SplashIntroScreen.kt`).

### مونده از لیست فیچرهای اخیر کاربر (هنوز شروع/تکمیل نشده)
اینا رو کاربر با عکس از یه اپ رقیب (شبیه دورِ قبلیِ پرکردنِ شکاف با VAMMAN) خواسته، هنوز پیاده نشدن:
- تو فرم افزودن/ویرایش چک: بخش «مستندات چک» (پیوست عکس، با همون الگوی `PhotoAttachmentCard`ی که الان
  فقط تو `ChequeDetailScreen` هست، نه تو خودِ فرم افزودن)، بخش اختیاریِ «اطلاعات بیشتر» (شناسه ملی/کد
  ملی، مانده‌ی قبلی، واریزی، جمع و مانده‌ی محاسبه‌شده — فیلدهای جدید، نیاز به تغییر schema داره)، و
  بازطراحی بخش «دسته چک» (toggle نوع چک + دراپ‌داون شیک‌تر + فیلد شناسه‌ی ۱۶ رقمی صیادی که الان اصلاً
  نیست).
- ساعت زنده (HH:mm:ss) + تاریخ جلالی بالای صفحه‌ی اصلیِ «امور چک» (`ChequeScreen.kt`) — الان هیچ صفحه‌ای
  تو اپ ساعت زنده نداره، باید از صفر با `LaunchedEffect` تیک‌خور + `core/JalaliCalendar.kt` ساخته بشه.
- صفحه‌ی واقعیِ «استعلام چک صیادی» — الان فقط یه دکمه با URL placeholder تو `ChequeDetailScreen.kt`ه
  (`SAYAD_INQUIRY_URL_PLACEHOLDER`). کاربر محتوای واقعی (لینک `cbi.ir/EstelamSayad/24090.aspx`، فرمت
  پیامکی `*1*1*شناسه‌ی۱۶رقمی#چک` به شماره‌ی `701701`، هزینه/سقف روزانه/زمان پاسخ، دکمه‌های اپ‌های
  استعلام، راهنمای رنگ وضعیت چک) رو با عکس فرستاده — باید یه صفحه‌ی جدید ساخته بشه.
- منوی کشویی (drawer) برای «امور چک» با آیتم‌های: گزارش‌دهی (داشبورد تحلیلی/آماری چک — الان نیست)،
  بایگانی (الان فقط یه اکشن تک‌چکی تو جزئیاته، نه یه صفحه‌ی لیست)، استعلام چک صیادی، تنظیمات یادآوری
  چک (الان صفحه‌ی جدا نداره)، پشتیبان‌گیری و بازیابی (الان دکمه‌ی ساده رو صفحه‌ی لیسته، نه تو منو)،
  دسته چک. الان هیچ `ModalDrawer`ای تو ماژول چک نیست، باید از صفر ساخته بشه.
- باکس‌های آمار بدون خط دور (بدون border، حس مدرن‌تر) + رنگ پس‌زمینه‌ی سبزِ ملایم‌تر برای باکس درآمد —
  کاربر عکس مرجع فرستاده، هنوز شروع نشده.

## فرمول‌های حساسِ محاسبه (خیلی مهم، تست‌شده و تاییدشده)
پیاده‌سازی مرجع الان `native-android/.../core/LoanCalculator.kt`ه (قبلاً تو `www/index.html` بود که
حذف شده؛ منطق عیناً پورت شد، فرمول‌ها عوض نشدن):
- وام‌های عادی (نرخ سود بالای ۴٪): فرمول اقساط مساوی نزولی، i = نرخ سالانه/100 × (فاصله هر قسط به روز/365)
- وام‌های قرض‌الحسنه (ازدواج، فرزندآوری، قرض‌الحسنه — نرخ ۴٪ یا کمتر): این‌ها اصلاً سود مرکب ندارن! قسط‌های ۱، ۱۳، ۲۵، ۳۷... (ابتدای هر سال) کاملاً و فقط مخصوص کارمزد سالانه‌ان (روی مانده‌ی همون لحظه) و هیچ اصل وامی توشون نیست؛ اصل وام فقط بین بقیه‌ی اقساط (یعنی n − ⌈n/12⌉ قسط غیرکارمزدی) مساوی تقسیم میشه. این تو `LoanMethod.QARZ` پیاده شده و با فرمول واقعیِ یه اپ محاسبه‌گر وام مرجع (decompile شده) تطبیق و تایید شده — تغییرش نده مگر با تست دقیق (`LoanCalculatorRegressionTest.kt`) روی چند مثال واقعی.

## هشدار درباره‌ی منبع لوگوها
موقع بررسی یه اپ محاسبه‌گر وام مرجع دیگه (decompile شده) معلوم شد که کد پایه‌ی خیلی اولیه‌ی همین پروژه
حداقل یه لوگوی اشتباه داشته (مثلاً چیزی که اسمش «بانک سامان» بود در واقع لوگوی بانک دیگه‌ای بود). داده‌ی
بانک‌ها/خدمات اعتباری الان تو `native-android/.../data/Banks.kt` هست و لوگوها منابع اندرویدی
(`res/drawable`) هستن، از یه پک رسمی (tejiran.ir) بر اساس نام دانلود و با علم عمومی (شکل واقعی لوگوی هر
بانک) چک شدن — **صرفاً یکی‌بودن بایت با یه فایل قدیمی یا اپ دیگه رو ملاک درستی اسم بانک نگیر**؛ همیشه
لوگوی جدید رو با شکل واقعی و شناخته‌شده‌ی همون بانک تطبیق بده.

## تغییراتِ این نشست (۲۷ تیر ۱۴۰۵ / ۲۰۲۶-۰۷-۱۸)
جزئیاتِ کامل، برای اینکه اگه این چت پاک شد هیچی گم نشه:

- **versionCode/versionName خودکار**: `.github/workflows/build-native-android.yml` حالا هم
  `versionCode` هم `versionName` رو از `github.run_number` می‌سازه (`versionName = "1.0.<run_number>"`)
  - قبلاً فقط versionCode خودکار بود، versionName همیشه "1.0" ثابت می‌موند.
- **مایکت کامل شد**:
  - `MYKET_IAB_PUBLIC_KEY` (تو `app/src/myket/.../SubscriptionManager.kt`) از پنلِ دولوپرِ مایکت
    گرفته شد و جایگزینِ placeholder شد.
  - هر ۴ محصولِ اشتراک (`unlimited_loans_1m/3m/6m/1y`) تو پنلِ مایکت با همون قیمت‌های کافه‌بازار
    تعریف شدن: ۳۰۰٬۰۰۰ / ۸۱۰٬۰۰۰ / ۱٬۴۴۰٬۰۰۰ / ۲٬۵۲۰٬۰۰۰ ریال.
  - صحت‌سنجیِ خریدِ سمتِ سرور برای مایکت پیاده شد (`server/.../Myket.kt`، `validateMyketPurchase`،
    هدرِ `X-Access-Token`) - معادلِ `Cafebazaar.kt` ولی با توکنِ ثابت به‌جای فلوی OAuth.
    `MYKET_ACCESS_TOKEN=fb2604a0-cdb0-49e6-b045-7fa57972a829` رو VPS تو `.env` ست شده.
  - کلاینت الان `store` (`BuildConfig.FLAVOR`) رو تو `POST /api/subscription/verify` می‌فرسته
    (`VerifySubscriptionRequest.store`) تا سرور بدونه با کدوم API خرید رو تایید کنه؛ نسخه‌های
    قدیمی‌تر بدون این فیلد پیش‌فرض `cafebazaar` حساب می‌شن (سازگاریِ عقب‌رو).
- **باگ رفع‌شده - کارتِ خرید اشتراک تو دوره‌ی آزمایشی مخفی بود**: تو `SettingsScreen.kt` کارتِ «خرید
  اشتراک» با شرطِ `!subscribed` نشون داده می‌شد؛ چون دوره‌ی آزمایشیِ ۷روزه هم `subscribed=true` می‌کنه،
  کاربرِ تو دوره‌ی آزمایشی اصلاً نمی‌تونست زودتر بخره. الان با `onlyTrialSubscribed` هم موقعِ آزمایشی
  کارت نشون داده می‌شه.
- **باگ رفع‌شده - هنگ‌کردنِ ظاهریِ صفحه‌ی ورود**: دکمه‌ی «ارسال کد تایید»/«تایید و ورود» موقعِ
  صبرِ درخواستِ شبکه هیچ اسپینری نداشت (فقط غیرفعال می‌شد)، تاخیرِ چندثانیه‌ای حسِ هنگ‌کردن می‌داد.
  الان با `LottieSpinner` (همون الگوی SubscriptionScreen/حذفِ حساب) موقعِ loading اسپینر نشون می‌ده.
- **حادثه‌ی سرور (رفع شد)**: موقعِ دیپلویِ دستیِ jarِ جدید، پوشه‌ی مقصد (`~/loan-server/tmp`) وجود
  نداشت، `cat` رو فایل‌های تیکه شکست خورد ولی یه فایلِ **خالی** ساخت، و اون فایلِ خالی جایگزینِ jarِ
  سالم شد → سرور کرش کرد (pm2 «stopped»). با آپلودِ درستِ ۳ تیکه، ساختِ دوباره‌ی jar، تاییدِ
  sha256 (`72e15fab7fbbe515cd37d0d712f77a3f238a163eecb2b73cf48f03c952508153`) و `pm2 restart` حل شد؛
  دیتابیس (`data.sqlite`) هیچ‌وقت دست‌نخورد. **درسِ مهم**: همیشه قبل از `mv` روی jarِ زنده، هشِ فایلِ
  بازسازی‌شده رو تایید کن.
- **Play Protect Appeals واقعاً ارسال شد**: برای APKِ فلیورِ مایکت (رانِ CI شماره‌ی ۲۵۹)، با هشِ
  SHA256 برابرِ `d23a8ef52ee330ad18dca043e42bdda4477923ff7b8507f0a1e83c5311d2d4ad` (صفر تشخیصِ مخرب رو
  VirusTotal). تحقیق شد که مشکلِ Play Protect («Blocked by Play Protect» / Unknown Developer) اصلاً
  ربطی به کدِ اپ نداره - سورسِ واقعیِ `myket-billing-client` چک شد: `launchPurchaseFlow` از
  `onActivityResult` استفاده نمی‌کنه (یه `ProxyBillingActivity` داخلی + `BroadcastReceiver` داره)،
  پس `handleActivityResult` نوپ‌بودنش تو کدِ ما درسته. گوگل گفته تا یه هفته (یعنی حدودِ ۳ مردادِ ۱۴۰۵،
  ۲۰۲۶-۰۷-۲۵) طول می‌کشه و هیچ پیامی نمی‌ده - باید دستی دوباره تست بشه. **تا اون‌موقع نباید
  applicationId یا کلیدِ امضا عوض بشه.**
- **وضعیتِ ارسال به استورها**: اپ («وام من») هم رو **کافه‌بازار** هم رو **مایکت** ارسال شده و هر دو
  الان تو صفِ بررسی‌ان (کاربر خودش کافه‌بازارِ قبلی رو رد/جایگزین کرد تا نسخه‌ی جدید رو بفرسته).
- خریدِ واقعیِ مایکت هنوز تست نشده چون اپ رو مایکت هنوز تاییدنشده‌ست («پیش‌نویس» بود، «ارسال برای
  بررسی» زده شد) - مایکت اجازه‌ی خریدِ واقعیِ محصولاتِ درون‌برنامه‌ای رو به اپِ منتشرنشده نمی‌ده،
  حتی اگه قیمت‌ها درست لود بشن (که می‌شن).
- **پکیجِ انیمیشن/جلوه‌های بصری** (خواسته‌ی «حداقل ۴۰٪ تاثیر رو نمای کل، خودت انتخاب کن») - ۵ مورد
  پیاده شد، همه سبک و بدونِ blur واقعی (سازگار با گوشی ضعیف):
  ۱) `AuroraBackground.kt`: پس‌زمینه‌ی زنده‌ی شفق (سه هاله‌ی گرادیانِ متحرکِ سبزآبی+طلایی) پشتِ هر
  ۴ تبِ اصلی - Scaffold و TopAppBar تو MainActivity برای دیده‌شدنش Transparent شدن.
  ۲) `ProgressRing.kt`: حلقه‌ی پیشرفتِ گرادیانی (سبزآبی→طلایی، sweep) - رو کارتِ هر وام (درصدِ
  اقساطِ پرداخت‌شده) + گیجِ «سلامتِ مالی» تو کارتِ تحلیلِ درآمدِ داشبورد (قرمز اگه ratio > 0.65).
  ۳) `CoinCelebration.kt`: بارشِ سکه‌ی طلایی + ویبره موقعِ پرداختِ آخرین قسطِ یه وام
  (LoanDetailScreen، فقط رو گذرِ ناتمام→تمام تو همون صفحه، نه موقعِ بازکردنِ وامِ ازقبل‌تسویه).
  ۴) `GoldSheen.kt` (`GoldSheenBox`): موجِ نورِ طلاییِ هر ~۴ ثانیه رو کارتِ خرید اشتراک و بجِ
  دوره‌ی آزمایشی (متمایز از shimmerEffect که اسکلتونِ لودینگه).
  ۵) count-up (همون countUpDouble موجود) به نتیجه‌های DepositScreen و AffordScreen هم اضافه شد.
  ترنزیشنِ هیرو (shared element بین لیست↔جزئیات وام) عمداً عقب افتاد - پرریسک‌ترین بود، منتظرِ
  تاییدِ بقیه رو گوشیِ واقعی.

## تغییراتِ نشستِ ۲۹ تیر ۱۴۰۵ (۲۰۲۶-۰۷-۲۰)
جزئیاتِ کامل این نشست، برای اینکه اگه این چت پاک شد هیچی گم نشه:

### بررسیِ ریزبینانه و رفعِ باگِ محاسبات/تاریخ (وام، سود سپرده، چک)
کاربر با اسکرین‌شات گزارش داد: یه وامِ تنظیم‌شده برای «۴ام هرماه» تاریخ‌های سررسیدش قاطی می‌شد (مثلاً
۴/۳ → ۵/۲ → ۶/۱ → ۶/۳۱ به‌جای ۴/۴ → ۵/۴ → ۶/۴...). خواستِ صریحش «نگاه ریزبینانه» به کلِ منطقِ
محاسبه/تاریخ بود (وام، سود سپرده، چک)، نه فقط رفعِ همون یه علامتِ دیده‌شده.

- **ریشه‌ی باگ**: هم `LoanRepository.getRows()` هم `ResultScreen.kt` سررسیدِ هر قسط رو با
  `PersianCalendar.addDays(startDate, m * intervalDays)` حساب می‌کردن، حتی وقتی `intervalDays` مضربِ
  ۳۰ بود (یعنی فاصله‌ی ماهانه). تقویمِ فارسی ماه‌های یکسان ۳۰روزه نداره (۶ ماه ۳۱روزه‌ان)، پس «اضافه‌کردنِ
  N×۳۰ روز» با «اضافه‌کردنِ N ماه» فرق می‌کنه و به‌مرورِ ماه‌ها روزِ سررسید می‌لغزه.
- **رفع**: تو هر دو جا (`LoanRepository.getRows()` و `ResultScreen.kt`، شاملِ محاسبه‌ی دوره‌ی
  تنفس/grace) اگه `intervalDays % 30 == 0` باشه از `PersianCalendar.addMonths(startDate, m *
  (intervalDays/30))` استفاده می‌شه (ماهِ تقویمی واقعی، روزِ ماه رو نگه می‌داره)، وگرنه (فاصله‌ی
  هفتگی/دوهفتگی) همون `addDays` قبلی می‌مونه.
- **باگِ دومِ کنارش**: خودِ `PersianCalendar.addMonths` (تو `core/PersianDate.kt`) موقعِ clamp کردنِ
  روز به آخرِ ماه از `monthLength()` ساده (اسفندِ ثابت ۲۹روزه) استفاده می‌کرد، نه تقویمِ واقعی؛ الان
  از `JalaliCalendar.daysInMonth(y, m)` (الگوریتمِ دقیقِ Borkowski/jalaali-js، اسفندِ کبیسه رو درست
  حساب می‌کنه) استفاده می‌کنه.
- دو تا تستِ رگرسیون اضافه شد به `LoanCalculatorTest.kt`: `persianDateAddMonthsKeepsDayOfMonth` و
  `persianDateAddMonthsRespectsLeapEsfand`.
- فرمول‌های خودِ محاسبه‌ی وام/سود (`LoanCalculator.kt`) دست نخوردن - بازبینیِ کامل نشون داد مشکل فقط
  تو محاسبه‌ی تاریخِ سررسید بود، نه تو خودِ فرمولِ اقساط/سود.

### رفعِ باگِ پرش‌رقمِ فیلدهای مبلغ (۷ جا تو کلِ اپ)
کاربر گزارش داد: تو فرمِ افزودنِ وامِ دستی، عددِ `12749000` رو که تایپ می‌کرد، رقم‌ها جابه‌جا/قاطی
می‌شدن یا یه رقمِ اضافه می‌رفت آخر.

- **ریشه**: فیلدها مستقیم تو `onValueChange` مقدارِ state رو با یه رشته‌ی گروه‌بندی‌شده‌ی کاما جایگزین
  می‌کردن؛ چون رشته‌ی نهایی با چیزی که کاربر واقعاً تایپ کرد فرق داشت (کاماها اضافه شده)، تشخیصِ
  موقعیتِ cursor توسطِ Compose به‌هم می‌ریخت (الگوی معروفِ اشتباهِ «reformat state inside
  onValueChange»).
- **رفع**: یه `VisualTransformation` تازه‌ساخته‌شده (`ui/components/ThousandsTransformation.kt` -
  `ThousandsSeparatorTransformation`) با `OffsetMapping` درست (originalToTransformed +
  transformedToOriginal). حالا state همیشه فقط رقمِ خام نگه می‌داره، گروه‌بندیِ کاما فقط بصریه، نه رو
  خودِ state. اعمال شده رو: `AddManualLoanScreen` (فیلدِ قسط)، `BankLoanScreen` (مبلغِ وام)،
  `DepositScreen` (مبلغِ سپرده)، `AffordScreen` (پرداختِ ماهانه + دو فیلدِ `RateFinderCard`)،
  `AddEditChequeScreen` (مبلغِ چک)، `MyLoansScreen` (فیلدِ افزودنِ درآمد تو `DashboardSummary`).
- تو همون فرمِ افزودنِ وامِ دستی، ردیفِ تاریخِ ورودی هم عوض شد: به‌جای بازکردنِ یه
  `WheelDatePickerScreen` جدا با تپ، حالا مثلِ تقویمِ صفحه‌ی اصلی درجا قابل‌ویرایشه
  (`InlineJalaliDateRow`)، آیکونِ تقویم هم کنارش هست برای بازکردنِ `CalendarPickerScreen` کامل.

### رفعِ لرزش/قاطی‌شدنِ لیستِ اقساط تو صفحه‌ی جزئیاتِ وام
کاربر: «وقتی میای پایین اون وام‌های که داریم بزرگ میشه... انیمیشن نداره اصن بهینه نیست یهو بزرگ
میشه یهو میزنی بالا قاطی میکنه».

- **ریشه**: `LoanDetailScreen` یه `Column(verticalScroll)` بیرونی داشت که توش یه `LazyColumn` تودرتو
  با ارتفاعِ انیمیشنی (`animateDpAsState`) بینِ نمایشِ ۵ ردیف و ۱۰ ردیف جابه‌جا می‌شد، بر اساسِ یه
  آستانه‌ی موقعیتِ اسکرولِ همون Columnِ بیرونی - تغییرِ ارتفاع وسطِ درگ اتفاق می‌افتاد و آستانه هم
  flip-flop می‌کرد.
- **رفع**: کل صفحه از صفر به یه `LazyColumn` تخت تبدیل شد - هدر/دونات/کارتِ بانک/کارتِ عکس تو یه
  `item{}`، ردیف‌های قسط مستقیم با `items(rows, key={...})`، دکمه‌های یادآوری/تقویم/حذف تو یه
  `item{}` آخر. هیچ اسکرولِ تودرتو و هیچ انیمیشنِ ارتفاعی نمونده.

### حسابِ تستِ دائمی + رفعِ بجِ گمراه‌کننده‌ی «آزمایشی»
کاربر خواستِ یه «حسابِ تست» و یه «حسابِ دائمیِ خودش» که هردو تست بشن. پیاده‌سازی:

- سرور: `TEST_ACCOUNT_PHONE`/`TEST_ACCOUNT_CODE` (env، مثلِ الگوی بقیه‌ی کلیدهای `env()`) -
  `AuthRoutes.kt` تو مسیرِ `request-otp` اگه شماره برابرِ `TEST_ACCOUNT_PHONE` باشه، اصلاً پیامک
  نمی‌فرسته و کدِ ثابتِ `TEST_ACCOUNT_CODE` رو منتظر می‌مونه (نه یه کدِ تصادفی).
- **باگِ کشف‌شده و رفع‌شده**: سرور بدونِ توجه به این‌که «چرا» یه حساب مشترکه، همیشه
  `trialDaysLeft(user.createdAt)` رو برمی‌گردوند - پس حتی حسابی که دستی/دائمی مشترک شده بود
  (`subscribed=true` دستی، نه از دوره‌ی آزمایشی) بازم بجِ «X روز از آزمایشی مونده» می‌گرفت. رفع شد با
  `SubscriptionStatus.trialDaysLeftIfApplicable(user)`: اگه `user.subscribed` دستی true باشه یا
  `subscribedUntil` هنوز نگذشته باشه، null برمی‌گردونه (یعنی «آزمایشی نیست»)؛ فقط وقتی واقعاً هیچ‌کدوم
  از این دوتا نیست، `trialDaysLeft` واقعی حساب می‌شه. `verify-otp` و `/me` هر دو از این تابع استفاده
  می‌کنن.
- کلاینت: `subscribedUntil` (تاریخِ انقضای خریدِ زمان‌دار، یا null اگه دستی/دائمی) از سرور تا
  `AuthPrefs`/`AuthViewModel` پاس داده می‌شه. تو `SettingsScreen.kt` یه بجِ جدید اضافه شد: اگه
  `subscribed && subscribedUntil == null` → بجِ «اشتراک دائمی» (تو یه `GoldSheenBox`)، جدا از بجِ
  آزمایشیِ قبلی که حالا فقط وقتی واقعاً آزمایشیه نشون داده می‌شه.
- **تصمیمِ امنیتیِ صریحِ کاربر (خیلی مهم، حفظ بشه)**: کاربر اول خواست کدِ ثابتِ `۱۱۱۱۱` برای **هر دو**
  شماره (تستی + شماره‌ی واقعیِ خودش `09366963867`) کار کنه. توضیح داده شد که این خطرناکه - یه شماره‌ی
  واقعیِ شخصی با یه کدِ ثابتِ همگانی‌شناخته‌شده یعنی هرکسی که کد رو بدونه (مثلاً بازبینِ استور که این
  اطلاعات باهاش شیر می‌شه) می‌تونه وارد اون حساب بشه. کاربر صراحتاً پذیرفت: **«نه کار خوبی کردی»** -
  یعنی طراحیِ فعلی (فقط `TEST_ACCOUNT_PHONE` کدِ ثابت می‌گیره، شماره‌ی واقعی همیشه کدِ واقعیِ تصادفی
  از `pm2 logs` می‌خواد) باید همین‌جوری بمونه، دوباره درباره‌ش سوال پرسیده نشه مگر خودِ کاربر دوباره
  مطرحش کنه.
- **حادثه‌ی دیپلویِ سرور (این دور)**: موقعِ آپلودِ jarِ جدید برای این تغییرات، دوبار مشکل پیش اومد:
  (۱) کاربر فقط تیکه‌ی اولِ jar رو آپلود کرد (تیکه‌های ۱/۲ جا موندن)، `cat` یه فایلِ ناقص ساخت -
  قبل از هر `mv` رو jarِ زنده با چک‌کردنِ sha256 جلوش گرفته شد، کاربر دوباره کاملشو آپلود کرد.
  (۲) بارِ بعد کاربر گزارش داد «بالا اومده درست هست» ولی `pm2 logs` نشون می‌داد
  `Error: Unable to access jarfile` و `ls -la ~/loan-server` فقط `.env`+`data.sqlite` داشت (jar
  اصلاً نبود) - با یه اسکرین‌شاتِ فایل‌منیجر معلوم شد jarِ بازسازی‌شده اشتباهی تو `/home/ubuntu/`
  ریخته بود، نه `/home/ubuntu/loan-server/`؛ با `mv` به مسیرِ درست + `pm2 restart` حل شد. **درسِ
  تکرارشده**: همیشه بعدِ دیپلوی با `pm2 logs`/`ls -la` واقعاً تایید کن، به گزارشِ «بالا اومد» کاربر
  به‌تنهایی اکتفا نکن.
- **کارِ ناتمومِ این بخش**: `sqlite3` رو VPS نصب نبود (`Command 'sqlite3' not found`) - دستورِ
  `sudo apt install sqlite3 -y` داده شد تا بعدش دستورِ زیر برای ست‌کردنِ دستیِ اشتراکِ دائمیِ هر دو
  حساب (تست + شماره‌ی خودِ کاربر) اجرا بشه:
  ```bash
  sqlite3 ~/loan-server/data.sqlite \
    "UPDATE users SET subscribed = 1 WHERE phone IN ('09121111111','09366963867');"
  ```
  **تاییدِ اجرای موفقِ این دستور و دیده‌شدنِ بجِ «اشتراک دائمی» رو هر دو حساب هنوز از کاربر گرفته
  نشده** - اگه تو یه چتِ بعدی این موضوع مطرح شد، اول همینو پیگیری کن.

### اسپلش (سومین دورِ تغییر) + رفعِ رفتنِ فلشِ صفحه‌ی ورود زیرِ آنتن
کاربر سه‌تا نکته‌ی جدید داد:
1. «حلقه‌ی دورش» (یعنی هالهٔ بازتابِ نورِ چرخانِ ۱۵۰dp که دورِ دونات بود) حذف بشه، فقط خودِ «یک حلقه‌ی
   بسته» (دونات) بمونه؛ به‌جاش هاله‌ی پشتش انگار «آتیشِ روشن» باشه، سایه‌اش پشت افتاده باشه، بزرگ‌تر هم
   باشه، و هم‌زمان با چرخشِ حلقه یه تکونِ ریز بخوره.
2. پس‌زمینه‌ی اسپلش (که همیشه ثابت مشکی بود، عمداً مستقلِ از تم) حالا با دارک/لایت‌مود عوض بشه - سفید
   تو تمِ روشن، مشکی/سرمه‌ای تو تمِ تیره.
3. موقعِ واردکردنِ شماره/منتظرِ پیامک، بالای صفحه‌ی ورود (فلشِ بازگشت) بهم می‌ریخت و می‌رفت زیرِ نوارِ
   وضعیت/آنتنِ گوشی - «بالای صفحه رو یکم بیار پایین‌تر».

پیاده‌سازی (`ui/onboarding/SplashIntroScreen.kt`، `MainActivity.kt`، `ui/auth/LoginScreen.kt`):
- Canvasِ ۱۵۰dpِ هالهٔ بازتابِ نور (دو تا `drawArc` با `sweepGradient`) کاملاً حذف شد. فقط Canvasِ
  ۱۰۰dpِ دونات (پایه + کمانِ طلایی ۳۰٪ + کمانِ سبزآبی ۷۰٪) موند.
- هاله‌ی پشتِ حلقه بازطراحی شد به چهار لایه: یه لایه‌ی «سایه»ی تیره‌ی بزرگ (۳۶۰dp، کمی آفستِ پایین،
  گرادیانِ رادیالِ مشکی محو‌شونده)، یه هاله‌ی بیرونیِ آتیشی بزرگ (۴۰۰dp، رنگِ جدیدِ `FireCore`
  #FFFF5A1F نارنجی/قرمز + طلایی)، یه هاله‌ی میانیِ طلاییِ فشرده‌تر (۲۷۰dp)، و یه هاله‌ی نازکِ سبزآبیِ
  چسبیده به خودِ حلقه (۱۹۰dp، همون رنگِ برندِ اپ که بمونه، نه فقط آتیشی).
- تکونِ «wobble»: به‌جای یه انیمیشنِ جدا، مستقیم از رو `spin.value` (همون مقداری که حلقه رو می‌چرخونه)
  با `sin(Math.toRadians(spin.value * 3.0))` مشتق می‌شه و به‌صورتِ `scale` رو هر چهار لایه‌ی هاله اعمال
  می‌شه - پس دقیقاً «هم‌زمان با چرخش» می‌لرزه، نه یه ریتمِ جداگانه.
- پس‌زمینه/رنگِ متن‌ها: `SplashIntroScreen` حالا یه پارامترِ `isDarkTheme: Boolean` می‌گیره؛
  `SplashBgDark`(0xFF0D1321)/`SplashBgLight`(سفید) + رنگ‌های متنِ متناظر (`SplashNameDark/Light`،
  `SplashSubDark/Light`). `AppRoot` تو `MainActivity.kt` یه `ThemeViewModel` هم می‌گیره (قبلاً
  نداشت) و `themeMode == ThemeMode.DARK` رو به `SplashIntroScreen` پاس می‌ده.
- `LoginScreen.kt`: `Box` بیرونیِ کلِ صفحه یه `.statusBarsPadding()` گرفت (همون الگویی که
  `SettingsScreen.kt` قبلاً برای همین مشکل داشت) - چون `MainActivity` با `enableEdgeToEdge()` محتوا رو
  زیرِ نوارِ وضعیت هم می‌کشه، بدونِ این پدینگ فلشِ بازگشتِ align‌شده‌یِ TopStart دقیقاً زیرِ آنتن/ساعتِ
  گوشی می‌رفت.

### دو تا رفعِ باگِ کوچیکِ بعدی (همون نشست، بعدِ اسپلش)
با اسکرین‌شات کاربر دو تا باگِ دیگه گزارش داد:
- تو `LoanDetailScreen.kt`، دیالوگِ «ویرایشِ مبلغِ قسط» (آیکونِ مدادِ کنارِ هر ردیفِ قسط) هیچ جداکننده‌ی
  هزارگان و پسوندِ «ریال»ی نداشت (برخلافِ بقیه‌ی فیلدهای مبلغِ اپ که تو دورِ قبلی درست شدن) - چون
  اصلاً `ThousandsSeparatorTransformation()`/`suffix` بهش اضافه نشده بود. رفع شد؛ الگوش دقیقاً مثلِ
  فیلدِ «مبلغ هر قسط» تو `AddManualLoanScreen.kt`ه.
- «اعمال به همه‌ی اقساط» (بعدِ ویرایشِ یه قسط، تاییدِ «بله رو همه اعمال کن») مبلغِ همه‌ی ردیف‌ها رو تو
  دیتابیس عوض می‌کرد، ولی عددِ داخلِ دایره‌ی بالای صفحه («قسط ماهانه») و کارتِ «مبلغ هر قسط» ثابت
  می‌موند - چون این دوتا از `loan.installment` (فیلدِ خلاصه‌ی سطحِ بالای `LoanEntity`) می‌خونن، نه از
  خودِ ردیف‌ها، و `LoanRepository.saveRows()` این فیلد رو دست نمی‌زد. رفع شد: `setAllRowsInstallment`
  حالا `installment` رو هم به `saveRows` پاس می‌ده تا `loan.installment` هم هم‌قدم بشه (فقط تو حالتِ
  «اعمال به همه» - ویرایشِ تکی هنوز خلاصه‌ی بالا رو دست نمی‌زنه، چون دیگه همه‌ی اقساط برابر نیستن).

### بازطراحیِ جعبه‌ی اقساط تو `LoanDetailScreen` - اسکرولِ محدود با «رد شدنِ سریع»
کاربر با یه وامِ ۱۲۰قسطی گزارش داد: چون لیستِ اقساط الان کاملاً تخته (بدونِ محدودیتِ ارتفاع، همون رفعِ
باگِ دورِ قبل)، برای رسیدن به یادآوری/تقویم/حذفِ زیرِ لیست باید از کنارِ همه‌ی ۱۲۰ ردیف رد بشه. خواستِ
دقیقش: «روش می‌زنم [نگه می‌دارم] حرکت کنه [اسکرولِ اقساط]، ولی اگه بخوام از کنارش رد کنم [سواپِ سریع]،
در جا بره پایین [بپره به بعدِ جعبه]».

پیاده‌سازی (`LoanDetailScreen.kt`) - **این‌بار برخلافِ نسخه‌ی باگ‌دارِ قبلی، هیچ انیمیشنِ تغییرِ ارتفاع
و هیچ آستانه‌ی اسکرولِ محاسبه‌شده‌ای در کار نیست**:
- لیستِ اقساط از یه `items(rows)`ِ مستقیم تو `LazyColumn` بیرونی، به یه `item{}` تکی تبدیل شد که توش
  یه `Box` با **ارتفاعِ ثابت** (`installmentRowHeight * 5 + 8.dp * 4` ≈ همیشه ۵ ردیف، صرف‌نظر از
  تعدادِ کلِ اقساط) یه `LazyColumn` داخلیِ مستقل نگه می‌داره. رندرِ هر ردیف به یه کامپوننتِ جدا
  (`InstallmentRow`) استخراج شد تا هم تو این لیستِ داخلی هم (در آینده، اگه لازم شد) جای دیگه قابل‌استفاده
  باشه.
- **درگِ آهسته/نگه‌داشتن**: بدونِ هیچ کدِ اضافه، رفتارِ پیش‌فرضِ nested-scrollِ Compose بینِ دو
  `LazyColumn`ِ تودرتو خودش این رو می‌ده - وقتی لیستِ داخلی به لبه‌ی بالا/پایینش برسه و درگ ادامه پیدا
  کنه، خودکار «سرریز» می‌شه به لیستِ بیرونی.
- **سواپِ سریع**: یه `NestedScrollConnection` سفارشی (`installmentsFlingPassthrough`) رو خودِ Boxِ
  جعبه نصب شده که `onPreFling(available: Velocity)` رو چک می‌کنه - اگه قدرمطلقِ سرعتِ فلینگ از
  `INSTALLMENTS_FLING_SKIP_VELOCITY = 3500f` (px/s) بیشتر بود، به‌جای اجازه‌دادن به فلینگِ لیستِ
  داخلی، مستقیم `outerListState.animateScrollToItem(...)` صدا می‌شه (به آیتمِ بعد از جعبه اگه سواپ رو
  به پایین بود، به آیتمِ قبلش اگه رو به بالا بود) و کلِ سرعتِ فلینگ consume می‌شه تا لیستِ داخلی
  خودش فلینگ نکنه.
- **اگه کاربر گزارش داد آستانه حس نمی‌شه درست** (خیلی زود/دیر «رد شو» فعال می‌شه)، فقط همین یه عددِ
  ثابت (`INSTALLMENTS_FLING_SKIP_VELOCITY` تو `LoanDetailScreen.kt`) رو تنظیم کن - نیازی به تغییرِ
  معماری نیست.
- این کار هنوز رو گوشیِ واقعی تستِ لمسی نشده (فقط از رو کد پیاده و استدلال شده) - وقتی کاربر گزارش داد
  حسش چطوره (خصوصاً آستانه‌ی سرعت و اینکه ۵ ردیف اندازه‌ی مناسبیه یا نه)، طبقِ همون تنظیمش کن.

### رفعِ باگِ سیوِ تکراری + امکانِ ویرایشِ مشخصاتِ کلیِ وامِ دستی
کاربر گزارش داد: «یک وامی رو اومدم بزن ذخیره نگرفت یک لحظه هنگ کرد، چندبار زدم، بعد نزدیک ۶-۷ بار
دیدم سیو شده تو وام‌ها» + «می‌خوام خود هر وام رو بشه ادیت کرد... یک وامی ذخیره کردم به‌جای ده تا قسط
زدم یازده تا، دیگه نمی‌شد کاری کرد».

**باگِ سیوِ تکراری - ریشه**: دکمه‌ی «ذخیره وام» (هم تو `AddManualLoanScreen` هم تو `ResultScreen`)
هیچ حالتِ لودینگ/غیرفعال‌شدن نداشت. `saveManualLoan`/`saveComputedLoan` تو `MyLoansViewModel` اول
وام رو محلی می‌سازن، بعد اگه لاگین باشی یه `syncIfLoggedIn()` (پوشِ شبکه‌ای به سرور) هم می‌زنن و فقط
بعدِ اون `onSaved()` صدا زده می‌شه. اگه این پوش کند بود، دکمه بی‌حرکت به‌نظر می‌رسید، کاربر چندبار
می‌زد، و **هر تپ یه وامِ کاملاً جدید و مجزا می‌ساخت** (هیچ گاردی نبود).

**رفع**: یه state لودینگ (`saving`) به هر دو صفحه اضافه شد - دکمه موقعِ ذخیره غیرفعال می‌شه، اسپینر
(`LottieSpinner`، همون الگوی صفحه‌ی ورود) نشون می‌ده، و تویِ خودِ `onClick` هم یه گاردِ
`if (saving) return@GradientButton` هست تا حتی اگه رویداد قبل از رندرشدنِ حالتِ غیرفعال یه‌بارِ
دیگه fire بشه، بازم دومین ذخیره واقعاً اجرا نشه.

**امکانِ ویرایشِ کلیِ وام (فقط وام‌های دستی)**:
- `LoanRepository.updateManualLoan(loan, name, bank, installment, n, startDate)` (جدید): برخلافِ
  `setRowInstallment`/`setAllRowsInstallment` که فقط مبلغِ اقساط رو دست می‌زنن، این اسم/بانک/مبلغِ
  هر قسط/تعدادِ کل/تاریخِ شروع رو عوض می‌کنه. اگه `n` عوض بشه، ردیف‌ها **هوشمند بازسازی** می‌شن: قسط‌هایی
  که هنوز تو بازه‌ی جدیدن (وضعیتِ پرداخت/تاخیر/عکسِ رسیدشون) دست‌نخورده می‌مونن؛ قسط‌های جدید (اگه n
  زیاد شده) پرداخت‌نشده اضافه می‌شن؛ قسط‌های اضافی (اگه n کم شده) حذف می‌شن - **تاریخچه‌ی پرداختِ
  قسط‌های موجود پاک نمی‌شه**.
- `isManualLoan(loan)`/`getStartDate(loan)` هم به `LoanRepository`/`MyLoansViewModel` اضافه شدن (پاس
  دادنِ اطلاعات از :data به :app، چون :app مستقیم به Gson دسترسی نداره).
- `AddManualLoanScreen` یه پارامترِ اختیاریِ `editingLoan: LoanEntity? = null` گرفت - وقتی پاس داده
  بشه، فرم با مقادیرِ فعلیِ وام پر می‌شه، فیلدِ «تعداد پرداخت‌شده» (که تو حالتِ ویرایش دیگه بی‌معنیه،
  چون منبعِ حقیقتِ وضعیتِ هر قسط خودِ ردیف‌هاست) مخفی می‌شه، عنوان و متنِ دکمه عوض می‌شه («ویرایش وام»/
  «ذخیره تغییرات»)، و ذخیره به‌جای `saveManualLoan` به `updateManualLoan` می‌ره.
- تو `LoanDetailScreen`، یه آیکونِ مداد کنارِ اسمِ وام (تو هدر) اضافه شد - **فقط اگه وام دستی باشه**
  (`viewModel.isManualLoan(loan)`)، چون وام‌های محاسبه‌شده/قرض‌الحسنه ساختارِ اقساطِ نامساوی دارن که
  این فرمِ ساده براش معنی نداره؛ کاربر صراحتاً این محدودیت رو تایید کرد.
- تو `MyLoansScreen`، یه state جدید `editingLoanId` اضافه شد که **عمداً `openedLoanId` رو پاک نمی‌کنه**
  - فقط اولویتِ مسیریابی رو تو `screenKey` بالاتر می‌بره (`"edit"` قبل از `"detail"`)، پس بعدِ
  ذخیره/انصرافِ ویرایش، خودکار برمی‌گرده به همون صفحه‌ی جزئیاتِ وام، نه لیست.

### جعبه‌ی اقساط «هوشمند» (تمام‌صفحه موقعِ اسکرول) + نوعِ اشتراک تو تنظیمات
کاربر دو تا خواسته‌ی ریسکی رو صریحاً تایید کرد («انجام بده دوتا کار رو ریسکی رو - هم فول‌اسکرین هم
سرور») بعد از اینکه هشدارِ ریسک‌شون داده شد:

**۱) جعبه‌ی اقساط تمام‌صفحه موقعِ اسکرول (`LoanDetailScreen.kt`)**
خواسته: وقتی اسکرول می‌کنی و به جعبه‌ی اقساط می‌رسی، به‌آرومی تا نزدیکِ تمام‌صفحه بزرگ بشه (همون
فونت/سایز، فقط ردیفِ بیشتر دیده بشه)، و تو گوشه‌ها (وقتی ازش دوری) دوباره کوچیک بشه؛ کنارش یه
اسکرول‌بار باشه؛ و وامی که باز می‌کنی از رو مرزِ آخرین قسطِ پرداخت‌شده شروع بشه، نه از قسطِ ۱.

- **فرقِ کلیدی با نسخه‌ی خیلی قبل‌تر که باگ داشت** (همونی که قبلاً به یه جعبه‌ی ثابتِ ۵ردیفه ساده
  برگردونده شده بود): اون نسخه یه آستانه‌ی *تکی* داشت (بالا/پایینِ یه نقطه = عوضِ حالت) که دقیقاً رو
  مرز می‌لرزید و چون تغییرِ ارتفاع با یه انیمیشنِ مستقل هم‌زمان بود، محتوا زیرِ انگشتِ کاربر می‌پرید.
  این نسخه سه‌تا تفاوتِ فنی داره: (الف) **هیسترزیس** - دو آستانه‌ی جدا برای ورود (offset ≤ 40px از
  بالای صفحه) و خروج (offset ≥ 140px)، نه یکی، پس دیگه رو مرز نمی‌لرزه. (ب) تشخیصِ حالت با
  `snapshotFlow` رو `LazyListState.layoutInfo` (یعنی مستقیم از رو موقعیتِ واقعیِ اسکرول، نه یه
  ژستِ جداگانه) واکنش نشون می‌ده. (ج) وقتی جعبه بزرگه، منطقِ «رد شدنِ سریع»ی که تو دورِ قبل ساخته
  بودیم (`installmentsFlingPassthrough`) غیرفعال می‌شه (چون دیگه معنی نداره).
- ارتفاعِ حالتِ بزرگ از رو `viewportEndOffset - viewportStartOffset` (ارتفاعِ واقعیِ صفحه‌نمایشِ
  همون LazyColumn) × ۰.۹۲ حساب می‌شه، نه یه عددِ ثابتِ هاردکد - رو گوشی‌های مختلف خودش را تنظیم می‌شه.
- اسکرول‌بار: همون `lazyColumnScrollbar` که رو خودِ صفحه بود، حالا رو لیستِ داخلیِ جعبه هم اضافه شد.
- شروعِ خودکار از مرزِ پرداخت: یه `LaunchedEffect(rows)` اولین ردیفِ پرداخت‌نشده رو پیدا می‌کنه و
  `innerListState.scrollToItem` می‌زنه - فقط یه‌بار، موقعِ باز شدنِ صفحه.
- **این کار هنوز رو گوشیِ واقعی تست نشده** (فقط از رو استدلالِ فنی پیاده شده، سندباکس نمی‌تونه
  native-android رو بیلد/تست کنه). اگه کاربر گزارش داد بازم لرزش/جهش داره، اول این دو تا عدد رو
  تنظیم کن: آستانه‌ی ورود/خروجِ هیسترزیس و ضریبِ ارتفاعِ حالتِ بزرگ (۰.۹۲).

**رفعِ دورِ بعدی (بعدِ تستِ کاربر روی گوشیِ واقعی)**: کاربر گزارش داد «هنوز فول‌اسکرین نمی‌شه». باگ
پیدا شد: آستانه‌ی اولیه بر اساسِ موقعیتِ *مطلقِ* لبه‌ی جعبه رو صفحه بود (`offset ≤ 40px از بالای
صفحه`) - یعنی کاربر باید کلِ هدر (دکمه‌ی بازگشت + دونات + کارتِ بانک + کارتِ عکس، معمولاً ۵۰۰-۷۰۰dp)
رو کامل از رو صفحه رد می‌کرد تا لبه‌ی جعبه دقیقاً به بالای صفحه برسه - این خیلی بیشتر از یه اسکرولِ
معمولیه. رفع شد: آستانه الان بر اساسِ **درصدِ دیده‌شدنِ خودِ جعبه** (نه موقعیتِ مطلقش) حساب می‌شه -
ورود وقتی ≥۴۵٪ از ارتفاعِ جعبه رو صفحه دیده بشه، خروج وقتی ≤۲۰٪ - با یه اسکرولِ معمولی (نه یه
اسکرولِ خیلی طولانی که کلِ هدر رو رد کنه) هم قابلِ‌رسیدنه. کاربر همچنین خواستِ «وقتی اسکرول می‌کنه
خودش تا چند ثانیه بعد بره پایین» (یه momentum/اسکرولِ خودکارِ طولانی‌تر) - عمداً این اضافه نشد، چون
تشخیص داده شد ریشه‌ی واقعیِ حسِ «کار نمی‌کنه» همون آستانه‌ی خیلی سخت‌گیرانه بود، نه کمبودِ momentum؛
با آستانه‌ی جدید یه اسکرولِ طبیعی (بدونِ نیاز به اسکرولِ خودکارِ مصنوعی) باید کافی باشه - اگه کاربر
بازم گفت کافی نیست، اون‌موقع باید momentum/auto-continue رو جداگانه بررسی کرد.

**۲) نمایشِ دقیقِ نوعِ اشتراک تو تنظیمات (سرور + کلاینت)**
خواسته: زیرِ شماره‌موبایل تو `SettingsScreen`، به‌جای متنِ قبلیِ «مشترک — وام‌های من همگام‌سازی می‌شه»
(که کاربر با STT «با ماه من» شنیده بود)، دقیقاً نوعِ پلن نشون داده بشه: «اشتراک یک‌ماهه»/«سه‌ماهه»/
«شش‌ماهه»/«یک‌ساله».

- **مشکل**: سرور تا الان اصلاً ذخیره نمی‌کرد کاربر کدوم پلن (۱/۳/۶/۱۲ ماهه) رو خریده - فقط تاریخِ
  انقضا (`subscribed_until`) رو داشت.
- **سرور**: یه ستونِ جدید `subscription_tier TEXT` به جدولِ `users` اضافه شد (`Db.kt`، هم تو
  `CREATE TABLE` هم یه migrationِ `ALTER TABLE` برای دیتابیس‌های قدیمی‌تر). تو
  `SubscriptionRoutes.kt`، موقعِ تاییدِ موفقِ خرید، یه نگاشتِ جدید (`PRODUCT_TIER_CODE`: شناسه‌ی
  محصول → کدِ کوتاه `"1m"`/`"3m"`/`"6m"`/`"1y"`) این ستون رو هم‌زمان با `subscribed_until` آپدیت
  می‌کنه. `AuthRoutes.kt` (`/verify-otp` و `/me`) این فیلد رو تو `subscriptionTier` به کلاینت
  می‌فرستن؛ `UserRow`/`toUserRow()` تو `Db.kt` هم آپدیت شدن.
  **نکته‌ی مهم برای چت‌های بعدی**: چون `toUserRow()` ستون‌ها رو با اسم می‌خونه، هر جای دیگه‌ای از
  سرور که `SELECT ... FROM users` می‌زنه و بعد `toUserRow()` صداش می‌کنه باید حتماً `subscription_tier`
  رو هم تو لیستِ ستون‌ها داشته باشه وگرنه رو `SQLException` می‌خوره - این‌بار `LoansRoutes.kt` و
  `BackupRoutes.kt` هم که مستقل از `AuthRoutes.kt` بودن پیدا و اصلاح شدن.
- **کلاینت**: `subscriptionTier: String?` به `VerifyOtpResponse`/`MeResponse` (`ApiService.kt`) اضافه
  شد؛ `AuthPrefs` یه کلید/Flow/setter جدید گرفت؛ `AuthRepository.verifyOtp`/`refreshSubscriptionStatus`
  پاسش می‌دن؛ `AuthViewModel.subscriptionTier` جدید expose شد. تو `SettingsScreen.kt`، اگه
  `subscriptionTier` معلوم باشه («۱m»/«۳m»/«۶m»/«۱y») همون برچسبِ فارسیِ متناظرش نشون داده می‌شه؛
  اگه مشترک باشی ولی تیرش معلوم نباشه (آزمایشی/دستیِ دائمی/خریدِ قدیمی‌تر از قبلِ اضافه‌شدنِ این
  فیلد) فقط «مشترک» ساده (بدون خط تیره، بدون اشاره به «وام‌های من») نشون داده می‌شه؛ بجِ آزمایشی/
  اشتراکِ دائمیِ زیرش دست‌نخورده موند.
- **سرور رو سندباکس واقعاً بیلد/تست شد** (`gradle compileKotlin`/`test`/`fatJar` هر سه موفق، با
  گریدلِ سیستمی `/opt/gradle/bin/gradle` چون دانلودِ خودِ `gradlew` گاهی از `services.gradle.org`
  ۴۰۳ می‌گیره). **جارِ جدید واقعاً دیپلوی هم شد رو VPS** (۳۰ تیر ۱۴۰۵ / ۲۰۲۶-۰۷-۲۱) - تیکه‌های jar آپلود شدن،
  sha256 تایید شد (`13a55c8ab5644c285a9051b92ea738a9498f3c079884d6758ee668e67f14228a`)، و بعدِ یه
  دورِ اول که دستورِ `pm2 restart` به‌خاطرِ یه حرفِ فارسیِ اضافه‌ی چسبیده‌به‌دستور (تعویضِ زبونِ کیبوردِ
  گوشیِ کاربر) اجرا نشده بود (تشخیص داده شد چون `pm2 logs` نشون می‌داد سرور هنوز با jarِ قدیمی
  بالاست)، دورِ دوم موفق شد. با `sqlite3 ~/loan-server/data.sqlite ".schema users"` تایید شد که
  ستونِ `subscription_tier TEXT` واقعاً اضافه شده؛ دیتابیسِ موجود (`data.sqlite`) دست‌نخورده موند
  (migration با `ALTER TABLE`ی idempotent، نه بازسازیِ جدول).
- کاربرهایی که از قبل با اشتراکِ زمان‌دار خریده بودن (قبل از این تغییر)، `subscription_tier`شون تو
  دیتابیس `NULL` می‌مونه تا وقتی یه خریدِ جدید بزنن - این عمدیه (بدونِ داده‌ی واقعی از پنلِ
  کافه‌بازار/مایکت نمی‌شه گذشته رو بازسازی کرد)، فقط نشون‌دادنش «مشترک» ساده می‌مونه به‌جای نوعِ پلن.

### وضعیتِ برنچ/CI این نشست - نکته‌ی مهم برای چت‌های بعدی
برنچِ `ccr-07146a93-ufo9vg` (که هارنسِ CCR این نشست موظفش کرده بود روش کار کنم) رو گیت‌هاب موجود بود
ولی **خیلی عقب‌تر از `main`** (یه کامیتِ قدیمی که PRِ مربوطه‌ش قبلاً merge شده بود، دقیقاً همون حالتِ
«برنچِ merge‌شده» که راهنمای هارنس می‌گه باید ری‌استارت بشه). طبقِ همون راهنما، این برنچ از رو نوکِ
فعلیِ `main` بازسازی شد (`git checkout -B ccr-07146a93-ufo9vg main`) و کامیتِ این نشست (اسپلش+ورود)
روش پوش شد - **این یه استثناست، نه نقضِ قانونِ «همیشه از main شروع کن، رو برنچ‌های قدیمی نساز» که تو
بخشِ «کارهای درحال انجام» پایین‌تر نوشته شده**؛ اون قانون درباره‌ی برنچ‌سازیِ داوطلبانه‌ست، نه برنچِ
اجباریِ خودِ هارنسِ CCR.
- چون این پوش یه پوش به `main` نبود، workflow `build-native-android.yml` خودکار اجرا نشد - با
  `mcp__github__actions_run_trigger` (`workflow_dispatch`) دستی رو همین برنچ اجرا شد.
- **این کار هنوز merge/PR نشده تو `main`** - کاربر درخواستِ PR نداده (طبقِ قانونِ «فقط اگه صریح خواسته
  شد PR بساز»)؛ اگه کاربر بعداً پرسید «پس این تغییرات کجان»، یادت باشه رو `ccr-07146a93-ufo9vg` پوش
  شدن، نه هنوز رو `main`.

**⚠️ اصلاحیه‌ی ۳۰ تیر ۱۴۰۵ (۲۰۲۶-۰۷-۲۱) - فرضِ بالا (این برنچ زائد/بی‌ضرره) غلط از آب دراومد**: یه چتِ کاملاً دیگه،
همون روز، رو دقیقاً همین برنچ (`ccr-07146a93-ufo9vg`) کار کرد و ۲۱ کامیتِ واقعی و بزرگ روش ساخت (فونتِ
سراسریِ Vazirmatn، بازطراحیِ کاملِ صفحه‌ی ورود/OTP + SMS Retriever، سبکِ شیشه‌ای رو باکس‌ها/دکمه‌ها،
فول‌اسکرین‌شدنِ زیرصفحه‌های تنظیمات، تورِ راهنمای اولین ورود، چندتا رفعِ باگِ دارک‌مود/خدماتِ اعتباری،
و بیشتر) - و یه **PR بازِ #64** از این برنچ به `main` باز کرده بود (که تا الان هم merge نشده). یعنی
جمله‌ی «این برنچ زائد/merge‌شده‌ست، بی‌ضرره» تو بخشِ «کارهای درحال انجام» (که پایین‌تر بود) **از اول
اشتباه بود** - این برنچ نه‌تنها زائد نبود، بلکه تنها جاییه که واقعاً فیچرهای بزرگِ بالا توش هست؛
`main` نسبت بهش ۲۱ کامیت عقبه.

چتِ دیگه‌ای (همینی که این خطو می‌نویسه) این تناقض رو کشف کرد چون کاربر گفت یه تغییرِ فونت داده که تو
تاریخچه‌ی برنچِ کاریش (`claude/ios-box-light-intensity-p7ew76`) نبود؛ `git branch -r` + مقایسه‌ی
`git log origin/main..origin/ccr-07146a93-ufo9vg` نشون داد این ۲۱ کامیت اونجان. کاربر صریح گفت
«ادامه‌ی اون برنچ رو اینجا می‌خوام برم» - برنچِ این نشست (`claude/ios-box-light-intensity-p7ew76`) با
`git reset --hard origin/ccr-07146a93-ufo9vg` + پوش، دقیقاً هم‌سطحِ همون ۲۱ کامیت شد و کارِ این نشست
(دورِ سومِ شیشه‌ای‌شدن/Liquid Glass، تنظیمِ شدتِ Aurora، رفعِ باگ‌های bidiِ صفحه‌ی ورود - رجوع کن به
بخشِ «تغییراتِ نشستِ ۳۰ تیر» پایین‌تر) روش اضافه شد.

**درسِ مهم برای چت‌های بعدی**: یه برنچ رو صرفاً از رو نبودنِ ارجاع بهش تو یه چتِ خاص «زائد/merge‌شده»
فرض نکن - همیشه با `git log origin/main..origin/<branch>` (تعدادِ کامیت‌های واقعاً منحصربه‌فرد) و
`list_pull_requests`/PRهای بازِ گیت‌هاب چک کن، قبل از اینکه تو CLAUDE.md بنویسیش «بی‌ضرر»/«شلوغیِ
صرف». بخشِ «برنچ‌های زائد» تو «کارهای درحال انجام» پایین‌تر هم به همین اصلاحیه آپدیت شد.

### رفعِ دیرلود‌شدنِ لوگوهای بانک/خدماتِ اعتباری
کاربر گزارش داد: «خیلی اوقات عکس‌های خدماتِ اعتباری چند ثانیه دیر لود می‌شه». چک شد که این لوگوها
(`assets/banks/*`, `assets/services/*`) فایل‌های محلیِ چندکیلوبایتی‌ان (نه از سرور)، پس تاخیر از
شبکه نبود - از این بود که Coil (کتابخونه‌ی لودِ عکس) اولین باری که هر لوگو رو تو `BankLoanScreen`
می‌بینه تازه شروع به decode می‌کنه، و هیچ crossfade/پیش‌گرم‌کردنی هم نداشت (تصویر یهو «پاپ» می‌کرد).

رفع شد (`LoanCalcApplication.kt`):
- `LoanCalcApplication` حالا `ImageLoaderFactory` رو پیاده می‌کنه؛ `newImageLoader()` یه
  `ImageLoader` با `crossfade(200)` می‌سازه (رو کلِ اپ، همه‌ی `AsyncImage`ها - لوگو، عکسِ رسیدِ چک/وام).
- تو `onCreate()`، همون لحظه‌ی باز شدنِ اپ (هم‌زمان با اسپلش)، همه‌ی لوگوهای `banks + creditServices`
  با `Coil.imageLoader(this).enqueue(...)` تو کشِ حافظه‌ی Coil پیش‌گرم می‌شن - وقتی کاربر واقعاً به
  تبِ «وام بانکی» می‌رسه، decode از قبل تموم شده.
  **نکته‌ی مهم**: پیش‌گرم‌کردن حتماً باید رو singletonِ واقعیِ Coil انجام بشه (`Coil.imageLoader(this)`)،
  نه یه نمونه‌ی جداگانه از `newImageLoader()` مستقیم - وگرنه کشش با AsyncImageهای بقیه‌ی اپ به اشتراک
  گذاشته نمی‌شه و کل هدف بی‌اثر می‌مونه.
- این کار هنوز رو گوشیِ واقعی تست نشده (سندباکس بیلد نمی‌کنه).

### ویرایشِ مشخصات برای همه‌ی وام‌ها (نه فقط وام‌های دستی)
کاربر خواست: «ادیت کل وام هم برای همه وام‌ها فعال کن» - قبلاً آیکونِ ویرایش تو `LoanDetailScreen`
فقط رو وام‌های دستی نشون داده می‌شد (چون فرمِ کاملِ `AddManualLoanScreen`، شاملِ مبلغِ هر قسط و
تعدادِ کل، فقط برای ساختارِ «همه‌ی اقساط برابر» معنی داره).

- **تصمیم**: آیکونِ ویرایش الان رو **همه‌ی وام‌ها** هست، ولی مقصدش فرق می‌کنه:
  - وام‌های دستی: همون فرمِ کاملِ قبلی (اسم/بانک/مبلغِ قسط/تعدادِ کل/تاریخ) - بدونِ تغییر.
  - وام‌های محاسبه‌شده/قرض‌الحسنه: یه دیالوگِ سبکِ جدید همون‌جا تو `LoanDetailScreen`
    (`showEditMetaDialog`) که فقط اسم/بانک/وام‌گیرنده/تاریخِ شروع رو می‌ذاره عوض کنی -
    **عمداً مبلغ/نرخ/تعدادِ اقساط قابلِ‌ویرایش نیست** چون این‌ها از رو فرمولِ کاملِ
    `LoanCalculator` محاسبه شدن؛ عوض‌کردنشون نیاز به اجرای دوباره‌ی همون فرمول داره که یعنی
    تاریخچه‌ی پرداختِ تک‌تکِ اقساط (کدوم پرداخت شده/با تاخیر/عکسِ رسید) پاک می‌شه - این ریسک عمداً
    قبول نشد، فقط فیلدهای غیرمالیِ امن قابلِ‌ویرایشن.
- `LoanRepository.updateLoanMeta(loan, name, bank, borrower, startDate)` (جدید): فقط
  `name`/`bank`/`borrower`/`startDate` رو تو `dataJson` عوض می‌کنه، به `amount`/`installment`/`n`/
  `rows`/`paidCount` دست نمی‌زنه. `getBorrower(loan)` هم برای پرکردنِ فیلدِ وام‌گیرنده‌ی دیالوگ اضافه
  شد (وام‌های دستی این فیلد رو ندارن، «—» برمی‌گردونه).
- اگه بعداً کاربر خواستِ ویرایشِ مبلغ/نرخ/تعداد رو هم برای وام‌های محاسبه‌شده، باید صریح دوباره
  ازش پرسیده بشه که آیا قبولِ ریسکِ پاک‌شدنِ تاریخچه‌ی پرداخت هست یا نه - این‌جا از قبل تصمیم گرفته
  نشده، فقط مسیرِ امن پیاده شده.

### تورِ اولین ورود - نسخه‌ی نهایی: اورلیِ spotlight داخلِ خودِ اپ (بعدِ یه دورِ رد شدن)
اولین پیاده‌سازی (یه `PersonalizationScreen.kt` با سوالِ «الان دنبالِ چی هستی؟» قبل از ورود + یه
`TourScreen.kt` با کارت‌های متوالیِ تمام‌صفحه، هر دو به‌عنوانِ گیت‌های جدا تو `AppRoot` قبل از ورود
به اپ) کاربر بعدِ دیدنش **صریحاً رد کرد**: «ببین می‌خوام تو خود برنامه بگه کجا بری. و آموزش اولیه
بده نه قبل ورود / و اینکه اون تیکه اول هم که چی می‌خوای رو حذف کن خوب نشده تو همون آموزش اولیه خود
کاربر می‌فهمه دیگه». یعنی دو نکته: (۱) سوالِ «چی می‌خوای» اصلاً حذف بشه - کاربر خودش تو حینِ تور
می‌فهمه. (۲) خودِ تور باید **داخلِ برنامه‌ی واقعی** باشه (رو المان‌های واقعی)، نه یه صفحه‌ی جدا و
مجزا قبل از ورود.

**هر دو فایل (`PersonalizationScreen.kt`, `TourScreen.kt`) کاملاً حذف شدن** و به‌جاشون:
- **`TabTourOverlay`** (کامپوننتِ جدید، تو خودِ `MainActivity.kt`، نه فایلِ جدا): یه اورلیِ
  spotlightِ واقعی رو نوارِ تبِ پایینِ اپ - یه لایه‌ی تیره‌ی نیمه‌شفاف (`Canvas` + `drawPath`) کلِ
  صفحه رو می‌پوشونه با یه «سوراخِ» گردگوشه دقیقاً دورِ تبِ فعلی، با `Path().op(scrim, hole,
  PathOperation.Difference)` (تفاضلِ مسیرِ سبکِ Skia). مختصاتِ واقعیِ هر تب از رو
  `Modifier.onGloballyPositioned { it.boundsInRoot() }` رو خودِ `BottomNavItem` اندازه‌گیری و تو
  یه `mutableStateMapOf<BottomTab, Rect>` (`tabBounds`، تو `LoanCalcApp`) نگه داشته می‌شه - **نه
  یه مختصاتِ حدسی/هاردکد**، پس اگه بعداً اندازه/چیدمانِ نوارِ تب عوض بشه، سوراخ خودش هماهنگ می‌مونه.
  کارتِ توضیح (عنوانِ تب + `tourHint` - یه رشته‌ی جدید رو خودِ `BottomTab` enum، یکی به‌ازای هر تب)
  همیشه بالای نوارِ تب ثابت می‌مونه، فقط سوراخ جابه‌جا می‌شه؛ دکمه‌ی «بعدی»/«متوجه شدم» قدم رو جلو
  می‌بره، «رد کن» کل تور رو می‌بنده، لمسِ هرجای دیگه‌ی صفحه هم مثلِ «بعدی» عمل می‌کنه.
- نمایش: **بعدِ ورود به `LoanCalcApp`** (نه قبلش) - آخرین بچه‌ی `Box` بیرونیِ `LoanCalcApp`، رو
  همه‌چیزِ دیگه (پنلِ تنظیمات، بنرِ آپدیت) می‌شینه، گیت‌شده با `AuthPrefs.tourSeen` (همون کلید/الگوی
  قبلی، فقط دیگه به یه صفحه‌ی جدا اشاره نمی‌کنه - رجوع کن به کامنتِ آپدیت‌شده‌ش).
  `AppRoot`/`LoanCalcApp` دیگه هیچ گیتِ اضافه‌ای قبل از `AnimatedAppEntrance { LoanCalcApp() }`
  ندارن (نه `preferredTabRoute`، نه `initialRoute` رو سیگنیچرِ `LoanCalcApp`، نه سوالِ
  شخصی‌سازی) - `AppRoot` دقیقاً به حالتِ قبل از این فیچر برگشت.
- این کار هنوز رو گوشیِ واقعی تست نشده (سندباکس بیلد نمی‌کنه؛ نه `Path.op`ِ خودارجاع نه محاسبه‌ی
  `boundsInRoot` تا الان با یه بیلدِ واقعی تایید نشدن) - اگه کاربر گزارش داد سوراخ درست جای تب
  نمی‌شینه یا کارت جای بدی می‌شینه، اول `TabTourOverlay` تو `MainActivity.kt` رو چک کن.

### رفعِ باگِ «خدماتِ اعتباری نمیاد» (شیمر نامرئی رو تمِ روشن) + گسترشِ تور به AppTourOverlay
کاربر با اسکرین‌شات گزارش داد بخشِ «خدماتِ اعتباری» (دیجی‌پی/اسنپ‌پی/...) تو تبِ «وام بانکی» کلاً
خالی می‌مونه - نه لوگو، نه حتی اسکلتونِ لودینگ.

- **ریشه‌ی باگ**: `CreditRatesViewModel` همیشه با لیستِ استاتیکِ `creditServices` (۶ آیتم، تو
  `Banks.kt`) شروع می‌شه و فقط اگه fetchِ سرور (`GET /api/credit-rates`) موفق و غیرخالی بود
  جایگزینش می‌کنه - پس لیست هیچ‌وقت واقعاً خالی نمی‌مونه. مشکلِ واقعی تو خودِ اسکلتونِ لودینگ
  (`BankTileShimmer`/`shimmerEffect()` تو `ShimmerEffect.kt`) بود: گرادیانِ شیمر از `Color.White`
  با آلفای پایین (۰.۰۶-۰.۱۸) ساخته می‌شد - رو تمِ تیره (پس‌زمینه‌ی سرمه‌ای) قابل‌دیدن بود، ولی رو
  تمِ **روشن** (کاربر تو اسکرین‌شات تمِ روشن فعال داشت - `AppSurface` تو تمِ روشن دقیقاً
  `0xFFFFFFFF`ه) یه سفیدِ تقریباً شفاف رو یه پس‌زمینه‌ی سفید عملاً نامرئیه. یعنی تا ۱۵ ثانیه (تایم‌اوتِ
  `ApiClient`) خدماتِ اعتباری واقعاً داشت لود می‌شد، فقط اسکلتونش دیده نمی‌شد و به کاربر حسِ «هیچی
  نداره» می‌داد.
- **رفع**: `shimmerEffect()` الان از `AppMuted` (خاکستریِ میان‌رنگِ تم‌آگاه، به‌جای `Color.White`ِ
  ثابت) با آلفای ۰.۱۲-۰.۳۲ استفاده می‌کنه - رو هر دو تمِ روشن/تیره کنتراستِ کافی داره. این تنها
  مصرف‌کننده‌ی `shimmerEffect()` تو کلِ اپه، پس این تغییر فقط رو همین بخش اثر داره.
- **اگه کاربر بعداً بازم گزارش داد خدماتِ اعتباری خالیه (نه فقط دیر)**: چون فالبکِ استاتیک همیشه
  غیرخالیه، تنها راهی که واقعاً لیست خالی بمونه اینه که سرور (`GET /api/credit-rates`) موفق ولی
  یه fetchِ *غیرخالیِ اشتباه* برگردونه که موقعِ `map { it.toBankEntry() }` استثنا بندازه (بیرون از
  try/catch داخلیِ رنگ که با `runCatching` محافظت شده) - سندباکس نمی‌تونه به VPS متصل بشه
  (پراکسی 403 می‌ده) پس این مسیر از اینجا قابل‌تستِ مستقیم نیست؛ باید `pm2 logs` رو VPS چک بشه.

**گسترشِ تور** (کاربر همون پیام: «تو توضیحات اول حالت پرایوسی رو بگو، دارک خود رو بگو، تنظیمات بگو،
چک رو بگو، افزودنِ دستی صفحه‌ی آخر رو بگو، تو تنظیمات تقویمِ مالی رو بگو») - تورِ قبلی (رجوع کن به
بخشِ بالا) فقط ۴ تبِ نوارِ پایین رو پوشش می‌داد؛ الان به یه ساختارِ عمومی‌ترِ `TourTarget` (enum جدید،
تو `MainActivity.kt`، جایگزینِ `tourHint` رو خودِ `BottomTab`) گسترش پیدا کرد، با ۸ قدم به این
ترتیب: **حالتِ خصوصی → تمِ روشن/تاریک → تنظیمات (توضیحش هم چک هم تقویمِ مالی رو مستقیم اسم می‌بره،
چون هر دو زیرمجموعه‌ی همون پنلِ تنظیماتن - رجوع کن به `matches("امور چک")`/`matches("تقویم
مالی")` تو `SettingsScreen.kt`) → وام بانکی → محاسبه‌گر → سود سپرده → وام‌های من → افزودنِ دستیِ
وام (آخرین قدم)**.
- سه قدمِ اول (حالتِ خصوصی/تمِ روشن‌تاریک/تنظیمات) آیکون‌های خودِ `TopAppBar`ن (همیشه رو هر تبی
  رندر می‌شن) - هرکدوم یه `Modifier.onGloballyPositioned` گرفتن که مختصاتشون رو تو همون
  `tourBounds` (نگاشتِ عمومی‌شده‌ی `mutableStateMapOf<TourTarget, Rect>`، جایگزینِ `tabBounds`ِ
  قبلی) ثبت می‌کنه.
- قدمِ آخر (افزودنِ دستیِ وام) دکمه‌ی FAB تو `MyLoansScreen`ه - چون این دکمه فقط وقتی واقعاً رو تبِ
  «وام‌های من» هستی رندر می‌شه، `AppTourOverlay` یه پارامترِ جدید `onStepChanged: (TourTarget) ->
  Unit` گرفت که موقعِ عوض‌شدنِ هر قدم صدا زده می‌شه؛ `LoanCalcApp` توش چک می‌کنه اگه اون قدم به یه
  تبِ خاص وصله (`TourTarget.asBottomTab()`) و اگه الان رو اون تب نیستی، خودکار `navController.
  navigate(...)` می‌زنه. این یعنی تور الان واقعاً بینِ تب‌ها ناوبری می‌کنه (نه فقط آیکونِ نوارِ پایین
  رو نشون بده) - هم برای ۴ تبِ اصلی هم مخصوصاً برای قدمِ آخر که بدونِ این ناوبری اصلاً چیزی برای
  اسپاتلایت‌کردن نداشت. `MyLoansScreen` یه پارامترِ جدید `onManualAddFabPositioned: (Rect) -> Unit`
  گرفت که تو `composable(BottomTab.MY_LOANS.route) { ... }` به `tourBounds[TourTarget.MANUAL_ADD]`
  وصل می‌شه.
- کامپوننتِ اورلی از `TabTourOverlay` به `AppTourOverlay` تغییرِ اسم داد (چون دیگه فقط مخصوصِ
  تب‌های نوارِ پایین نیست).
- این کار هم مثلِ نسخه‌ی قبلیِ تور هنوز رو گوشیِ واقعی تست نشده - اگه کاربر گزارش داد ناوبریِ
  خودکارِ بینِ تب‌ها موقعِ تور حسِ عجیب/سریع می‌ده، یا قدمِ افزودنِ دستی درست FAB رو پیدا نمی‌کنه،
  اول `AppTourOverlay`/`onStepChanged` تو `MainActivity.kt` رو چک کن.

### گسترشِ تور به ردیف‌های داخلِ خودِ پنلِ تنظیمات (تقویمِ مالی + امورِ چک)
کاربر بعدِ دیدنِ گسترشِ قبلی (که چک/تقویمِ مالی رو فقط تو متنِ توضیحِ قدمِ SETTINGS ذکر می‌کرد، بدونِ
اسپاتلایتِ مستقل) گفت: «برای چک و تقویم مالی که تو تنظیمات هم هست بزار» - یعنی این دوتا باید قدمِ
مستقلِ خودشون رو با اسپاتلایتِ واقعی رو ردیفِ خودشون *داخلِ* پنلِ تنظیمات داشته باشن، نه فقط یه اشاره‌ی
متنی.

- دو `TourTarget` جدید اضافه شد: `SETTINGS_CALENDAR` و `SETTINGS_CHEQUE` (بینِ `SETTINGS` و
  `BANK_LOAN`، تو ترتیبِ تور). متنِ قدمِ `SETTINGS` خودش کوتاه‌تر شد (دیگه مستقیم اسمِ این دوتا رو
  نمی‌بره، چون حالا قدمِ اختصاصیِ خودشون رو دارن).
- **ترفندِ کلیدی برای پیداکردنِ ردیف بدونِ نیازِ اسکرول**: پنلِ تنظیمات از قبل یه نوارِ جستجوی واقعی
  داره (`searchQuery`/`matches()` تو `SettingsMainContent`) که لیستِ بلندِ بخش‌ها رو فیلتر می‌کنه.
  به‌جای پیاده‌سازیِ scroll-to-position دستی (که برای یه Column با `verticalScroll` نیازمندِ
  اندازه‌گیریِ آفستِ هر آیتم قبل از اسکرول‌کردنه)، وقتی این دو قدم فعاله، `SettingsScreen`/
  `SettingsMainContent` یه پارامترِ جدید `tourHighlightQuery: String?` می‌گیرن که مستقیم مقدارِ
  `searchQuery` رو ست می‌کنه (دقیقاً مثلِ این‌که کاربر خودش «تقویم مالی» یا «امور چک» رو تو نوارِ
  جستجو تایپ کرده باشه) - همینِ فیلتر خودش تضمین می‌کنه فقط همون یه ردیف (به‌علاوه‌ی چندتا کارتِ
  همیشه‌نمایانِ بالای لیست که به `matches()` وابسته نیستن) نمایان بمونه، بدونِ نیاز به اسکرولِ اضافه.
- مسیرِ داده: `LoanCalcApp` یه `currentTourTarget: TourTarget?` تازه نگه می‌داره (از رو
  `onStepChanged` پر می‌شه)؛ موقعِ `SETTINGS_CALENDAR`/`SETTINGS_CHEQUE`، `showSettings = true` هم
  ست می‌شه (پنل خودکار باز بشه) و از رو `currentTourTarget` مقدارِ `tourHighlightQuery` مناسب
  ("تقویم مالی" یا "امور چک") به `SettingsScreen(...)` پاس داده می‌شه؛ `onTourRowPositioned` هم
  مختصاتِ ردیفِ واقعاً رندرشده رو برمی‌گردونه و تو `tourBounds[currentTourTarget]` ذخیره می‌شه. برای
  همه‌ی قدم‌های دیگه (تب‌ها/آیکون‌های TopAppBar)، `showSettings` صریحاً `false` می‌شه تا پنل رو
  چهره‌ی نوارِ پایین/محتوای تب نمونه.
- تو خودِ `SettingsMainContent.kt`، دو تا `AppCard` (ردیفِ «تقویم مالی» و «امور چک»، هرکدوم زیرِ
  `if (matches(...))` خودشون) یه `Modifier.onGloballyPositioned { onTourRowPositioned(it.
  boundsInRoot()) }` گرفتن - دقیقاً همون الگوی `BottomNavItem`/آیکون‌های `TopAppBar`، نه یه
  مختصاتِ حدسی.
- این گسترش هم مثلِ بقیه‌ی تور هنوز رو گوشیِ واقعی تست نشده - اگه کاربر گزارش داد پنلِ تنظیمات موقعِ
  این دو قدم دیر باز می‌شه/ردیف هنوز پایینِ صفحه‌ست، اول `tourHighlightQuery`/`onTourRowPositioned`
  تو `SettingsScreen.kt` و `currentTourTarget` تو `MainActivity.kt` رو چک کن.

### باکس‌ها/دکمه‌های شیشه‌ای (سبکِ «تقویم من») + رفعِ پرشِ افکتِ طلاییِ دکمه‌ها
کاربر با اسکرین‌شات یه اپِ رقیب («تقویم من») خواستِ باکس‌های بزرگِ اپ («دقیقاً همون فاصله همون رنگ»)
شیشه‌ای بشن. **قبل از هر تغییری، یه پیش‌نمایشِ HTML/Artifact با تاگلِ قبل/بعد ساخته و لینکش داده
شد** (خواسته‌ی صریحِ کاربر: «یکبار بهم نشون بده قبل انجام دادن»)؛ بعدِ دو دور فیدبک (باکس‌های بخشِ
پایینِ فرمِ «وام بانکی» هم اضافه شد، بعد خودِ دکمه‌ی «محاسبه کن» هم) و تاییدِ نهایی («پیاده کن»)، واقعاً
تو کد پیاده شد.

- **`ui/theme/Color.kt`**: چهار توکنِ رنگِ جدید به `AppColorPalette` اضافه شد -
  `glassBase`/`glassGradientStart`/`glassGradientEnd`/`glassBorder` - هرکدوم برای دو تمِ روشن/تیره
  جداگانه تعریف شدن، **نه یه معکوسِ ساده‌ی هم**: تو تمِ تیره یه گرادیانِ نورِ *سفید* رو یه پایه‌ی
  سرمه‌ایِ نیمه‌شفاف می‌شینه (چون زمینه تیره‌ست، «نور» باید از زمینه روشن‌تر باشه)؛ تو تمِ روشن یه
  گرادیانِ *سایه‌ی تیره* رو یه پایه‌ی سفیدِ نیمه‌شفاف می‌شینه (چون زمینه از قبل روشنه، سفیدِ اضافه اثر
  نداره - سایه‌ی ظریف حسِ لبه‌ی شیشه رو می‌ده). `AppGlassBase`/`AppGlassGradientStart`/
  `AppGlassGradientEnd`/`AppGlassBorder` به همون الگویِ `@Composable get()` بقیه‌ی توکن‌ها expose شدن.
- **`ui/components/AppCard.kt`**: چون Material3 `Surface` فقط `color: Color` تخت قبول می‌کنه نه
  `Brush`، حالتِ پیش‌فرض (بدونِ `backgroundColor` override) از `Surface` به یه `Column` با
  `Modifier.shadow().clip().background(glassBase).background(glassGradient).border()` تغییر کرد.
  **مسیرِ `backgroundColor` صریح دست‌نخورده موند** (همون `Surface` تخت‌رنگِ قدیمی) - چون قبلاً معلوم
  شده بود رنگِ نیمه‌شفافِ اضافه رو یه پس‌زمینه‌ی از قبل رنگی دوتُنی/کثیف می‌شه (رجوع کن به کامنتِ
  خودِ فایل، دربارهٔ کارتِ تحلیلِ درآمدِ `MyLoansScreen`). `borderColor` صریح (مثلِ کارتِ «وام‌های
  پرتکرار») هم دست‌نخورده - همچنان override می‌شه، فقط پیش‌فرضِ بدونِ override از `Color.Transparent`
  به `AppGlassBorder` عوض شد.
- **`ui/components/GradientButton.kt`** (دکمه‌ی CTA اصلیِ مشترکِ تقریباً همه‌ی صفحه‌ها، نه فقط
  «محاسبه کن»):
  - پس‌زمینه از گرادیانِ تخت‌رنگِ `AppPrimary`→`AppPrimaryDim` به یه گرادیانِ فیروزه‌ایِ نیمه‌شفاف رو
    همون `AppGlassBase`ی باکس‌ها تغییر کرد - هم زبونِ شیشه‌ای رو می‌گیره هم هویتِ رنگیِ CTA (فیروزه‌ای)
    گم نمی‌شه. رنگِ متن از سیاهِ ثابتِ `Color(0xFF04211C)` (مخصوصِ زمینه‌ی روشنِ قبلی) به `AppText`ِ
    تم‌آگاه عوض شد چون رو زمینه‌ی شیشه‌ایِ تیره‌ترِ جدید (چه روشن چه تیره) خوانا می‌مونه.
  - **باگِ پرشِ نوارِ نورِ طلایی (رفع شد)**: قبلاً بازه‌ی حرکتِ نوار یه عددِ ثابتِ حدسی بود
    (`shimmerX * 300f`، بدونِ توجه به عرضِ واقعیِ دکمه) - رو دکمه‌های پهن‌تر (مثلِ «محاسبه کن» که
    `fillMaxWidth()`ه)، چرخه‌ی بعدی (`RepeatMode.Restart`) وقتی نوار هنوز *داخلِ* دکمه بود ریست
    می‌شد، پس هر ~۲.۲ ثانیه یه پرشِ دیدنی داشت. الان `Modifier.onSizeChanged` عرضِ واقعیِ دکمه رو
    اندازه می‌گیره و مرکزِ نوار از `-(نصفِ عرض + نصفِ عرضِ نوار)` تا `+(نصفِ عرض + نصفِ عرضِ نوار)`
    حرکت می‌کنه - یعنی هر دو سرِ حرکت **کاملاً بیرون از لبه‌های دکمه‌ست**، پس لحظه‌ی ریست‌شدنِ چرخه
    دقیقاً وقتیه که نوار نامرئیه و هیچ پرشی حس نمی‌شه.
- هر دو تغییر هنوز رو گوشیِ واقعی تست نشدن (سندباکس بیلد نمی‌کنه) - اگه کاربر گزارش داد شیشه‌ای‌بودن
  کنتراستِ کافی نداره (خصوصاً تمِ روشن) یا نوارِ طلایی هنوز پرش داره، اول همین چهار توکنِ `AppGlass*`
  تو `Color.kt` و محاسبه‌ی `centerX`/`halfStreak` تو `GradientButton.kt` رو چک کن.
- پیش‌نمایشِ HTML که برای تاییدِ کاربر ساخته شده بود (Artifact، نه بخشی از ریپو) فقط برای نمایش/تاییدِ
  اولیه بود؛ منبعِ حقیقتِ نهایی همیشه همین کدِ Compose بالاست.

### ورود/OTP: بازطراحیِ چیدمان + پرکردنِ خودکارِ کاملاً بی‌صدا (SMS Retriever) + نوارِ طلاییِ ظریف‌تر
کاربر با دو اسکرین‌شات از یه اپِ دیگه (فرمِ ورود با شماره + صفحه‌ی کدِ تایید با باکس‌های جداگانه)
سه خواسته داد: (۱) چیدمانِ این دو صفحه شبیه‌شون بشه، (۲) پرکردنِ خودکارِ کد **بدونِ هیچ دیالوگ/
مجوزی** («بعضی برنامه‌ها اجازه می‌گیرن این ولی نبود» - یعنی دقیقاً برعکسِ چیزی که قبلاً پیاده بود)،
(۳) نوارِ طلاییِ رو دکمه‌ها خیلی ظریف‌تر/کم‌رنگ‌تر بشه، «انگار یک سایه‌ست».

- **`ui/components/GradientButton.kt`**: آلفای نوارِ طلایی از ۰.۶ به ۰.۱۴ کم شد.
- **`ui/auth/SmsOtpAutofill.kt`** (بازنویسیِ کامل، رفعِ سوءتفاهمِ قدیمی): نسخه‌ی قبلی با
  `SmsRetriever.startSmsUserConsent()` کار می‌کرد که یه دیالوگِ سیستمیِ «اجازه می‌دی بخونم؟» نشون
  می‌داد - دقیقاً همون چیزی که کاربر نمی‌خواستش. الان با **`startSmsRetriever()`** (کاملاً بی‌صدا،
  بدونِ هیچ UI/تاییدی) جایگزین شد؛ تابع از `SmsUserConsentEffect` به `SmsRetrieverEffect` تغییرِ
  اسم داد.
  **محدودیتِ مهمِ گوگل**: SMS Retriever برخلافِ User Consent فقط پیامکی رو می‌گیره که متنش با یه
  هشِ ۱۱کاراکتریِ مخصوصِ همین اپ (بسته‌نام + گواهیِ امضا) تموم بشه - این هش رو خودِ گوگل رو کلاینت
  حساب نمی‌کنه، باید تو *متنِ پترنِ پیامک* (سمتِ ملی‌پیامک) هم اضافه بشه. چون این هش به کلیدِ امضا
  وابسته‌ست (دیباگ ≠ ریلیز) و کلیدِ ریلیزِ واقعی رو سندباکس در دسترس نیست، از اینجا قابلِ‌محاسبه
  نبود - یه فایلِ جدید `ui/auth/SmsRetrieverHash.kt` این هش رو (با همون الگوریتمِ رسمیِ
  `AppSignatureHelper`ی گوگل) رو خودِ گوشیِ کاربر محاسبه و لاگ می‌کنه
  (`LoanCalcApplication.onCreate()` صداش می‌زنه). **کارِ باقی‌مونده برای کاربر**: هر بیلدی رو نصب
  کنه (حتی یه دیباگِ CI برای تستِ اولیه کافیه)، با `adb logcat -s SmsRetrieverHash` هش رو بگیره، و
  به‌عنوانِ خطِ آخرِ پترنِ ملی‌پیامک اضافه کنه - رجوع کن به بخشِ OTP بالا برای جزئیاتِ کامل. تا اون‌موقع
  پرکردنِ خودکار بی‌صدا هیچ‌کاری نمی‌کنه (نه کرش، فقط منتظر می‌مونه)؛ تایپِ دستیِ کد همیشه کار می‌کنه.
- **`ui/auth/LoginScreen.kt`** (بازطراحیِ چیدمان):
  - مرحله‌ی شماره‌موبایل: فیلد+دکمه حالا تویِ یه `AppCard` (شیشه‌ای) قرار دارن - همون حسِ «کارتِ
    خوش‌آمد» رفرنس، با یه هدینگِ گرم‌ترِ «به «وام من» خوش اومدی!» بالاش.
  - مرحله‌ی کد: کاملاً بازطراحی شد -
    - `OtpBoxRow` (کامپوننتِ جدید): ۵ باکسِ جداگانه (به‌جای یه `OutlinedTextField` خطی) - یه
      `BasicTextField` نامرئی (اندازه‌ی صفر، فقط برای IME/فوکوسِ واقعی) زیرِ یه `decorationBox`
      سفارشی که باکس‌ها رو رسم می‌کنه؛ تپ‌کردن رو خودِ باکس‌ها هم فوکوس رو برمی‌گردونه.
    - شماره‌ی واردشده + آیکونِ مداد بالای باکس‌ها نشون داده می‌شه - تپ‌کردنش برمی‌گردونه به مرحله‌ی
      شماره برای ویرایش (جایگزینِ نداشتنِ هیچ راهِ برگشتی قبلی).
    - **دکمه‌ی «ارسالِ مجدد» جدید** (قبلاً اصلاً نبود) کنارِ دکمه‌ی «تایید و ورود»، با شمارش‌معکوسِ
      ۶۰ثانیه‌ای - دقیقاً هم‌قدم با `OTP_RESEND_COOLDOWN_MS` سمتِ سرور (`AuthRoutes.kt`) تا وقتی
      فعال می‌شه سرور واقعاً درخواستِ جدید رو قبول کنه.
    - **اتوساب‌میت**: چه کد از رو پیامک خودکار پر بشه چه کاربر دستی تایپ کنه، به محضِ کامل‌شدنِ ۵
      رقم خودکار `verify()` صدا زده می‌شه (حسِ «همینکه کد اومد وارد شد» که کاربر خواسته بود).
- این کار هنوز رو گوشیِ واقعی تست نشده (سندباکس بیلد نمی‌کنه) - چون واقعاً هیچ OTPِ واقعی‌ای هنوز
  پیامک نمی‌شه (رجوع کن به بخشِ OTP بالا)، خودِ پرکردنِ خودکار هم قابلِ‌تستِ کاملِ end-to-end نیست
  تا اون فعال بشه؛ تایپِ دستی و بقیه‌ی چیدمان رو می‌شه همین الان تست کرد.

### دورِ اولِ فیدبکِ واقعیِ گوشی رو تغییراتِ شیشه‌ای/طلایی/OTP (۴ باگِ کوچیک)
کاربر بعدِ نصبِ واقعیِ بیلدِ جدید، با سه اسکرین‌شات (دارک‌مود، لایت‌مود، تبِ محاسبه‌گر) ۴ تا مشکلِ
مشخص گزارش داد:

- **متنِ «وام‌های پرتکرار» تو دارک‌مود نامرئی بود**: `PresetCard.kt` - `Text(text = title, ...)`ِ
  عنوانِ هر کارت (مثل «وام مسکن»، «وام ازدواج») اصلاً `color` نداشت، پس رنگِ پیش‌فرضِ آمبیانتِ
  Compose (که تو این نقطه مشکی درمیاد) رو می‌گرفت - رو زمینه‌ی روشنِ تمِ روشن به‌سختی دیده می‌شد ولی
  رو زمینه‌ی سرمه‌ایِ دارک‌مود کاملاً نامرئی بود. حاشیه‌ی کارت (`AppText.copy(alpha=0.5f)`) از قبل
  درست تم‌آگاه بود، فقط خودِ متن جا افتاده بود. رفع شد با اضافه‌کردنِ `color = AppText`.
- **باکس‌های تمِ روشن اصلاً شیشه‌ای دیده نمی‌شدن**: مقادیرِ اولیه‌ی `LightAppColors` (پایه‌ی سفیدِ
  ۷۰٪ + گرادیانِ سایه‌ی ۸٪ + حاشیه‌ی ۱۲٪) رو زمینه‌ی از قبل تقریباً سفیدِ تمِ روشن عملاً اثری نداشت -
  با یه سفیدِ نیمه‌شفاف رو یه سفیدِ کامل، تفاوت به‌سختی حس می‌شه. کنتراست پررنگ‌تر شد: پایه به ۵۰٪
  نیمه‌شفاف‌تر شد (پس‌زمینه/Aurora بیشتر ازش رد می‌شه)، گرادیانِ سایه به ۱۸٪ و حاشیه به ۲۵٪ افزایش
  پیدا کرد - رجوع کن به `Color.kt`.
- **خدماتِ اعتباری هنوز دیر لود می‌شد**: باگِ نامرئی‌بودنِ شیمر (دورِ قبل) رفع شده بود، ولی خودِ
  fetch از سرور واقعاً کند بود - تا تایم‌اوتِ سراسریِ OkHttp (۱۵ ثانیه) طول می‌کشید تا fallback
  استاتیک نشون داده بشه. چون این فقط یه به‌روزرسانیِ نرخِ جانبیه (نه یه عملیاتِ حیاتی مثل ورود/سینک
  که نباید تایم‌اوتش سراسری کوتاه بشه)، `CreditRatesViewModel` الان خودش با
  `withTimeoutOrNull(4000L)` این یه fetch رو جدا و کوتاه‌تر محدود می‌کنه - بعد از ۴ ثانیه، حتی اگه
  سرور هنوز جواب نداده، فالبکِ استاتیک نشون داده می‌شه.
- **گوشه‌ی دکمه‌ی گرادینتی با بقیه‌ی دکمه‌ها هم‌شکل نبود**: کاربر خواستِ گوشه‌ی دکمه‌ی «محاسبه
  حداکثر وام»/«محاسبه کن» (که `GradientButton`ه) دقیقاً هم‌اندازه‌ی گوشه‌ی دکمه‌ی «محاسبه نرخ سود»
  (یه `OutlinedButton` معمولی) بشه. `GradientButton.kt` قبلاً یه `RoundedCornerShape(10.dp)`ِ
  مستقل/هاردکد داشت؛ `OutlinedButton`های اپ شکلشون رو از `MaterialTheme.shapes.small`
  (`AppShapes` تو `Theme.kt`، = `14.dp`) می‌گیرن. الان `GradientButton` هم مستقیم از همون توکن
  استفاده می‌کنه، پس همیشه با دکمه‌های outline هم‌شکل می‌مونه.
- این ۴ رفع هنوز خودِ کاربر رو گوشیِ واقعی تایید نکرده - اگه بازم گزارش داد کنتراستِ تمِ روشن کافی
  نیست یا خدماتِ اعتباری هنوز کند حس می‌شه، اول همین چهار محل (`PresetCard.kt`، `Color.kt`،
  `CreditRatesViewModel.kt`، `GradientButton.kt`) رو چک کن.

### رفعِ ریشه‌ایِ متنِ نامرئیِ دارک‌مود - AppCard دیگه LocalContentColor رو گم نمی‌کنه
کاربر بلافاصله بعدِ رفعِ `PresetCard.kt` گزارش داد اسمِ بانک‌ها (تو `BankTile.kt`) هم رو دارک‌مود
مشکی/نامرئیه. به‌جای رفعِ تک‌تکِ همین‌جور مواردی که ممکنه هنوز جایی تو اپ باشن، ریشه‌ی واقعی پیدا و
رفع شد:

- **ریشه**: `Surface` (که `AppCard` قبل از شیشه‌ای‌شدن ازش استفاده می‌کرد) خودش خودکار
  `LocalContentColor` رو با `contentColorFor(color)` از رو رنگِ زمینه‌ش حساب و ست می‌کرد - برای
  همین `Text`های داخلِ هر `AppCard` که `color` صریح نداشتن (مثلِ اسمِ بانک تو `BankTile`، عنوانِ
  `PresetCard`) خودکار رنگِ درستِ تم‌آگاه می‌گرفتن، بدونِ نیاز به‌کدنویسیِ صریح. وقتی مسیرِ پیش‌فرضِ
  `AppCard` (بخشِ «باکس‌های شیشه‌ای») از `Surface` به یه `Column` خام تغییر کرد (چون `Surface` فقط
  `Color` تخت قبول می‌کنه نه `Brush`، رجوع کن به بخشِ «باکس‌ها/دکمه‌های شیشه‌ای» بالا)، این محاسبه‌ی
  خودکار گم شد و هر `Text` بدونِ رنگِ صریح داخلِ هر `AppCard`ی، دارک‌مود بالقوه مشکی/نامرئی می‌شد -
  نه فقط `PresetCard`/`BankTile`، بلکه هر جای دیگه‌ی مشابهی هم تو کل اپ که تا الان گزارش نشده.
- **رفع**: `AppCard.kt` تو مسیرِ شیشه‌ای الان محتوا رو با
  `CompositionLocalProvider(LocalContentColor provides AppText) { ... }` می‌پیچونه - دقیقاً همون
  رفتاری که `Surface` خودکار می‌داد، این‌بار صریح. مسیرِ `backgroundColor` (که هنوز از `Surface`
  استفاده می‌کنه) نیازی به این تغییر نداشت.
- **نکته‌ی مهم برای چت‌های بعدی**: تغییرِ تو `PresetCard.kt` (اضافه‌کردنِ `color = AppText` صریح)
  دیگه لازم نبود ولی بی‌ضرره (redundant، نه اشتباه) - پاکش نکردیم. اگه کاربر بازم گزارش داد جایی تو
  اپ متنِ نامرئی/مشکیِ دیگه‌ای دیده، این‌بار باید یه علتِ *دیگه*باشه (چون ریشه‌ی اصلی همینجا رفع شد) -
  اول چک کن اون `Text` خاص شاید بیرون از هر `AppCard`ی باشه (مثلاً مستقیم تو `Scaffold`), که اونجا
  `LocalContentColor` از یه مسیرِ کاملاً متفاوت میاد.

### فول‌اسکرین‌شدنِ «ورود» از تنظیمات + رفعِ جهتِ معکوسِ رقم‌ها + رفعِ پرشِ خودکارِ لیستِ اقساط
سه گزارشِ جداگانه از کاربر با اسکرین‌شات:

- **«ورود» از تنظیمات فول‌اسکرین نبود**: وقتی از پنلِ تنظیمات (که خودش یه `AnimatedVisibility` با
  `fillMaxWidth(0.85f)`ه، رجوع کن به `MainActivity.kt`) رو «ورود» می‌زدی، `LoginScreen` همون‌جا
  تو `SettingsScreen.kt`ی `AnimatedContent` رندر می‌شد - یعنی محدود به همون عرضِ ۸۵٪ می‌موند، نه
  کاملِ صفحه (پسِ‌زمینه/پنلِ تنظیمات از کنارش بیرون می‌زد). رفع شد: مسیرِ `"login"` تو
  `SettingsScreen.kt` الان `LoginScreen` رو تو یه `Dialog(properties = DialogProperties
  (usePlatformDefaultWidth = false))` نشون می‌ده - `Dialog` تو ویندویی کاملاً جدا رندر می‌شه، پس
  از محدودیتِ عرضِ والد (پنلِ تنظیمات) کاملاً مستقله.
- **رقم‌های شماره‌موبایل برعکس/از سمتِ اشتباه وارد می‌شدن**: چون کلِ اپ globally RTL ه
  (`LocalLayoutDirection.Rtl` تو `MainActivity.kt`)، فیلدِ شماره‌موبایل (که ذاتاً یه رشته‌ی چپ‌به‌راستِ
  صرفاً رقمیه) هم همون جهتِ RTL رو می‌گرفت و رقم‌های تازه‌تایپ‌شده از سمتِ راست جا می‌گرفتن، نه چپ.
  تو `LoginScreen.kt` فقط همون `OutlinedTextField`ِ شماره‌موبایل با
  `CompositionLocalProvider(LocalLayoutDirection provides LayoutDirection.Ltr)` پیچیده شد - چیدمانِ
  بیرونیِ Row (ترتیبِ فیلد/چیپِ +۹۸) دست‌نخورده موند، فقط جهتِ داخلیِ خودِ فیلد عوض شد.
- **لیستِ اقساط موقعِ پرداختِ دستی یهو می‌پرید**: تو `LoanDetailScreen.kt`، افکتِ اسکرولِ خودکار
  (که قراره فقط موقعِ اولین بازشدنِ صفحه به مرزِ اولین قسطِ پرداخت‌نشده بپره) کلیدش خودِ `rows` بود -
  چون `rows` با هر تغییرِ وضعیتِ پرداختِ یه قسط (even وسطِ کارِ کاربر) یه لیستِ *جدید* می‌شه، هر بار
  کاربر رو همون صفحه یه قسط رو «پرداخت» می‌زد این افکت دوباره اجرا می‌شد و صفحه به‌زور به قسطِ بعدی
  می‌پرید - دقیقاً برخلافِ خواسته‌ی کاربر («همینجا بمونه»). رفع شد: کلید به `Unit` عوض شد (فقط
  یه‌بار، موقعِ اولین بازشدن) و مقصدِ اسکرول هم از `firstUnpaid` به `firstUnpaid - 1` تغییر کرد -
  خواسته‌ی دومِ کاربر («یدونه قبلی هم معلوم باشه») رو هم برآورده می‌کنه: آخرین قسطِ پرداخت‌شده هم تو
  دیدِ اولیه می‌مونه، نه فقط اولین قسطِ پرداخت‌نشده.
- این سه رفع هنوز رو گوشیِ واقعی تست نشدن - خصوصاً `Dialog` تو یه پنلِ تودرتوی دیگه (Settings خودش
  یه overlay رو `LoanCalcApp`ه) نیازمندِ تاییدِ بصریِ واقعیه (مثلاً این‌که `statusBarsPadding` داخلِ
  `LoginScreen` تو ویندویِ Dialog درست insetِ واقعی رو می‌گیره یا نه).

### فول‌اسکرین‌شدنِ همه‌ی زیرصفحه‌های تنظیمات + انتقالِ «حذف حساب» به پایینِ لیست
کاربر گفت همون باگِ فول‌اسکرین‌نشدنِ «ورود» (رجوع کن به بخشِ بالا) رو این‌ها هم داره: یادآوریِ
سررسید، تقویمِ مالی، آمار و گزارشات، تاریخچه‌ی محاسبات، امور چک، حساب‌های بانکی - و خواستِ «حذفِ
حسابِ کاربری» به پایینِ پایینِ لیستِ تنظیمات منتقل بشه (قبلاً بالای لیست، تویِ کارتِ وضعیتِ حساب بود).

- **فول‌اسکرین**: به‌جای تکرارِ همون کدِ `Dialog(properties = DialogProperties
  (usePlatformDefaultWidth = false))` رو هفت‌تا `when(key)` branch جدا (شاملِ «اشتراک» هم که کاربر
  مستقیم اسمش رو نبرده بود ولی دقیقاً همون الگوی سایرِ زیرصفحه‌هارو داشت - رفعش عمدیه، نه اضافه‌کاری
  بی‌ربط)، یه کامپوننتِ مشترکِ `FullScreenDialog(onDismissRequest, content)` تو
  `SettingsScreen.kt` اضافه شد و همه‌ی هفت‌تا زیرصفحه (ورود، تقویمِ مالی، آمار، تاریخچه، چک،
  حساب‌های بانکی، اشتراک) رو همون یه‌جا استفاده می‌کنن.
- **انتقالِ حذف حساب**: دکمه‌ی «حذف حساب کاربری» از تویِ کارتِ وضعیتِ حساب (بلافاصله بعدِ «خروج از
  حساب») برداشته شد و به‌عنوانِ آخرین آیتمِ کلِ لیستِ تنظیمات (بعدِ «حریم خصوصی») اضافه شد - یه
  `AppCard` جدا، گیت‌شده با `gateState == GateState.LOGGED_IN` (فقط وقتی واقعاً حسابی برای حذف
  هست) و `matches("حذف حساب")` (سرچ‌پذیر مثلِ بقیه‌ی لیست). خودِ `AlertDialog`ِ تاییدِ حذف
  (`showDeleteAccountConfirm`) و منطقِ `authViewModel.deleteAccount(...)` دست‌نخورده موندن، فقط
  دکمه‌ی *محرکش* جابه‌جا شد.
- این‌ها هم مثلِ بقیه‌ی تغییراتِ اخیر هنوز رو گوشیِ واقعی تست نشدن.

## تغییراتِ نشستِ ۳۰ تیر ۱۴۰۵ (۲۰۲۶-۰۷-۲۱)
جزئیاتِ کامل، برای اینکه اگه این چت پاک شد هیچی گم نشه:

### کشفِ اشتباهِ CLAUDE.md درباره‌ی برنچ + یکی‌شدنِ برنچِ این نشست با کارِ واقعی
رجوع کن به اصلاحیه‌ی بخشِ «وضعیتِ برنچ/CI این نشست» بالاتر برای جزئیاتِ کامل. خلاصه: یادداشتِ قدیمی‌تر
می‌گفت `ccr-07146a93-ufo9vg` زائد/merge‌شده‌ست؛ غلط بود - اون برنچ ۲۱ کامیتِ واقعی (فونتِ Vazirmatn،
بازطراحیِ ورود/OTP، شیشه‌ای‌شدنِ باکس‌ها، تورِ اولین ورود، فول‌اسکرین‌شدنِ تنظیمات و...) داشت که هنوز
تو `main` نبودن، با یه PR بازِ #64. کاربر صریح خواستِ «ادامه‌ی اون برنچ رو اینجا برم» - برنچِ این نشست
(`claude/ios-box-light-intensity-p7ew76`) با `git reset --hard origin/ccr-07146a93-ufo9vg` بازسازی و
پوش شد، پس الان شاملِ همون ۲۱ کامیت + کارِ زیرِ همینِ سرفصل.

### دورِ سومِ شیشه‌ای‌شدن: «Liquid Glass» (سبکِ iOS) رو AppCard
کاربر با اسکرین‌شات از صفحه‌ی محاسبه‌گرِ واقعیِ اپ («خیلی خوب نیست») خواستِ ۴ گزینه‌ی طرحِ شیشه‌ای
پیش‌نمایش بشه، ترجیحاً یکی به سبکِ iOS. یه پیش‌نمایشِ HTML/Artifact (نه بخشِ ریپو) با ۴ طرح
(فعلی/پررنگ‌تر، مات‌نرم، لبه‌دار، لیکوئید-iOS) رو همون سه باکسِ واقعیِ صفحه‌ی محاسبه‌گر ساخته شد؛ کاربر
طرحِ «اپل/لیکوئید» رو انتخاب کرد. پیاده‌سازیِ واقعی (`ui/components/AppCard.kt`، `ui/theme/Color.kt`):
- گوشه‌های `AppCard` گردتر شد (۱۸dp → ۲۲dp)، سایه نرم‌تر/پخش‌تر شد (elevation ۸dp→۱۴dp، آلفا
  ۰.۲۲→۰.۱۴)، پایه‌ی شیشه‌ای شفاف‌تر شد (تیره ۴۲٪→۳۲٪، روشن ۵۰٪→۴۰٪)، حاشیه رو تمِ روشن از تیره/سایه به
  سفید/روشن عوض شد.
- توکنِ رنگیِ جدیدِ `glassHighlight` (+ `AppGlassHighlight`) اضافه شد و یه `Brush.radialGradient` تو
  گوشه‌ی بالا-چپِ هر `AppCard` هایلایتِ نوریِ شیشه‌ی واقعی رو تقلید می‌کنه - چون این پروژه عمداً بدونِ
  بلورِ واقعی (RenderEffect) پیاده شده (رجوع کن به کامنتِ `AuroraBackground.kt`)، این هایلایتِ ثابت
  جایگزینِ ارزون‌ترشه.
- CI (`build-native-android.yml`، `workflow_dispatch` رو `claude/ios-box-light-intensity-p7ew76`)
  بعدِ این تغییر واقعاً اجرا و **موفق** بود.

### رفعِ دو باگِ صفحه‌ی ورود (bidi/جهت)
کاربر با اسکرین‌شات گزارش داد: (۱) چیپِ «+۹۸» بالای فیلدِ شماره‌موبایل گاهی به دو خط می‌شکست («۹+» بالا،
«۸» پایین)، (۲) تو صفحه‌ی کدِ تایید (`OtpBoxRow`، ۵ باکسِ جداگانه)، رقمِ تایپ‌شده از سمتِ راست وارد
می‌شد، در حالی‌که باید از چپ می‌بود (هم‌راستا با رفعِ قبلیِ فیلدِ شماره‌موبایل).
- **علتِ محتملِ چیپِ +۹۸**: تویِ ambientِ RTLِ کلِ اپ، رشته‌ی مختلطِ «+»/ارقام موقعِ اندازه‌گیری با
  bidi می‌شکست. رفع: همون Boxِ چیپ تو `LoginScreen.kt` با `CompositionLocalProvider
  (LocalLayoutDirection provides LayoutDirection.Ltr)` پیچیده شد (مثلِ فیلدِ شماره که قبلاً همین
  مشکل رو داشت) + `maxLines=1`/`softWrap=false` برای تضمینِ تک‌خط‌بودن صرف‌نظر از مقیاسِ فونتِ سیستم.
  **این علتِ حدسیه، رو گوشیِ واقعی تاییدنشده** (سندباکس بیلد نمی‌کنه) - اگه بازم دیده شد، این اولین
  جاییه که باید چک بشه.
- **رفعِ جهتِ OtpBoxRow**: Rowِ ۵ باکسِ رقم هم با همون الگو (`LocalLayoutDirection` → `Ltr`) پیچیده
  شد - رقمِ اول حالا سمتِ چپ می‌شینه، رقم‌های بعدی از چپ به راست اضافه می‌شن.
- CI بعدِ این تغییر هم موفق بود.

### تنظیمِ شدتِ Aurora (دو دور)
تو همین نشست، بعدِ یه پیش‌نمایشِ HTMLِ ۴سطحی (از رو همون فرمول‌های `AuroraBackground.kt`)، کاربر اول
پررنگ‌ترین (سطحِ ۴: آلفای ۰.۸۵/۰.۶۸ تیره، ۰.۶۴/۰.۵۰ روشن) رو خواست، بعد گفت «دو شماره کم کن» - یعنی
سطحِ ۲ (۰.۵۲/۰.۴۰ تیره، ۰.۳۹/۰.۲۹ روشن). مقدارِ فعلیِ `primaryAlpha`/`accentAlpha` تو
`AuroraBackground.kt` همینِ سطحِ ۲ ئه - اگه باز خواستِ تغییر داد، همین دو خط رو عوض کن (یا از رو همون
جدولِ ۴سطحی که تو تاریخچه‌ی چت هست).

### نکته‌ی مهم - هیچ‌کدوم از تغییراتِ بالا رو گوشیِ واقعی دیده نشده
هم شیشه‌ی Liquid Glass هم رفعِ bidi هم تنظیمِ Aurora، فقط از رو استدلالِ کد پیاده و با CI (که فقط
کامپایل‌شدن رو تایید می‌کنه، نه ظاهرِ درست) تاییدشدن - نه با تستِ واقعیِ رو گوشی. اگه کاربر گزارشِ
جدیدی داد، اول همین‌ها رو مظنون بدون.

### تعمیمِ رفعِ باگِ RTL/LTR + کامپوننتِ مشترکِ Ltr.kt
کاربر با نقدِ خودم (تکرارِ کدِ `CompositionLocalProvider(LayoutDirection.Ltr)` تو ۳ جای
`LoginScreen.kt`) موافقت کرد و خواست خودم بگردم بقیه‌ی اپ رو هم چک کنم. یه کامپوننتِ مشترک
(`ui/components/Ltr.kt`) ساخته شد و جایگزینِ اون تکرارها شد؛ همون باگِ «عدد از سمتِ راست وارد
می‌شه» تو این‌جاهای دیگه هم پیدا و رفع شد: `LockScreen.kt` (فیلدِ PIN)، `SettingsScreen.kt`
(`PinSetupDialog`)، `AddEditChequeScreen.kt` (شناسه‌ملی، شماره‌چک، شناسه‌ی صیادی)، `ChequeBooksScreen.kt`
(شماره‌ی شروع/پایانِ دسته‌چک). فیلدهای مبلغ (که از `ThousandsSeparatorTransformation` استفاده می‌کنن)
عمداً دست نخوردن چون گزارشی نبود و ساختارشون فرق داره.

### رفعِ دو باگِ ظاهریِ گزارش‌شده با عکس
- **خطِ اسکرول‌بار تو باکسِ اقساط**: تو `LoanDetailScreen.kt`، جعبه‌ی اقساط دقیقاً هم‌عرضِ Boxِ
  بیرونی بود، پس خطِ `lazyColumnScrollbar` رو حاشیه‌ی هر ردیف می‌افتاد. `contentPadding(end=10.dp)`
  رو همون `LazyColumn` اضافه شد تا بینِ لبه‌ی ردیف‌ها و خط فاصله بیفته.
- **خطِ زیرِ نوارِ وضعیت پشتِ پنلِ تنظیمات**: پنلِ تنظیمات فقط ۸۵٪ عرض می‌گیره؛ چون هم خودِ پنل
  (`AppSurface`) هم اسکرمِ تیره‌ی پشتش تا زیرِ نوارِ وضعیت هم کشیده می‌شن، این دو رنگ درست زیرِ
  آیکون‌های نوارِ وضعیت به‌هم می‌رسیدن. تو `MainActivity.kt` یه نوارِ تمام‌عرض دقیقاً به‌ارتفاعِ
  `WindowInsets.statusBars` با رنگِ یکدستِ `AppSurface` رو همه‌چیزِ دیگه اضافه شد.

### آماده‌سازیِ عکس‌های استور برای کافه‌بازار/مایکت (فقط تحویل به کاربر - بخشی از کدِ ریپو نیست)
کاربر ۱۵ تا اسکرین‌شاتِ خامِ رو گوشیِ واقعی فرستاد و خواستِ نسخه‌ی نهایی برای هر دو استور. کارِ
انجام‌شده (با Python/PIL، سندباکس‌محور، خروجی فقط برای کاربر فرستاده شد، هیچ‌کدوم تویِ ریپو کامیت
نشدن):
- نوارِ وضعیتِ هر ۱۵ عکس پاک‌سازی شد: آلارم/عددِ روزِ ماه (چپ) و نوشته‌ی 4.5G + آنتنِ کم‌رنگِ دوم
  (راست) حذف شدن، فقط ساعت + یه آنتن + باتری موند. یه جعبه‌ی خاکستریِ آرتیفکتِ گوشه‌ی بالا-راست
  (تو ۶ تا از عکس‌ها بود، نه همه) هم پاک شد.
- یه کارتِ کپشنِ «Liquid Glass» (دقیقاً هم‌الگو با `AppCard.kt`/`Color.kt`ِ خودِ اپ - توکن‌های
  glassBase/glassGradient/glassHighlight/glassBorder) رو بالای هر عکس ساخته شد، با فونتِ واقعیِ اپ
  (`vazirmatn_bold.ttf` از `native-android/app/src/main/res/font/`).
- **خواسته‌ی مایکت**: حداقل ۳ عکس، حداکثر ۳۰۰۰px، نسبتِ دقیقِ ۱۶:۹ یا ۹:۱۶، حداکثر ۳ مگابایت. چون
  اسکرین‌شاتِ اصلی (۱۰۸۰×۲۴۰۰) خیلی بلندتر از این نسبته، بعدِ کلی رفت‌وبرگشت (اول کراپِ بالای عکس،
  بعد به‌درخواستِ کاربر یه قابِ گوشیِ واقع‌گرایانه که کلِ اسکرین‌شات بدونِ کراپ توش جا بشه) نسخه‌ی
  نهایی: بومِ ۱۰۸۰×۱۹۲۰ (۹:۱۶ دقیق)، قابِ گوشی (بدنه‌ی گرادیانی + بریدگیِ دوربین + لبه‌ی نوری) با
  کلِ اسکرین‌شات مقیاس‌شده توش، یه هاله‌ی طلایی/سبزآبیِ حلقه‌ای کنارِ گوشی، و باکسِ کپشنِ بالا
  دقیقاً هم‌عرضِ خودِ قابِ گوشی (نه تمام‌عرضِ بوم).
- متن‌های کپشن (۱۵ تا، یکی به‌ازای هر عکس) با لحنِ رسمی نوشته شدن (مثلاً «حالتِ خصوصی؛ مبالغِ شما
  فقط نزدِ خودتان است») - اگه کاربر خواستِ عکسِ استورِ جدیدی درست بشه، اسکریپت‌ها (`make_caption_card.py`،
  `make_phone_frame_card.py`، `make_all_phoneframe.py`) تو اسکرچ‌پدِ همون سشن موندن، نه تو ریپو -
  یعنی تو یه چتِ جدید باید از نو ساخته بشن مگر کاربر خودش فایل‌ها رو نگه داشته باشه.

### توضیحِ مجوزهای «خطرناک» برای بازار
کاربر با عکس نشون داد بازار `READ_CALENDAR`/`WRITE_CALENDAR` رو «دسترسیِ خطرناک» علامت زده و
توضیح خواسته. این دو مجوز برای قابلیتِ «افزودنِ سررسیدها به تقویمِ گوشی» تو `LoanDetailScreen.kt`ه
(دکمه‌ی «افزودن سررسیدها به تقویم گوشی» + `runCalendarExport`) - یه متنِ توضیحِ آماده براش نوشته و
به کاربر داده شد تا تو فیلدِ «توضیحاتِ بیشتر برای بررسی‌کننده» بذاره.

### وضعیتِ ارسال به استورها (این نشست)
کاربر نسخه‌ی جدید (شاملِ همه‌ی تغییراتِ بالا) رو دوباره هم رو کافه‌بازار هم مایکت برای بررسی فرستاده.
حسابِ تست (`09121111111` / کدِ `11111`) و متنِ «چیزهای تازه» (بروزرسانیِ ظاهر + رفعِ باگ) به هر دو
داده شده. **خواسته‌ی کاربر: به‌محضِ تاییدِ حداقل یکی، فوراً برو سراغِ فعال‌سازیِ OTP** - رجوع کن به
بخشِ «کارهای درحال انجام» پایین.

## تغییراتِ نشستِ ۳۱ تیر ۱۴۰۵ (۲۰۲۶-۰۷-۲۲) - دیپلویِ خودکارِ سرور + تستِ اسکرین‌شات + مرتب‌سازیِ دیتابیس
سه‌تا کارِ جدا، هر سه رو برنچِ `claude/ios-box-light-intensity-p7ew76` (کامیتِ `47e85db` +
کامیتِ بعدی برای دیتابیس):

### ۱) دیپلویِ خودکارِ سرور رو VPS (CI/CD)
جایگزینِ آپلودِ دستیِ تیکه‌تیکه‌ی jar که چندبار (jarِ خالی از cat ناقص، jar تو مسیرِ اشتباه) سرورِ زنده
رو کرش کرده بود (رجوع کن به حادثه‌های بخشِ سرور بالا). `.github/workflows/build-server.yml` یه jobِ
`deploy` جدید داره (بعدِ jobِ `build`، فقط رو پوشِ `main`): با `appleboy/scp-action`/`appleboy/
ssh-action` jarِ جدید رو به `~/loan-server/incoming/` می‌فرسته، سلامتش رو (غیرخالی + zip معتبر) چک
می‌کنه، فقط اگه سالم بود جایگزینِ jarِ زنده می‌کنه (با بکاپِ `.bak` قبلش)، `pm2 restart` می‌زنه، و اگه
بعدِ ۳ ثانیه سرویس "online" نبود خودکار به `.bak` برمی‌گرده. اگه سکرت‌های `VPS_HOST`/`VPS_USER`/
`VPS_SSH_KEY` ست نشده باشن، jobِ deploy فقط warning می‌ده و skip می‌شه (به رانِ build لطمه نمی‌زنه).
**کارِ باقی‌مونده برای کاربر**: راه‌اندازیِ یه‌بارِ اولیه طبقِ `server/README.md` (بخشِ «دیپلویِ خودکار
(CI/CD)») - ساختِ یه کلیدِ SSHِ مخصوصِ دیپلوی رو VPS + اضافه‌کردنِ ۳ سکرتِ گیت‌هاب. تا وقتی این کار
نشه، همچنان باید مثلِ قبل دستی دیپلوی کرد.

### ۲) تستِ اسکرین‌شاتِ Compose با Paparazzi
پلاگینِ `app.cash.paparazzi` (نسخه‌ی ۱.۳.۵) به `native-android/build.gradle.kts`/`app/build.gradle.kts`
اضافه شد - تستِ اسکرین‌شاتِ Compose رو JVM (با layoutlib، بدونِ نیازِ گوشی/امولاتور)، برای گرفتنِ
باگ‌های ظاهریِ ساده (سایه/گوشه/رنگِ متنِ نامرئی رو دارک‌مود - دقیقاً همون دسته‌باگی که چندبار این نشست‌ها
با گزارشِ کاربر پیدا شد، نه با تستِ خودکار) قبل از رسیدن به دستِ کاربر. فایلِ اولیه‌ی تست:
`app/src/test/kotlin/ir/sadteam/loancalc/ScreenshotTest.kt` - دو تست (`appCardLight`/`appCardDark`)
رو `AppCard`، هرکدوم با `LoanCalcTheme` + `CompositionLocalProvider(LocalLayoutDirection provides
LayoutDirection.Rtl)` (چون MainActivity کلِ اپِ واقعی رو تو یه Rtl provider می‌پیچه که Paparazzi با
رندرِ مستقلِ کامپوزیبل‌ها ازش خبر نداره). اجرا: `./gradlew :app:testDebugUnitTest` (رکورد اولیه با
`recordPaparazziDebug`). چون این پلاگین به `dl.google.com` نیاز داره، فقط تو CI واقعی
(`build-native-android.yml`) قابلِ‌اجراست، نه تو سندباکس. **اگه بعداً خواستی تست‌های بیشتری اضافه
کنی**: یه Composableِ Hilt-free (بدونِ `hiltViewModel()`) انتخاب کن (مثلِ `AppCard`)، نه یه صفحه‌ی
کامل که به ViewModel/DI نیاز داره - Paparazzi از این‌ها پشتیبانی نمی‌کنه بدونِ mock کردنِ کاملِ گراف.

### ۳) مرتب‌سازیِ دیتابیس - جداکردنِ واقعیِ ردیف‌های قسط به یه جدولِ Room جدا
کاربر با علم به ریسک صراحتاً گزینه‌ی «جداکردنِ واقعیِ rows به یه جدولِ Room جدا» رو انتخاب کرد (نه
گزینه‌ی امن‌ترِ «فقط تایپ‌سیف‌کردنِ داخلی»). این عمیق‌ترین تغییرِ معماریِ لایه‌ی داده‌ی کلِ پروژه تا
الانه - جزئیاتِ کامل برای اینکه اگه این چت پاک شد هیچی گم نشه:

- **مشکلِ قبلی**: `LoanEntity.dataJson` (یه blobِ JSONِ خام) هم اطلاعاتِ کلیِ وام هم آرایه‌ی `rows`
  (وضعیتِ پرداختِ تک‌تکِ اقساط) رو باهم نگه می‌داشت؛ هرجا لازم بود با `as? Number`/`as? String` دستی
  از رو `Map<String, Any?>` خونده می‌شد - نه type-safe، نه قابلِ query مستقیم، مستعدِ باگِ تایپی تو
  اسمِ کلید.
- **جدولِ جدید**: `LoanRowEntity`/`loan_rows` (`data/db/LoanRowEntity.kt` + `LoanRowDao.kt`) - ستون‌های
  تایپ‌شده‌ی واقعی (`loanId`, `m`, `installment`, `paid`, `paidLate`, `paidDateY/M/D`, `photoPath`)،
  primary keyِ `(loanId, m)`. `AppDatabase` نسخه‌ش از ۱۱ به ۱۲ رفت.
- **Migrationِ ۱۱→۱۲ (`AppDatabase.kt`)**: عمداً **فقط افزایشی**ه - فقط جدولِ جدید رو می‌سازه و
  بهترین‌تلاش رو برای پرکردنش از رو `rows`ِ داخلِ `dataJson` هر وامِ موجود می‌کنه (با `org.json.
  JSONObject`، چون Migration مستقیم رو `SupportSQLiteDatabase` کار می‌کنه، نه از رو Gson). خودِ
  `loans.dataJson` رو دست‌نمی‌زنه/استریپ نمی‌کنه (نه ALTER، نه حذفِ کلیدِ "rows") - یعنی حتی اگه
  پارس‌کردنِ یه وامِ خاص شکست بخوره (per-loan try/catch)، فقط همون یه وام تو جدولِ جدید خالی می‌مونه،
  نه اینکه کلِ مهاجرت با کرش متوقف بشه یا داده‌ای واقعاً از دست بره.
- **فال‌بکِ خودترمیم‌شونده (`LoanRepository.getOrMigrateRows`)**: اگه به هر دلیلی (لبه‌ی نادرِ
  مهاجرت) یه وام تو `loan_rows` هیچ ردیفی نداشت، از رو `dataJson.rows`ِ قدیمی (اگه هنوز اونجا مونده)
  یا پیش‌فرضِ اقساطِ برابر بازسازی می‌شه و همون‌جا persist می‌شه - تضمین می‌کنه تاریخچه‌ی پرداختِ هیچ
  کاربری با این تغییر گم نشه، حتی اگه یه لبه‌ی migration رو میس کرده باشه.
- **`LoanRepository.kt` کاملاً بازنویسی شد** تا از `LoanRowDao` به‌جای `data["rows"]` استفاده کنه -
  `addManualLoan`/`updateManualLoan`/`saveComputedLoan`/`setRow*`/`getRows` همه از رو جدولِ جدید کار
  می‌کنن. **قراردادِ عمومیِ خروجیِ `getRows()`** (لیستی از `Map<String, Any?>` با کلیدهای دقیقاً
  `m`/`installment`/`paid`/`paidLate`/`paidDate`/`photoPath`/`dueDate`) کاملاً حفظ شده - پس هیچ‌کدوم
  از ۸+ جای مصرفِ این map تو `:app` (LoanDetailScreen، StatsViewModel، LoanWidget،
  FinancialCalendarViewModel، DueDateReminderWorker) نیازی به تغییرِ منطقِ داخلیشون نداشتن.
  سینک با سرور/بک‌آپ (`toWebMap`/`fromWebMap`/`exportBackupJson`/`importBackupJson`) هم دست‌نخورده
  مونده - همون شکلِ کاملِ JSON (meta + rows) رو می‌فرستن/می‌گیرن، فقط ردیف‌ها دیگه همیشه تازه از رو
  `loan_rows` بازسازی می‌شن، نه از رو یه کپیِ احتمالاً روبه‌زوال.
- **باگِ جدیِ کشف‌شده و رفع‌شده حینِ همین تغییر**: چون خواندنِ `loan_rows` (Room) نیاز به یه کوئریِ
  واقعیِ دیتابیس داره (برخلافِ Gsonِ درون‌حافظه‌ایِ قبلی که رو خودِ entityِ ازقبل‌لودشده کار می‌کرد)،
  `LoanRepository.getRows()` باید `suspend` بشه - ولی Room پیش‌فرض رو thread اصلی هیچ کوئری‌ای
  (حتی غیر-suspend) رو اجازه نمی‌ده (`IllegalStateException`، مگر `allowMainThreadQueries()` که این
  پروژه نداره). سه‌تا جای مصرفِ synchronous (تویِ `remember{}`ِ Compose، رو thread اصلی) پیدا شدن که
  اگه اصلاح نمی‌شدن اپ رو کرش می‌کردن: `LoanDetailScreen.kt` (`remember(loan) { viewModel.getRows
  (loan) }`)، `StatsScreen.kt` (`remember(loans) { viewModel.paymentHistory(loans) }`)،
  `FinancialCalendarScreen.kt` (`remember(loans) { viewModel.dueItemsByDate(loans) }`). هر سه به
  الگوی `var ... by remember { mutableStateOf(...) }` + `LaunchedEffect(key) { ... = viewModel.
  ...() }` تبدیل شدن (و `MyLoansViewModel.getRows`/`StatsViewModel.paymentHistory`/
  `FinancialCalendarViewModel.dueItemsByDate` هم `suspend` شدن). یه اثرِ جانبیِ ظریف‌تر هم پیدا و رفع
  شد: تویِ `LoanDetailScreen.kt` یه `LaunchedEffect(Unit) { ...اسکرول به مرزِ آخرین قسطِ پرداخت‌شده... }`
  بود که چون `rows` دیگه اول خالیه (تا لود شدنِ async)، با کلیدِ ثابتِ `Unit` دقیقاً همون لحظه‌ی خالی
  اجرا می‌شد و اسکرولِ خودکار هیچ‌وقت کار نمی‌کرد؛ الان کلید دوباره `rows`ه ولی با پرچمِ یه‌بارمصرفِ
  `hasAutoScrolled` محافظت شده - هم منتظرِ لودشدنِ واقعی می‌مونه هم فقط یه‌بار اسکرول می‌کنه (نه با هر
  تغییرِ بعدیِ وضعیتِ پرداخت).
- **این تغییر رو گوشیِ واقعی تست نشده** (سندباکس نمی‌تونه `native-android` رو بیلد کنه) - خصوصاً خودِ
  Migrationِ ۱۱→۱۲ (که از رو دیتابیسِ واقعیِ نصب‌شده‌ی کاربر اجرا می‌شه، نه یه دیتابیسِ خالی) باید با
  دقت تایید بشه: بعدِ نصبِ این نسخه، وام‌های ازقبل‌ذخیره‌شده (خصوصاً وضعیتِ پرداخت/تاخیر/عکسِ رسیدِ
  هر قسط) باید دقیقاً همون‌جوری که قبل از آپدیت بودن نشون داده بشن. **اگه کاربر گزارش داد بعدِ آپدیت
  یه وام قسط‌هاش خالی/صفر شده**، اول `MIGRATION_11_12` تو `AppDatabase.kt` و
  `LoanRepository.getOrMigrateRows` رو چک کن - فال‌بک باید خودکار درستش کنه، ولی اگه نکرد یعنی خودِ
  فال‌بک هم یه لبه رو میس کرده.

## کارهای درحال انجام / تصمیم‌های معلق
- **⚠️ تاییدِ migrationِ دیتابیس (۱۱→۱۲) رو گوشیِ واقعی**: رجوع کن به بخشِ «مرتب‌سازیِ دیتابیس» بالا -
  بعدِ نصبِ اولین بیلدی که این تغییر توشه، حتماً وام‌های ازقبل‌ذخیره‌شده (خصوصاً وضعیتِ پرداخت/تاخیر/
  عکسِ رسیدِ هر قسط) رو چک کن که دقیقاً همون‌جوری که قبل از آپدیت بودن مونده باشن. این هنوز از کاربر
  تاییدنشده - اگه گزارشِ مشکلی اومد، اول همین بخش رو بخون.
- **⚠️ اولویتِ فوری - به‌محضِ تاییدِ استور، OTP رو فعال کن**: کاربر نسخه‌ی جدید رو (۳۱ تیر ۱۴۰۵) دوباره
  هم رو کافه‌بازار هم مایکت برای بررسی فرستاده و منتظرِ نتیجه‌ست؛ صراحتاً گفته «اگه اوکی شد می‌خوام
  سریع پیامک رو فعال کنم» - یعنی تو اولین چتِ بعدی که کاربر گفت یکی از استورها تاییدش کرد، باید
  بلافاصله بریم سراغِ مراحلِ کاملِ فعال‌سازیِ OTP (رجوع کن به بخشِ OTP بالا: ساختِ پترنِ تاییدشده‌ی
  ملی‌پیامک با هشِ SMS Retriever تهش + گرفتنِ یوزرنیم/پسورد + ست‌کردنِ سه‌تا env رو VPS + ری‌استارت)،
  نه اینکه منتظرِ درخواستِ دوباره‌ش بمونیم.
- **OTP رو فعال کن** (رجوع کن به بخش OTP بالا) — منتظر پترن تاییدشده‌ی ملی‌پیامک + یوزرنیم/پسورد از کاربره.
- **هشِ SMS Retriever رو به پترنِ ملی‌پیامک اضافه کن** (رجوع کن به بخشِ «ورود/OTP: بازطراحی...» و
  بخشِ OTP بالا) — کاربر باید یه بیلد نصب کنه، هش رو با `adb logcat -s SmsRetrieverHash` بگیره، و
  به آخرِ متنِ پترن اضافه کنه (قبل/همزمان با تاییدِ خودِ پترن، چون تغییرِ متن بعد از تاییدش احتمالاً
  نیاز به تاییدِ دوباره داره) - وگرنه پرکردنِ خودکارِ کد تو صفحه‌ی OTP هیچ‌وقت کار نمی‌کنه.
- **`main` عقب‌مونده، PR #64 هنوز بازه** (رجوع کن به اصلاحیه‌ی بخشِ «وضعیتِ برنچ/CI» بالا برای
  جزئیاتِ کاملِ کشف) - `main` نسبت به کارِ واقعیِ انجام‌شده ۲۱+ کامیت عقبه؛ همه‌ی اون کار (+ کارِ نشستِ
  ۳۰ تیر روش، رجوع کن به بخشِ «تغییراتِ نشستِ ۳۰ تیر» پایین‌تر) الان رو برنچِ
  `claude/ios-box-light-intensity-p7ew76`ه، نه رو `main`. یه PR بازِ #64 (از `ccr-07146a93-ufo9vg` به
  `main`) هست که این PR دیگه با نوکِ فعلیِ کارِ ماشین هم‌سطح نیست (چون بعدش رو برنچِ این نشست کامیتِ
  بیشتر خورد) - اگه کاربر خواستِ merge‌کردنِ نهاییِ همه‌ی این‌ها تو `main` رو داد، یا باید PR #64 رو
  آپدیت/بازسازی کرد از رو `claude/ios-box-light-intensity-p7ew76`، یا یه PR جدید از همون برنچ باز کرد؛
  جزئیاتش (کدوم برنچ، هدف چیه) رو از خودِ کاربر بپرس، خودسرانه merge نکن.
- **تاییدِ نصبِ `sqlite3` رو VPS + اجرای موفقِ دستورِ `UPDATE users SET subscribed=1...`** برای دو
  حسابِ تست/دائمی (رجوع کن به بخشِ «حسابِ تستِ دائمی» بالا) - هنوز از کاربر تاییدنشده.
- برنچ‌های زائدِ واقعاً بی‌ضرر رو گیت‌هاب (چندتا `claude/*` قدیمی که واقعاً تو `main` merge شدن، **نه**
  `ccr-07146a93-ufo9vg` که اصلاحیه‌ی بالا رو بخون) از سندباکس قابل پاک‌کردن نبودن (پراکسی گیت 403 داد،
  ابزار GitHub هم delete-branch نداره) — بی‌ضررن، فقط شلوغی. ولی طبقِ درسِ اصلاحیه‌ی بالا، قبل از اینکه
  یه برنچ رو «زائد» ببندی، حتماً `git log origin/main..origin/<branch>` رو چک کن، نه فقط حدس بزن. برنچِ
  جدیدی رو داوطلبانه نساز، همیشه از `main` شروع کن (به‌جز وقتی خودِ هارنسِ CCR یه برنچِ مشخص رو اجباری
  کرده، یا کاربر صریحاً خواستِ ادامه‌ی یه برنچِ دیگه رو - رجوع کن به بخشِ «وضعیتِ برنچ/CI» بالا).
- «دیوار» فقط یه اشتباه تایپی/گفتاری کاربر بود (منظورش چیز دیگه‌ای نبود) — این مورد بسته‌ست.
- **ویجت (`ui/widget/LoanWidget.kt`) باید دوباره کار بشه** — کاربر گفته الان خیلی ساده‌ست و وقتی روش
  می‌زنه، اپ باز نمی‌شه/هنگ می‌کنه. کلیک فعلی `clickable(actionStartActivity(Intent(context,
  MainActivity::class.java)))`ه (خط ۸۹) — چون context اینجا Activity نیست، به‌احتمال زیاد نبودِ
  `Intent.FLAG_ACTIVITY_NEW_TASK` علتشه؛ باید تست/دیباگ واقعی بشه، نه فقط حدس. کاربر گفته «چند روزه
  دیگه، وقتی اپ رو منتشر کردم» بهش یادآوری کنم که این رو درست کنیم — پس تا وقتی خودِ کاربر تو یه چتِ
  بعدی نگفته «الان وقتشه»، این کار شروع نشه، فقط رجوع کن به همین یادداشت.
- **رتبه اعتباری (فاز بعد)**: کاربر خواسته یه قابلیتِ آینده که از یه سایتِ بیرونی (مثلاً فینوتک) رتبه‌ی
  اعتباریِ کاربر رو بخونه و تو اپ (چه‌کسی ضامنِ چه‌کسی شده، چقدر وام گرفته، تو کدوم بانک‌ها می‌تونه وام
  بگیره) ثبت کنه. صراحتاً گفته «الان نمی‌خوام، برای نسخه‌ی بعد» — فقط یادآوری/ثبت شده، هنوز هیچ طراحی یا
  کدی براش نیست.
- **تبلیغ تو نسخه‌ی رایگان (فاز بعد)**: کاربر پرسید چجور می‌تونه تبلیغ بذاره؛ پیشنهاد شد **تپسل** یا
  **آدیوری/صباویژن** (سازگار با کافه‌بازار، برخلافِ AdMob که تو ایران مشکلِ پرداخت/دسترسی داره)، فقط
  برای کاربرهای غیرمشترک (`!subscribed`)، مثل بنر ثابت یا تبلیغ بینابینی. کاربر گفته **فعلاً نه** —
  می‌خواد اول اپ رو منتشر کنه و یه مدت نصب جمع کنه، بعداً برمی‌گرده رو این موضوع. تا وقتی خودش نگفته
  «الان وقتشه»، شروعش نکن؛ وقتی خواست، اول باید خودش تو تپسل/آدیوری حساب بسازه و Zone ID بگیره (این
  بخش رو کسی جز خودِ کاربر نمی‌تونه انجام بده).

## مایکت (فلیورِ دوم، کنارِ کافه‌بازار) — کد آماده‌ست، فقط یه مقدار مونده
`native-android/app/build.gradle.kts` دو فلیور داره: `cafebazaar` (Poolakey، همونی که تا امروز بود)
و `myket` (کتابخونه‌ی رسمیِ `com.github.myketstore:myket-billing-client`، الگوی کلاسیکِ Android IAB
v3 - `IabHelper`/`Purchase`/`Inventory`). هر فلیور `SubscriptionManager` خودش رو داره
(`app/src/cafebazaar/.../subscription/` و `app/src/myket/.../subscription/`)، بقیه‌ی اپ (`main`
source set) هیچ فرقی بینِ دوتا نمی‌بینه. مانیفستِ کتابخونه‌ی مایکت از placeholder استفاده می‌کنه، نه
مقدارِ ثابت - مقادیرش (`ir.mservices.market` و ...) تو `manifestPlaceholders` همون فلیور تو
build.gradle.kts ست شدن، نه یه فایلِ AndroidManifest جدا.

**`MYKET_IAB_PUBLIC_KEY` دیگه placeholder نیست** - کاربر اپ رو تو پنلِ دولوپرِ مایکت ثبت کرد و کلیدِ
عمومیِ واقعی رو از اونجا گرفت؛ الان تو
`app/src/myket/kotlin/ir/sadteam/loancalc/subscription/SubscriptionManager.kt` مقدارِ واقعیه. همون
۴ شناسه‌ی محصولِ اشتراک (`unlimited_loans_1m/3m/6m/1y`، با همون قیمت‌های کافه‌بازار: ۳۰۰۰۰۰/۸۱۰۰۰۰/
۱۴۴۰۰۰۰/۲۵۲۰۰۰۰ ریال) هم تو پنلِ مایکت به‌عنوانِ محصولِ درون‌برنامه‌ای تعریف شدن.

**صحت‌سنجیِ خریدِ سمتِ سرور برای مایکت هم پیاده شد** (`server/.../Myket.kt`، تابع
`validateMyketPurchase`) - معادلِ `Cafebazaar.kt`، ولی ساده‌تر چون مایکت به‌جای فلوی OAuth یه
توکنِ دسترسیِ ثابت می‌ده (`MYKET_ACCESS_TOKEN` تو `.env`، هنوز رو VPS ست نشده - رجوع کن به
`server/README.md`). کلاینت الان `store` (`BuildConfig.FLAVOR`) رو هم تو
`POST /api/subscription/verify` می‌فرسته (`VerifySubscriptionRequest.store`) تا
`SubscriptionRoutes.kt` بدونه با کدوم API (کافه‌بازار یا مایکت) تاییدِ خرید کنه؛ نسخه‌های قدیمی‌تر
که این فیلد رو ندارن پیش‌فرض `cafebazaar` حساب می‌شن. ⚠️ فرمتِ دقیقِ ریسپانسِ موفقِ API مایکت تو
مستنداتشون نبود، پس فعلاً فقط بر اساسِ کدِ HTTP (۲xx) تصمیم می‌گیریم، نه یه فیلدِ خاص - بعدِ اولین
خریدِ واقعیِ تستی، لاگ سرور رو چک کن ببین این فرض درست بوده.

**اطلاعاتِ Play Protect**: اولین نصبِ APKِ مایکت (کلیدِ امضای تازه) رو گوشیِ واقعی «Blocked by Play
Protect» می‌ده (Unknown Developer - طبیعیه برای هر کلیدِ امضای جدید، نه باگِ کد). راه‌حلِ رسمیِ مایکت:
فرمِ Google «Play Protect Appeals» (لینکش تو myket.ir/kb/pages/blocked-by-play-protect-fa/ هست، فقط
با VPN باز می‌شه) با هشِ SHA256 از APK رو VirusTotal + اطلاعاتِ اپ.

**این فرم تو ۲۷ تیر ۱۴۰۵ (۲۰۲۶-۰۷-۱۸) واقعاً ارسال شد** - برای APKِ فلیورِ مایکت، رانِ CI شماره‌ی ۲۵۹
(versionCode=259)، با هشِ SHA256 برابرِ
`d23a8ef52ee330ad18dca043e42bdda4477923ff7b8507f0a1e83c5311d2d4ad` (صفر تشخیصِ مخرب رو VirusTotal).
گوگل گفته تا یه هفته (یعنی حدودِ ۳ مردادِ ۱۴۰۵، ۲۰۲۶-۰۷-۲۵) طول می‌کشه و هیچ پیامی درباره‌ی نتیجه
نمی‌ده - باید دستی دوباره نصب و تست بشه. **تا اون‌موقع نباید applicationId یا کلیدِ امضای release
عوض بشه**، وگرنه این بررسی از اول باید انجام بشه.

CI الان هر ۴ تا APK (کافه‌بازار دیباگ/ریلیز + مایکت دیباگ/ریلیز) رو می‌سازه و به‌عنوانِ آرتیفکتِ جدا
آپلود می‌کنه (`loan-calculator-native-myket-debug-apk` و `-myket-release-apk`).

## آپدیتِ خودکار
سرور یه جدولِ تک‌ردیفه‌ی `app_version` داره (`server/.../routes/AppVersionRoutes.kt`،
`GET /api/app-version`) که آخرین versionCode + لینکِ هر استور رو نگه می‌داره؛ اپ هر بار باز می‌شه
چک می‌کنه و اگه قدیمی بود یه بنر نشون می‌ده. **این جدول دستیه، خودکار آپدیت نمی‌شه** - بعدِ هر
انتشارِ واقعیِ نسخه‌ی جدید رو کافه‌بازار/مایکت، باید رو VPS این دستور زده بشه (دقیقش تو کامنتِ بالای
AppVersionRoutes.kt هم هست):
```bash
sqlite3 ~/loan-server/data.sqlite \
  "UPDATE app_version SET latest_version_code = <شماره‌نسخه>,
   cafebazaar_url = 'https://cafebazaar.ir/app/ir.sadteam.loancalc',
   myket_url = 'https://myket.ir/app/ir.sadteam.loancalc' WHERE id = 1;"
```
