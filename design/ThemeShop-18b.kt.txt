/*
 * فروشگاهِ ظاهر — فریمِ ۱۸b و ۱۸bd
 *
 * **کشیده شده. فریم مرجع است، عددبه‌عدد.** این فایل سه کار می‌کند:
 * (الف) چیزی که با تصمیم‌های بعدی عوض شد، (ب) جدولِ رنگِ چهار تم که
 * فریم ندارد، (ج) حالت‌هایی که فریم نکشیده — شیتِ خرید، موجودیِ کم،
 * و کارتِ ترمیمِ استریک.
 *
 * ثبت شده: سکه هنوز هیچ‌جا خرج نمی‌شود (spend() صدا زده نمی‌شود)، پس
 * این صفحه **اولین مصرفِ سکه در برنامه است.** هدفِ پس‌انداز نیست، پس
 * نشانِ ۷ قفل با «به‌زودی». محتاط = میانگینِ ۳۰ روزه.
 *
 *
 * ══════════════════ الف · یک تغییر در فریم ══════════════════
 *
 * **ردیفِ سه‌تبِ «تمِ رنگی · آیکونِ دسته · نشانِ نام» را حذف کن.**
 * دو تبِ آخر جنس ندارند (کارِ گرافیکیِ تازه لازم دارند) و تبِ خالی
 * بدتر از تبِ نبوده است: کاربر می‌زند، خالی می‌بیند، و فروشگاه را
 * ناتمام می‌فهمد.
 *
 * جای خالی‌اش را پر نکن — کارتِ موجودی مستقیم به فهرستِ تم‌ها وصل
 * شود، با همان فاصله‌ی ۱۴dp. بقیه‌ی صفحه دست‌نخورده.
 *
 * وقتی جنسِ تبِ دوم آماده شد، ردیفِ تب عیناً همان‌طور که در فریم است
 * برمی‌گردد: چیپِ فعال پرِ #B45F00 با سایه‌ی سختِ ۰ ۳dp ۰ #8B4900،
 * چیپِ خاموش بی‌پر با متنِ AppMuted.
 *
 * تیترِ صفحه هم تا آن روز «تمِ رنگی» بهتر است تا «فروشگاهِ ظاهر» —
 * چیزی را وعده نده که یک قلم است.
 *
 *
 * ══════════════════ ب · جدولِ رنگِ چهار تم ══════════════════
 *
 * قاعده‌ی قفل‌شده: تم فقط `AppPrimary` و چهار مشتقش را عوض می‌کند.
 * بستر، سطح، خط، متن، و رنگ‌های معنایی (قرمز، نارنجی، آبیِ اطلاع)
 * **هرگز**. یعنی هر تم = ۵ مقدار × ۲ حالت = ۱۰ عدد، و تمام.
 *
 * ⚠ نکته‌ی مهم که فریم نمی‌تواند بگوید: در هر تم، `Primary` پرِ
 * دکمه‌ی اصلی با متنِ **سفید** است. رنگِ روشنِ هر پالت برای این کار
 * کنتراست ندارد (#1CB0F6 با سفید حدودِ ۲٫۴:۱ است — خوانا نیست).
 * پس در هر پالت **تیره‌تر Primary است و روشن‌تر سایه‌ی روشن** —
 * دقیقاً همان نسبتی که سبز دارد (#0EA968 در برابرِ #3DDC96).
 * سه نوارِ پیش‌نمایشِ فریم همین را نشان می‌دهد و به‌هم نزن.
 *
 * ┌─ سبزِ جیبک (پیش‌فرض، از TOKENS.md — دست نزن) ─────────────┐
 *   Primary #0EA968 · Dim #0B8C57 · Ink #0B8C57
 *   Pill #E9F7EF · Border #9FE0BC
 *   تیره: Ink #3DDC96 · Pill 0x243DDC96 · Border 0x593DDC96
 * └──────────────────────────────────────────────────────────┘
 *
 * ┌─ آبیِ آرام — ۱۵۰ سکه ────────────────────────────────────┐
 *   Primary #1381B8 · Dim #0F6892 · Ink #1381B8
 *   Pill #E7F4FC · Border #A9D6F7
 *   تیره: Ink #6FC8F2 · Pill 0x241CB0F6 · Border 0x591CB0F6
 *   نوارِ پیش‌نمایش: #1381B8 · #1CB0F6 · #F2F9FF   (از فریم)
 * └──────────────────────────────────────────────────────────┘
 *
 * ┌─ بنفشِ شب — ۱۵۰ سکه ─────────────────────────────────────┐
 *   Primary #7440C9 · Dim #5C2FA8 · Ink #7440C9
 *   Pill #F2EDFC · Border #CDB6F2
 *   تیره: Ink #BE97FF · Pill 0x24A56EFF · Border 0x59A56EFF
 *   نوارِ پیش‌نمایش: #7440C9 · #A56EFF · #F8F4FF   (از فریم)
 * └──────────────────────────────────────────────────────────┘
 *
 * ┌─ طلاییِ کهنه — با نشان باز می‌شود، نه با سکه ─────────────┐
 *   Primary #A87A16 · Dim #8B6314 · Ink #8B6F3D
 *   Pill #FDF4E0 · Border #EBD9B4
 *   تیره: Ink #C99A2E · Pill 0x24C99A2E · Border 0x59C99A2E
 *   نوارِ پیش‌نمایش: #5A3E12 · #C99A2E · #FFFCF4   (از فریم)
 *   شرطش: نشانِ «ماهِ منظم» (۳۰ روزِ پشتِ‌سرهم). همان‌طور بماند —
 *   قاعده‌ی خوبی است که سکه همه‌چیز را نمی‌خرد.
 * └──────────────────────────────────────────────────────────┘
 *
 * قیمتِ قفسه: ۲ × ۱۵۰ = **۳۰۰ سکه.** (در فایلِ اقتصاد ۴۵۰ گفتم، با
 * فرضِ سه تمِ خریدنی؛ طلایی سکه نمی‌گیرد، پس ۳۰۰.) یعنی کاربرِ منظم
 * قفسه را در **شش روز** تمام می‌کند و **ترمیمِ استریک اجباری است**،
 * وگرنه سکه از هفته‌ی دوم بی‌مصرف می‌شود.
 *
 * ⚠ سکه در همه‌ی تم‌ها طلایی می‌ماند (متنِ خودِ فریم). سکه نشانِ
 * برند است، نه رنگِ تم. همین‌طور نشانِ جیبک و آدمک‌ها.
 *
 *
 * ══════════════════ ج · حالت‌هایی که فریم ندارد ══════════════════
 *
 * ── شیتِ خرید ──
 * BottomSheet، پدینگِ ۲۰، وسط‌چین:
 *   نوارِ پیش‌نمایشِ همان تم در اندازه‌ی بزرگ: ۱۰۴×۵۶، گوشه‌ی ۱۴.
 *   نامِ تم ۱۵/۹۰۰، فاصله‌ی ۱۲dp.
 *   قیمت: ردیفِ افقیِ وسط‌چین — عددِ ۱۷/۹۰۰ AppText + سکه‌ی ۱۸dp،
 *     فاصله‌ی ۵dp.
 *   خطِ ۱۰/۷۰۰ AppMuted: «بعد از خرید ۳۳۰ سکه می‌مانَد.»
 *     ← این خط مهم است. کاربر باید قبلِ خرید بداند چه می‌ماند.
 *   دکمه‌ی اصلیِ تمام‌عرض: «بخر و روشن کن».
 *   عملِ متنیِ «بعداً» زیرش.
 *
 * پس از خرید: شیت بسته می‌شود و **تم فوراً اعمال می‌شود** — با همان
 * انیمیشنِ ۲۲۰ms رنگِ صفحه‌ی ظاهر. Snackbar: «آبیِ آرام روشن شد».
 * دیالوگِ «خریدت موفق بود» نگذار؛ خودِ عوض‌شدنِ رنگ رسیدِ خرید است.
 *
 * خریدِ برگشت‌ناپذیر است و در شیت **نگو** «برگشت‌ناپذیر» — تمِ
 * خریده‌شده مالِ کاربر می‌ماند و هر وقت بخواهد بینشان سوئیچ می‌کند.
 * یعنی خرید یک‌بار است، انتخاب بی‌نهایت.
 *
 * ── موجودیِ کم ──
 * چیپِ قیمت روی کارت **عوض نمی‌شود** (خاکستری نکن، قفل نزن) — کاربر
 * باید قیمت را ببیند. ضربه که زد، همان شیتِ خرید باز شود ولی:
 *   جای خطِ «بعد از خرید...»: «۹۰ سکه کم داری» با AppDangerInk.
 *   دکمه‌ی اصلی غیرفعال.
 *   و زیرش سه خطِ ۱۰/۷۰۰ AppMuted با نقطه‌ی ابتدای سطر:
 *     «هر روزِ ثبت ۱۰ سکه»
 *     «هفت روزِ پشتِ‌سرهم ۵۰ سکه»
 *     «نشانِ تازه ۲۵ تا ۱۵۰ سکه»
 *   و عملِ متنیِ «دیدنِ دستاوردها» → ۱۸a.
 * بن‌بست نساز. این تنها جایی است که کاربر انگیزه دارد بداند سکه از
 * کجا می‌آید.
 *
 * ── تمِ قفل‌شده با نشان ──
 * فریم کشیده (خط‌چینِ #C9D6CF، نوارِ آلفای ۰٫۴۵، قفلِ گردِ ۲۶dp).
 * ضربه رویش شیتِ خرید **نمی‌دهد** — شیتِ توضیح می‌دهد:
 *   نامِ تم، نوارِ پیش‌نمایشِ کامل (بی آلفا — بگذار ببیند چه می‌برد)،
 *   مدالِ «ماهِ منظم» در اندازه‌ی ۴۸dp با حلقه‌ی پیشرفتش،
 *   خطِ «۱۸ روزِ دیگر ثبتِ پشتِ‌سرهم» با AppMuted،
 *   دکمه‌ی ثانویه‌ی «دیدنِ دستاوردها».
 * قابلِ خرید با سکه نیست و دکمه‌ی خریدی هم نداشته باشد.
 *
 * ── پیش‌نمایشِ زنده ──
 * کارتِ ته فریم بماند و **مهم‌ترین ابتکارِ این صفحه است**: اجزای
 * واقعیِ برنامه (نوارِ بودجه، دکمه‌ی اصلی، دکمه‌ی لغو) با تمِ
 * انتخابی، نه یک مستطیلِ رنگی.
 * قاعده‌اش: پیش‌نمایش **تمِ زیرِ انگشت** را نشان دهد، نه تمِ فعال.
 * یعنی ضربه روی یک کارتِ خریدنی، اول پیش‌نمایش را عوض کند و شیت را
 * بعد باز کند — یا حتی شیت را باز نکند و خرید از خودِ کارت باشد.
 * پیشنهاد: ضربه‌ی اول پیش‌نمایش، ضربه‌ی دوم شیت. کاربر می‌تواند
 * بی‌خرید ببیند.
 * متنِ ۹٫۵/۷۰۰ ته کارت عیناً از فریم بماند.
 *
 *
 * ══════════════════ ترمیمِ استریک ══════════════════
 *
 * کشیده‌نشده و **در فروشگاه نیست** — خریدِ لحظه‌ای است، نه قفسه‌ای.
 * جایش کارتِ خانه، فقط در روزی که استریک دیروز شکسته.
 *
 * کارت: AppWarningPill، حاشیه‌ی ۲ #FFD79A، پدینگِ ۱۶، فاصله‌ی ۱۴dp:
 *   سمتِ راست: زنجیرِ شکسته‌ی بخشِ ۳۴ در اندازه‌ی ۴۴dp (همان نشانِ
 *     «زنجیرِ فعال» در حالتِ شکسته — کشیده شده، نو نساز).
 *   عنوان ۱۳/۹۰۰ AppText: «۱۲ روزِ پشتِ‌سرهم شکست»
 *   زیرنویس ۱۰/۷۰۰ #8B5A00: «با ۱۰۰ سکه برش می‌گردانی. ماهی یک‌بار.»
 *   دکمه‌ی تمام‌عرضِ زیرش، سبکِ نارنجی (پرِ #FF9600، سایه‌ی سختِ
 *     ۰ ۳dp ۰ #C97500، متنِ ۱۱/۹۰۰ سفید): «برگرداندنِ استریک»
 *   عملِ متنیِ «بگذار برود» — کارت را برای همیشه‌ی این شکست می‌بندد.
 *
 * سه قاعده:
 *   · فقط تا **۴۸ ساعت** پس از شکستن دیده می‌شود. بعد از آن نه.
 *   · ماهی یک‌بار. بارِ دوم در همان ماه، کارت اصلاً نیاید — نه با
 *     دکمه‌ی غیرفعال. (تعدادِ ترمیم‌ها از دفترِ سکه خوانده می‌شود،
 *     `type = streak_repair`.)
 *   · موجودی کم بود: کارت بیاید، دکمه غیرفعال، زیرنویس «۴۰ سکه کم
 *     داری». صادقانه‌تر از پنهان‌کردنِ کارت است.
 *
 * ⚠ ترمیم عددِ استریک را برمی‌گرداند، ولی **روزِ نبوده را روزِ ثبت
 * نمی‌کند.** سکه‌ی روزانه‌ی آن روز داده نمی‌شود و نشانِ «ماهِ منظم»
 * هم باید ترمیم را قبول کند یا نکند — تصمیمش پایین.
 */

@Composable
fun ThemeShopScreen(
    coinBalance: Int,
    themes: List<ShopTheme>,               // فعال · خریده‌شده · خریدنی · قفلِ نشان
    activeThemeId: ThemeId,
    previewThemeId: ThemeId,               // تمِ زیرِ انگشت، پیش‌نمایشِ زنده
    onPreview: (ThemeId) -> Unit,
    onBuy: (ThemeId) -> Unit,
    onSelect: (ThemeId) -> Unit,           // خریده‌شده‌ی غیرفعال
    onOpenBadges: () -> Unit,
    onBack: () -> Unit,
)

@Composable
fun StreakRepairCard(
    brokenStreakDays: Int,
    coinBalance: Int,
    repairPrice: Int = 100,
    onRepair: () -> Unit,
    onDismiss: () -> Unit,
)


/*
 * ══════════════════ تصمیم‌های لازم ══════════════════
 *
 * ۱. **ترمیمِ استریک روی نشانِ «ماهِ منظم» حساب شود؟** پیشنهاد: بله.
 *    اگر نه، کاربری که ۱۰۰ سکه داده و استریکش برگشته، در ۱۸a
 *    می‌بیند حلقه‌اش صفر شده — و این بدترین حسِ ممکن پس از یک خرید
 *    است. سقفِ ماهی یک‌بار خودش جلوی خریدنِ نشان را می‌گیرد.
 *
 * ۲. **دو تمِ ۱۵۰ سکه‌ای کافی است برای انتشار؟** با ۳۰۰ سکه‌ی کلِ
 *    قفسه، کاربرِ منظم شش‌روزه تمامش می‌کند. اگر ترمیمِ استریک هم
 *    همین دور پیاده نمی‌شود، پیشنهاد می‌کنم فروشگاه را **عقب
 *    بیندازی** و فقط ردیفِ تمِ رنگی را با دو تمِ رایگان بدهی —
 *    فروشگاهی که در هفته‌ی اول خالی می‌شود یک‌بار بد جا می‌افتد و
 *    دیگر جمع نمی‌شود.
 *
 * ۳. **تمِ خریده‌شده‌ی غیرفعال چه ظاهری دارد؟** فریم فقط «فعال» و
 *    «خریدنی» را دارد. پیشنهاد: مثلِ فعال ولی بی سایه‌ی سخت و
 *    بی چیپِ «فعال» — جایش چیپِ خنثیِ «روشن کن» با AppSurface2 و
 *    متنِ AppMuted. یعنی خریده‌شده‌ها با یک ضربه عوض می‌شوند.
 *
 * ۴. **جدولِ رنگ را کجا نگه داریم؟** پیشنهاد: `ColorTheme` یک enum
 *    با ۱۰ مقدار به‌ازای هر عضو، و `Color.kt` فقط سبز را ثابت نگه
 *    دارد. یعنی خواندنِ `AppPrimary` از تم می‌آید نه از ثابت — و
 *    این یعنی همه‌ی جاهایی که هارد‌کد شده بود (همان ۴۹ موردی که
 *    بستیم) باید واقعاً از توکن بخوانند، وگرنه تم نصفه عوض می‌شود.
 *    ⚠ این را قبل از انتشارِ تم آزمایش کن: یک تمِ آزمایشیِ زشت
 *    (مثلاً صورتیِ تند) بساز و کلِ برنامه را با آن بگرد. هر جایی
 *    که سبز ماند، هارد‌کد است.
 */
