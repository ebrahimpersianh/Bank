/*
 * دو صفحه: وام (فریمِ ۲۷a) و چک‌ها (فریمِ بخشِ ۳۶)
 *
 * داده از پارامتر می‌آید. ViewModel و ناوبری نیست. مبلغ‌ها Double.
 *
 * ══════════════════ اختلاف‌ها و تصمیم‌های لازم ══════════════════
 *
 * ۱. یک تصحیحِ خودم: دورِ قبل گفتم #A8E0C4 «فقط ستونِ غیرفعالِ نمودار» است.
 *    غلط بود — حاشیه‌ی چیپِ سبزِ پرشده هم هست (چیپِ «برگردان» در صفحه‌ی چک،
 *    و چند جای دیگر). پس دو حاشیه‌ی سبز واقعاً داریم:
 *      #9FE0BC روی قابِ آیکونِ ۳۲ (حاشیه‌ی ۱٫۵)
 *      #A8E0C4 روی چیپِ ۹۹۹ (حاشیه‌ی ۱)
 *    پیشنهادم: هر دو #9FE0BC شوند و AppPrimaryBorder یکی بماند. اختلافشان
 *    روی حاشیه‌ی ۱dp دیده نمی‌شود. تأیید کن تا در TOKENS.md هم اصلاح کنم.
 *
 * ۲. ضخامتِ ۲٫۸ برای آیکونِ «+» دکمه‌ی سرِ صفحه‌ی وام. AppStroke چهارمین
 *    مقدار نمی‌خواهد — AppStroke.iconActive (۲٫۶) بگذار.
 *
 * ۳. ریلِ SegmentedToggle در صفحه‌ی چک #E7EDF1 است، که آبی‌تر از
 *    AppChipBg (#F1F5F2) است و توکن نیست. SegmentedToggle از قبل داری —
 *    بگو ریلش الان چه رنگی است تا یکی‌شان کنیم. اگر AppChipBg است، همان
 *    بماند و #E7EDF1 را دور بریز.
 *
 * ۴. خطِ جداکننده‌ی صفحه‌ی چک #DFE7EC است. توکن نیست. AppLine بگذار.
 *
 * ۵. قابِ آیکونِ ۳۰ در ردیفِ پایانیِ صفحه‌ی وام #F5F8F6 است.
 *    AppSurface2 (#F7FAF8) بگذار.
 *
 * ۶. رنگِ قابِ آیکونِ چک بر اساسِ نوعش #7C4DD1 و #1E6FD9 با شفافیتِ ۱۴٪
 *    است — این دو عیناً دو رنگِ پالتِ آدمک‌اند، نه AppPurple/AppInfo.
 *    تصمیم لازم است: یا قاب‌ها به AppPurplePill/AppInfoPill بروند (پیشنهادِ
 *    من، چون رنگِ آدمک مالِ کاربر است نه دسته‌بندیِ چک)، یا اگر عمداً
 *    همان هیو را می‌خواهی بگو تا مقدارِ ماتشان را بدهم.
 *
 * ۷. مدالِ روبان‌دارِ وامِ تسویه‌شده در BrandMarks.kt نیست. اگر می‌خواهی
 *    پیاده شود بگو تا شکلش را عددبه‌عدد بدهم؛ فعلاً در کد به‌جایش
 *    CoinIcon گذاشتم که موجود است.
 *
 *
 * ═══════════════════════ صفحه‌ی وام — فریمِ ۲۷a ═══════════════════════
 *
 * پدینگِ کل AppSpace.page، فاصله‌ی AppSpace.betweenCards. اسکرول‌شونده.
 * ترتیب: سرِ صفحه · تب‌ها · کارتِ خلاصه‌ی طلایی · ردیف‌های وام · ردیفِ پشتیبان.
 */

@Composable
fun LoansScreen(
    summary: LoanSummary,
    loans: List<Loan>,
    selectedTab: LoanTab,
    onTabChange: (LoanTab) -> Unit,
    onAddLoan: () -> Unit,
    onLoanClick: (Loan) -> Unit,
    onPayInstallment: (Loan) -> Unit,
    onBackupClick: () -> Unit,
)

/*
 * ── سرِ صفحه ──
 * «وام» ۱۸/۹۰۰. دکمه‌ی افزودن سمتِ چپ: ۳۲dp، گوشه‌ی ۱۰، پرِ AppPrimaryPill،
 * حاشیه‌ی ۱٫۵ AppPrimaryBorder، آیکونِ «+» ۱۵dp با stroke = AppPrimary و
 * ضخامتِ AppStroke.iconActive. دورش ۴۴dp هدفِ لمسی بگذار.
 *
 * ── ردیفِ تب ──
 * سه تب: وام‌های من · سپرده · محاسبه‌گر. فاصله‌ی ۵dp. ریل ندارد.
 *   فعال:    پرِ AppPrimary، گوشه‌ی ۹۹۹، پدینگِ 6dp×11dp، متنِ ۱۰/۸۰۰ سفید،
 *            سایه‌ی AppShadow.inRow با AppPrimaryDim.
 *   غیرفعال: بی‌پس‌زمینه، پدینگِ 6dp×9dp، متنِ ۱۰/۷۰۰ AppMuted.
 * توجه: این با SegmentedToggleِ صفحه‌ی چک یکی نیست — آن ریل دارد و این ندارد.
 * دو الگوی متفاوت‌اند و عمدی: اینجا تب‌های ناهم‌عرضِ ناوبری، آنجا فیلترِ
 * سه‌حالته‌ی هم‌عرض. اگر می‌خواهی یکی‌شان کنم بگو، ولی توصیه نمی‌کنم.
 *
 * ── کارتِ خلاصه‌ی طلایی ──
 * AppGoldCard، پدینگِ ۱۶، گوشه‌ی card، حاشیه‌ی ۱٫۵ AppGoldBorder،
 * گرادیانِ ۱۶۵ درجه AppGoldFrom→AppGoldTo.
 * هاله: دایره‌ی ۸۴dp، گرادیانِ شعاعیِ rgba(247,187,53,.18) تا شفاف در ۷۰٪،
 * مرکزش ۲۰dp بیرونِ گوشه‌ی بالا-چپ. clipToBounds.
 *
 * سه ردیف با جداکننده‌ی خط‌چینِ ۱dp رنگِ AppGoldInk با آلفای ۰٫۳۲.
 * پدینگِ عمودیِ هر ردیف ۱۱dp (اولی فقط پایین، آخری فقط بالا).
 *   برچسب‌ها: ۱۰٫۵/۸۰۰ با AppGoldInk.copy(alpha = .65f)
 *   ماندهٔ کلِ بدهی → ۱۵/۹۰۰ AppGoldInk
 *   قسطِ ماهانه    → ۱۳/۹۰۰ AppPrimaryInk   ← عمداً سبز، چون رقمِ عمل است
 *   تا آزادیِ کامل → ۱۳/۹۰۰ AppGoldInk
 *
 * ── ردیفِ وام: سه حالت ──
 * هر سه: گوشه‌ی card، پدینگِ ۱۴، ردیفِ افقی با فاصله‌ی ۱۲dp، حلقه‌ی ۵۲dp.
 * حلقه: ضخامتِ ۶٫۵dp (نسبتِ فریم ۵ از ۴۰)، strokeCap = Round، شروع از
 * ساعتِ ۱۲ پادساعت‌گرد.
 *
 * الف) سررسیدِ نزدیک — پرِ AppDangerPill، حاشیه‌ی ۲dp #FFC9C9،
 *      سایه‌ی AppShadow.raised با #FFECEC.
 *      حلقه: ریلِ #FFE3E3، کمانِ AppDanger. مرکز: درصد ۱۱/۹۰۰ AppDangerInk.
 *      سکه‌ی ۱۱dp روی گوشه‌ی بالا-چپِ حلقه با آفستِ (-1,-1) — نشانِ
 *      «نزدیک‌ترین قسط». فقط روی همین حالت.
 *      دکمه‌ی پرداخت: پرِ AppPrimary، گوشه‌ی ۹۹۹، پدینگِ 9dp×13dp،
 *      متنِ ۱۰٫۵/۹۰۰ سفید، سایه‌ی inRow با AppPrimaryDim، هدفِ لمسیِ ۴۴.
 *
 * ب) در جریان — AppCard معمولی.
 *      حلقه: ریلِ AppChipBg، کمانِ AppPrimary. مرکز: درصد ۱۱/۹۰۰ AppPrimaryInk.
 *      انتهای ردیف شِوْرونِ ۱۴dp با AppLabel، رو به چپ (RTL).
 *
 * ج) تسویه‌شده — پرِ AppSurface2، حاشیه‌ی ۲dp AppLineRow، آلفای کلِ کارت ۰٫۷.
 *      حلقه: ریلِ AppPrimaryPill، کمانِ کاملِ AppPrimary، مرکز تیکِ ۱۸dp
 *      با AppPrimary و ضخامتِ ۳.
 *      انتهای ردیف مدال — موردِ ۷ بالا.
 *
 * متنِ هر سه (فاصله‌ی ۳dp و ۲dp):
 *   عنوان   ۱۲٫۵/۹۰۰ AppText
 *   سطرِ ۲  ۹٫۵/۷۰۰ — در حالتِ الف AppDangerInk، در ج AppPrimaryInk،
 *            در ب AppMuted. قالب: «<وضعیت> · <مبلغ>»
 *   سطرِ ۳  ۹/۷۰۰ AppMuted — «<n> از <m> قسط · <بانک>». در حالتِ ج نیست.
 *
 * ── ردیفِ پشتیبان‌گیری ──
 * گوشه‌ی AppRadius.row، پدینگِ 12dp×14dp، پرِ AppSurface، حاشیه‌ی ۲dp
 * AppLineRow. قابِ آیکون ۳۰dp گوشه‌ی ۹ پرِ AppSurface2، آیکونِ ۱۴dp با
 * AppMuted و ضخامتِ AppStroke.icon. متنِ ۱۱/۸۰۰. شِوْرونِ ۱۳dp AppLabel.
 * فاصله‌ی درونی ۱۰dp.
 *
 *
 * ═════════════════════ صفحه‌ی چک‌ها — فریمِ ۳۶ ═════════════════════
 *
 * پدینگِ AppSpace.page، فاصله‌ی ۱۳dp (نه ۱۲ — این صفحه ردیف‌های
 * کم‌ارتفاع‌تری دارد و ۱۳ تنفسش را درست می‌کند).
 */

@Composable
fun ChequesScreen(
    cheques: List<Cheque>,
    tab: ChequeTab,                 // InFlight · Cleared · Archived
    onTabChange: (ChequeTab) -> Unit,
    onChequeClick: (Cheque) -> Unit,
    onArchive: (Cheque) -> Unit,
    onRestore: (Cheque) -> Unit,
    onCalendarClick: () -> Unit,
    onSortClick: () -> Unit,
)

/*
 * ── سرِ صفحه ──
 * «چک‌ها» ۱۵/۹۰۰ — نه ۱۸. این صفحه از تبِ پایین باز نمی‌شود بلکه یک لایه
 * تودرتوست، و تیترِ کوچک‌تر همین را می‌گوید. اگر در ساختارِ تو صفحه‌ی
 * سطحِ اول است، ۱۸ بگذار.
 *
 * دو دکمه‌ی سمتِ چپ با فاصله‌ی ۷dp: تقویم و مرتب‌سازی.
 * هر کدام ۳۰dp، گوشه‌ی ۱۰، پرِ AppSurface، حاشیه‌ی ۱٫۵ AppLine،
 * آیکونِ ۱۵dp با AppMuted و ضخامتِ AppStroke.icon. هدفِ لمسیِ ۴۴.
 *
 * ── SegmentedToggle ──
 * سه حالت: در جریان · پاس‌شده · بایگانی.
 * ریل: گوشه‌ی ۹۹۹، پدینگِ ۳dp، فاصله‌ی ۶dp. رنگش موردِ ۳ بالا.
 * هر حالت flex برابر، پدینگِ عمودیِ ۸dp، متنِ ۱۱/۹۰۰.
 *   فعال:    پرِ AppPrimary، گوشه‌ی ۹۹۹، متنِ سفید.
 *   غیرفعال: بی‌پس‌زمینه، متنِ AppMuted.
 * تبِ «بایگانی» آیکونِ ۱۲dp قبلِ متن دارد با فاصله‌ی ۴dp — چون تنها تبی
 * است که محتوایش جنسِ دیگری دارد. دو تبِ دیگر آیکون نمی‌گیرند.
 *
 * ── ردیفِ چک ──
 * پرِ AppSurface، حاشیه‌ی ۱٫۵ AppLineRow، گوشه‌ی ۱۸، پدینگِ ۱۳، فاصله‌ی ۱۱dp.
 * فاصله‌ی بینِ ردیف‌ها ۹dp — نه betweenCards، چون این‌ها یک فهرست‌اند نه
 * چند کارتِ مستقل.
 *
 * قابِ آیکون: ۳۴dp، گوشه‌ی ۱۱، آیکونِ ۱۶dp با ضخامتِ AppStroke.icon.
 * رنگش از نوعِ چک می‌آید — موردِ ۶ بالا.
 *
 * متن: عنوان ۱۲/۸۰۰ («<بانک> · <چهار رقمِ آخر>»)، زیرش ۹٫۵/۷۰۰ AppMuted
 * با فاصله‌ی ۲dp. مبلغ سمتِ چپ ۱۲/۹۰۰ AppText.
 *
 * شماره‌ی چک ذاتاً چپ‌به‌راست است — تکه‌ی «۸۴۲۱» را در Ltr بپیچ، نه کلِ سطر.
 *
 * ── کشیدنِ ردیف به چپ ──
 * SwipeToDelete موجود را با کنشِ «بایگانی» به‌کار بگیر، نه حذف.
 * پنلِ کنش: عرضِ ۷۰dp، پرِ AppMuted، آیکونِ ۱۷dp سفید با ضخامتِ ۲٫۴،
 * برچسبِ ۹/۹۰۰ سفید، فاصله‌ی ۴dp، عمودی وسط‌چین.
 * کلِ ردیف و پنل داخلِ یک clip با گوشه‌ی ۱۸ و حاشیه‌ی ۱٫۵ AppLineRow
 * می‌نشینند تا پنل از حاشیه بیرون نزند.
 * جهت مهم است: در RTL «کشیدن به چپ» یعنی به سمتِ انتهای منطقیِ ردیف.
 * اگر SwipeToDelete جهت را از layoutDirection می‌گیرد، دست نزن.
 *
 * ── ردیفِ بایگانی‌شده (داخلِ تبِ سوم) ──
 * همان ردیف با آلفای ۰٫۷۲. قابِ آیکون پرِ AppChipBg با آیکونِ AppLabel.
 * مبلغ ندارد؛ جایش چیپِ «برگردان»: ۹٫۵/۹۰۰ AppPrimaryInk، پرِ
 * AppPrimaryPill، حاشیه‌ی ۱dp AppPrimaryBorder، پدینگِ 5dp×10dp، گوشه‌ی ۹۹۹.
 * چیپ کلیک‌پذیر است، پس هدفِ لمسیِ ۴۴ لازم دارد.
 *
 * ── حالتِ خالی ──
 * EmptyState موجود. متنِ فریم: «چکی ثبت نشده» ۱۴/۹۰۰ + توضیحِ ۱۱/۷۰۰
 * AppMuted با lineHeight ۱٫۹، و دکمه‌ی تمام‌عرضِ AppPrimary با پدینگِ ۱۳،
 * متنِ ۱۲٫۵/۹۰۰، سایه‌ی AppShadow.raised.
 *
 *
 * ── مدل‌های داده ──
 *
 * LoanSummary(totalDebt: Double, monthlyInstallment: Double, monthsLeft: Int)
 * Loan(
 *   title: String, bank: String, amount: Double,
 *   paidCount: Int, totalCount: Int,
 *   dueLabel: String,                  // از قبل فرمت‌شده
 *   state: LoanState,                  // DueSoon · Active · Settled
 *   isNearest: Boolean,                // سکه‌ی روی حلقه
 * )
 * Cheque(
 *   bank: String, lastFour: String, amount: Double,
 *   dueLabel: String, kind: ChequeKind, state: ChequeState,
 * )
 *
 * درصدِ حلقه‌ی وام = paidCount / totalCount. جدا نفرست، محاسبه‌اش همان‌جا.
 * isNearest فقط برای یک وام true است؛ اگر دو وام یک روز سررسید داشتند،
 * آن با مبلغِ بیشتر.
 */
