# جیبک — سندِ کاملِ تحویل (Handover)

**تاریخِ این سند:** ۲۶ شهریور ۱۴۰۵ / ۲۰۲۶-۰۹-۱۷ · **آخرین بیلدِ موفق: ۵۵۹** (تگِ `build-559`)

این سند برای این نوشته شده که اگر پروژه به یک هوشِ مصنوعیِ دیگر یا یک برنامه‌نویسِ
تازه سپرده شود، **بدونِ هیچ دانشِ قبلی** بتواند کار را ادامه بدهد. هیچ‌چیز خلاصه نشده.

---

# فهرست

1. برنامه چیست
2. مخزن، برنچ‌ها و گردشِ کار
3. معماریِ کد
4. دیتابیس
5. سرور
6. استورها و درآمد
7. قاعده‌های فنیِ نشکستنی
8. بررسی‌های ایستا (۲۲ بررسی)
9. کارِ طراحی — تاریخچه‌ی کامل
10. وضعیتِ فعلی: چه چیزی ساخته شده
11. چه چیزی باقی مانده
12. ایرادها و ریسک‌هایی که باید گرفته شوند
13. چک‌لیستِ تستِ روی دستگاه
14. دسترسی‌ها و سکرت‌هایی که لازم است

---

# ۱. برنامه چیست

**جیبک** — برنامه‌ی اندرویدِ فارسیِ حسابداریِ شخصی، برای کافه‌بازار و مایکت. تیمِ «Sad Team».

- **نامِ نمایشی:** جیبک (۲۱ مرداد از «حسابدار من» عوض شد).
- 🚨 **`applicationId` همیشه `ir.sadteam.loancalc`** — هیچ‌وقت عوض نشود. عوض‌کردنش یعنی
  از دیدِ استورها یک برنامه‌ی جدید، و همه‌ی نصب‌ها و نظرها از بین می‌رود.
- `minSdk = 24` · `targetSdk = 34` · ۱۰۰٪ Kotlin · Jetpack Compose.
- `versionCode`/`versionName` خودکار از `github.run_number` تزریق می‌شوند.

## تاریخچه‌ی کوتاهِ محصول
برنامه اولش فقط **محاسبه‌گرِ اقساطِ وام** بود. بعد ماژولِ حسابداریِ شخصی اضافه شد
(دخل/خرج، بودجه، گزارش، دارایی، طلب‌وبدهی) و حالا وام و چک فقط **بخشی** از یک
برنامه‌ی حسابداریِ کامل‌اند.

## ساختارِ ناوبری
**پنج تبِ نوارِ پایین:** خانه · دارایی · گزارش · بودجه · سررسید.
«وام» و «چک» **تب نیستند** — از «خانه»/«سررسید» به‌عنوانِ صفحه‌ی پوش‌شده باز می‌شوند.
تبِ وام زیرناوبریِ داخلی دارد: وام‌های من · سپرده · محاسبه‌گر.

⚠️ نوارِ پایین **قابلِ شخصی‌سازی** است (بخشِ ۴۱): خانه‌ها از `navSlots` در DataStore
می‌آیند، نه از یک enum ثابت. فقط اسلاتِ ۰ (خانه) قفل است.

---

# ۲. مخزن، برنچ‌ها و گردشِ کار

## مخزن
`github.com/ebrahimpersianh/Bank`

## برنچ‌ها
- 🚨 **کارِ فعلی روی `claude/bank-9fiumm`** است. این برنچِ اجباریِ هارنسِ CCR است.
- `main` حدودِ **۴۹۹ کامیت عقب** است. فقط تعمیراتِ امنیتیِ سرور جداگانه و با تاییدِ
  صریحِ کاربر روی `main` چری‌پیک شده‌اند.
- **مرج به `main` هیچ‌وقت خودسرانه انجام نمی‌شود** — فقط با تاییدِ صریحِ کاربر.
- برنچِ قدیمی‌ترِ `claude/loan-chat-program-continue-4vpy1r` با PR بازِ #66 هم هست؛
  برنچِ فعلی از روی همان ساخته شده و همه‌ی کارش را دارد.
- PRِ بازِ فعلی: **#67**.

## ورک‌فلوهای GitHub Actions (۱۳ تا)
| فایل | کار |
|---|---|
| `build-native-android.yml` | بیلدِ APK هر دو فلیور + انتشارِ GitHub Release با تگِ `build-<شماره>` |
| `build-server.yml` | بیلد و دیپلویِ خودکارِ سرور روی هر پوشِ `main` که `server/**` را عوض کند |
| `instrumented-tests.yml` | تستِ migration روی امولاتورِ واقعی |
| `record-paparazzi-snapshots.yml` | بازسازیِ عکس‌های مرجعِ تستِ اسکرین‌شات (دستی) |
| `publish-release.yml` | انتشار |
| `set-melipayamak-env.yml` | نوشتنِ سکرتِ پیامک روی `.env`ِ سرور |
| `set-cafebazaar-env.yml` | همان برای کافه‌بازار |
| `set-price-env.yml` | همان برای کلیدِ سرویسِ قیمت |
| `set-app-version.yml` | به‌روزرسانیِ جدولِ نسخه‌ی سرور |
| `check-server-health.yml` | سلامتِ سرور + چاپِ ۵ کرشِ آخر + قیمت‌ها |
| `check-recent-signups.yml` | ثبت‌نام‌های اخیر |
| `backup-server-db.yml` | پشتیبانِ دیتابیسِ سرور |
| `export-db-schema.yml` | خروجیِ اسکیما |

## 🚨 قاعده‌ی حیاتیِ بیلد — سهمیه‌ی Actions
- سهمیه‌ی ماهانه‌ی کاربر **محدود** است و هر رانِ اندروید ≈ **۱۵ تا ۲۲ دقیقه**.
- **هر کامیتِ میانی باید `[skip ci]` داشته باشد.** بیلد فقط دستی با `workflow_dispatch`.
- 🚨 **فیلترِ `paths:` روی ایونتِ `pull_request` بی‌اثر است.** گیت‌هاب کلِ دیفِ PR را
  نگاه می‌کند نه آخرین پوش را؛ چون PR #67 صدها فایلِ اندرویدی دارد، **حتی یک کامیتِ
  فقط-مستندات هم یک بیلدِ کامل راه می‌اندازد**. پس نمی‌شود به فیلترِ مسیر تکیه کرد.
- APKها به‌عنوانِ **GitHub Release** منتشر می‌شوند نه Artifact (سهمیه‌ی فضا پر می‌شد).

## سندباکس نمی‌تواند اندروید را بیلد کند
`dl.google.com` مسدود است. **تنها راهِ تاییدِ کامپایل، اجرای ورک‌فلو است.** `server/`
را ولی می‌شود محلی بیلد و تست کرد.

## دستورهای واقعی
```bash
# اندروید (از native-android/)
./gradlew :core:test                          # تستِ موتورِ محاسبه (JVM، سریع)
./gradlew :app:testCafebazaarDebugUnitTest    # تستِ اسکرین‌شاتِ Paparazzi
./gradlew :app:assembleRelease                # هر دو فلیور
./gradlew :data:connectedDebugAndroidTest     # تستِ migration - امولاتور لازم دارد

# سرور (از server/)
./gradlew test
./gradlew fatJar        # جارِ runnable که به VPS می‌رود
```

---

# ۳. معماریِ کد

```
Bank/
├── native-android/          # اپ - ۳۳۰ فایلِ Kotlin
│   ├── app/                 # UI (Compose)، Hilt، ناوبری
│   ├── data/                # Room + SQLCipher، DataStore، Retrofit
│   ├── core/                # محاسباتِ خالص، بدونِ وابستگیِ اندرویدی
│   └── tools/static-checks/ # ۳۰ اسکریپتِ پایتون، ۲۲ تا فعال
├── server/                  # Ktor + SQLite - ۲۳ فایلِ Kotlin
├── design/                  # ۹۷ فایل: فریم‌ها، پیام‌ها، جواب‌ها
│   └── frames/              # ۱۳ فایلِ .dc.html
├── .github/workflows/
├── CLAUDE.md                # حافظه‌ی پروژه - مهم‌ترین فایل برای ادامه‌دهنده
└── ci-debug.keystore        # 🚨 کلیدِ ثابتِ دیباگ - پاکش نکن
```

## ماژولِ `:core` — منطقِ خالص (۲۳ فایل)
`LoanCalculator` · `AffordabilityCalculator` · `DepositCalculator` · `RateFinderCalculator` ·
`FinancialMath` · `JalaliCalendar` · `PersianDate` · `PersianFormat` · `BankSmsParser` ·
`BankAppMatcher` · `MerchantCategoryGuesser` · `RecurringDetector` · `MonthForecast` ·
`ActiveStreak` · `NavSuggestion` · `NameMatch` · `ChequeRisk` · `Dang` · `Account` ·
`Cheque` · `Income` · `ReminderOffsets` · `Security`

⚠️ **`:core` فقط `kotlin("test")` را در کلاس‌پثِ تست دارد، نه JUnit4** — در تست‌های این
ماژول از `kotlin.test.*` ایمپورت کنید.

## ماژولِ `:app` — بخش‌های UI
`account` · `accounting` · `asset` · `auth` · `background` · `calendar` · `category` ·
`cheque` · `coin` · `components` · `debt` · `due` · `goal` · `haptics` · `history` ·
`home` · `inbox` · `jibak` · `myloans` · `nav` · `note` · `onboarding` · `privacy` ·
`profile` · `rating` · `security` · `settings` · `shop` · `sound` · `stats` ·
`subscription` · `theme` · `update` · `widget`

## کامپوننت‌های مشترک (قبل از ساختنِ چیزِ تازه اول این‌ها را ببینید)
`AppCard` (+`accentGradient`، چهار گونه) · `GradientButton` (پنج گونه) · `AppChip` ·
`SegmentedToggle` · `EmptyState` · `SwipeToDeleteRow` · `ConfirmDialog` (سه لحن:
DESTRUCTIVE/PAYMENT/HEAVY_CHANGE) · `AccountPickerDialog` · `BankBadge` · `TodayCard` ·
`SuccessCheckmarkOverlay` · `StaggerIn` · `Motion.kt` · `pressScaleClickable` · `Ltr.kt` ·
`ThousandsTransformation.kt` · `InAppBannerHost` · `FullScreenDialog` · `AppFab` ·
`AppHeroCard` · `ProgressRing` · `AvatarFrame` · `CompactTransactionRow`

---

# ۴. دیتابیس

## کلیات
- **Room + SQLCipher** (`net.sqlcipher`، نه SQLiteِ استاندارد) — رمزنگاری‌شده.
- **نسخه‌ی فعلی: ۳۲** · **۴۷ ارجاع به مایگریشن** در `AppDatabase.kt`.
- `exportSchema = true`؛ اسکیماها در `native-android/data/schemas/`.
- ⚠️ **فقط `12.json` و `29.json` موجودند** — یعنی تستِ خودکارِ مایگریشن برای بقیه‌ی
  نسخه‌ها ممکن نیست (حالتِ «قبل» در دسترس نیست). این یک بدهیِ فنیِ واقعی است.
- چون SQLCipher است، تستِ migration روی JVM ممکن نیست؛ **امولاتور لازم است**.

## جدول‌های اصلی
حساب‌ها (`accounts`) · تراکنش‌ها (`account_transactions`) · وام‌ها (`loans`) ·
ردیف‌های قسط (`loan_rows`) · چک‌ها (`cheques`) · دسته‌چک‌ها · دسته‌بندی‌های دلخواه ·
ترتیبِ دسته‌ها · بودجه‌ها · دارایی‌ها · طلب‌وبدهی · طرف‌حساب‌ها · دنگ (رویداد/شرکت‌کننده/
قلم/سهم) · یادداشت‌ها · قاعده‌های تشخیصِ پیامک · تاریخچه‌ی محاسبات · دستاوردها
(`achievements`) · دفترِ سکه (`coin_events`) · صندوقِ پیام (`inbox_messages`) ·
پرداخت‌های تکراری · درآمدها · **هدف‌های پس‌انداز (`savings_goals` — نسخه‌ی ۳۲)**

## 🚨 درس‌های گران‌قیمتِ دیتابیس
1. **دو منبعِ حقیقت برای یک عدد = فاجعه.** `LoanEntity` هم ستونِ `paidCount` داشت هم
   کپی‌اش داخلِ `dataJson`. فقط ستون به‌روز می‌شد ولی خروجیِ سرور/پشتیبان از JSON
   می‌خواند → بعد از خروج و ورودِ دوباره، **پیشرفتِ همه‌ی وام‌ها صفر** می‌شد. رفع:
   `withPaidCount()` هر دو را با هم می‌نویسد و `repairPaidCounts()` داده‌ی خرابِ قبلی
   را خودش ترمیم می‌کند.
2. **هر `CREATE INDEX` در مایگریشنِ دستی باید عیناً در `indices = [...]`ِ همان
   `@Entity` هم اعلام شود.** یک‌بار نشد و نسخه‌ی ۱.۰.۳۱۵ برای هر کسی که آپدیت می‌کرد
   کرش می‌کرد. فقط مسیرِ آپگرید؛ نصبِ تازه این باگ را نمی‌گیرد.
3. **`LoanRepository.getRows()` یک `suspend` است** — هیچ‌وقت داخلِ `remember{}`
   صدایش نکنید (کرشِ تردِ اصلی). الگوی درست: `LaunchedEffect`.
4. **`AccountRepository.addTransaction` پارامترِ `id` اختیاری دارد** — هر جا داخلِ
   حلقه تراکنش می‌سازید شمارنده‌ی صریح بدهید، وگرنه `@Upsert` بی‌صدا رویشان می‌نویسد.
5. **نام به‌عنوانِ کلید.** `AccountTransactionEntity.category`، `BudgetEntity.categoryName`،
   `RecurringPaymentEntity.categoryName` و `ParsingRuleEntity.category` همه **نام** را
   ذخیره می‌کنند نه id. تغییرِ نامِ دسته باید همه‌جا مهاجرت کند
   (`AccountRepository.renameCategoryEverywhere`).

## واحدِ پول — مهم‌ترین قاعده
🚨 **دیتابیس همه‌جا ریال، نمایش همه‌جا تومان.** تبدیل **فقط** روی لبه‌ی UI با
`tomanToRial`/`rialToToman`. چاپِ مستقیمِ مقدارِ خامِ دیتابیس با برچسبِ «تومان» یعنی
عددِ ۱۰برابر — این باگ تا امروز **چهار بار** رخ داده (صفحه‌ی نقدی، آمارِ وام، دیالوگِ
ویرایشِ قسط، گزارشِ روزانه).

---

# ۵. سرور

## زیرساخت
- **VPS آروان‌کلاود**، IP `37.152.187.44`
- دامنه: `server-8a887fd3-2459-4e6d-ace8-884dc36ca3db.ir-thr-fr1.arvancompute.ir`
- HTTPS با **certbot** و تمدیدِ خودکار
- **nginx** روی ۸۰/۴۴۳ به `localhost:3000` پراکسی می‌کند
  (`/etc/nginx/sites-available/loan-api`)
- جار در `~/VameMan/` با **pm2** (نامِ پروسه: `loan-calc-api`)
- `.env` کنارِ jar

## کدِ سرور (۲۳ فایل)
`Application.kt` · `Db.kt` (اسکیما + مایگریشن) · `Auth.kt` · `Sms.kt` ·
`SubscriptionStatus.kt` · `Cafebazaar.kt` · `Myket.kt` · `PriceService.kt` ·
`RateLimit.kt` · `Log.kt` · `Env.kt` · `SqlExt.kt` + هشت فایلِ `routes/`

## مسیرهای API
| مسیر | کار |
|---|---|
| `POST /api/auth/request-otp` | درخواستِ کدِ تایید |
| `POST /api/auth/verify-otp` | تاییدِ کد و صدورِ JWT |
| `GET /api/auth/me` | وضعیتِ کاربر و اشتراک |
| `DELETE /api/auth/account` | حذفِ حسابِ کاربری |
| `/api/loans` | سینکِ وام‌ها |
| `POST /api/subscription/verify` | صحت‌سنجیِ خریدِ اشتراک |
| `GET /api/subscription/history` | تاریخچه‌ی خرید |
| `GET /api/app-version` | آخرین نسخه + لینکِ استورها |
| `GET /api/credit-rates` | نرخ‌های اعتباری |
| `GET /api/prices` | قیمتِ لحظه‌ایِ ۵۶ نماد |
| `GET /api/prices/history?symbol=BTC&days=30` | تاریخچه‌ی قیمت |
| `/api/crash` | گزارشِ کرش |
| `/api/backup` | پشتیبان |

## 🚨 دیپلوی
**هر پوش به `main` که `server/**` را عوض کند** → `build-server.yml` خودکار بیلد
می‌کند، با scp می‌فرستد، سلامت را چک می‌کند، جای jar را با بکاپِ `.bak` عوض می‌کند،
`pm2 restart` می‌زند و **اگر بالا نیامد خودکار برمی‌گرداند**.

🚨 **هیچ‌وقت دستی jar آپلود نکنید** — سه بار همین کار سرورِ زنده را خواباند.

⚠️ درسِ دیپلوی: ورک‌فلو قبلاً jar را بینِ دو job با artifact دست‌به‌دست می‌کرد و سهمیه‌ی
artifact پر شد و **۴ بار پشتِ‌هم شکست خورد**. حالا **یک jobِ واحد** است که مستقیم scp
می‌کند. اگر ورک‌فلوی دیگری همین مشکل را گرفت، همین الگو را اعمال کنید.

## OTP — ✅ کامل و تست‌شده
- سرویس: **ملی‌پیامک** (SOAP، `SendByBaseNumber`)
- پترنِ تاییدشده `bodyId=496763` که هشِ SMS Retriever (`FuPsBGlTa0q`) در خطِ آخرش است
- کاربر با شماره‌ی واقعی تست کرد و رسید
- تعویضِ سرویسِ پیامکی = فقط `Sms.kt` (`sendOtpSms`)
- 🚨 **درس:** این سرویس‌ها «رمزِ ورودِ پنل» و «APIKeyِ وبسرویس» **جدا** دارند. در
  `MELIPAYAMAK_PASSWORD` حتماً آنی که برای **وبسرویس** است — یک‌بار همین اشتباه
  «ارسالِ کد ناموفق» داد.
- ⚠️ **هشِ SMS Retriever به بسته‌نام + کلیدِ امضا بستگی دارد** (دیباگ و ریلیز دو هشِ
  متفاوت). `ui/auth/SmsRetrieverHash.kt` هشِ واقعی را لاگ می‌کند
  (`adb logcat -s SmsRetrieverHash`). اگر کلیدِ امضای ریلیز عوض شود، هشِ تازه باید در
  پترنِ ملی‌پیامک هم آپدیت شود.

## قیمتِ طلا/ارز/رمزارز — ✅ حل‌شده، منبع: servix.cc
تاریخچه‌ی دردناکش: **نوسان** (۴۰۱ داد) → **gheymat.online** (کلید را رد کرد) →
**servix.cc** (کار کرد).

- کلید در **هدر** می‌رود (`X-API-Key`)، نه در URL
- `GET /api/v1/assets` همه‌ی نمادها را یکجا می‌دهد، فقط ۱ واحد از سهمیه
- مقادیرِ `..._RLS` مستقیم ریالند
- **۵۶ نماد** پشتیبانی می‌شود؛ نگاشت در `CATALOG_TO_CODE` داخلِ `PriceService.kt`
- **تازه‌سازی هر ۲۹ دقیقه** (۴۹ فراخوان از سقفِ ۵۰ در روز؛ ۱۴۴۰÷۵۰=۲۸٫۸ → ۲۹)
- نگاشتِ نمادها **عمداً سمتِ سرور** است تا اصلاحش آپدیتِ اپ نخواهد
- تاریخچه از اسنپ‌شاتِ خودمان (`price_history`)، نه endpointِ تاریخچه‌ی سرویس
- ⚠️ `TON` و `SILVER_999` نگاشتِ ریالی ندارند («—» درست است، باگ نیست)
- 🚨 **`attributionRequired: true`**: سرویس **قراردادی** می‌خواهد منبع دیده شود. الان
  دو جا هست: ردیفِ «منبعِ قیمت‌ها · Servix.cc» در «درباره‌ی برنامه» + خطِ ۹px پای
  صفحه‌ی «قیمتِ روز». **بی تاییدِ صریحِ کاربر حذفش نکنید** — ریسکش قطعِ دسترسیِ قیمت
  برای همه‌ی کاربران است.

## آپدیتِ خودکارِ برنامه
جدولِ تک‌ردیفه‌ی `app_version`. **دستی است** — بعد از هر انتشارِ واقعی روی VPS بزنید:
```bash
sqlite3 ~/VameMan/data.sqlite \
  "UPDATE app_version SET latest_version_code = <شماره‌نسخه>,
   cafebazaar_url = 'https://cafebazaar.ir/app/ir.sadteam.loancalc',
   myket_url = 'https://myket.ir/app/ir.sadteam.loancalc' WHERE id = 1;"
```

## نکته‌ی فنیِ سرور
`toUserRow()` ستون‌ها را **با اسم** می‌خواند: هر `SELECT ... FROM users` باید همه‌ی
ستون‌ها (از جمله `subscription_tier`) را داشته باشد وگرنه `SQLException`.

---

# ۶. استورها و درآمد

## دو فلیور
- **`cafebazaar`** — کتابخانه‌ی Poolakey
- **`myket`** — کتابخانه‌ی رسمیِ `com.github.myketstore:myket-billing-client` (الگوی
  کلاسیکِ IAB v3)

هر فلیور `SubscriptionManager`ِ خودش را دارد (`app/src/{cafebazaar,myket}/...`)؛
بقیه‌ی برنامه فرقی نمی‌بیند.

## چهار محصولِ اشتراک
`unlimited_loans_1m` / `3m` / `6m` / `1y` — به ریال: ۳۰۰۰۰۰ · ۸۱۰۰۰۰ · ۱۴۴۰۰۰۰ · ۲۵۲۰۰۰۰
(روی هر دو پنل تعریف شده‌اند)

- 🚨 **مایکت `ITEM_TYPE_INAPP` می‌خواهد نه `ITEM_TYPE_SUBS`** (پنلش اصلاً «نوعِ محصول» ندارد)
- صحت‌سنجیِ سمتِ سرورِ مایکت در `Myket.kt` با توکنِ ثابتِ `MYKET_ACCESS_TOKEN`
- ⚠️ **فرمتِ ریسپانسِ موفقِ مایکت مستند نبود** — فعلاً فقط بر اساسِ کدِ HTTP (۲xx).
  بعد از اولین خریدِ واقعی لاگِ سرور را چک کنید.

## 🚨 حادثه‌ی خریدِ گیرکرده‌ی مایکت — درسِ مهم
بازبینِ مایکت گفت «بعد از پرداختِ موفق، محصول فعال نمی‌شود».
- **تشخیصِ اولیه غلط بود** و کامیتش برگردانده شد: فرض شده بود IabHelperِ مایکت مثلِ
  گوگل `handleActivityResult` دارد — **ندارد و اصلاً کامپایل نشد**.
- **مکانیزمِ واقعی** (تاییدشده از سورس): `launchPurchaseFlow` نتیجه را از
  `ProxyBillingActivity`ِ داخلیِ خودِ کتابخانه می‌گیرد، مستقل از `onActivityResult`.
- **علتِ محتمل:** کشته‌شدنِ پروسه در پس‌زمینه حینِ پرداخت → از بین رفتنِ listener.
- **رفعِ پابرجا:** `SubscriptionManager.restorePurchases()` — هر بار صفحه‌ی اشتراک باز
  می‌شود، از روی inventory چک می‌کند کاربر مالکِ چه SKUهایی است و بی‌صدا برای تاییدِ
  سرور می‌فرستد.
- 🚨 **درسِ کلی:** قبل از فرضِ رفتارِ یک کتابخانه‌ی خارجی بر اساسِ شباهت با کتابخانه‌ی
  مشابه، **سورسِ واقعی‌اش را چک کنید** — خصوصاً در مسیرِ پرداخت.

## Play Protect
اولین نصبِ APKِ مایکت (کلیدِ امضای تازه) «Blocked by Play Protect» می‌دهد — طبیعی است.
فرمِ Google Play Protect Appeals در ۲۷ تیر ۱۴۰۵ برای رانِ ۲۵۹ ارسال شد
(SHA256 `d23a8ef52ee330ad18dca043e42bdda4477923ff7b8507f0a1e83c5311d2d4ad`).
⚠️ **تا معلوم‌شدنِ نتیجه، `applicationId` و کلیدِ امضای release را عوض نکنید.**

---

# ۷. قاعده‌های فنیِ نشکستنی

## تاریخِ فارسی (`core/PersianDate.kt`)
- 🚨 `addDays`/`addMonths` **باید مسیرِ جدا برای عددِ منفی داشته باشند** — `repeat(n)`
  در Kotlin برای nِ منفی بی‌صدا هیچ کاری نمی‌کند. این باگ «روزِ قبل» و نمودارِ ۷روزه
  را خراب کرده بود. تستِ رگرسیون دارد.
- برای سررسید، اگر `intervalDays % 30 == 0` از `addMonths` استفاده کنید نه `addDays`
  (تقویمِ فارسی ماهِ یکسانِ ۳۰روزه ندارد) وگرنه روزِ سررسید می‌لغزد.
- `addMonths` برای clampِ روز از `JalaliCalendar.daysInMonth` استفاده می‌کند (اسفندِ کبیسه).
- **«تاریخ دریافتِ وام» خودش سررسیدِ قسطِ اول است** (تصمیمِ صریحِ کاربر). استثنا:
  وام‌های دارای دوره‌ی تنفس. تاریخ‌ها ذخیره نمی‌شوند، پویا حساب می‌شوند.

## فرمول‌های حساسِ محاسبه (`core/LoanCalculator.kt`)
- **وام‌های عادی (نرخ > ۴٪):** اقساطِ مساویِ نزولی،
  `i = نرخ سالانه/100 × (فاصله‌ی هر قسط به روز/365)`
- 🚨 **قرض‌الحسنه (≤ ۴٪):** سودِ مرکب ندارند. قسط‌های ۱، ۱۳، ۲۵، ۳۷… (ابتدای هر سال)
  فقط **کارمزدِ سالانه**‌اند (روی مانده‌ی همان لحظه) و اصلِ وام در آن‌ها نیست؛ اصل فقط
  بینِ `n − ⌈n/12⌉` قسطِ دیگر مساوی تقسیم می‌شود.
  **تغییرش ندهید مگر با تستِ دقیق** (`LoanCalculatorRegressionTest.kt`).
- ⚠️ **ناهماهنگیِ بازِ شناخته‌شده:** برای وامِ ازدواج (۳ میلیارد ریال، ۱۲۰ ماهه، ۴٪)
  بانک ۲۸٬۱۵۵٬۰۰۰ اعلام کرده، برنامه ۲۷٬۲۷۲٬۷۲۷. با اجرای دستیِ فرمول تایید شد که
  **اجرای کد درست است**؛ هیچ‌کدام از سه الگو به عددِ بانک نمی‌رسد. کاربر فعلاً منصرف
  شد. اگر دوباره مطرح شد: اول **عکسِ جدولِ کاملِ ۱۲۰ ردیف** را بخواهید.

## Compose — قاعده‌هایی که نقضشان باگ داده
- 🚨 **`return` وسطِ کامپوزبل کلِ لیست را از کامپوزیشن برمی‌دارد** → اسکرول صفر می‌شود.
  **شش بار** رخ داده. راهِ درست: پوششِ تمام‌صفحه یا `ModalBottomSheet` روی همان لیست.
- 🚨 **`LazyColumn` داخلِ `Column(verticalScroll)` = کرش** (اسکرولِ تودرتو با ارتفاعِ
  بی‌نهایت). **قاعده: هر صفحه‌ای که `LazyColumn` دارد هرگز داخلِ
  `SettingsSubPageScaffold` نرود.**
- **توکن‌های رنگ (`AppPrimary`، `AppText`…) `@Composable`اند** — داخلِ `Canvas` (که
  `DrawScope` است) خوانده نمی‌شوند. همیشه قبلش در یک `val` محلی بخوانید.
- **هیچ کارتی را دستی با `clip/background/border` نسازید — همیشه `AppCard`.**
- `AppCard` محتوا را در `CompositionLocalProvider(LocalContentColor provides AppText)`
  می‌پیچد — `Text`ِ بی‌رنگ که نامرئی شد، اول چک کنید بیرونِ `AppCard` نباشد.
- **`LazyVerticalGrid` داخلِ لیستِ تنبلِ دیگر** نمی‌تواند wrap-content باشد → به‌جایش
  `items.chunked(n).forEach { Row { … } }`.
- **فیلدهای مبلغ: هیچ‌وقت داخلِ `onValueChange` دوباره فرمت نکنید** (cursor به‌هم
  می‌ریزد). state فقط رقمِ خام، فرمت فقط بصری با `ThousandsSeparatorTransformation`.
- **حالتِ خصوصی:** هر جای تازه‌ای که مبلغ نشان می‌دهد باید از `LocalPrivacyMode.current`
  + `maskIfPrivate(...)` رد شود (بیش از ۱۸۱ جای مصرف).
- **دو تابعِ هم‌نامِ `toFa`:** `core.toFa(x)` سراسری است، `ui.jibak.toFa()` اکستنشن.
  جابه‌جا نوشتنشان بیلد را می‌شکند (بیلدِ ۵۰۷).
- **RTL:** برنامه globally RTL است؛ فیلدهای ذاتاً چپ‌به‌راست (موبایل، PIN، شماره‌چک،
  شناسه‌ی صیادی، شماره‌ی دسته‌چک، شناسه‌ی ملی، سرشماره‌ی پیامک) باید در `Ltr.kt` پیچیده شوند.
- **اسکرول‌بار در RTL:** `contentPadding = PaddingValues(start = 10.dp)`.
- **اسنپ‌شات از UI:** `rememberGraphicsLayer()`، **نه `View.drawToBitmap()`**.
- **انتقالِ یک Composable به فایلِ جدید: ایمپورت‌هایش خودکار نمی‌آیند** — چند بار CI را شکسته.
- **لامبدای انتهایی همیشه به آخرین پارامتر می‌چسبد** (بیلدِ ۵۴۱ سرِ همین شکست).
- **`ModalBottomSheet` بدونِ `@OptIn(ExperimentalMaterial3Api::class)` کامپایل نمی‌شود**
  (بیلدِ ۵۴۵).

## تم و رنگ
- همه‌ی رنگ‌ها از `ui/theme/Color.kt` (`DarkAppColors`/`LightAppColors`).
- **طلایی (`AppAccent`) فقط نشانه‌ی پرمیوم/پول است** — روی حاشیه‌ی دکمه‌های معمولی نه.
- پروژه عمداً **بلورِ واقعی (`RenderEffect`) ندارد** — همه‌جا با گرادیانِ ثابت.
- قفلِ اسکرین‌شات (`FLAG_SECURE`) عمداً حذف شده (تصمیمِ صریحِ کاربر).
- **فونتِ سراسری Vazirmatn** با شش وزنِ واقعی (۴۰۰/۵۰۰/۶۰۰/۷۰۰/۸۰۰/۹۰۰).
  ⚠️ وزنِ ۸۰۰ و ۹۰۰ اگر فایلِ واقعی نداشته باشند، اندروید «چاقِ مصنوعی» می‌سازد که
  حروفِ فارسی را پخش و بدترکیب نشان می‌دهد.

## ساختِ تصویر با Python/PIL
- **متنِ فارسی:** `ImageFont.truetype(..., layout_engine=ImageFont.Layout.RAQM)` +
  `draw.text(..., direction="rtl", language="fa")`
- **آیکون از تصویرِ رندرشده:** اول با اسکنِ پیکسل باکسِ سفتِ محتوا را پیدا و کراپ کنید.
- **JPEG در برابرِ PNG:** تصویرِ بزرگِ بدونِ شفافیت → JPEG (کیفیت ۹۲) حدودِ یک‌هفتمِ PNG.

## هشدارِ لوگوی بانک‌ها
موقعِ بررسیِ یک برنامه‌ی مرجعِ decompile‌شده معلوم شد کدِ پایه‌ی خیلی اولیه‌ی این پروژه
دستِ‌کم یک لوگوی اشتباه داشته. داده در `data/Banks.kt` و لوگوها در `res/drawable`
(پکِ رسمیِ tejiran.ir). **یکی‌بودنِ بایت با یک فایلِ قدیمی ملاکِ درستیِ اسم نیست.**

---

# ۸. بررسی‌های ایستا — ۲۲ بررسی

```bash
bash native-android/tools/static-checks/run-all.sh
```
🚨 **قبل از هر بیلد باید سبز باشد.** هر کدام از یک شکستِ واقعیِ بیلد زاده شده:

| بررسی | از کجا آمد |
|---|---|
| `verify.py` | ایمپورت‌های گم‌شده |
| `icons.py` | `Icons.Filled.X` بدونِ ایمپورت |
| `callstyle.py` | شکستِ بیلدِ ۵۰۷ — `toFa` تابعی/اکستنشن |
| `receivers.py` | شکستِ ۵۱۰ — گیرنده‌ی اعلان‌نشده |
| `redeclare.py` | شکستِ ۵۱۲ — دو فایلِ هم‌محتوای طراح |
| `palette.py` | شکستِ ۴۶۵ — فیلدِ ناموجودِ پالت |
| `moneytypes.py` | شکستِ ۴۶۵ — `Long` به‌جای `Double` |
| `trailinglambda.py` | شکستِ ۵۴۱ |
| `experimental.py` | شکستِ ۵۴۵ — `@OptIn` گم‌شده |
| `lazyscope.py` | `@Composable` مستقیم داخلِ لیستِ تنبل |
| `dupelocal.py` | شکستِ ۵۵۳ — دو متغیرِ هم‌نام در یک اسکوپ |
| `apisurface.py` | امضای تابع و ثابتِ enum |
| + ده بررسیِ دیگر | فرمتِ عدد، مقدارِ منفی، companion تکراری، … |

⚠️ سه اسکریپت (`typography.py`، `opacity.py`، `oldstyle.py`) در پوشه هستند ولی در
`run-all.sh` صدا زده **نمی‌شوند** — پرسروصدایند.

⚠️ **بررسیِ ایستا همه‌چیز را نمی‌گیرد:** شکستِ بیلدِ ۵۵۷ از یک بلوکِ `item{}` بود که
**بیرونِ** `LazyColumn` افتاده بود؛ براکت‌ها متوازن بودند و هیچ بررسی‌ای نگرفتش.

---

# ۹. کارِ طراحی — تاریخچه‌ی کامل

## روال
طراح یک چتِ **جدا**ی Claude است («کلاد دیزاین»). هیچ اتصالِ مستقیمی بینِ آن و این
پروژه نیست: **کاربر واسطه است**. چرخه:
`ما فایل/سوال می‌نویسیم → کاربر می‌برد → جواب و بسته می‌آید → ما اعمال می‌کنیم`.

- فایل‌های طراحی داخلِ مخزن‌اند: `design/` (۹۷ فایل) و `design/frames/` (۱۳ فایلِ `.dc.html`).
- `design/TOKENS.md` نگاشتِ هر مقدارِ طرح به توکنِ `ui/theme` است.

## قاعده‌های اعمالِ بسته‌ی طراح
- مقادیر را از attributeهای `style` در HTML بردارید. **حدس نزنید، گرد نکنید، «بهتر» نکنید.**
- هیچ رنگی که در `design/سیستمِ طراحی جیبک.dc.html` نیست استفاده نشود.
- سایه‌ها همه **سخت**اند: `0 4px 0` هم‌رنگِ تیره‌ترِ خودش. هیچ blur، هیچ سایه‌ی رنگی.
- تمِ تیره فقط جابه‌جاییِ توکن است. هیچ صفحه‌ای دو بار چیده نمی‌شود.
- عددها فارسی با جداکننده‌ی `٬`؛ منفی با `−` نه پرانتز.
- کوچک‌ترین هدفِ لمسی **۴۴dp**.
- 🚨 **قبل از اعمالِ هر بسته، `git diff` روی فایل‌های مشترک را بخوانید.** بسته‌ی
  `home-final` یک‌بار دیالوگِ تاییدِ پرداختی را که همان روز اضافه شده بود **بی‌صدا پاک
  کرده بود** — یعنی دقیقاً همان چیزی که کاربر شکایتش را کرده بود برگشت.
  **کپیِ کورِ فایل ممنوع.**
- هر تغییرِ صفحه در یک commit جدا با شماره‌ی فریم: `redesign(15a): home normal state`

## 🚨 درسِ گران‌قیمتِ دورِ اول
سیستمِ طراحی (رنگ/کارت/دکمه/سایه) کامل پیاده شد ولی **فریم‌ها کارت‌به‌کارت باز نشدند**.
نتیجه: اسکلتِ برنامه‌ی قدیمی سرِ جایش ماند و فقط لباسِ نو پوشید. کاربر گفت «طراحیِ
صفحات مثلِ برنامه‌ی قبلی است، فقط چهارتا آیتم اضافه شده» و **حق داشت**.
**قاعده‌ی جایگزین:** قبل از دست‌زدن به هر صفحه، اول فهرستِ کارت‌های همان فریم را
دربیاورید، بعد کد بزنید. «همه‌ی برنامه را بازطراحی کن» ممنوع است.

## بخش‌های طراحی که اعمال شده‌اند (به‌ترتیب)
- **۳۷ بخشِ اولیه / ۱۸۹ فریم** — سیستمِ طراحی + ۵ تبِ اصلی + ۱۶ صفحه‌ی بی‌طرح
- **۴۱** — شخصی‌سازیِ نوارِ پایین (کارتِ پیشنهاد + ویرایشگر)
- **۵۱** — بازطراحیِ تبِ سررسید (سه گروهِ فوریت به‌جای سه تب)
- **۵۲ / ۵۳ / ۵۵** — کارت‌های کشف، کشوی میان‌بر، هدرِ خانه
- **۵۶ / ۵۷** — کیفِ سکه و ویجت
- **۵۸ / ۶۱** — آیکونِ سکه و پژمردگیِ ۳۲پله‌ی چهار آیکون
- **۶۰** — فروشگاهِ سکه
- **۶۲** — بازچیدمانِ «قیمتِ روز» (۵۶ نماد، چهار سرگروه، جست‌وجو)
- **۶۳** — اشتراک‌یاب و پیش‌بینیِ کسری
- **۶۴ / ۶۵ / ۶۶** — فهرستِ کاملِ اقساط، نسخه‌ی شبِ سررسید، ردیفِ قسط
- **۶۷ / ۶۸ / ۶۹ / ۷۰** — کلِ مسیرِ وام: فرمِ دستی، نتیجه، قسط و سود، سه صفحه‌ی آخر
- **۷۱** — صفحه‌ی نقدی، خرجِ امروز، تاریخچه‌ی اعلان
- **۷۲** — فروشگاهِ کامل (ستِ نماد، قابِ آواتار، تمِ مناسبتی)
- **۷۳** — پنج فریمِ متفرقه (تقویم، یادداشت، مجوزها، انتخابِ تاریخ)
- **۷۴** — مدیریتِ دسته‌ها و افزودنِ حساب
- **۷۵** — ادغامِ کیفِ سکه و فروشگاه
- **۷۶** — هدرِ فشرده‌ی تبِ وام (۲۴۸dp → ۸۸dp)
- **۷۸** — فروشگاهِ کامل: پس‌زمینه‌ی زنده، قلمِ هفته، مالِ من ← **آخرین بسته**

## تصمیم‌های طراحیِ قفل‌شده (بازشان نکنید)
- 🚨 **مسکات/شخصیتِ کارتونی: نه، هیچ‌جا.** نه بالای صفحه، نه در حالتِ خالی. راهِ میانیِ
  «فقط در حالت‌های خالی» هم پیشنهاد شد و **صریحاً رد شد**.
- **دکمه‌ی «+» برجسته‌ی وسطِ نوارِ ناوبری** — اضافه شد و به‌خواستِ کاربر برداشته شد.
- **نمودارِ روندِ ۶ماهه + شبکه‌ی «دسترسیِ سریع» در تبِ خانه** — اضافه و برگردانده شد.
- **مقصدِ FABِ تبِ وام: محاسبه‌گر، نه فرمِ دستی** — تصمیمِ صریحِ کاربر.
- **پرداختِ قسط فقط در نمای کاملِ اقساط** (فریمِ `36i`).
- **بازطراحیِ کاملِ «وام‌های من» مثلِ برنامه‌ی مرجع** — کاربر **پس گرفت**. دوباره پیشنهاد نکنید.

---

# ۱۰. وضعیتِ فعلی — چه چیزی ساخته شده

## حسابداریِ شخصی
حساب‌کتاب‌ها (بانکی/نقدی) · تراکنش (خرج/دخل/جابه‌جایی) · دسته‌بندیِ **تودرتو** ·
بودجه · گزارش با نمودارِ دوناتی و میله‌ای · گزارشِ سفارشی + خروجیِ PDF و اکسل ·
پرداخت‌های تکراری · جست‌وجو · ویرایش و حذفِ تراکنش با تایید

## وام و چک
محاسبه‌گرِ قسط و سود · توانِ بازپرداخت · سودِ سپرده · نرخ‌یاب · وام‌های من با فهرستِ
کاملِ اقساط · پرداختِ گروهی · عکسِ رسید و یادداشتِ قسط · **ماژولِ کاملِ چک** (دسته‌چک،
شناسه‌ی صیادی، استعلام، یادآور، بایگانی، امتیازِ ریسکِ برگشت) · طلب‌وبدهی · **دنگ**
(چهار روشِ تقسیم + حالتِ مهمانی + صورت‌حسابِ عکسی)

## دارایی
طلا/سکه/ارز/رمزارز/دلخواه · **قیمتِ زنده‌ی ۵۶ نماد** · تاریخچه و درصدِ تغییر ·
خرید/فروش · هشدارِ رشدِ رمزارز

## خودکارسازی
**خواندنِ پیامکِ بانکی** (سرشماره وصل به حساب) · **خواندنِ نوتیفیکیشنِ بانکی** (برای
بلوبانک و مشابهش) · قاعده‌های تشخیصِ دلخواه · آزمایشِ تشخیص · **دریافتِ پیامک از طریقِ
اشتراک‌گذاری** (`ACTION_SEND`)

⚠️ **محدودیتِ اندروید:** «انتخابگرِ پیامکِ سیستمی» وجود ندارد. تنها پلِ رسمی
`ACTION_SEND` است: نگه‌داشتنِ پیام → اشتراک‌گذاری → جیبک.

## گیمیفیکیشن و اقتصادِ سکه
دفترِ سکه (`coin_events`) — **موجودی همیشه مشتق است** (`SUM`)، هیچ‌وقت شمارنده‌ی
ذخیره‌شده · نُه نشان · زنجیره‌ی «فعال» + ترمیمِ زنجیره · **فروشگاه با شش نوع قلم**:
۱۵ تم · ۴ آیکونِ برنامه · ۲ ستِ نماد · ۵ قابِ آواتار · ۳ قلمِ متن · **۴ پس‌زمینه‌ی زنده** ·
**پژمردگیِ ۳۲پله‌ی آیکونِ برنامه** (۴ طرح × ۸ پله) · کارتِ «قلمِ این هفته» · فیلترِ «مالِ من»

## دیگر
قفلِ PIN + اثرِ انگشت · حالتِ خصوصی (ماسکِ هفت‌نقطه + تپِ ۴ثانیه‌ای) · تمِ روشن/تیره/سیستم ·
ویجتِ صفحه‌ی خانه · تقویمِ مالی · هدف‌های پس‌انداز · مرکزِ پیام‌ها · پشتیبان‌گیری ·
سینکِ ابری · آپدیتِ خودکار · اعلانِ برگشتِ کاربر

---

# ۱۱. چه چیزی باقی مانده

## پیشِ طراح (جوابِ بازِ ما)
1. **بازطراحیِ تقویمِ مالی** — بسته فرستاده شد، جواب نیامده
2. **بازطراحیِ صفحه‌ی جزئیاتِ وام** — بسته فرستاده شد («خیلی خشک است»)
3. **لوگو/آیکونِ تازه‌ی برنامه** — طراح گفت فایلِ هنریِ واقعی می‌خواهد (+ نُه پله‌ی پژمردگی)
4. **پشتِ کارتِ رسیدِ دنگ (`63c`)** — نیامده
5. **بافتِ زمینه‌ی تمِ هنری** — طراح گفت تصویر تولید نمی‌کند؛ فعلاً قیمت از ۸۰۰ به ۴۰۰ آمد

## کارِ کدیِ باقی‌مانده
1. **آرشیوِ سالانه** — نه کد دارد نه طرح
2. **سه ابزارِ فریمِ `75c`** (اشتراک‌یاب · دنگ · استعلامِ صیادی) هنوز به صفحه‌ی
   «ابزارها» نیامده‌اند — هر سه لایه‌ی داده‌ی تازه می‌خواهند
3. **کارتِ مجموعه‌ها** (`ShopCollections`) — فایلش از طراح آمد، هنوز در UI ننشسته
4. **نشانِ `COLLECTION_STARRY_NIGHT`** — در طرح هست، در `Badge` نیست
5. **هدیه‌ی اشتراکِ تشویقی** — ثبت‌شده، پیاده نشده (جزئیات پایین)
6. **دسته‌بندیِ هوشمندِ تراکنشِ خودکار** — تصمیم گرفته شده، کد نخورده
7. **دسته‌ی «سفر»** — تاییدِ صریح نگرفته
8. **~۳۰ موردِ `design/FEEDBACK-round2.md`** — به‌خواستِ کاربر معلق
9. **وب‌اپ** — فازِ بزرگِ آینده، هیچ کدی ندارد
10. **بنچمارک/تستِ بار** — شروع نشده
11. **سینکِ سررسید با اپِ «تقویم من»** — متوقف

## تصمیم‌های محصولیِ منتظرِ کاربر
- جی‌میلِ پشتیبانی + آیدیِ تلگرام
- جزئیاتِ هدیه‌ی اشتراک
- تاییدِ دسته‌ی «سفر»
- زمانِ شروعِ وب‌اپ

## دو چیزی که طراح گفت **ساخته نشوند**
- **قدرتِ خرید** — از قبل به‌عنوانِ بجِ گزارش وجود دارد
- **حسابِ خانواده** — لایه‌ی سینکِ چندکاربره می‌خواهد؛ تصمیمِ محصولی است نه طراحی

---

# ۱۲. ایرادها و ریسک‌هایی که باید گرفته شوند

## 🔴 امنیتی — فوری
1. **کلیدِ API نوسان یک‌بار داخلِ مخزن کامیت شده بود.** آن سرویس دیگر استفاده نمی‌شود
   ولی **کلید باید باطل (revoke) شود** — هنوز انجام نشده.
2. **`MYKET_IAB_PUBLIC_KEY` مقدارِ واقعی است و داخلِ کد نشسته**
   (`app/src/myket/.../SubscriptionManager.kt`). این کلیدِ عمومی است و در الگوی IAB
   معمول است، ولی باید آگاهانه پذیرفته شود.
3. **هیچ کلیدِ دیگری نباید وارد مخزن شود** — فقط GitHub Secrets و `~/VameMan/.env`.

## 🟠 کیفیت و پایداری
4. **تستِ مایگریشن برای نسخه‌های ۱۳ تا ۳۲ ممکن نیست** — `data/schemas/` فقط `12.json`
   و `29.json` را دارد. هر مایگریشنِ تازه‌ای کورکورانه می‌رود. **این بزرگ‌ترین بدهیِ فنی است.**
5. **خریدِ واقعیِ مایکت هنوز روی گوشیِ واقعی تست نشده** — هم مسیرِ اصلی هم `restorePurchases`.
6. **فرمتِ ریسپانسِ موفقِ مایکت مستند نیست** — فقط کدِ HTTP چک می‌شود.
7. **نتیجه‌ی Play Protect Appeals هنوز نیامده.**
8. **ویجت روی گوشیِ واقعی تایید نشده** (تپ برنامه را باز می‌کند یا نه).

## 🟡 باگ‌های گزارش‌شده‌ی تاییدنشده
9. **کارتِ کشیده‌شده** — علتش هیچ‌وقت پیدا نشد؛ کارت از خانه برداشته شد ولی اگر در
   کیفِ سکه هم کشیده دیده شود یعنی ریشه در خودِ کارت است.
10. **صفحه‌ی سفیدِ تبِ خانه** — اسکلتِ لودینگ اضافه شد، روی گوشی تایید نشده.
11. **کرشِ «آزمایشِ تشخیص»** — رفع شد، تایید نشده.
12. **نوتیفیکیشنِ بلوبانک** — گزارشِ کاربر، ریشه‌یابی نشده.

## 🟢 بدهیِ فنیِ ساختاری
13. **`main` حدودِ ۵۰۰ کامیت عقب است.** هرچه بیشتر بگذرد مرجش سخت‌تر می‌شود.
14. **`TourTarget` پس از حذفِ تبِ اسپاتلایت‌شده مختصاتِ کهنه نگه می‌دارد** (ریسکِ ناچیز).
15. **`AutoTxNotifier` ردیفِ صندوقِ پیام دارد ولی اعلانِ آزمایشیِ تنظیمات عمداً ندارد.**

## ⚠️ چیزی که قبل از انتشارِ عمومی باید چک شود
- **ردیفِ «شارژِ آزمایشیِ سکه» حذف شد** ✅ (دورِ ۱۳)
- تاییدِ اینکه هیچ ابزارِ دیباگی در نسخه‌ی release نمانده

---

# ۱۳. چک‌لیستِ تستِ روی دستگاه

## باگ‌هایی که باید تایید شوند
- صفحه‌ی اجازه‌ی باتری/اعلان بعد از اسپلش فلش نزند
- حالتِ خصوصی هم روشن شود هم **خاموش**
- تپ روی ردیف‌های تبِ سررسید جزئیات را باز کند
- حذفِ دسته‌بندیِ باتراکنش بپرسد به کدام دسته منتقل شود
- آیکونِ برنامه لبه‌به‌لبه‌ی سبز باشد
- ویجت با تپ برنامه را باز کند

## قابلیت‌های تازه (بیلدِ ۵۵۹)
- **فروشگاه:** پس‌زمینه‌ی زنده (یک خرید = چهار طرح) · «چرخشِ شب» فقط در تمِ تیره ·
  کارتِ «قلمِ این هفته» · قرصِ «مالِ من» · چهار تمِ تازه
- **کارت‌ها وقتی پس‌زمینه‌ی زنده روشن است ۷۲٪ شفاف شوند**
- **آدمکِ هدرِ خانه** نشان‌ها را باز کند
- **ابزارها** دو گروهِ نام‌دار داشته باشد و «شارژِ آزمایشی» نباشد
- **تبِ وام:** کارتِ طلاییِ سه‌لایه · «۱۰۹٪ از درآمدت…» داخلِ کارت · آمار در تبِ
  گزارش · پشتیبان‌گیری تهِ فهرست · بدونِ نوارِ «N وام عقب‌افتاده»
- **حالتِ خصوصی:** ماسک همیشه هفت نقطه · تپ روی داراییِ کل ۴ ثانیه نشان بدهد
- **هدف‌های پس‌انداز:** حالتِ خالی · مدالِ هدفِ رسیده · خطِ سرعت

## چیزهایی که هنوز نیستند و نباید انتظارشان را داشت
آرشیوِ سالانه · حسابِ خانواده · قدرتِ خرید · کارتِ مجموعه‌ها · اشتراکِ جایزه‌ای
(ردیف دارد ولی `comingSoon`)

---

# ۱۴. دسترسی‌ها و سکرت‌هایی که لازم است

## GitHub Secrets (روی مخزن)
| سکرت | کار |
|---|---|
| `MELIPAYAMAK_USERNAME` / `MELIPAYAMAK_PASSWORD` | ارسالِ OTP — رمزِ **وبسرویس** نه پنل |
| `PRICE_API_KEY` | کلیدِ servix.cc |
| `CAFEBAZAAR_*` | صحت‌سنجیِ خریدِ کافه‌بازار |
| `MYKET_ACCESS_TOKEN` | صحت‌سنجیِ خریدِ مایکت |
| کلیدهای SSH/VPS | برای scp و pm2 در دیپلوی |

## VPS
دسترسیِ SSH به `37.152.187.44`، کاربرِ دارای `~/VameMan/`

## حساب‌های بیرونی
پنلِ کافه‌بازار · پنلِ مایکت · پنلِ ملی‌پیامک · حسابِ servix.cc · حسابِ آروان‌کلاود

---

# پیوست: فایل‌هایی که ادامه‌دهنده باید اول بخواند

1. **`CLAUDE.md`** — حافظه‌ی کاملِ پروژه (طولانی است ولی همه‌چیز آن‌جاست)
2. `native-android/README.md` — جزئیاتِ فیچرهای اپ
3. `server/README.md` — جزئیاتِ سرور
4. `design/README.md` + `design/TOKENS.md` — قاعده‌های طراحی و نگاشتِ توکن
5. `design/COVERAGE.md` — پوششِ فریم‌ها
6. `design/MESSAGE-round*.md` — تاریخچه‌ی گفت‌وگو با طراح (۱۳ دور)
